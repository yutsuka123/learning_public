# ソース概要.md

[重要] 本ドキュメントは「どこに何のソースがあるか」をフォルダ単位で説明します。
[厳守] フォルダを追加/大きく構成変更したら更新します。理由: 変更点の見落としを防ぐため。

[相互参照] 「なぜこの技術を学ぶか（概要・需要・用途・分野）」は `技術スタック学習ガイド.md` を参照。
[相互参照] Pythonライブラリの分野別まとめ（人気・需要・日本海外の傾向）は
`python/LIBRARY_LANDSCAPE_GUIDE.md` を参照。

## ルート直下
- `c/`: C の学習用サンプル
- `cpp/`: C++ の学習用サンプル（CMake/Visual Studio など）
- `cpp_m/`: モダンC++（C++11/14/17/20/23/modern）の学習用サンプル（単体コンパイル前提）
- `csharp/`: C# の学習用サンプル
- `java/`: Java の学習用サンプル
- `kotlin/`: Kotlin の学習用サンプル
- `python/`: Python の学習用サンプル
- `python/examples/`: PyTorch / TensorFlow / FastAPI / Django の実践例（分割配置）
- `python/embedded_micropython/`: MicroPython（GPIO/割り込み/並列処理/通信/ポインタ的操作）
- `python/interop_c_cpp/`: Python⇔C/C++ 相互呼び出し（ctypes / pybind11 / Python埋め込み）
- `python/image_processing/`: Pillow / numpy / OpenCV / scikit-learn / torchvision
- `python/database/`: SQLite / PostgreSQL / スキーマ設計
- `python/data_analysis/`: pandas / matplotlib・seaborn / 時系列データ処理
- `python/cad_3d_printing/`: CadQuery / SolidPython+OpenSCAD（3Dプリンター向けCAD）
- `python/web_scraping/`: requests + BeautifulSoup によるスクレイピング
- `python/testing/`: pytest による自動テスト
- `python/office_automation/`: openpyxl / python-docx / python-pptx
- `python/ocr/`: pytesseract によるOCR
- `python/gui/`: tkinter / FreeSimpleGUI
- `python/stdlib_mastery/`: 標準ライブラリ(os/sys/math/re他)使いこなし
- `python/ros2_basics/`: ROS2（RoboStack経由、conda環境が別途必要）
- `typescript/`: TypeScript の学習用サンプル
- `rust/`: Rust の学習用サンプル（Cargo）

## C#
- `csharp/modern-features/`
  - モダンC#（C# 9以降〜最新）の学習用サンプル
  - `ModernCSharpApp/`: ラムダ式（キャプチャ差）と最新機能の小例をConsole出力で確認

## C言語
- `c/structures/struct_pointer_array_deep_dive.c`
  - 構造体ポインタ・配列・ポインタ演算・多次元配列/ポインタ配列の違い・構造体メンバ(配列 vs
    ポインタ)・関数ポインタ・自己参照構造体・ビット演算・構造体ビットフィールド・
    パディング/アライメントを10章で深掘り

## C++ Modern
- `cpp_m/`
  - `main.cpp`: このフォルダの入口（コンパイラ/標準バージョンの確認と案内）
  - `cpp11.cpp`, `cpp14.cpp`, `cpp17.cpp`, `cpp20.cpp`, `cpp23.cpp`: バージョン別の学習サンプル（各ファイルを該当標準で単体ビルド）
  - `modern.cpp`: 最新（原則 C++23）を前提に、主要機能を総合的に扱うサンプル
  - `cpp_basics_class_memory_ownership.cpp`: バージョン横断の基礎。class/struct、
    new/delete vs スマートポインタ、所有権(unique_ptr/shared_ptr/weak_ptr)、std::vector、
    C言語との違い、cout/printf比較、ラムダ詳細
  - `INSTALL.md`, `README.md`: ビルド/実行手順と学習の進め方

## Rust
- `rust/hello_world/`
  - Cargo プロジェクト（`Cargo.toml`, `src/main.rs`）
  - `cargo run -- [name]` で HelloWorld を表示
- `rust/basics_ownership_borrowing/`
  - 所有権(move)・借用(&T/&mut T)・スライス・構造体とimpl・Option/Result・?演算子・
    trait・Vec<T>・Box/Rc/Weak（C++のunique_ptr/shared_ptr/weak_ptrと対比）

## Python 実践例（`python/examples/`）
- `pytorch_practice.py`: テンソル・自動微分・訓練ループ
- `tensorflow_practice.py`: PyTorch版と同じ題材で書き方を比較（別venv `.venv-tensorflow` 必須）
- `fastapi_practice.py`: Depends()によるDI、統一例外ハンドラ
- `django_minimal/`: Model/migrate/管理画面/JSON API、N+1問題とselect_relatedの実演

## Python 画像処理（`python/image_processing/`）
- `pillow_basics.py`: リサイズ/クロップ/回転/フィルタ/描画/フォーマット変換
- `numpy_pixel_manipulation.py`: 画像を配列として直接操作
- `opencv_basics.py`: グレースケール/エッジ検出(Canny)/輪郭検出
- `kmeans_color_segmentation.py`: scikit-learn KMeansによる教師なし色分割、エルボー法
- `semantic_segmentation_pytorch.py`: 学習済みモデルによるセマンティックセグメンテーション

## Python データベース（`python/database/`）
- `sqlite_basics.py`: CRUD、パラメータ化クエリ、トランザクション、`with`の落とし穴
- `sql_injection_demo.py`: SQLインジェクションの実演（攻撃成立と防御の両方）
- `schema_design_good_vs_bad.py`: 型/制約/インデックスの効果を実測
- `postgresql_basics.py`: `RETURNING`句、SQLiteとの`with`の意味の違い
- `NORMALIZATION_GUIDE.md`: 正規化(1NF〜3NF)・ER図・キー設計をEC注文管理を題材に解説
- `normalization_example.py`: 上記ガイドの3種の異常（更新/挿入/削除）を実際に再現・解消

## Python 組み込み（`python/embedded_micropython/`）
- `concurrency_examples.py`: `_thread` vs `asyncio`
- `communication_examples.py`: `socket`通信、`struct`バイナリプロトコル
- `memory_and_language_features.py`: `gc`/`micropython.const`とCPythonとの違い
- `pointers_and_memory_c_vs_micropython_vs_cpython.py`: C/CPython/MicroPythonのメモリモデル比較
- `hardware_gpio_interrupt_examples.py`: GPIO/PWM/ADC/UART/I2C/SPI/タイマー割り込み（実機必須）

## Python ⇔ C/C++ 相互呼び出し（`python/interop_c_cpp/`）
- `ctypes_call_c/`: Python→C（ctypes）
- `pybind11_call_cpp/`: Python→C++（pybind11）
- `embed_python_in_c/`: C/C++→Python（インタプリタ埋め込み、C版とC++(RAII)版）

## Python データ分析（`python/data_analysis/`）
- `pandas_basics.py`: DataFrame操作、groupby、merge、欠損値処理、describe/corr
- `matplotlib_seaborn_basics.py`: グラフ作成（ヘッドレス環境向け`Agg`バックエンド）
- `timeseries_pandas.py`: resample/rolling/shift/diffによる時系列処理

## Python CAD/3Dプリンター（`python/cad_3d_printing/`）
- `cadquery_basics.py`: 箱/穴/面取り、パラメトリックなブラケット設計、STL/STEP出力（主力サンプル）
- `solidpython_openscad_basics.py`: SolidPython2+OpenSCAD（参考用。macOSでのGatekeeper非推奨警告を実確認）
- `README.md`: CadQuery採用理由の詳細

## Python Webスクレイピング（`python/web_scraping/`）
- `scraping_and_analysis.py`: requests+BeautifulSoupでの取得、robots.txt確認、pandas集計（タグの`.explode()`分析）

## Python 自動テスト（`python/testing/`）
- `inventory.py`: テストしやすい設計（「今日の日付」を引数注入する等）
- `test_inventory.py`: フィクスチャ、`@pytest.mark.parametrize`、`pytest.raises`、`monkeypatch`、`tmp_path`

## Python Office自動化（`python/office_automation/`）
- `excel_automation.py`: openpyxlでのセル操作/数式/書式、pandas ExcelWriter連携
- `word_powerpoint_automation.py`: python-docx / python-pptx によるWord/PowerPoint生成

## Python OCR（`python/ocr/`）
- `tesseract_ocr_basics.py`: pytesseractでのOCR、フォントサイズと精度の関係を実測、日本語OCR

## Python GUI（`python/gui/`）
- `tkinter_basics.py`: 標準ライブラリtkinter（コールバック方式）
- `freesimplegui_basics.py`: FreeSimpleGUI（`window.read()`イベントループ方式）
- `README.md`: PySimpleGUIのライセンス変更（2023年、有償化）を実確認した上での採用理由

## Python 標準ライブラリ使いこなし（`python/stdlib_mastery/`）
- `stdlib_essentials.py`: os/os.path/pathlib、sys、math、re、collections、itertools、functools、jsonの実務頻出パターン

## Python ROS2（`python/ros2_basics/`）
- `minimal_pub_sub.py`: rclpyによるPublisher/Subscriberノード間のメッセージ送受信
- `README.md`: macOS公式非対応の中、RoboStack（conda-forge）経由でネイティブ動作を確認した経緯・環境構築手順
