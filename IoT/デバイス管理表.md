# IoT デバイス管理表

[重要] 本書は実機ごとの識別情報、暗号化状態、`eFuse` 状態、試験利用履歴を管理する。  
理由: 1台目試験機と量産対象の取り違えを防ぎ、不可逆処理の適用履歴を後追い監査できるようにするため。

[厳守] `eFuse`、鍵、FW版、試験番号、判定履歴を更新した場合は、本書と `試験記録書.md` を同日更新する。  
理由: 実機状態と試験証跡の不整合を防ぐため。

[禁止] 秘密鍵実体、認証パスワード実値、raw secret を記載しない。  
理由: 管理表から機密漏えいを起こさないため。

## 1. 管理ルール
- [厳守] 管理番号は `DEV-xxx` の連番とし、今回試験に使用した個体を `DEV-001` とする。
- [厳守] `MAC` は可能な範囲で「チップ実測値」と「運用表示値」の両方を残す。
- [重要] `NVS` は「設計意図」と「実運用パターン」を分けて記載する。
- [重要] 出荷可否は `007-C` のレビューゲート完了後に確定する。

## 2. 項目定義
- 管理番号: 本プロジェクト内での固定識別子。
- 用途区分: 試験機、本番1台目、量産候補、隔離個体などの区分。
- チップ実測MAC: `espefuse summary` などの低レイヤ確認値。
- 運用表示識別子: `deviceName`、`public_id`、ラベル表示値などの運用上の識別子。
- セキュア状態: `Flash Encryption`、`NVS(HMAC)`, `Secure Boot v2`, `JTAG封鎖`, `Secure Download` の適用状態。
- NVS実行パターン: `patternA` / `patternC` のどちらで動作しているか。
- 最新判定: `007-B` 完了、`007-C` レビュー中、出荷可、隔離 など。

## 3. デバイス一覧
| 管理番号 | 用途区分 | チップ実測MAC | 運用表示識別子 | 接続/ポート | FW版 | Flash Encryption | NVS HMAC鍵 | NVS実行パターン | Secure Boot v2 | JTAG封鎖 | Secure Download | USBシリアルログ | 最新判定 | 備考 |
|------|------|------|------|------|------|------|------|------|------|------|------|------|------|------|
| `DEV-001` | 本番セキュア化1台目 | `80:B5:4E:F9:D0:F0` | `deviceName=IoT_F0D0F94EB580` / `MAC表示=F0:D0:F9:4E:B5:80` / `public_id=`未設定 | `COM9` / `mqtt` / `172.17.1.200` | `1.1.0-beta.29`（復旧試行後は起動不可） | 有効 | `BLOCK_KEY2` 投入済 | `patternA`（設計上は将来 `patternC` 両立化を検討） | 有効 (`BLOCK_KEY1` digest) | `DIS_PAD_JTAG` / `DIS_USB_JTAG` 有効 | `ENABLE_SECURITY_DOWNLOAD` 有効 | 可（危険操作は不可） | `007-C` レビュー中（解析保全固定） | `7025=OK`、`7100=OK`、`7099=OK(DEV-002で再観測完了)`、`write_flash --force` 復旧は失敗（#0024） |
| `DEV-002` | 2台目（生デバイス・事前安定性確認用） | `80:B5:4E:F9:CE:04` | `deviceName=IoT_04CEF94EB580` / `MAC表示=04:CE:F9:4E:B5:80` | `COM4` / `mqtt` / `172.17.1.201` | `1.1.0-beta.31` | 未適用 | 未投入 | `patternA`（開発ビルド） | 未適用 | 未適用 | 未適用 | 可（開発段階） | `007-C` 事前確認中（final secure 前） | `#0023` 解決、`7099`(AP5/STA5/OTA2)=OK。次は `7101` 最終確認と `DEV-001` 復帰確認 |

## 4. `DEV-001` 詳細メモ
- [最重要][2026-04-05] `DEV-001` の問題は `DEV-001問題点クイックリファレンス.md` を起点に参照する。詳細根拠は `問題点記録書.md` `#0024` / `#0025` を正とする。
- [重要][2026-03-27] 本個体は `FE -> NVS(HMAC) -> SB -> 封鎖` の順で不可逆工程を実施済み。
- [重要][2026-03-27] `DIS_USB_SERIAL_JTAG` と `DIS_DOWNLOAD_MODE` は未適用方針とし、シリアルログを維持している。
- [重要][2026-03-27] `get_security_info` では `Secure Boot: Enabled`、`Flash Encryption: Enabled`、`JTAG: Permanently Disabled`、`Secure Download Mode` を確認済み。
- [重要][2026-03-27] `read_mem` / `read_flash` は `Secure Download Mode` で拒否されることを確認済み。
- [要確認][2026-03-27] `NVS` は現状 `patternA` 動作である。設計意図の `patternC` と開発時の安全性を両立する方式は `008` で継続検討する。
- [重要][2026-04-04] 次回接続前に、`MAC` / `COM` / `FW版` / `最新判定` を本書で声出し確認してから接続する。
- [重要][2026-04-04] 次回接続時の build env は `esp32s3_secure_final` を候補とするが、接続直前に `試験記録書.md` `7095/7099/7101` と `eFuse適用チェックリスト.md` `PC-012` を埋めてから確定する。
- [厳守][2026-04-04] `DEV-001` 以外の個体をつなぐ場合は、本書へ新規行を追加し、`DEV-001` と混在記録しない。
- [進捗][2026-04-04] 実機再接続後に `COM9` を再確認し、`get_security_info` で `Secure Boot: Enabled`、`Flash Encryption: Enabled`、`BLOCK_KEY2=HMAC_UP`、`JTAG: Permanently Disabled` を再確認した。
- [重要][2026-04-04] `read_mem` / `read_flash` は引き続き `Secure Download Mode` で拒否されたため、現セッションでも「危険操作は不可」の判断を維持する。
- [進捗][2026-04-04] `Wi-Fi_lab` で `192.168.4.100` を取得し、`ping 192.168.4.1` と `GET /api/health` が `OK` となったため、AP 保守面は現セッションでも到達可能と確認した。
- [重要][2026-04-04] `7041` の `targetDeviceMatched` 判定は SoC 実測MAC `80:B5:4E:F9:D0:F0` ではなく、`MAC表示=F0:D0:F9:4E:B5:80` を `expectedMac` とした場合に `OK` となった。`DEV-001` の AP 側照合は本欄の `MAC表示` を正とする。
- [進捗][2026-04-04] `SecretCore` を既定 Pipe `\\\\.\\pipe\\iot-secret-core-ipc` で手動待受させることで、`7087:normal` / `7087` は `OK` まで回復した。
- [解決][2026-04-04] NIC 再接続後に `172.17.1.100:8883` の到達性が回復し、`LocalServer` で `onlineState=online`、`detail=StartUp` を再確認した。`7083(iterations=2)` も `OK` まで回復した。
- [重要][2026-04-04] 今回の `7083` 停止要因は `secureNvsInit` ではなく、broker/CoreDNS 用 NIC 未接続によるホスト側経路断だったと判断する。
- [更新][2026-04-05] `DEV-002` 側で `7099`（AP5/STA5/OTA2）を再観測し `OK` まで回復した。次フェーズは `DEV-001` を問題発生前の運用レベルへ戻す安全手順を優先する。
- [更新][2026-04-05] `DEV-001` で `write_flash --force`（平文）復旧を1回試行したが、`invalid header` 連続で起動復帰できなかった。以後は解析保全固定とし、`DEV-002` を主系として進める。

## 4.1 `DEV-002` 詳細メモ（生デバイス）
- [重要][2026-04-04] `DEV-001` のセキュア化後個体は解析保全とし、2台目 `DEV-002` は「final secure 前までの安全確認専用」として扱う。
- [厳守][2026-04-04] `DEV-002` は `DEV-001` と同一方針（`AP/Wi-Fi/NTP/k-device` 設定、`expectedMac` は AP 表示値を正）で設定する。
- [厳守][2026-04-04] `7099`（AP/STA/OTA 安定性）と `7101`（ProductionTool 連携）を先に満たすまで、`DEV-002` で `007-B` 不可逆工程へ進まない。
- [禁止][2026-04-04] 「とりあえず secure_final を先に適用して様子を見る」運用は禁止とする。理由: 起動停止時の回復経路を失うため。
- [推奨][2026-04-04] 接続直後に `デバイス管理表.md` の `チップ実測MAC` / `運用表示識別子` / `接続ポート` を先に埋め、`試験記録書.md` の当日欄へ同じ値を転記する。
- [更新][2026-04-05] `mainTask` に `Reply` 定期publish（30秒）を追加した `esp32s3_secure` を適用後、`offline timeout` は再発せず、`7099` をクローズした。

## 5. 更新手順
- `eFuse` 更新時: `espefuse summary` before/after を採取し、本書の状態列を更新する。
- OTA更新時: `FW版`、`備考`、関連試験番号を更新する。
- レビューゲート時: `最新判定` を更新し、必要なら `問題点記録書.md` と相互参照を追記する。

## 6. 変更履歴
- 2026-04-05: `DEV-001` 詳細メモへクイック参照導線（`DEV-001問題点クイックリファレンス.md` -> `#0024/#0025`）を追加。理由: 実機作業中に `DEV-001` の既知問題を最短で参照し、誤った復旧操作を防ぐため。
- 2026-04-05: `DEV-001` 行へ「`write_flash --force` 復旧失敗（#0024）」と解析保全固定を追記。理由: 本番個体での破壊的復旧は再実施しない運用判断を管理表から即参照できるようにするため。
- 2026-04-05: `DEV-002` の実測識別子（MAC/COM/IP）と `7099=OK` 結果を反映し、`DEV-001` 行へ `7099` 反映状況を追記。理由: 2台運用での判定進捗を管理表だけで即確認できるようにするため。
- 2026-04-04: `DEV-002` 行と `4.1 DEV-002 詳細メモ` を追加。理由: `DEV-001` を解析保全しつつ、2台目を同一設定で安全に事前確認する運用へ切り替えたため。
- 2026-04-04: `DEV-001` 詳細メモへ NIC 再接続後の MQTT/OTA 回復と、停止主因が NIC 未接続だった点を追記。理由: `7083` の一時失敗をセキュア化不具合と誤認しないよう、個体管理表から原因と回復条件を即確認できるようにするため。
- 2026-04-04: `DEV-001` 詳細メモへ固定 Pipe `SecretCore` による `7087` 回復と、`7083` / MQTT 未回復による No-Go 継続を追記。理由: `ProductionTool` 側問題は解消したが、OTA 主経路の未回復が新たな差し止め理由になったため、最新判定を管理表から即確認できるようにするため。
- 2026-04-04: `DEV-001` 詳細メモへ AP 到達回復、`7041` の `expectedMac` 正値、`7087:normal` 未回復による No-Go 継続を追記。理由: 同一個体でも SoC 実測MAC と AP 表示MAC を取り違えると `7101` の判定がぶれるため、今回の再確認結果を管理表から即参照できるようにするため。
- 2026-04-04: `DEV-001` 詳細メモへ実機再接続後の `COM9` / `get_security_info` / `read_mem` / `read_flash` 再確認結果を追記。理由: 現セッションでも対象機識別と非破壊安全状態が維持されていることを、管理表から即確認できるようにするため。
- 2026-04-04: `DEV-001` 詳細メモへ接続前確認事項と build env 確定条件を追記。理由: 実機接続直前に個体識別と build 方針がぶれないよう、管理表自体を起点に確認できるようにするため。
- 2026-03-27: 新規作成。理由: 試験に使用した1台目を `DEV-001` として固定し、`MAC`、暗号化状態、`eFuse` 状態、レビューゲート状況を一元管理するため。
