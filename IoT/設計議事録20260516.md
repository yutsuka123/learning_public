# 設計議事録 2026-05-16

[重要] 本書は 2026-05-16 の作業終了時点の引継ぎ記録である。  
理由: 次セッションへ変わっても、完了タスク、判断根拠、次回開始点を同じ前提で再開するため。

[禁止] raw `k-device`、実パスワード、秘密鍵、復元可能な機密値は本書へ記載しない。  
理由: 議事録は参照頻度が高く、機密漏えいリスクを避けるため。

## 1. 今日の完了タスク
- [完了] `020-0003`: Router `WAN 管理` / `UPnP` / `DMZ` / `port forward` の扱いを文書化し、完了退避。
- [進捗] `009-0001`: `LocalServer` Task Scheduler 自動起動手順を `コマンド仕様書.md` / `LocalServer/README.md` へ具体化した。

## 2. 本日の判断
- [厳守] `WAN 管理` は **無効** を正とする。
- [厳守] `UPnP` は **無効** を正とする。
- [厳守] `DMZ` は **未設定** を正とする。
- [厳守] `3100` / `4443` / `8883` / `53` / `123` へのインターネット側 `port forward` は **行わない**。
- [推奨] 遠隔保守が必要な場合は、ルータ公開ではなく VPN またはゼロトラストトンネル等の認証付き閉域経路を使う。
- [厳守] `LocalServer` の Task Scheduler 登録は **管理者PowerShell** を正とする。理由: 2026-05-16 の実機確認で、通常権限 `Register-ScheduledTask` は `0x80070005 (Access denied)` で失敗したため。
- [重要] `LocalServer` の起動ラッパーは `npm` ではなく **`node.exe` 実体解決 + `node dist/server.js` 直起動** を正とする。理由: Task Scheduler の非対話環境で `npm` PATH 解決へ依存しないため。
- [重要] 2026-05-16 終了時点の残ブロッカーは、**`S4U` 実行方式または Task Scheduler 実行コンテキスト**である。理由: 管理者PowerShellでの再登録後も `schtasks /run` では `LastRunTime` が更新されず、`LastTaskResult=267011` / `/api/health=NG` が残ったため。

## 3. 反映した正本
- `ネットワーク運用仕様書.md`
  - 7.4.4 のルータ方針を確定し、7.4.4a / 7.4.4b に確認方法と手動確認手順を追加した。
- `試験仕様書.md`
  - `7034` に、ルータ公開面の確認を合否条件として戻した。
- `試験記録書.md`
  - `7034` に、`020-0003` の結論と次回現物確認時の追記ルールを追加した。
- `todo.md`
  - 未完了件数を `39 / 69` へ更新し、次回開始点を `009-0001` へ進めた。
- `todo_old20260516.md`
  - `020-0003` を完了退避した。
- `コマンド仕様書.md`
  - §5 に `IoT_LocalServer_AutoStart`、`install-local-server.ps1 -InstallTaskScheduler`、`Start-ScheduledTask`、`/api/health`、管理者PowerShell必須条件、`node dist/server.js` 直起動、未解決ブロッカーを追記した。
- `LocalServer/README.md`
  - §6 に Task Scheduler 自動起動の前提条件、`node dist/server.js` 直起動、未解決ブロッカーを追記した。
- `試験記録書.md`
  - `7001` に `009-0001` の実機確認結果（未登録確認、通常権限 `0x80070005`、管理者PowerShellでの登録復旧、`schtasks /run` 後も `LastRunTime` 未更新）を追記した。
- `問題点記録書.md`
  - `#0042` として Task Scheduler 登録時の権限不足から、`S4U` / 実行コンテキスト切り分けまでを追加した。

## 4. 次回開始点
- [最重要] 次回は `todo.md` 冒頭 → `次セッション開始メモ` → `009-0001` の順で開始する。
- [次作業] `009-0001`: `install-task-scheduler.ps1` の **`LogonType S4U`** を起点に、実行ユーザー方式または実行コンテキストを比較し、`schtasks /run` 後に `LastRunTime` と `3100` 待受が更新される条件を特定する。
- [補足] `020-0003` は文書化完了済みだが、現物ルータ管理画面での値確認は `7034` 再試験時に追記する。

## 5. 正本参照
- `todo.md`: 未完了件数 `39 / 69`、次回開始点 `009-0001`
- `todo_old20260516.md`: `020-0003` の完了退避
- `ネットワーク運用仕様書.md` 7.4.4 / 7.4.4a / 7.4.4b: Router 公開面の正
- `試験仕様書.md` `7034`: ルータ公開面を含む踏み台化防止確認
- `試験記録書.md` `7034`: 次回現物確認時の追記先
- `コマンド仕様書.md` §5: `LocalServer` Task Scheduler 導入手順の正
- `LocalServer/README.md` §6: モジュール側の短縮版手順
- `試験記録書.md` `7001`: `009-0001` 実機確認の時系列
- `問題点記録書.md` `#0042`: `S4U` / 実行コンテキスト切り分けブロッカー

## 6. 変更履歴
- 2026-05-16: `009-0001` の現在地を、Task Scheduler 登録復旧・`node dist/server.js` 直起動化・`S4U`/実行コンテキスト切り分け待ちまで更新した。理由: 次セッションで「管理者PowerShell再実施」からではなく、残っている本質的ブロッカーから再開できるようにするため。
- 2026-05-16: `009-0001` の進捗として Task Scheduler 手順の文書化と、通常権限 `Register-ScheduledTask` の `0x80070005` 失敗を追記した。理由: 同日の `020-0003` 完了後に着手した次タスクの現在地と再開条件を同じ日付の議事録へ残すため。
- 2026-05-16: 新規作成。理由: `020-0003` 完了と次回開始点 `009-0001` を次セッションへ安全に引き継ぐため。
