# IoT TODO 完了済み（退避: 2026-05-13）

[補足] 本書は `todo.md` から退避した完了済みタスクの記録である。  
理由: `todo.md` を未完了タスク中心に保ち、現在の確認対象を必要最小限に絞るため。

## 退避済みタスク

- [x] [008-0030] [2026-03-08][2026-05-13完了][重要] SecretCore Phase3（同一PC復旧 + 暗号化バックアップによる別PC復旧）を実装する。
  - [完了][2026-05-13] `SecretCore/src/key_manager.rs` に `wrapped_secret` の version / createdAt / integrity 検証付き復元と、`k-user` の export/import / ファイル出力を実装し、復旧時に平文鍵を露出しない経路を確立した。
  - [完了][2026-05-13] `LocalServer/src/server.ts` に `device_db` 退避/復元と `k-user` 暗号化バックアップの API を追加し、`LocalServer/public/settings.html` から管理者認証後に操作できる復旧導線を整備した。
  - [完了][2026-05-13] `IF仕様書.md` / `試験仕様書.md` / `LocalServer/README.md` / `ソース概要.md` / `機能仕様書.md` を更新し、`7068` / `7069` / `7072` / `7073` の試験受け皿と参照索引を揃えた。
  - [厳守] 別PC復旧では `wrapped_secret` 単体に依存しない。理由: `wrapped_secret` は TPM に紐づくため、別PCでは暗号化済みバックアップファイルとパスワードによる復旧経路を正とする。
- [x] [008-0032] [2026-03-09][2026-05-13完了][重要] 高リスクワークフローの結合試験を `試験仕様書.md` へ追加する。
  - [完了][2026-05-13] `試験仕様書.md` に `7040` `runKeyRotationSession()` 完了判定試験、`7041` `runProductionSecureFlow()` 段階実行試験、`7042` `runSignedOtaCommand()` 署名付き OTA 開始試験が揃っていることを確認した。
  - [完了][2026-05-13] `試験記録書.md` に `7040` / `7041` / `7042` の実施済み記録があることを確認し、結合試験の受け皿と結果が 1 対 1 で追跡できる状態へ整理した。
- [x] [008-0034] [2026-03-07][2026-05-13完了][推奨] SecretCore のリリースビルド設定をハードニング構成にする（strip / LTO / panic=abort 等）。
  - [完了][2026-05-13] `IoT/SecretCore/Cargo.toml` に `profile.release` を追加し、`codegen-units=1`、`lto=true`、`panic=abort`、`strip=symbols` を固定した。
  - [完了][2026-05-13] `LocalServer秘密処理別層仕様書.md` と `使用技術仕様書.md` の既存方針とも整合しており、運用配布時の解析面縮小方針をコード側へ反映できた。
- [x] [008-0035] [2026-03-07][2026-05-13完了][推奨] SecretCore の IPC 傍受試験・戻り値検査試験を `試験仕様書.md` に追記する。
  - [完了][2026-05-13] `試験仕様書.md` の `7072` を `SecretCore 境界IF固定試験`、`7073` を `SecretCore IPC 保護試験` として確認し、IPC 傍受・戻り値保護の試験受け皿が存在することを確認した。
  - [完了][2026-05-13] `試験記録書.md` に `7072` / `7073` の OK 記録があり、`SecretCoreFacade` 境界固定と保護付き IPC の成立が試験で追跡できる状態になっていた。

## 変更履歴
- 2026-05-13: 新規作成。理由: `008-0030` を完了退避し、`todo.md` の未完了管理を次段の `008-0031` へ進めるため。
