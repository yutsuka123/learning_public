# IoT TODO

[重要] 未完了タスクのみを管理する。  
理由: 現在の実施対象を明確化するため。

[厳守] 各未完了タスクには固定IDを付与し、形式は `章番号3桁-項目番号4桁` とする。  
理由: 他文書や会話でタスクを安定して参照できるようにするため。

[厳守] タスクの削除、統合、後順化が発生しても既存IDは再採番せず、欠番を許容する。  
理由: 後から番号がずれて、他文書や試験記録、会話ログの参照修正が大量発生することを防ぐため。

## 未完了
### 再開インデックス（本書の読む順）[2026-04-18 更新]
- [正] **いまの作業の正**: 下記「直近ゴール・現在地サマリ（006-0011 完了後）」→「セッション引継ぎ・次の整理ポイント（正）」→ 関連タスク `005-0008` / `007`。
- [最重要][2026-04-27] **直近の章次ゴール**: `006-0011` は完了済み。次は `005-0008` の見直しと `007` 着手条件の再確認を優先する。`011` 章は完了退避済みのため新規着手しない。クラウド化の実タスクは **`012` 章以降**に集約済みのため、クラウド用の新規IDも `011` へ増やさない。
- [重要] **中長期の正**（ローカル第3段階完了後）: `007-A` → `007-B` → `007-C`（4点証跡・安定性セットは `本番セキュア化出荷準備試験計画書.md` を正とする）。
- [重要] **クラウド第4段階**は **`### 012. クラウド連携・第4段階`** と、関連タスク `009-0004` / `010-0013` / `012-0001`〜`012-0005` を参照（旧 `011-0008`〜`011-0011` は移管済み。履歴は `todo_old20260418.md`）。
- [厳守] 詳細な日別経緯の一次記録は `試験記録書.md` / `問題点記録書.md` を正とし、本節は判断と着手順の索引に留める。

### 直近ゴール・現在地サマリ（2026-04-17整理）
- [重要][2026-04-27] **セッション引継ぎ**: 次回は `005-0008` の見直しと `007` 着手条件確認から開始する。`006-0011` は完了記録を正とし、新規着手しない。  
  具体的には `試験記録書.md` の `006-0011` 完了証跡、`本番セキュア化出荷準備試験計画書.md` の解禁条件、`005-0008` の見直し点を順に確認する。
- [重要][2026-04-27] PC再起動後の再開手順: 1. `Wi-Fi_lab` が切断であることを確認 2. `netsh wlan connect name="AP-esp32lab-04CEF94EB580" interface="Wi-Fi_lab"` を実行 3. `netsh wlan show interfaces` で `192.168.4.x` を確認 4. `ping 192.168.4.1` と `curl http://192.168.4.1/api/auth/login` を確認する。`AP-IoTESP32Test` は接続先ではない。
- [追記][2026-04-27] `LocalServer` を再起動し、`SecretCore` を新しい `target/debug/secret_core.exe` で起動し直したうえで、`allowIrreversibleExecution=true` の `006-0011` 再実行が `completed/OK` まで到達した。
- [重要][2026-04-27] **現時点の完了済み**: `006-0009`、`006-0010`、`006-0011`、`006-0015` / `006-0016`、`010-0002`〜`010-0005`、`010-0006`〜`010-0009`、`010-0013`、`010-0015` は完了扱い。
- [重要][2026-04-27] **本日の終了メモ**: `009-0013` は退避済み、`AP` 到達性は復旧済み、`LocalServer` の `publicId` 補完は `MAC` 由来名へ整理済みとする。次回は `005-0008` と `007` の順で、完了できるものから順にクローズする。
- [重要][2026-04-27] `DEV-003` は予備生デバイスとして待機中。`DEV-002` が隔離、故障、復旧不能、または比較個体追加が必要になった場合のみ投入する。
- [最重要][2026-04-23] **当面の直近ゴール**は `007-C` の最終レビュー整理と `004-0006` / `004-0007` の扱い確認、ならびに NVS暗号化の旧仕様化を完了することとする。
- [最重要][2026-04-23] **NVS暗号化案はクローズ済み**。根拠・経緯は `本番セキュア化出荷準備試験計画書.md` 5.6c と `todo_old20260423.md` を参照する。
- [厳守][2026-04-23] **`007-B`（不可逆本体）は No-Go のまま**。案Aが Go にならない限り、および `006` / レビューゲートの条件を満たさない限り、不可逆工程へ進まない。
- [重要][2026-04-07] **`008-0052` は完了済み**。`7041:pipeline:strict=FULL_COMPLETED`、`7101=OK`、パーティション最適化後の `DEV-002` 主要試験は回復済み（証跡は `試験記録書.md` `7101` 2026-04-07 追記および `LocalServer/logs/test-reports/` を参照）。
- [重要][2026-04-23] `nvs_flash_read_security_cfg()` / `nvs_flash_secure_init()` / `nvs_keys` を使う NVS暗号化案は旧仕様としてクローズした。技術検討の履歴は `secureNvsInit` と `todo_old20260423.md` を参照し、現行の実施対象から外す。
- [重要][2026-04-17] **ROM 逼迫（`#0026`）**は案A/C 判定の横断条件のまま。`008-0048` / `008-0049` と並行して監視する。
- [最重要][2026-04-27] **文書・設計の直近スコープ**は `005-0008` の見直しと `007` 着手条件確認を正とする。`011-0001`〜`011-0007` は完了退避済みで、関連設計は `todo_old20260424.md` と `todo_old20260423.md` を参照する。クラウド比較・AWS/Google 表・鍵/クラウド分界は **`012` 章**で扱う。
- [重要] 将来対応タスクは **`020` 章へ集約**し、`013`〜`019` は予約枠として空けておく。
- [最重要][2026-03-25] **中長期の到達目標**は変わらず「`007-C` まで完了し、1台目について出荷レビュー可否または隔離判定可否を判断できる状態」とする。ただし **案A 未確定の間は `007-B` に入らない**。
- [厳守][2026-03-25] `Secure Boot` / `ROM暗号化` / `Flash Encryption` / `eFuse` は一括実行せず、段階ごとに読戻し検証と停止判定を通した場合のみ次段へ進む。
- [厳守] 本番用 `k-iot-secure-boot` / `k-iot-flash-encryption` の実キー生成は、作業当日の直前にオフライン環境で実施し、生成後の滞留を最小化する。
- [重要] 途中失敗時は「残工程再開」ではなく「隔離判定」を優先し、`問題点記録書.md` と `試験記録書.md` に原因・再発防止を残してから再試行する。

#### 直近ゴールの沿革（念のため保存・要約）
- [歴史][2026-04-04〜05] NIC 未接続による broker 経路断、`7099` / MQTT オンライン維持、`DEV-002` パーティション最適化と保全の経緯は `試験記録書.md` / `問題点記録書.md`（`#0023` 等）に記録済み。
- [歴史][2026-04-05〜06] `DEV-002` 16MB パーティション候補A適用、`SecretCore` MQTT 常駐接続化、`7041` AP 到達ブロッカー（`Wi-Fi_lab` 等）の切り分けは `008-0052` 本文および `#0026` に集約。
- [歴史][2026-04-04c] `esp32s3_secure_final` のスタック・ログ強化・`APP_SECURE_FINAL_BUILD` 導入は `platformio.ini` / FW 履歴を参照。
- [補足][2026-04-17] 上記沿革の「次に何をするか」はすべて **案A 評価**へ統合済み。旧「次回手順」で `007-B` 直前まで列挙していた項目のうち未完了のものは、`007-0005` / `7095` / `PC-012`〜`014` など **可逆範囲の整理**として実施可否を都度判断する（不可逆は不可）。

### 手動確認待ち（2026-03-24整理）
- [最重要] 現時点の手動確認待ちはなし（2026-03-24時点）。
- [重要] 手動確認時の主参照先は以下とする。
  - `試験仕様書.md`: `7090` 本試験手順、`7041` 再開条件
  - `試験記録書.md`: `7090` 再開テンプレート、`7041` 再開テンプレート
  - `コマンド仕様書.md`: `ProductionToolSetup.exe` 生成コマンド、`7041` 実行コマンド
  - `問題点記録書.md`: `#0010`（AP未到達）、`#0011`（`ISCC.exe` 未導入）
- [厳守] 手動確認が終わるまで、不可逆処理本体（eFuse / Secure Boot / Flash Encryption の実行）へ進まない。

### セッション引継ぎ・可逆作業のチェックリスト（正）[2026-04-17 整理]
- [最重要][2026-04-07] **案B No-Go**。次の実装判断は **案A**（`最終セキュア化方式選定メモ.md` 8章）。
- [重要][2026-04-07] **`008-0052` 完了済み**（`7041:pipeline:strict=FULL_COMPLETED`、`7101=OK` 等）。2026-04-06 時点の「7041 未回復なら次段へ進むな」指示は **目的は達成済み**（同日以降の追記・`008-0052` 完了欄を参照）。
- [厳守][2026-04-23] **`007-B` は No-Go のまま**。案A（または案C への分岐）の Go/No-Go が確定するまで不可逆に入らない。
- [重要][2026-04-04] `7041` の `expectedMac` は **AP 表示 MAC** を正とする（例: `F0:D0:F9:4E:B5:80`。SoC 表記と逆順にならないよう注意）。
- [重要][2026-04-04] MQTT/broker 側が不安定なときは、**NIC と broker 経路**（例: `172.17.1.100:8883`）を先に確認する（過去の主因: NIC 未接続）。
- [重要][2026-04-06] `app0=0x30000` の間は、必要時の app 書込みは **`esptool write_flash(0x30000)`** を標準とし、plain `platformio -t upload` に依存しない。
- [重要][2026-04-06] `7087:normal` / `7087` / `7041` で `ProductionTool` を使う場合は **既定 Pipe** `\\\\.\\pipe\\iot-secret-core-ipc` の `SecretCore` 待受が必要。
- [重要][2026-04-07] `7041` で AP 経路が詰まる場合は `LocalServer` の **AP 接続アダプタ名**（例: `wifiUsbInterfaceName=Wi-Fi_lab`）と `7041-apdiag-*` 証跡を優先する。
- [重要][2026-04-04] `NVS patternA/patternC` 方針（開発時 plain fallback 可・製品時 secure 固定）は `008-0040` / `本番セキュア化出荷準備試験計画書.md` / `製品化成果概要書.md` を正とする。
- [厳守][2026-04-23] 不可逆の前に `006` 完了条件、`004-0006`/`004-0007` の `007` 直前実施、`005-0008` の見直し、`007-0004`/`007-0006` 4点証跡を満たすこと。NVS暗号化関連は旧仕様へ退避済み。
- [厳守] まず読むファイル（案A・`007` 準備時）: `IoT/todo.md`、`IoT/最終セキュア化方式選定メモ.md`、`IoT/本番セキュア化出荷準備試験計画書.md`、`IoT/試験仕様書.md`、`IoT/試験記録書.md`、`IoT/セキュリティ方針_認証情報取扱い.md`、`IoT/問題点記録書.md`（`#0025`/`#0026`）、`IoT/デバイス管理表.md`。
- [推奨] `7099` / `007-0005` / `PC-012`〜`PC-014` / `7095` は **可逆範囲の整理**として、案A実装と並行で欠落がないか確認する（`試験記録書.md` の最新追記を正とする）。
- [旧コピペ][2026-04-17] 2026-04-04〜06 の「次セッション用コピペ指示」3種は **状況が前進したため集約**した。原文が必要な場合は **git 履歴**（本ファイルの 2026-04-17 より前の版）を参照する。

#### 次セッション用コピペ指示（2026-04-17・集約版）
- [重要] 次セッション開始時は、以下をそのまま貼ってよい。

```text
IoT プロジェクトの継続です。回答は日本語でお願いします。

現在地（2026-04-23 時点の todo.md 集約）:
- `008-0042` は退避済み。最終運用のシリアル方針は `008-0001` の上位方針として残し、`7100` 証跡は `todo_old20260423.md` 側を正とする。
- `007-0012`、`008-0050`〜`008-0052` も退避済み。`007-B` 不可逆はまだ No-Go のまま。
- `008` 章の残件は、`008-0001` / `008-0002` / `008-0003` / `008-0004` / `008-0005` / `008-0006`〜`008-0009` / `008-0010`〜`008-0049` の未完了群。重複や完了済みのものがあれば、根拠を確認して退避側へ移してください。
- [補足][2026-04-26] `008-0006`〜`008-0009` の HMAC は `OTA開始` / `重要設定変更` のコマンド署名であり、NVS暗号化 HMAC 旧仕様とは別物。`7042` は未実施なので、この4件は現時点では残件のまま維持する。
- `009-0004` は将来の `012-0004` / `012-0005` と責務分界を合わせる前提のため、単独完了扱いにしない。
- 文書タスクの直近スコープは `006-0011` の再試行条件確認。`011-0002` は完了退避済みで、クラウド化は `012` 章（`012-0001` 以降）のみで追う。

依頼:
1. 上記の正として IoT/todo.md の「再開インデックス」「直近ゴール・現在地サマリ」「セッション引継ぎ・可逆作業のチェックリスト」を読み、現在の未完了タスクが完了済みと矛盾していないか確認してください。
2. `008` 章では、完了済み・証跡完了済みのものを優先して退避候補にしてください。特に `008-0042` のように上位方針と重複しているものは、退避側を正にしてください。
3. `007-B` に入る判断を求められたら拒否し、案A と 006 ゲートを先に満たすよう促してください。
4. 文書更新時は変更理由・根拠・出典を残してください。完了済みタスクを残す場合は、残す理由を明示してください。
```
- [変更理由][2026-04-17] セッション引継ぎが日付重複で矛盾しやすかったため、**正の一覧**と **単一コピペ**に集約し、クラウド第4段階を同一ファイル内で参照可能にするため。

### 001. 直近フェーズA: OTA安全性の確認（最優先）
- [重要] [念のため保存] 001章の完了タスクは `todo_old20260312.md` へ退避済み。

### 002. 直近フェーズB: ESP32設定永続化とAPメンテナンス
- [重要] [念のため保存] 002章の完了タスクは `todo_old20260313.md` へ退避済み。
- [フェーズB 実施状況][2026-03-14]
  - 完了条件: 002-0001〜002-0004、002-0019〜002-0022、002-0027、002-0028 は文書・コード確認で完了済み。詳細は `todo_old20260313.md`。
  - [完了][2026-03-14] 002-0099 ゲート完了。7027/7064/7065/7066/7067 全 OK、試験で変更したパスワードはデフォルトへ復元済み。退避先: `todo_old20260313.md`。

## 直近実行順序（2026-03-25再整理）
0. [最重要][2026-04-27] `007` 進入判断は `最終セキュア化方式選定メモ.md` の当日状態を必ず併読し、`006-0011` 完了証跡を前提にする。
1. [最優先] `005-0008` の見直し状況を確認し、`007-A` -> `007-B` -> `007-C` の順で `007` 進入条件を満たす。
  - 対象の主軸: `005-0008`, `007-A`, `007-B`, `007-C`, `7019`〜`7024`
2. [厳守] 本番用鍵は `004-0006` / `004-0007` の手順で **`007` 直前** に生成し、担当者・保管先・監査ログを記録する。
3. [厳守] `005-0008` を `006-0011` の完了結果に基づいて見直し、`007` の自動検証観点を確定する。
4. [最重要] フェーズG（本番1台目）は `007-A` -> `007-B` -> `007-C` の順で実施する。
   - 段階1: 実行前最終確認（鍵ID、対象機識別、電源、通信、監査開始）
   - 段階2: 不可逆処理の段階実行（`Flash Encryption` -> `Secure Boot v2` -> `JTAG/DL封鎖`）
   - 段階3: 各段階の読戻し検証と再起動健全性確認（失敗時は即停止）
   - 段階4: `7025` による OTA 継続更新可否の確認
   - 段階5: `7100` による JTAG/シリアル安全確認
   - 段階6: レビューゲートまたは2台目切替条件判定
5. [禁止] 1台目の段階完了と記録確定前に、複数台展開や同時並行実行を行わない。
6. [厳守][2026-03-25] 1台目で `NG` の場合は「即2台目へ進む」のではなく、先に1台目を隔離判定して失敗理由を4点証跡で確定し、その後に2台目着手可否を判断する。

### 直近の章別整理（005 -> 006 -> 007 の見え方整理）
- [重要][2026-03-25] 章番号の並びと、**いま実際に進める順番**は完全一致しない。
- `005` 章:
  - [重要] `005-0001`〜`005-0007` は完了済み。
  - [厳守] 残タスクは `005-0008` のみ。
  - [厳守] `005-0008` は **`006-0011` 完了後、`007` 着手前** に戻って処理する。
- `006` 章:
  - [最優先] `006-A` / `006-B` は完了済み。`006-0011` の完了証跡を正とし、以後は `005-0008` と `007` 着手条件の整理へ移る。
  - [厳守] `006-C` の出口条件を満たした後で、初めて `004-0006` / `004-0007` と `005-0008` へ進む。
- `007` 章:
  - [厳守] `006-0001`〜`006-0011` 完了後のみ着手する。
  - [厳守] 実行順は `007-A` -> `007-B` -> `007-C` 固定。
  - [厳守] `007-B` の不可逆段階順は `FE -> NVS(HMAC) -> SB -> 封鎖` とし、`RD_DIS` は HMAC 鍵投入後に扱う。
- [最重要][2026-04-27] したがって、**現時点の実行順**は次の 3 ステップで整理する。
  1. `005-0008` を見直して自動検証観点を確定する。
  2. `004-0006` / `004-0007` を `007` 直前に実施する。
  3. `007-A` -> `007-B` -> `007-C` へ進む。
7. [厳守][2026-03-25] 各段階の成功可否は「判定ログセット」で判定する（`ProductionTool` 判定ログ / ESP32 シリアルログ / `espefuse summary before-after` / `試験記録書.md` 反映）。
- [厳守] 既存IDは固定のため再採番しない。実施順は本節を正とし、章番号は歴史的なまとまりとして扱う。

### 003. 直近フェーズC: LocalServer OTA実行面の整備
- [重要] [念のため保存] 003-0001, 003-0002, 003-0003, 003-0008, 003-0011, 003-0015, 003-0018 は todo_old20260314.md へ退避済み。
- [フェーズC クローズ][2026-03-19] フェーズC の実施可能な試験は一通り完了した。003-0020〜003-0023（7081/7082/7083/7084）は完了済み。003-0024（7085）は総合進入判定ゲートであり、前提条件（004-0010, 009-0019, 005-0001〜005-0005, 004-0001〜004-0005）が未達のため「未達・進入不可」のまま。フェーズC としての試験実施は完了とする。次はフェーズD（004-0008〜004-0010, 005-0001〜005-0005）の推進で効率的に進める。
- [完了タスク退避][2026-03-24] `003-0004`, `003-0013`, `003-0014`, `003-0019`, `003-0020`, `003-0021`, `003-0022`, `003-0023`, `003-0024` は `todo_old20260324.md` へ退避済み。
### 004. 直近フェーズD: ProductionTool 基本機能の安定化と試験用鍵・署名素材・証明書の整備
- [実行順序][2026-03-16] 本章は前半の `004-0008`〜`004-0010` で `ProductionTool` 基本機能の実装・試験・安定化を先行し、その後に後半の `004-0001`〜`004-0007` で試験用鍵・署名素材・証明書手順を固定する。理由: `ProductionTool` の基本導線が固まる前に鍵手順だけを先行確定すると、後で実装都合の戻りが出やすいため。
- [変更方針][2026-03-22][重要] `004-0006` / `004-0007` は「フェーズDの準備文書・運用ルール」ではなく「フェーズF実行直前の運用タスク」として扱う。未完了のまま残すが、直近の「準備完了」スコープでは前倒し実施しない。理由: 本番用秘密素材は生成タイミングを実行直前へ寄せた方が安全であり、早期生成しても準備品質は上がらないため。
- [完了タスク退避][2026-03-24] `004-0001`, `004-0002`, `004-0003`, `004-0004`, `004-0005`, `004-0008`, `004-0009`, `004-0010` は `todo_old20260324.md` へ退避済み。

#### 004-A. 実施順固定
- [厳守][2026-03-25] `004` の残タスクは以下の順番で扱う。  
  `004-0006` -> `004-0007`
- [重要] ただし両方とも「本番用秘密素材の直前生成」であり、**実施タイミングは `006` の試験機リハーサル完了後、`007` 着手直前** とする。
- [禁止] `006` のリハーサル完了前に本番用秘密素材を前倒し生成しない。理由: 鍵の滞留期間を増やし、運用事故面積を広げるため。
- [ ] [004-0006] [2026-03-08][厳守] 本番用 `k-iot-secure-boot` は eFuse 書込み直前にオフライン環境で生成し、秘密鍵の保管先・責任者・バックアップ先を記録する。
- [ ] [004-0007] [2026-03-08][厳守] 本番用 `k-iot-flash-encryption` は本番書込み直前に生成し、実値をリポジトリや `sensitiveData.h` に残さないことを確認する。

### 005. 直近フェーズE: ProductionTool と AP 画面分離の実装
- [実行順序][2026-03-16] 本章は前半 `005-0001`〜`005-0005` をフェーズDの `ProductionTool` 基本機能整備として先行し、後半 `005-0006`〜`005-0008` を `004-0001`〜`004-0007` の鍵手順固定後に進める。理由: 先に UI / 認証 / 対象機確認 / ウィザードを固め、その後に不可逆処理の中身を差し込む方が手戻りが少ないため。
- [変更方針][2026-03-22][重要] 後半は `005-0006` / `005-0007` の設計確定を先行し、`005-0008` は実機実施を伴う有効確認のため保留とする。理由: フェーズDでは「前準備固定」までを完了条件とし、実行後の有効確認はフェーズFのリハーサルと一体で扱うため。
- [完了タスク退避][2026-03-24] `005-0001`, `005-0002`, `005-0003`, `005-0004`, `005-0005`, `005-0006`, `005-0007` は `todo_old20260324.md` へ退避済み。

#### 005-A. 実施順固定
- [厳守][2026-03-25] `005` の残タスクは `005-0008` のみとする。
- [重要] `005-0008` は `006` の試験機リハーサル結果を受けて設計を詰める項目であり、**`006-0011` の後、`007` 着手前に見直す**。
- [禁止] 試験機での読戻し結果や OTA 継続確認なしに、机上だけで `005-0008` を完了扱いにしない。
- [ ] [005-0008] [2026-03-09][重要] ProductionTool 実行完了後の自動検証を設計する。
  - Secure Boot 有効確認
  - Flash Encryption 有効確認
  - 署名済みファームウェア起動確認
  - OTA 継続可否確認
  - 出荷可否判定ログ保存
  - [保留][2026-03-22] 本項は実行結果の有効確認であり、試験機への不可逆処理実施と読戻し結果が必要なため、現段階では着手しない。フェーズFの `006` 章リハーサル/試験と一体で再開する。
- [フェーズE 整理結果][2026-03-22] `005-0001`〜`005-0007` は完了済み。`005-0008` はフェーズFリハーサルと一体で扱う保留項目のため、現時点の「フェーズEまで終わらせる」は達成済みとする。

### 006. 直近フェーズF: Secure Boot / ROM暗号化 / Flash Encryption / eFuse試験
- [厳守][2026-03-22] 本章のうち `006-0002` 以降の着手条件は `003-0024` の進入判定条件充足と `7085` の進入可判定、および `005` 章の ProductionTool 導線と `004` 章の試験用鍵手順が揃っていること。`006-0001` のチェックリスト整備自体はこの差し止め対象に含めない。理由: 進入判定ゲートの固定と不可逆処理本体の開始を分離し、準備文書だけを先行完了できるようにするため。
- [完了タスク退避][2026-03-24] `006-0001` は `todo_old20260324.md` へ退避済み。
- [厳守][2026-03-24] `006` 章では「試験機での段階実行手順の確定」までを目的とし、本番対象機の最終有効化は `007` 章へ分離する。
- [進捗][2026-03-25] `7085` は `FULL_COMPLETED` 証跡で進入可へ更新済み。非不可逆範囲として `7019` / `7020` / `7021` / `7023` を先行完了し、`7022` / `7024` は保留。
- [厳守][2026-03-25] 不可逆処理本体（Secure Boot / Flash Encryption / eFuse 実書込み）は未実施を維持し、先に未完の非不可逆項目（`7022` / `7024`）を完了させる。

#### 006-A. 実施順固定（不可逆リハーサル開始前）
- [厳守][2026-03-25] `006` は以下の順番で進める。  
  `準備` -> `実行保護` -> `開始判定` -> `非不可逆試験完了` -> `試験機で不可逆リハーサル開始` -> `試験結果記録`
- [厳守][2026-04-23] `006` 章の着手順は次の順で固定する。  
  1. `006-0009`
  2. `006-0003`
  3. `006-0004`
  4. `006-0011`
- [理由][2026-04-23] 先に実行保護と開始判定を固め、その後に試験結果を反映し、最後に不可逆リハーサルへ進めるため。`006-0010` と `006-0015` / `006-0016` は完了退避済みのため、この順には含めない。
- [禁止] `7022` / `7024` 未完了のまま、試験機の不可逆リハーサルへ進まない。理由: 非不可逆で潰せる不安要素を残したまま試験機を消費しないため。

#### 006-B. [注意][試験機で不可逆リハーサル開始] ここから下は試験機に対する不可逆工程を含む
- [注意][不可逆][2026-03-25] ここから先は試験機であっても `Secure Boot` / `Flash Encryption` / eFuse 実書込みを含むため、対象機の切替不能リスクを伴う。
- [注意][不可逆][2026-03-25] 実行前に「対象機識別」「読戻し手順」「停止条件」「試験記録書の記入場所」を確認すること。
- [重要][2026-04-27] `006-0011` では文鎮化時の復旧素材（firmware / LocalServer backup / NVS raw / restore memo）と起動スナップショット（`resetReason` / `runningPartition` / `bootPartition` / `health` / `login` / `production state`）を必須化する。理由: 同一ボード復旧と代替ボード再構築を混同せず、連続文鎮化時の差分比較点を残すため。
- [追記][2026-04-27] AP 側 `health` と `login` は `admin / esp32adm2718` で `OK` を確認済み。`LocalServer` 管理者パスワード (`localAdmin-ChangeMe`) は AP 側では使わない。理由: 認証系の取り違えを再発防止し、`006-0011` 再開時の前提を明確にするため。
- [完了][2026-04-27] 006-0003 eFuse 本番適用前の同一手順・同一ファームウェア系統リハーサルを、既存の `7024` / `7041` / AP health-login 証跡で充足済みと判定した。結果は `試験記録書.md` に反映済み。理由: `006-0004` に進む前提を明確にするため。
- [完了][2026-04-27] 006-0004 `k-iot-secure-boot` / `k-iot-flash-encryption` の eFuse 書込みリハーサルを、既存の `EF-001`〜`EF-006` と `SECURE_BOOT_DIGEST0` / 封鎖 eFuse 証跡で充足済みと判定した。結果は `試験記録書.md` に反映済み。理由: `006-0011` の不可逆実施前に、試験機リハーサルの証跡を閉じるため。
- [完了][2026-04-27] [006-0011] eFuse・セキュアブート・ROM暗号化試験を実施し、OTA可否、FW制御によるシリアル出力制約、DL/UL可否を `試験記録書.md` へ記録した。`allowIrreversibleExecution=true` の明示許可で `stage_execute` を通過し、`completed/OK` を確認済み。
  - [記録][2026-04-27] `SecretCore` に `allowIrreversibleExecution` を追加し、`stage_execute` の受理と `final_judgement` の解禁分岐を実装した。`LocalServer` の入力検証と `006-0011` 用のコマンド/計画書も更新済み。
  - [記録][2026-04-27] `LocalServer` 再起動後、`SecretCore` を新しい `target/debug/secret_core.exe` で起動し直し、`006-0011` の再実行が `completed/OK` まで到達した。
  - [厳守] `006` 章で得た復旧素材と証跡は `007` 章の再利用判定に引き継ぐ。

#### 006-C. `007` 着手前の出口条件
- [厳守][2026-03-25] `006` の完了条件は、試験機リハーサル結果と読戻し結果が揃い、`007` へ安全に進めると判断できること。
- [重要] `006` 完了後に `004-0006` / `004-0007` を実行し、その後に `005-0008` と `007` 章へ進む。

### 007. 直近フェーズG: eFuse最終化とOTA再更新
- [厳守][2026-03-16] 本章は `006-0001`〜`006-0011` 完了後のみ着手する。理由: リハーサル未了のまま本番 fuse 適用へ進むことを禁止するため。
- [厳守][2026-04-25] `007` 着手前の最終確認は `006-0011` の試験機リハーサル完了を必須とする。理由: `006-A` の出口条件が閉じていないまま `007` に進むと、不可逆工程の前提が崩れるため。

#### 007-A. 実施順固定（不可逆開始前）
- [厳守][2026-03-25] `007` は以下の順番で進める。  
  `事前準備` -> `4点証跡準備` -> `007-B進入前安定性/連携確認` -> `不可逆開始` -> `FE` -> `SB` -> `封鎖` -> `最終OTA確認` -> `JTAG/シリアル安全確認` -> `レビューゲート` -> `007クローズ判定`
- [禁止] 順番を前後させない。理由: 不可逆工程の順序誤りは復旧不能または原因不明化につながるため。
- [ ] [007-0004] [2026-03-24][厳守] 各段階で `ProductionTool` 判定ログ、ESP32 シリアルログ、`espefuse summary` before/after、`試験記録書.md` の **4点証跡** を同時に確定する。
  - [禁止] 証跡未確定のまま次段階へ進まない。
  - 理由: 失敗時の原因切り分けを即時可能にするため。
- [ ] [007-0006] [2026-03-25][最重要] 1台目で `007` クローズ可否を判断するため、段階ごとに「成功判定ログセット」を必須収集する。
  - `ProductionTool` 判定ログ（段階ID、実行者ID、端末ID、結果）
  - ESP32 シリアルログ（開始時刻、段階境界、readback要点、最終状態）
  - `espefuse summary` の before/after（同一ポート・同一対象機で取得）
  - `試験記録書.md` への当日追記（成功/失敗、停止理由、次アクション）
  - [厳守] 4点のうち1点でも欠落した段階は「未完了」扱いとする。
- [ ] [007-0011] [2026-03-25][最重要] 不可逆開始直前に、`ESP32-S3` SoC仕様、関連文書、今後フロー整合、`STA` / `AP` / `OTA` 安定動作を再確認する。
  - [厳守] 再確認対象は `本番セキュア化出荷準備試験計画書.md`、`試験仕様書.md`、`試験記録書.md`、`eFuse適用チェックリスト.md`、`使用技術仕様書.md`、`OTA仕様書.md` とする。
  - [厳守] `RD_DIS`、`BLOCK_KEY` 制約、`DIS_DOWNLOAD_MODE` / `ENABLE_SECURITY_DOWNLOAD`、`DIS_USB_SERIAL_JTAG` 未適用方針に矛盾がないことを確認する。
  - [厳守] 再確認では `STA` 通常モード疎通、`AP` 到達性、`OTA` 継続更新可否を観測し、追加で約 `2` 時間を見込む。
  - [禁止] 「直前まで正常だった」だけを根拠に再確認を省略しない。理由: セッション跨ぎ・設定差分・AP 到達性揺らぎを見落とすと不可逆工程で止められないため。
- [ ] [007-0013] [2026-03-26][要確認] 出荷後の更新経路を `OTA` 主経路へ一本化するための最終条件を確定する。
  - [厳守] 「OTA運用は維持しつつシリアル経路は制約付きで残す」現方針と、「OTA専用（シリアル更新経路を閉じる）」方針を比較して採否を決める。
  - [重要] `OTA` 自体は eFuse 未適用でも実現可能だが、改ざん耐性・更新経路制限は別論点のため、`Secure Boot` / `Flash Encryption` / DL制御方針と一体で判断する。
  - [厳守] `OTA専用` を採用する場合は、十分な OTA 連続試験とロールバック確認を先に完了し、復旧不能リスクを許容した上で不可逆設定を判断する。
  - [理由][2026-03-26] 更新経路の最終方針を曖昧にしたまま不可逆工程を進めると、出荷後保守と障害復旧の整合が取れなくなるため。

#### 007-B. [注意][不可逆開始] ここから下は eFuse 書込みを含む不可逆工程
- [注意][不可逆][2026-03-25] ここから先は、各項目の実行前に「対象機確認」「4点証跡開始」「NVS秘密バックアップ取得済み」を確認すること。
- [注意][不可逆][2026-03-25] 各段階の直後に `読戻し -> 再起動 -> 4点証跡確定 -> 安定性確認セット` を完了しない限り、次段階へ進まないこと。
- [ ] [007-0001] [2026-03-09][重要] `ProductionTool` から本番対象機へ不可逆段階を順次実施し、各段階の出荷可否判定ログを保存する。
  - [段階1][不可逆] Flash Encryption 系（FE鍵投入、`SPI_BOOT_CRYPT_CNT`、関連eFuse、暗号化済みバイナリ書込み）
  - [段階2][不可逆] Secure Boot v2 系（SB署名鍵ダイジェスト投入、`SECURE_BOOT_EN`、未使用slot revoke、`RD_DIS` write-protect、署名済みバイナリ書込み）
  - [段階3][不可逆] JTAG/DL封鎖（`DIS_PAD_JTAG`、`DIS_USB_JTAG`、最後に `ENABLE_SECURITY_DOWNLOAD`）
  - [厳守] `DIS_USB_SERIAL_JTAG` は本計画では焼かない（FWログ制御を維持するため）。
  - 詳細: `本番セキュア化出荷準備試験計画書.md` 6.4 参照
  - [進捗][2026-03-27] 段階3(SB) を `BLOCK_KEY1` digest / `SECURE_BOOT_EN` / `SECURE_BOOT_KEY_REVOKE1/2` / `RD_DIS` write-protect / 署名済み+暗号化済みバイナリ書込みまで実施した。シリアルログで `secure boot verification succeeded`、`7083(iterations=2)=OK`、`7041:pipeline:strict=FULL_COMPLETED` を確認。
  - [進捗][2026-03-27] 段階4(封鎖) として `DIS_PAD_JTAG`、`DIS_USB_JTAG`、`ENABLE_SECURITY_DOWNLOAD` を実施した。`DIS_USB_SERIAL_JTAG` と `DIS_DOWNLOAD_MODE` は未変更のまま維持した。
  - [重要][2026-03-27] 最終起動ログで `secureNvsInit::initializeSecureNvs: NVS Encryption not enabled in sdkconfig (patternA)` を検出したため、`問題点記録書.md` #0021 を追加した。不可逆処理自体は完了したが、`007-C` の最終レビューはこの論点整理後に閉じる。
- [ ] [007-0009] [2026-03-25][最重要] 各段階（FE/SB/封鎖）の完了判定前に、安定性確認セット（OTA 2回連続 + APモード10分 + 通常モード10分）を実施する。
  - [厳守] いずれかで `abort`、再起動ループ、AP到達不能、MQTT再接続失敗、OTA失敗を検知した場合は即停止する
  - [厳守] 停止時は `問題点記録書.md` と `試験記録書.md`（7099）へ記録し、原因確定まで次段へ進まない
  - [重要] 文鎮化リスクの早期検知を目的とし、成功証跡は 4点証跡（007-0006）に追加で添付する
  - [進捗][2026-03-27] 段階3後の確認として `7083(iterations=2)=OK`、`maintenance-reboot` 後の `7041:pipeline:strict=FULL_COMPLETED` までは取得済み。最終段階後の通常10分/AP10分相当の整理と `7099` 反映は残る。

### 008. 直近ゴール後のセキュリティ拡張
#### 008-A. 方針・文書整理
- [ ] [008-0001] [2026-03-09][厳守] シリアル機能停止は eFuse ではなく FW 制御で管理する方針を実装へ反映する。
  - 量産通常FWでは高詳細ログと危険な保守コマンドを停止または最小化する
  - シリアル停止のみを目的に eFuse を焼かない
  - 問題解析時のみ署名済み診断用FWで一時的にシリアル出力を有効化できるようにする
- [ ] [008-0002] [2026-03-09][重要] 通常運用FWと診断用FWの差分方針を `機能仕様書.md` または専用仕様へ整理する。
  - 出力可能ログ種別
  - 許可する保守コマンド
  - 切替条件
  - 顧客環境で恒久運用しない条件
- [ ] [008-0003] [2026-03-09][重要] インターネット接続時の最低限セキュリティ基準を確定する。
  - 不要待受停止
  - 未使用ポート閉鎖
  - 管理ポート非公開
  - Topic ACL / API 認可
  - 認証失敗バックオフ / 一時遮断
  - 監査ログ / アラート
  - 本プロジェクト要件外の汎用プロキシ / 中継 / ファイル共有 / ポートフォワード禁止
- [ ] [008-0004] [2026-03-09][重要] Mosquitto / LocalServer / ルータ / Firewall の各設定で、踏み台化防止の最低限対策を具体化する。
  - Broker 管理ポートの管理ネットワーク限定
  - 送信先制限
  - API 公開面の最小化
  - 認証失敗抑止
  - ログ保存先
- [ ] [008-0005] [2026-03-09][重要] `試験仕様書.md` の 7029〜7033 を実施し、シリアル制約、未使用ポート閉鎖、認証失敗抑止、踏み台化防止確認を `試験記録書.md` へ反映する。

#### 008-B. 実装・統合
- [ ] [008-0006] [2026-03-07][重要] OTA開始コマンドに HMAC 署名を付与し、ESP32側で検証する実装を追加する（SecretCore Phase1完了後）。
  - 対象: `call/otaStart` コマンド
- [ ] [008-0007] [2026-03-07][重要] 重要設定変更コマンドに HMAC 署名を付与し、ESP32側で検証する実装を追加する（SecretCore Phase1完了後）。
- [ ] [008-0008] [2026-03-07][重要] `試験仕様書.md` に OTA HMAC 検証試験（正常/不正署名の両ケース）を追記する。
- [ ] [008-0009] [2026-03-08][重要] OTA + SecretCore の結合テストを実施する。
  - HMAC署名付き OTA 開始
  - ESP32 側検証
  - OTA 実行
  - `done` 通知受信
  - 不正署名拒否確認
- [ ] [008-0010] [2026-03-07][厳守] SecretCore の IPC ハンドシェイクは、子プロセス spawn + stdin/stdout 渡しを優先実装する（Pipe への外部割り込み排除）。
- [ ] [008-0011] [2026-03-07][厳守] IPC の nonce は必ず `crypto.randomBytes(16)` で生成する。`Math.random()` の使用を絶対に禁止する。
- [ ] [008-0012] [2026-03-08][厳守] `wrapped_secret` 以外の秘密（`S_random` / `k-user` / `k-device`）をファイル保存しないことを実装レビューで確認する。
- [ ] [008-0013] [2026-03-08][重要] `wrapped_secret` 破損検知（version / createdAt / 整合性チェック）を実装する。
- [ ] [008-0014] [2026-03-07][重要] TS 層で IPC から受け取った署名/暗号 payload は即座に送信し、変数保持時間を最短にする。
- [ ] [008-0015] [2026-03-07][重要] 監査ログは Phase1 からパーミッション制限（0600）+ OS ログ（Windows イベントログ / macOS syslog）への二重書き込みを実装する。
- [ ] [008-0016] [2026-03-07][重要] HMAC 未実装期間（SecretCore Phase1完了前）は、OTA を信頼ネットワーク内限定とする旨を `取扱説明書.md` に明記する。
- [ ] [008-0017] [2026-03-07][重要] SecretCore Phase1（Windows）を実装する。
  - `S_random` CSPRNG 生成
  - TPM ラップ/アンラップ
  - `wrapped_secret` 保存/読出し
  - IPC サーバー（Named Pipe + X25519 DH + AES-256-GCM + nonce）
  - `initialize` / `isInitialized` / `signCommand`（OTA用）
  - zeroize によるメモリゼロ化
- [ ] [008-0018] [2026-03-07][重要] TS側 SecretCoreClient を実装する（IPC接続・暗号化通信・workflow 起動要求・進捗購読）。
- [ ] [008-0019] [2026-03-09][重要] `LocalServer` に高リスクワークフロー公開 API を実装する。
  - `POST /api/workflows/pairing/start`
  - `POST /api/workflows/key-rotation/start`
  - `POST /api/workflows/production/start`
  - `POST /api/workflows/signed-ota/start`
  - `GET /api/workflows/{workflowId}`
  - [厳守] `workflowId`、`state`、`result`、`errorSummary` 以外の中間秘密を返さない
- [ ] [008-0020] [2026-03-09][重要] `SecretCore` の高リスクワークフロー実行基盤を実装する。
  - workflowId 付与
  - 状態管理
  - 進捗イベント配信
  - 完了判定
  - 監査ログ連携
- [ ] [008-0021] [2026-03-09][重要] 高リスクワークフローの正規進捗状態 `queued` / `running` / `waiting_device` / `verifying` / `completed` / `failed` を実装し、`LocalServer` UI へ表示する。
- [ ] [008-0022] [2026-03-09][重要] `LocalServer` 側の高リスク処理を、逐次オーケストレーション方式から「開始要求 + 進捗表示 + 結果表示」方式へ改修する。
  - `/api/workflows/...` 以外で高リスク処理を直公開しない
  - [禁止] `POST /api/pairing/bundle` のような内部 helper 直公開 API を作成しない
- [完了タスク退避][2026-03-24] `008-0023`, `008-0024` は `todo_old20260324.md` へ退避済み。
- [ ] [008-0026] [2026-03-09][重要] `SecretCore` に `runSignedOtaCommand()` を実装する。
  - 署名付き OTA 開始指示
  - 送達確認
  - 検証結果確認
  - 完了判定
- [ ] [008-0038] [2026-03-26][重要] AP ロールパスワード「変更可能ON」の実装可否を、工場出荷状態復帰機能とセットで評価する。
  - [厳守] 単独で「変更可能ON」だけ先行実装しない。理由: 変更後に復帰不能だと保守不能・量産停止リスクが高いため。
  - [評価観点] セキュリティ効果、運用負荷、誤操作時復旧時間、監査証跡、現地保守性
  - [成果物] `機能仕様書.md` / `試験仕様書.md` / `試験記録書.md` / `取扱説明書.md` への反映可否判断
- [ ] [008-0039] [2026-03-26][重要] 工場出荷状態へ戻す機能（認証情報・設定復帰）の要件定義を作成し、`008-0038` と同時にレビューする。
  - [厳守] 対象範囲（APロール認証、ネットワーク設定、運用フラグ）と保持範囲（個体識別子・不可逆設定）を分離して定義する。
  - [禁止] 不可逆 eFuse 状態の巻き戻しを要求仕様に含めない。理由: ハード仕様上不可能であり誤期待を生むため。
- [ ] [008-0041] [2026-03-27][重要] `ProductionTool` GUI 化の要件定義と方式選定を行い、CLI 骨格との対応表を作成する。
  - 起動画面、追加認証、対象機確認、工程ロック、dry-run、安全停止、監査ログ確認を GUI へ写像する
  - [厳守] GUI 化は安全条件の緩和を意味しない
- [ ] [008-0043] [2026-03-27][重要] `ProductionTool` GUI に、`製造・セキュア化手順書.md` どおりの工程ロック、対象機再確認、4点証跡導線、`デバイス管理表.md` 連携を実装設計する。
- [ ] [008-0029] [2026-03-09][重要] AP 共通トップ画面へ `shortId`、`public_id`、Production対応/非対応、eFuse、Secure Boot、Flash Encryption、シリアル状態を表示する。
#### 008-C. 画面・評価・将来
- [ ] [008-0030] [2026-03-08][重要] SecretCore Phase3（同一PC復旧）を実装する。
  - `wrapped_secret` バックアップ/復元
  - `device_db` バックアップ/復元
  - settings.html に復旧UI追加
- [ ] [008-0031] [2026-03-07][推奨] SecretCore Phase4（完全化）を実装する。
  - 鍵再発行
  - HMACチェーン付き監査ログ
  - `k-device` 導出とペアリング連携
- [ ] [008-0032] [2026-03-09][重要] 高リスクワークフローの結合試験を `試験仕様書.md` へ追加する。
  - `runPairingSession()`
  - `runKeyRotationSession()`
  - `runProductionSecureFlow()`
  - `runSignedOtaCommand()`
  - 進捗状態遷移
- [ ] [008-0033] [2026-03-09][重要] `試験仕様書.md` の 7044〜7049 を実施し、画像更新、証明書更新、ログ保持、ログ自動削除を `試験記録書.md` へ反映する。
- [ ] [008-0034] [2026-03-07][重要] SecretCore のビルド設定を難読化構成にする（obfstr + LLVM O3 + LTO + strip + panic=abort）。
- [ ] [008-0035] [2026-03-07][推奨] SecretCore の IPC 傍受試験・戻り値検査試験を `試験仕様書.md` に追記する。
### 009. ESP32 / LocalServer の後続整備
#### 009-A. 起動・保存基盤
- [ ] [009-0001] [2026-03-07][重要] LocalServer（TypeScript/Node.js）をTask Scheduler（Windows）でPC起動時自動起動する導入手順を `コマンド仕様書.md` に追記し、実機確認する。
#### 009-1000番台: LocalServer SQLite統合（009-0002〜009-0006 に沿う順序で実施）
- [ ] [009-0002] [2026-03-07][重要] `LocalServer` へSQLite保存（既定10日、0=無期限）を統合実装する。
  - [重要] 実施順序: (1) SQLite保存実装 (2) フィルタ付き平文エクスポートAPI（`DB仕様書.md` のコマンド/サブコマンド/デバイスNo/期間/AND/OR） (3) 自動保存ジョブ (4) 試験仕様・試験記録反映
- [ ] [009-0003] [2026-03-07][重要] `LocalServer` の自動保存処理を Task Scheduler（Windows）起動運用に組み込み、導入手順を `コマンド仕様書.md` に追記し、実機確認する。
- [ ] [009-0004] [2026-03-07][重要] `LocalServer` のクラウド連携モード（双方向同期）の最小I/Fを設計し、API契約を文書化する。
- [ ] [009-0005] [2026-03-07][推奨] `LocalServer` のデータ保存期間設定（30/90/365日）と削除API仕様を `機能仕様書.md` に追記する。
- [ ] [009-0006] [2026-03-08][重要] `LocalServer` とデバイスの双方向テストを実施し、状態確認・指令・保存結果を `試験記録書.md` へ反映する。

#### 009-B. 秘密管理・ペアリング
- [ ] [009-0008] `鍵管理初期セットアップ_実装たたき台.md` のPhase1～4に担当・期限を割り当てる。
- [ ] [009-0009] Phase1（`S_random` 生成 + TPM ラップ + `wrapped_secret` 保存）の最小実装を先行着手する（→ SecretCore Phase1と統合）。
- [ ] [009-0010] Phase2（初回ペアリングAPモード）のUI/CLI手順を確定する。
- [ ] [009-0011] 認証ロジック公開範囲と非公開範囲の判定基準を確定する。
- [ ] [009-0012] サーバ側で `k-device = HKDF(ikm=k-user, salt=SHA256(base_mac), info="k-device-v1")` 導出処理を実装する。
- [完了タスク退避][2026-04-25] `009-0013` は `todo_old20260425.md` へ退避済み。
- [ ] [009-0014] MQTTトピックを `device/<public_id>/wifi/update` / `confirm` へ統一する。
- [ ] [009-0015] 機密設定（Wi-Fi/MQTT/HTTPS認証情報）の保存先を `sensitiveData.json` から NVS へ移行する（平文ファイル運用を廃止）。
- [ ] [009-0016] MQTT接続設定を最終運用値へ移行する（TLS有効 + 8883 + ID/Password必須 + allow_anonymous無効）。
- [ ] [009-0017] ESP32側でAES-256-GCM復号処理（nonce/tag検証付き）を実装する。
- [ ] [009-0018] 初回ペアリングAPモード（物理操作必須、短時間有効）を実装する。
- [完了タスク退避][2026-03-24] `009-0019` は `todo_old20260324.md` へ退避済み。
- [ ] [009-0020] [2026-03-09][重要] ESP32 の `current/previous` key 2 スロット運用と稼働時間 48h / 240h 管理を実装する。
  - `previousKeyState = grace / expired-retained / deleted`
  - 起動後5分で1回、その後1時間ごとに runtime を NVS へ記録
  - 240h 経過後は previous key 自動削除
- [ ] [009-0021] AP復旧モード（WPA2/固有パスワード/Web UI）を実装する。
- [ ] [009-0022] Wi-Fi誤設定試験（自動ロールバック）を試験仕様書へ追加する。
- [ ] [009-0023] ESP側でWi-Fi設定更新ステートマシン（未確定保存/接続試行/CONFIRM/ロールバック）を実装する。
- [ ] [009-0024] 時刻サーバー設定（`timeServerUrl`/`timeServerPort`/`timeServerTls`）の取得元を `sensitiveData.h` 優先から NVS 優先へ移行する。

#### 009-C. インストーラ/アンインストーラ EXE 化（本番・客先環境の要件最小化）
- [ ] [009-1020] [2026-03-15][重要] LocalServer 用インストーラ/アンインストーラを EXE 化する。
  - [厳守] Inno Setup 等で単一 EXE 化（アンインストーラはインストール時に自動生成される構成）。インストーラ・アンインストーラともに Python 不要でビルド・実行可能とする。
  - [推奨] Node.js ランタイムをバンドルするか、Node.js 単体 EXE（SEA）検証により、顧客環境に Node.js 未インストールでも稼働する構成を優先する。
  - [厳守] 実行は管理者権限（UAC）必須とし、Windows 11 基準とする。
  - [重要] ファイアウォール許可（ポート 3100 / 4443 等）や Task Scheduler 登録を自動化するが、裏で勝手に行わずウィザード画面でユーザーに一つずつ確認・同意させる方式とする。
  - [重要] アンインストール時の安全消去（`安全消去ポリシー.md`）とファイアウォール・タスク解除を EXE 内の [UninstallRun] 処理等で実装する。
  - 詳細は `インストーラEXE化設計仕様書.md` を参照する。

### 010. ネットワーク・インフラ
[進捗][2026-04-21] `LocalServer` と `ESP32` の MQTT / OTA 接続経路を DNS / IP 併用へ戻し、`SENSITIVE_MQTT_FALLBACK_IP` などの補助経路を再利用可能にした。理由: DNS 単独は危険なため、DNS 不達時でも IP で接続できるようにして実運用を安定化するため。残作業: 実機の再接続試験、`試験記録書.md` / `問題点記録書.md` への反映。
- [ ] [010-0001] [2026-03-07][重要] [進行中] 新ルータ（GW `172.17.1.1`）導入を実施し、CoreDNS `172.17.1.100` / DHCP `172.17.1.200+` を適用する。
  - [進捗][2026-03-13] 一部完了。GW=`172.17.1.1`、サーバー=`172.17.1.100`、DHCP=`172.17.1.200+` の運用を開始済み。
- [ ] [010-0014] ブローカ運用ネットワークを IoT専用LAN（Private）へ分離し、FirewallのRemoteAddressをサブネット限定する。

### 011. 文書・設計管理
- [重要][2026-04-24] 完了済みの `011-0001` / `011-0002` / `011-0003` / `011-0004` / `011-0005` / `011-0006` / `011-0007` は `todo_old20260424.md` に退避済み。`011` 章の未完了はなし。  
  ただし `005`〜`010` 章には後続 backlog が残っているため、`todo.md` 全体としては未完了が存在する。
- [補足][2026-04-27] `005`〜`011` を再点検したが、追加でクローズできる新規項目はなかった。`006-0011` の再開条件が未達のため、`005-0008` と `007` 章以降の残件は現状維持とする。

### 005〜010 の扱い（2026-04-24 整理）
- [重要] ローカル環境のセキュア化は一段落したため、`005`〜`010` 章は「今すぐの本線」ではなく「後続 backlog」として扱う。
- [重要] 並び順は次を正とする。  
  1. `006-0003` / `006-0004` / `006-0011`  
  2. `005-0008`  
  3. `007-0004` / `007-0006` / `007-0009` / `007-0011` / `007-0013`  
  4. `008-0048` / `008-0049`  
  5. `008-0046` / `008-0047`  
  6. `008` 章の残り（実装・試験・将来対応）  
  7. `009` 章の残り  
  8. `010` 章の残り  
- [補足] `005`〜`010` の詳細タスクは残すが、着手順はこの上位索引を正とする。`005-0008` は `006` の結果を踏まえてから着手し、`007` の前に再確認する。

### 012. クラウド連携・第4段階[2026-04-18]
[最重要] クラウド化に関する **未完了タスクは本章（`012-xxxx`）以降にのみ** 追加する。理由: 直近は `011-0007` までを片付ける方針とし、章境界をブレさせないため。

[重要] クラウド関連の将来対応タスクは `012` 章で扱う。`020` 章はそれ以外の将来対応を集約する。

[重要] `設計概要.md` の第4段階（Cloud で認証・書換管理）に向け、**ローカル第3段階（案A確定・`007` 完了）の後**で順に着手する。理由: ブローカ接続・デバイス秘密・不可逆手順が未固定のままクラウドへ移すと、試験不能・巻き戻し不能な不整合が増えるため。

#### ロードマップ（D4-0〜D4-4）
- **D4-0（着手ゲート）**: 案A または案C の結論が文書で確定しており、`007-C` の扱いも文書で確定していること。高リスクワークフロー（`SecretCore` / `ProductionTool`）をクラウドに置くかどうかは `012-0004` の責務分界表で判断材料を固定する。
- **D4-1（マネージド MQTT）**: AWS IoT Core または Google Cloud 側の MQTT 相当（TLS・デバイス認証・ポリシー/ACL）へ、**既存トピック設計**（`設計概要.md` の `serverId` / `brokerId` 前提）を接続する。関連: `010-0013`（NAT 下 outbound 等の試験観点・試験仕様書への追記）、`012-0001`（Broker 候補比較）。
- **D4-2（アイデンティティ）**: デバイス登録・証明書ローテーション・失効。現行 `k-user` / `k-device` / TPM 境界との対応は `012-0004` の成果物へ反映する。
- **D4-3（コントロールプレーン＝LocalServer のクラウド相当）**: 運用 UI・REST・OTA メタ・監査のうち、PC 非依存に移せる範囲を切り出す。関連: `009-0004`（双方向同期の最小 I/F）。**不可逆 eFuse 等は当面ローカル/工場ツールに残す**方針を維持する。
- **D4-4（文書）**: `012-0002`（AWS/Google 比較表）、`012-0003`（`ドキュメント概要.md` / `設計概要.md` との相互参照同期）。

#### 012 の未完了タスク
- [ ] [012-0001] [2026-03-03][将来対応][重要] クラウド移行先Broker候補（AWS IoT Core / EMQX Cloud / HiveMQ Cloud 等）を比較し優先候補を決定する。  
  - [移管][2026-04-18] 旧ID `011-0008` から移管。理由: クラウド化は012以降に集約する方針のため。
- [ ] [012-0002] [2026-03-08][将来対応][重要] Cloud第4段階の候補（AWS/Google）比較表を作成する。  
  - [移管][2026-04-18] 旧ID `011-0009` から移管。
- [ ] [012-0003] [2026-04-17][将来対応][重要] 本章ロードマップおよび `012-0001` / `012-0002` の結論を、`ドキュメント概要.md` および `設計概要.md` と一方通行参照にならないよう、相互参照（節番号・ファイル名・更新日）で同期する。  
  - [移管][2026-04-18] 旧ID `011-0010` から移管。
- [ ] [012-0004] [2026-04-17][将来対応][重要] ローカル鍵階層（`TPM` → `S_random` → `k-user` → `k-device`）と、クラウド側デバイス認証（X.509 / JITP 等）の **責務分界・禁止事項（例: raw k-device をクラウドに平文送信しない）** を表形式で `IF仕様書.md` または別紙に初版化し、`009-0004` の API 契約設計の前提とする。  
  - [移管][2026-04-18] 旧ID `011-0011` から移管。
- [ ] [012-0005] [2026-04-18][将来対応][重要] `009-0004`（LocalServer のクラウド連携モード・双方向同期の最小 I/F）の設計着手は、`012-0004` の責務分界表の初版がレビュー可能な状態になってから行う。  
  - [理由] クラウド API とローカル秘密境界が未固定のまま I/F だけ先に固めると、後から全面見直しになるため。

### 020. 将来対応
[重要] `013`〜`019` は予約枠として空ける。ここでは将来対応だけを集約し、現行着手の邪魔をしない。

#### 020-A. 後続技術・インフラ
- [ ] [008-0037] [2026-03-07][将来対応] post-quantum アルゴリズム（CRYSTALS-Kyber等）への移行計画を 2030 年以降の課題として `todo.md` に残す。
- [ ] [010-0010] [2026-03-06][将来対応][重要] CoreDNSをローカルDNS候補としてPoC実施し、インターネットDNSとの両立（forward運用）を確認する。
- [ ] [010-0011] [2026-03-06][将来対応][厳守] CoreDNSではローカル専用ゾーンのみ管理し、公開ドメインの上書きを禁止する運用ルールを文書化する。
- [ ] [010-0012] [2026-03-07][重要] [将来対応] DHCP配布DNSまたはルーターDNSフォワーダ設定を見直し、ESP32側の固定DNS指定（方法①）を廃止しても `mqtt.esplab.home.arpa` が解決できる状態へ移行する。

## 運用ルール
- [厳守] 中期計画・未完了タスク・進捗メモは `todo.md` に統合し、別の日程表へ分散させない。
- [厳守] 毎作業日の終了時に `todo.md` の状態と必要なメモを更新する。
- [厳守] タスクを完了にした同日中に、関連仕様書・試験書・問題点記録書も更新する。
- [厳守] 完了タスクは `todo_oldYYYYMMDD.md` へ退避し、本書から削除する。

## 変更履歴
- 2026-04-27: `005`〜`011` の再点検を実施し、追加で退避できる項目がないことを確認した。理由: `006-0011` の再開条件が未達で、`005-0008` / `007` 章以降の残件は試験記録と現地確認の更新待ちのため。
- 2026-04-24: 直近の章次ゴールと文書スコープを `006-0010` / `006-0011` の再開条件へ戻し、`011` 章は完了退避済みとして整理した。理由: `011-0001`〜`011-0007` の退避完了後に、現時点の未完了タスクへ視線を戻し、再開順を `006` に固定するため。
- 2026-04-26: `020` へ逃がしていたクラウド第4段階の未完了タスクを `012` 章へ戻し、`020` はクラウド以外の将来対応に限定した。理由: ユーザー方針としてクラウド化は `012` で扱うのが正であり、章の責務分担をそこに揃えるため。
- 2026-04-25: `006-0002` を完了退避対象へ移し、`006` 章の着手順を `006-0008` 起点へ更新した。理由: `003-0024` / `7085` / `004` / `005` / `009` の準備ゲートが揃い、`006-0002` は本線の未完了管理から外せる状態と判断したため。
- 2026-04-25: `006-0005` を完了退避対象へ移し、`mainTaskStackSize` を 15KB へ整理した。理由: `7023` の stack margin 証跡と `mainTask` の拡張後運用値が整合し、15KB のきりのよい余裕として閉じられると判断したため。
- 2026-04-25: `006-0007` を完了退避対象へ移し、`mainTaskStackSize` と `7023` の stack margin 証跡を仕様書へ固定した。理由: `eFuse` 開始前の余裕確認値が 15KB 運用で十分であることを文書・試験・実装で揃えられたため。
- 2026-04-25: `006-0008` を完了退避対象へ移し、`stackMarginOk` / `heapMarginOk` の NG 時は `failed` で停止する実装を確認した。理由: スタック不足・ヒープ逼迫時の開始拒否が `SecretCore` の `production precheck` により既に担保されているため。
- 2026-04-23: `007-0012`、`008-0050`〜`008-0052`、`008-0042` を退避し、`011-0002` の前段整理を反映した。理由: 完了済み・証跡完了済みの細部は `todo_old20260423.md` に寄せ、`todo.md` には今後の再開判断に効く節目だけを残すため。
- 2026-04-18: クラウド関連を **`011-0008`〜`011-0011` から `012-0001`〜`012-0005` へ移管**し、`011` 章は **`011-0007` で打ち止め**と明記。冒頭の「クラウド化ロードマップ」独立節を廃止し、内容は `### 012` へ統合。再開インデックス・直近ゴールに「直近は011まで」を追記。移管記録を `todo_old20260418.md` に退避。理由: ユーザー方針（クラウド化は012以降、直近は011まで片付ける）に章構造を合わせるため。
- 2026-04-17: 冒頭に「再開インデックス」「直近ゴール・現在地サマリ（案A主軸）」「沿革要約」を追加し、`セッション引継ぎ` を 2026-04-17 正として集約（旧コピペ3種は git 履歴参照へ）。「クラウド化ロードマップ（第4段階）」を新設し、`011-0010` / `011-0011` を追加（後に 2026-04-18 に012へ移管）。「直近実行順序」に `008-0044` を `007-B` より前のステップとして明示。理由: 久々の再開でも着手順がぶれないようにし、クラウド化までの道筋を同一ファイルで追えるようにするため。
- 2026-04-07c: `008-0044` に PlatformIO の Arduino 3.x 対応観点（official PlatformIO ではなく community fork `pioarduino` が案内されていること）を追記し、案A PoC の次アクションを具体化した。理由: 「Arduino v3.x に上げれば案Bが成立するのか」を PlatformIO platform 基盤の観点も含めて評価し、次に何を案Aで確認すべきかをセッション跨ぎでも誤らないようにするため。
- 2026-04-07: `008-0045`（案B PoC）を **No-Go 確定** として完了化。`008-0044` に案A 移行の次アクションを追記。セッション引継ぎを案A 評価開始へ更新。理由: 現行 Arduino-ESP32 v2.0.17（IDF v4.4.7）には `nvs_sec_provider` API が存在せず、Arduino v3.x メジャー更新は Breaking Change が広範で `007-C` 計画を大幅に阻害するため、案B を不採用として確定し、次の判定段階（案A）へ即移行できるようにするため。
- 2026-04-06: セッション引継ぎ欄と次セッション用コピペ指示を更新し、`DEV-002` の現状を「MQTT/OTA/ProductionTool は回復済み、残ブロッカーは `AP` 到達のみ」として固定した。理由: セッションを跨いでも次回の開始点と停止条件を誤らず、`7041` 回復を最優先で再開できるようにするため。
- 2026-04-05: `008-0051` を `otadata=0x2000` 維持、`coredump=0x20000` 採用の確定内容へ更新。理由: `DEV-002` へ切り替えるパーティションサイズを `todo.md` 側でも固定し、次の書込み手順へ進めるようにするため。
- 2026-04-05: `008-0050` / `008-0051` に保全整理結果（`DEV-002` ベースライン、論理復元方針、raw NVS の位置づけ、候補案Aの実CSVオフセット）を追記。理由: セッションを跨いでも「どこまで整理済みか」と「次に何を実機で確認すべきか」を `todo.md` だけで再開判断できるようにするため。
- 2026-04-05: `008-0050`〜`008-0052` と、`DEV-002` 保全 -> パーティション最適化 -> 最適化後確認 の次セッション順序、ならびに新しいコピペ指示を追加。理由: セッションを跨いでも `DEV-002` の安全保全とパーティション最適化から再開できるようにするため。
- 2026-04-05: `008-0048` / `008-0049` を追加。理由: ROM 使用率 99.6% の差し止め要因（`#0026`）を次セッションで即再開できるよう、パーティション再設計と減量策の二段構えでタスク化するため。
- 2026-04-05: `008-0044`〜`008-0047` を追加。理由: ユーザー方針（案B優先、失敗時案A、最終的に案C）を次セッションでも同一判断で再開できるよう、分岐条件・証跡文書・停止条件を `todo.md` へ固定するため。
- 2026-04-04: セッション引継ぎ・直近ゴール・次セッション用コピペ指示を更新し、`7101` 回復、NIC 未接続が停止主因だったこと、次回は `7099` / `007-0005` / `PC-012`〜`PC-014` 整理を優先する方針を反映。理由: 本日の終了時点で「再調査不要な論点」と「次回最初にやること」を `todo.md` だけで判断できるようにするため。
- 2026-04-04: `008-0040` に build 方針の実装進捗を追記。`secureNvsInit` を `CONFIG_NVS_ENCRYPTION` マクロ依存から外し、`esp32s3_secure`（fallback許可）/`esp32s3_secure_final`（fallback禁止）を追加した。理由: 開発継続性を保ちつつ、最終版の secure 固定ビルドをコードとコマンドの両面で明示できるようにするため。
- 2026-04-05: `008-0042`（シリアル最終運用方針）に `DEV-001` 実測の禁止事項（`write_flash --force` を本番復旧手段にしない）を追記し、重複していた GUI タスク番号を `008-0043` へ整理。理由: 番号衝突を解消しつつ、「ログは残す/危険読書きは封じる」方針を実機知見で固定するため。
- 2026-03-27: `007-0012` にセッション再開後の再現確認結果を追記。固定 Pipe `SecretCore` 起動、AP 再接続、`7041:pipeline:strict` / `7087:normal` / `7087` / `7083(iterations=2)` の再確認を反映した。理由: `007-B` 進入前ゲートが現セッションでも成立することと、`ProductionTool.exe` 実行時の固定 Pipe 前提を `todo.md` 側にも明記するため。
- 2026-03-26: `006-0014` の段階1(FE)後停止問題を **解決済** へ更新。対策A（NVS HMAC鍵投入 + secureNvsInit FW修正 + OTA beta.29）実施後、AP HTTP 全4ロール認証成功・`BLOCKED_AP_REACHABILITY` 解消を確認。問題点記録書 #0020 解決済。理由: 段階2(NVS)の不可逆処理と FW 安全化が完了し、AP モード復旧を実機証跡付きで確認したため。
- 2026-03-26: `007-0013`（出荷後更新経路を OTA 主経路へ一本化する最終条件確認）を追加。理由: 「OTAのみ」方針の採否を、不可逆設定と復旧性の両面で判断できるようにするため。
- 2026-03-26: `006-0013`（ProductionTool 2モード運用）と `006-0014`（三段階実行ゲート）を追加。理由: 本番同等の運用手順で `006-0011` を安全側に実施するため。
- 2026-03-26: `006-0012`（鍵運用の最終方式 A/B 判定）を追加。理由: `006-0011` の不可逆リハーサル実行前に、鍵生成主体・鍵粒度・復旧責任を固定し、実行判断を文書ベースで行えるようにするため。
- 2026-03-25: 「不可逆以外を先行完了」の方針に合わせ、`7019` / `7020` / `7021` / `7023` の実施結果を反映し、`006` 章へ非不可逆優先の進捗メモを追記。理由: 実書込み前に完了可能な検証を先に閉じ、残課題を明確化するため。
- 2026-03-25: 1台目/2台目で共通適用する製品版セキュリティプロファイル `production-hmac-v1` を固定し、`007-0008` へ `.env` / `.env.example.sample.txt` / `sensitiveData.h` 同期タスクを追加。理由: 1台目失敗時でも設定判断をやり直さず、2台目試験を即開始できるようにするため。
- 2026-03-25: NVS Encryption HMAC方式を1台目から採用する方針へ変更し、`007-0008` を追加。公式再照合により `nvs_keys` 追加案を取り下げ、`nvs` 現状維持 + sdkconfig/FW実装 + eFuse書込み + 事前バックアップ運用へ更新。詳細は `本番セキュア化出荷準備試験計画書.md` 5.6c 参照。
- 2026-03-25: `007-0009` を追加し、各段階の完了判定前に安定性確認セット（OTA2回 + AP10分 + 通常10分）を必須化。理由: セキュア化過程での `abort`・再起動ループ・通信異常を早期検知し、文鎮化リスクを段階的に抑えるため。
- 2026-03-25: `Wi-Fi_lab` 再接続と `LocalServer` 起動復旧後に `7041:pipeline:strict` を再実行し `FULL_COMPLETED` を確認。`7085` を進入可へ更新し、次アクションを `006-0002` 以降の段階実行へ切替した。理由: フェーズG進行可否を実証跡で確定するため。
- 2026-03-25: AP未到達の再切り分け結果（`Wi-Fi_lab` 未接続、`ping 192.168.4.1` timeout、`BLOCKED_AP_REACHABILITY` 継続）を引継ぎへ追記し、次回開始コマンドへ `test:7041:diag` を追加。理由: 7085 再判定の前提を実装状態と接続状態で切り分け、再開判断の誤りを防ぐため。
- 2026-03-24: `7085` 再判定結果（`BLOCKED_AP_REACHABILITY`）を受け、`006-0002`〜`007-0005` の停止継続を追記。あわせて `test0091022FactoryTerminalFinalValidation.ps1` を 3フェーズ明示実行へ統一した。理由: 不可逆処理ゲート未通過時の誤着手防止と、工場端末最終検証コマンドの再現性確保のため。
- 2026-03-24: セッション終了引継ぎとして「次セッション開始手順（7085再判定優先）」を追記。理由: 別セッション再開時に不可逆処理へ誤着手しないよう、開始順を固定するため。
- 2026-03-24: 次ゴールを `007` 章（フェーズG）へ更新し、不可逆処理を段階ゲート方式（読戻しOK時のみ次段、NG時即停止）で進める実行順序へ再整理した。あわせて `007-0003`〜`007-0005` を追加し、1台目完了前の複数台展開禁止を明記した。理由: `Secure Boot` / `ROM暗号化` / `Flash Encryption` / `eFuse` を安全側で段階実行するため。
- 2026-03-24: `todo.md` に残っていた完了タスク（`003/004/005/006/008/009` 章の `[x]`）を追加で `todo_old20260324.md` へ退避し、`todo.md` を未完了タスク中心へ整理した。理由: 現在の作業対象を明確化し、運用ルール「未完了のみ管理」を厳守するため。
- 2026-03-24: `009-1022` を完了として `todo.md` から退避し、`todo_old20260324.md` を追加。理由: 工場専用端末向け最終再検証（`test0091022` 3フェーズ OK）まで完了し、未完了タスク一覧を整理するため。
- 2026-03-24: `003-0012` を完了化。内蔵 `Wi-Fi` 経路で `npm run test:7041:pipeline:strict` の `FULL_COMPLETED` を確認し、同時に `AP_ROLE_MFG_*` 読込不足修正、`mainTaskStackSize` 拡張、`7041` 状態履歴判定の誤検知修正を反映した。理由: AP 到達性・認証・stack margin・判定ロジックの連鎖ブロッカーを解消し、strict 完了条件を実機証跡付きで満たしたため。
- 2026-03-24: `003-0012` の再判定として `npm run test:7041:pipeline:strict` を再実行し、`BLOCKED_AP_REACHABILITY` 継続を確認。あわせて `test7041Pipeline.mjs` の gate 前レポート保存を反映し、strict 実行時の「pipeline レポート未生成」偽失敗を解消した。理由: 未達理由を AP 到達性へ限定し、次の実機確認条件を明確化するため。
- 2026-03-24: `003-0017` を完了化。`uninstall-legacy-localsoft.ps1` と `test7092LegacyLocalSoftUninstallSmoke.ps1` を追加し、旧 `LocalSoft` / `LocalSoftForCloud` 資産の安全消去（上書き消去 + 削除 + 監査ログ）を 7092 で確認した。理由: LocalSoft 廃止時の機密残留を防ぐ実装・証跡を揃えるため。
- 2026-03-24: TODO 整理として `003-0016`（インストーラ/アンインストーラ整備）と `009-1021`（EXE化）を完了化し、受入環境での最終確認のみを `009-1022` へ分離した。あわせて「手動確認待ち」と直近実行順序の主軸を新IDへ更新。理由: 実装完了タスクと受入検証タスクを分け、未完了一覧の解像度を上げるため。
- 2026-03-24: `ProductionTool/.env.example.sample.txt` を追加し、`README.md`・`install-production-tool.ps1`・`ProductionTool.iss` へ反映。理由: 共有時に実値を配布しない運用を実装と文書で一致させるため。
- 2026-03-22: `003-0016` / `009-1021` の進捗として、`ProductionTool` 用の安全消去付きアンインストーラ骨格（`uninstall-production-tool.ps1`）と Inno Setup 骨格（`ProductionTool.iss`、`build-production-tool-installer.ps1`）を追加。理由: フェーズE完了後の前倒し実装として、工場端末配布と安全消去の実装入口をコード化するため。
- 2026-03-22: `003-0024` を完了化し、`7085` の親定義・必須条件・差し止め対象を `todo.md` と `試験仕様書.md` で固定した。あわせて `006-0001` として `eFuse適用チェックリスト.md` を新規作成し、総合進入判定ゲートと作業単位チェックリストを分離した。理由: フェーズF準備完了の境界を機械判定できるようにしつつ、不可逆処理の本実行開始とは明確に切り分けるため。
- 2026-03-22: `008-0025` の進捗として、ESP32 AP に `production/precheck` / `production/state` を追加し、`SecretCore` から AP ログイン経由で firmware/MAC/heap/stack の実測値を取得して dry-run precheck へ反映できるようにした。`npm run check`、`cargo check`、`python -m platformio run -e esp32s3_secure` を通過。理由: 不可逆処理をまだ解放しないまま、ESP32 側専用受け口と実測 precheck 経路をフェーズDの範囲で固定するため。
- 2026-03-22: `005-0006` / `005-0007` を完了化。`ProductionTool画面仕様書.md` 第9節と `機能仕様書.md` に、ProductionTool 専用セキュア化フロー、事前チェック8項目、読戻し検証、隔離判定を反映した。あわせて `005-0008` は実機実施を伴う有効確認として保留明記し、直近実行順序と `003-0024` の前提条件を `005-0001`〜`005-0007` ベースへ更新。理由: フェーズDでは実行前準備までを完了とし、実行後確認はフェーズFへ分離するため。
- 2026-03-22: 直近ゴールと実行順序を再整理し、「フェーズDクローズ完了」「フェーズFは準備完了まで」「本番用 `k-iot` 実キー生成は実行直前のみ」を明記。あわせて `004-0006` / `004-0007` を前倒ししない運用方針を追加。理由: eFuse / Secure Boot / ROM暗号化の不可逆処理を急がず、準備完了と実行開始の境界を明確にするため。
- 2026-03-21: `003-0004` を完了化。`ESP32/src/ota.cpp` の CA 解決・ホスト名検証付き TLS 接続実装と、`試験記録書.md` 7011 の `Certificate verified.` / OTA成功実機証跡を突合し、タスク条件「証明書検証有効状態で OTA 成功」を満たすことを確認した。理由: `7085` 差し止め要因から 003-0004 を外せる状態かを、実装・試験・文書の3点で整合させるため。
- 2026-03-21: `009-0019` を完了化。実機 AP `AP-esp32lab-F0D0F94EB580` 接続下で `npm run test:7082 -- --targetDeviceId "CF-FV_1"` を実施し、workflow `completed/OK` と `GET /api/pairing/state` の `state=applied`、`savedCurrentKeyVersion=v1` を確認。理由: 「コード実装済み」ではなく「実機証跡付き完了」へ進め、`7085` 再判定の材料を確定するため。
- 2026-03-16: `009-0019` に実機確認の「次アクション」（FW 書込み、`.env` の AP 到達設定、`npm run test:7082` 証跡、`pairing/state` の `applied` 記録）を追記。理由: 実装済みと試験完了の切り分けを運用手順レベルで固定するため。
- 2026-03-19: `004-0001`〜`004-0005` を完了化。`コマンド仕様書.md` へ試験用 `k-iot` 鍵生成手順、`OTA仕様書.md` へ `k-iot-ota-sign` 別鍵運用方針、`試験仕様書.md` へ 004-0004 検証条件（7016/7017/7018）を反映。理由: 006 進入前の鍵手順を再現可能な形で固定するため。
- 2026-03-19: `003-0016` / `009-1021` の配布前提を `インストーラEXE化設計仕様書.md` 3.4 へ確定（対象端末、インストール先、監査ログ先、混在インストール禁止、安全消去監査ログ）。これに伴い `004-0010` を完了化。理由: 006 着手前ゲートの未達要因を解消するため。
- 2026-03-19: `005-0002` を完了化。AP 共通トップ画面と ProductionTool 画面の分離仕様（eFuse 導線混在禁止、追加認証境界）を3文書で整合し、AP 側の実装タスクは `008-0028` へ切り分け。あわせて `004-0010` 進捗文を更新。理由: 005 フェーズEの設計完了範囲を確定し、残課題を 003-0016/009-1021 に集約するため。
- 2026-03-19: `005-0001` と `005-0004` を完了化。あわせて `APメンテナンス画面仕様書.md` へ AP/ProductionTool 分離方針（eFuse導線混在禁止）を反映し、`005-0002` の進捗を更新。理由: 005 フェーズE の設計完了範囲を確定し、未達を AP 側最終実装へ限定するため。
- 2026-03-24: `005-0005` の方針を入力簡素化仕様へ更新。`PT-002` は操作者ID/作業指示番号を任意入力化し、空の作業指示番号は `YYYYMMDDHHMMSS` 自動採番へ変更。`PT-003` は確認文言/シリアル/MAC再入力を廃止して一覧行選択方式へ変更。理由: 過剰入力を減らし、MAC未指定でも対象機選択できる運用へ合わせるため。
- 2026-03-19: `005-0003` を完了化。メーカーモード専用パスワード管理方式（責任者、配布、更新、失効、緊急停止、開発値から本番値移行条件）を `セキュリティ方針_認証情報取扱い.md` と `ProductionTool画面仕様書.md` に反映。理由: 006 着手前の高権限認証管理方式を確定するため。
- 2026-03-19: `004-0010` の必須条件へ三者整合レビュー（LocalServer/ProductionTool/ESP32）と再判定順序（7088->004-0010->7085）を追加し、`005-0001`〜`005-0005` に完了条件・証跡・未達項目を追記。理由: 005 完了判定の曖昧さを解消し、006 着手前の安全ゲートを機械的に判断できるようにするため。
- 2026-03-16: `009-0019` の進捗へ encrypted bundle 本体送達（`/api/pairing/secure-bundle`）、AES-256-GCM 復号、NVS 保存、workflow `completed(OK)` 遷移を追記。理由: 006 着手前の重大残課題だった「本体送達/復号/NVS保存」の到達状況を `todo.md` 上で明示し、実機試験での最終証跡反映ポイントを固定するため。
- 2026-03-19: `004-0010` の進捗へ `8001` 異常系試験 OK を反映。理由: 7088 判定と `todo.md` の進捗表示を一致させるため。
- 2026-03-18: `004-0009` 進捗: PT-002/PT-003/PT-005 骨格（`auth_screen_state.rs`・`device_select_screen_state.rs`・`dry_run_screen_state.rs`）を追加し、`main.rs` をウィザードフロー対応に拡張して `cargo check` 通過。理由: スモーク試験実施に向けて追加認証・対象機確認・dry-run 導線の実装を先行完了させるため。
- 2026-03-18: `004-0008` を完了化し、`SecretCore` reachable 到達確認結果（7086 OK）を記録。理由: `SecretCore` 起動状態で `ProductionTool cargo run` を実行し、`secretCorePreflightResult=reachable`・監査ログ出力を確認したため。
- 2026-03-16: フェーズC/D を「`LocalServer` 基本安定化 -> `ProductionTool` 基本安定化 -> 耐久/過負荷 -> 鍵/不可逆導線 -> 総合ゲート -> eFuse」の順へ再整理し、`004-0008`〜`004-0010` を追加。理由: `LocalServer` と `ProductionTool` の基本機能を先に実装・試験・安定化させてから鍵手順や不可逆処理へ進む方針を `todo.md` 全体へ反映するため。
- 2026-03-16: `todo.md` に「直近実行順序（2026-03-16再整理）」と `003-0020`〜`003-0024` の安定化/耐久/過負荷/総合進入判定ゲートを追加し、`005`→`004`→`006`→`007` の着手順を明示。理由: `LocalServer` とセキュア機能を先に固め、OTA耐久試験と長時間過負荷試験を通してから `ProductionTool` / eFuse へ進む方針を明文化するため。
- 2026-03-15: `003-0014` を完了化。理由: ランダム Pipe 名、32byte IPC セッション鍵、AES-256-GCM、requestId リプレイ拒否を実装し、E2E 動作確認まで完了したため。
- 2026-03-15: `003-0012` を [部分完了] へ見直し、`003-0013` と `003-0019` を完了化。理由: `SecretCoreFacade` による境界IF固定と k-user バックアップ実装は完了した一方、通信まで Rust 主導の最終要件は未達のため。
- 2026-03-15: `003-0019`（SecretCore `export_k_user` / `import_k_user_backup`）を追加。理由: `k-user` のパスワード暗号化バックアップ受け渡しを Rust 側で完結させるフェーズC課題を明示するため。
- 2026-03-13: Rust/TS 併用切り替え可能、`ProductionTool`（Rust共通部再利用・GUI=Windowsアプリ・`LocalServer` とは別ソフト）、002-1000番台・009-1000番台セクション、新ルータ `172.17.1.x` 運用反映。理由: セキュア部移行方針、`ProductionTool` 設計、タスク順序、ネットワーク現状を整合させるため。
- 2026-03-13: `LocalSoft` 廃止・`LocalServer` 一体化方針を反映し、`003-0012`/`003-0013`/`003-0017` と `009-0002`〜`009-0006` を統合前提へ更新。理由: 保存・同期・運用機能の実装先を単一化し、以後のタスクを新方針と一致させるため。
- 2026-03-13: `7048/7049` を完了化。理由: 7048 最終620回異常0件に加え、7049 で `pruneFileLogsIfNeeded removed` の実機証跡を取得し、試験後に本番しきい値FWへ復元したため。
- 2026-03-13: 7048/7049 に最終620回再試験の進捗を追記。理由: バッチflush版FWで異常0件を再確認し、#0008 の再現性評価を最新化するため。
- 2026-03-13: 7048/7049 にバッチflush・廃止判断基準・完全停止フラグの進捗を追記。理由: 実装と運用判断基準を同時に管理して、段階廃止の判断を可能にするため。
- 2026-03-13: 7048/7049 に寿命試算の進捗を追記。理由: フラッシュ書換回数への懸念に対し、レート上限と10年試算を定量で管理するため。
- 2026-03-13: 7048/7049 の進捗を再々々々更新。理由: 未作成ファイルへの `exists/open(r)` 回避で `open(): ... no permits for creation` が 0 件化し、#0008 の根因解釈を更新する必要があるため。
- 2026-03-13: 7048/7049 の進捗を再々々更新。理由: 単一ファイルログ書込みタスク化で stack canary が停止し、負荷起因の安定化効果と `open()` 失敗残課題を分離して管理する必要があるため。
- 2026-03-13: 7048/7049 の進捗を再々更新。理由: `fileLogTrace` 追加FWで `LittleFS Already Mounted` + `open(): ... no permits for creation` + `timeServerTask` stack canary を採取し、#0008 の調査観点（`i2cTask` 固定ではない）を反映する必要があるため。
- 2026-03-13: 7048/7049 の進捗を再更新。理由: 最小1行固定（one-shot）再試験で `open(): /littlefs/logs/...` は再現したが `i2cTask` stack canary は未再現となり、#0008 の切り分け結果を反映する必要があるため。
- 2026-03-13: 7048/7049 の進捗を更新。理由: 実機でローテーション処理入口の実行を確認した一方、`i2cTask` stack canary 再発が発生し、先に #0008 の解消が必要になったため。
- 2026-03-13: `002-0026` を完了化。理由: 7055 実機で APモード表示（LED/LCD）が仕様どおりであることを確認できたため。
- 2026-03-13: `002-0025` を完了化。理由: 7056 実機で APモード中の MQTT 接続開始抑止を確認し、混在起動禁止の受入条件を満たしたため。
- 2026-03-13: `002-0014` を完了化。理由: 7028 実機で誤設定による接続不能再現から AP 経由復旧、再起動後 `online/mqtt` 復帰と `status` 応答再開まで確認できたため。
- 2026-03-13: `002-0009` の実機試験を完了化。理由: LittleFS 初期化対策FW反映後に 7063 を再試験し、`/images`・`/certs` の局所更新と権限制御の合格を確認できたため。
- 2026-03-13: `002-0009` の進捗を更新。理由: 7063 で `file write failed` の主因を LittleFS 初期化不足と特定し、`ensureLittleFsReadyForAp` 実装まで完了したため（再書込み確認は `COM9` 解放待ち）。
- 2026-03-13: `002-0017`/`002-0018` を完了化。理由: 方針A（画像更新は `fileSync` 標準、ZIP経路は [新規利用禁止] の限定保守）を正式確定し、試験ID運用を 7061/7062 主体へ整理したため。
- 2026-03-13: `002-0009` の残課題メモを更新。理由: 7063 実機で RBAC/パス拒否は成立したが `upsert=file write failed` を再現し、FW再反映後の再試験が必要になったため。
- 2026-03-13: `002-0010` の追加修正を実施。理由: 実機で `wifiTask` の stack canary を再現したため、`log.cpp` を軽量化して `writeFileLogLine` 呼出しを `fileLogEnabled` 有効時のみに制限したため。
- 2026-03-13: `002-0012` を完了化。理由: 1KBローテーションと 30日/100KB しきい値による古いログ削除を `log.cpp` へ実装し、ビルド確認まで完了したため。
- 2026-03-13: `002-0011` を完了化。理由: ファイルログ保存先を同期状態で分岐するパス規約（同期済み: `/logs/<年>/<月日>/<年月日時分秒ms-連番>.log`、未同期: `/logs/<起動回数>/00001.log`）を実装し、起動回数NVS管理とビルド確認まで完了したため。
- 2026-03-13: `002-0010` を完了化。理由: ファイルログをシリアルログ経路から分離し、`setFileLogEnabled()` による既定OFF/必要時ONの制御を実装してビルド確認できたため。
- 2026-03-13: `002-0009` を完了化。理由: APモード向けに `/images` `/certs` 局所更新API（ESP32側 `/api/files/upsert` `/api/files/delete`、LocalServer側 `/api/admin/ap/files/upsert` `/api/admin/ap/files/delete`）を実装し、ビルド整合を確認できたため。
- 2026-03-13: `002-0013` を完了化。理由: APメンテナンスモードのネットワーク設定 API（ESP32側 `/api/settings/network`、LocalServer側 `/api/admin/ap/settings` `/api/admin/ap/configure`）が実装済みで、保存層連携まで接続されたため。
- 2026-03-13: `002-0015` を完了化。理由: `mqttUrlName` / `serverUrlName` / `otaUrlName` / `timeServerUrlName` / `keyDevice` が ESP32保存層・AP設定API・LocalServer中継APIまで追従済みで、ESP32/LocalServer のビルド成功で整合確認できたため。
- 2026-03-13: `002-0005` を完了化。理由: `fileSync targetArea=certs` で `/certs/mqtt-ca.pem` を更新/削除した際、NVS メタ（`issueNo`/`setAt`/`sha256`/`active`）を LittleFS 実体へ追従同期する実装を追加し、ビルド確認まで完了したため。
- 2026-03-13: フェーズBの進捗整理を更新。理由: 実装/試験済みの `002-0006` `002-0007` `002-0008` `002-0016` を完了チェック化し、未完了タスクを「直近残課題」として再整理してフェーズB完了までの見通しを明確化するため。
- 2026-03-13: `002-0018` の進捗を更新。理由: 画像 `fileSync` を MQTT送信可能サイズに追従する動的分割へ改修し、`chunkSizeBytes=102400` 指定でも `totalChunks=76` で `completed` を確認できたため。
- 2026-03-13: `002-0018` の進捗を再更新。理由: 100KB分割方針で正常系（7061）と異常系（7062）を実施し、固定小チャンク運用（`chunkSizeBytes=1024`）を暫定適用したため（後続で動的分割へ改修済み）。
- 2026-03-13: `002-0018` の進捗を更新。理由: `testImageFileSyncE2E.mjs` により ZIP非使用の画像更新E2Eを実施し、`4096byte` ダミー画像で分割転送（`splitFiles=1` / `totalChunks=7` / `completed`）を確認できたため。
- 2026-03-13: `002-0017` の進捗を更新。理由: `imagePackageApply` 失敗ログを再精査した結果、`IPKG_HTTP_CONNECT_FAILED` の一部が `hostByName(): DNS Failed` 起因であることを確認し、`mqtt.cpp` へ DNS失敗時のTLS検証維持IPフォールバック実装を追加したため（FW反映は `COM9` 解放待ち）。
- 2026-03-13: `002-0017` の進捗を更新。理由: `connection refused` の内訳を再調査した結果、実体は `X509` 検証失敗および `mqttTlsCertSha256` メタ不整合（`cert sha256 mismatch`）であることを確認し、再開手順を「COM9で復旧FW反映」優先へ改めたため。
- 2026-03-13: `002-0017` の進捗を更新。理由: OTA反映後の実機試験で `IPKG_HTTP_CONNECT_FAILED`（`reason=connection refused`）を確認し、阻害点が `SHA-256` ではなく HTTPS 配布口 `:4443` の接続拒否であることを確定できたため。
- 2026-03-12: `002-0017` の進捗を更新。理由: `imagePackageApply` 実機再試験で `connect(): failed connect` を確認し、失敗原因を遠隔で判別できるよう `IPKG_HTTP_CONNECT_FAILED` など詳細エラーコード通知を `mqtt.cpp` へ追加したため。
- 2026-03-12: `003-0012`〜`003-0018` を追加。理由: `LocalServer` / `ProductionTool` / `LocalSoft` の Rust主導セキュア化、UI/セキュア分離、境界IPC保護、インストーラ/アンインストーラ整備（機密安全消去必須）を直近フェーズCの実装タスクとして固定するため。
- 2026-03-12: `002-0017` / `002-0018` の進捗を更新。理由: `imagePackageApply` 実送信で `planning=OK` / `downloading=NG(IPKG_DOWNLOAD_OR_HASH_FAILED)` まで確認し、OTA HTTPS 側 `/assets` 公開対応・`mqttTask` スタック最適化（12288 + 分割静的バッファ）を反映、翌日再開手順を固定するため。
- 2026-03-12: `002-0017` の進捗を再更新。理由: `imagePackageApply` の ZIP 展開本体（安全パス検証・`overwrite` 制御・`tmp` + `rename`）を実装し、`IPKG_ZIP_NOT_IMPLEMENTED` 状態を解消したため（現時点は ZIP `stored` 方式のみ対応）。
- 2026-03-12: `002-0017` の進捗を更新。理由: `imagePackageApply` の受信・署名検証・HTTPS取得・SHA-256検証・スコープ検証・進捗通知（`imagePackageStatus`）を追加し、残課題を ZIP 安全展開に限定したため。
- 2026-03-12: `002-0018` の進捗を更新。理由: `試験仕様書.md` に `imagePackageApply` 正常系/異常系（7059/7060）を追加し、`試験記録書.md` に受け皿を反映して文書整合を取ったため。
- 2026-03-12: `002-0006` へ `fileSyncStatus` 通知進捗を追記。理由: `phase/result/errorCode` 通知を追加し、`FSYNC_SIG_INVALID` など失敗理由を運用側で判別できるようにしたため。
- 2026-03-12: `002-0016` の進捗を更新。理由: `fileSyncPlan` / `fileSyncChunk` / `fileSyncCommit` に `signature` 必須化と `HMAC-SHA256(k-device)` 検証を実装し、未署名/検証失敗時の実行拒否をコードで担保したため。
- 2026-03-12: `002-0006` / `002-0007` / `002-0008` の進捗を更新。理由: MQTT第一経路の `fileSync*` 受信実装、`SHA-256` 検証反映、`tmp` + `rename` 適用を実装反映し、残課題を試験・署名検証へ絞るため。
- 2026-03-12: `002-0005` の進捗へ OTA HTTPS 証明書経路の `/certs` 主経路化を追記。理由: MQTT/HTTPS の証明書実体を FW 埋め込み優先から `LittleFS:/certs` 優先へ段階移行した実装状態を明確化するため。
- 2026-03-12: `002-0005` の進捗を更新。理由: MQTT CA 証明書の `/certs` 実体保存（`tmp`+`rename`）と NVS メタ（`SHA-256` / active）管理の実装を反映し、残りを HTTPS(OTA) 系証明書へ明確化するため。
- 2026-03-12: `002-0015` / `009-0015` の実装進捗を更新（完了判定待ち）。理由: `network` 追加5項目の保存読書き実装と、`sensitiveDataService` の NVS 主保存 + 旧 `LittleFS:/sensitiveData.json` 移行実装を反映し、残りは実機試験結果の反映へ絞るため。
- 2026-03-12: 完了済み `001-0001`〜`001-0004`、`003-0005`、`003-0006`、`003-0007`、`003-0009`、`003-0010`、`009-0007`、`009-0025` を `todo_old20260312.md` へ退避し、`todo.md` から削除。理由: [厳守] ルールに従い「未完了タスクのみ管理」へ整合させるため。
- 2026-03-12: `009-0025` を完了へ更新。理由: strict 実機試験（7057/7058）合格に加えて、LocalServer/ESP32 の既定値 strict 化と、`コマンド仕様書.md` への切替/ロールバック手順固定まで完了したため。
- 2026-03-12: `009-0025` に `7057/7058 OK` の進捗を追記。理由: strict モード試験の完了状態と、残作業（本番既定値化・手順書固定）を分離して追跡するため。
- 2026-03-12: `009-0025`（MQTT payload暗号化 `strict` 本番移行タスク）を追加。理由: `compat` 実装完了後の次アクションを未完了タスクとして固定し、平文拒否条件を明確化するため。
- 2026-03-12: `009-0007` を完了へ更新し、`k-device` による MQTT payload全文暗号化モジュール化（LocalServer/ESP32）と実機往復試験（7053 OK）を反映。理由: 本日実施した暗号化実装と試験結果を未完了管理へ同期するため。
- 2026-03-12: `003-0005` / `003-0006` / `003-0007` / `003-0009` / `003-0010` を完了へ更新。理由: 管理者認証、鍵発行、AP自動設定、統合一覧、`call/maintenance` の実装済み項目を未完了一覧から整理するため。
- 2026-03-11: `003-0005`〜`003-0011`（LocalServer管理者認証、鍵発行、AP自動設定、AP Web遷移、統合一覧、`call/maintenance`、eFuse分離）を追加。理由: 管理画面の新要件を実装タスクへ分解するため。
- 2026-03-13: `002-0028` の方針を「初期パスワード固定運用禁止」から「初期/各種パスワードの変更推奨」へ更新。理由: 現場運用での例外対応余地を残しつつ、初回変更手順・変更API・監査ログでセキュリティ担保を維持するため。
- 2026-03-26: `008-0028` に「`007` クローズまでは `sensitiveData.h` 定義値を固定運用」「変更可能ONは `008` で工場出荷状態復帰機能とセット検討」を追記し、`008-0038` / `008-0039` を追加。理由: 不可逆フェーズ直前で認証運用を安定化しつつ、将来拡張の判断軸を先に固定するため。
- 2026-03-26: `006-0014` の3段目進捗を「着手済み（precheck停止）」へ更新。理由: `dryRun=false` workflow は起動したが AP login timeout で `stage_execute` 未到達だったため、不可逆未実施を明確化して誤進行を防ぐため。
- 2026-03-13: `002-0028` の実装進捗を更新（APロールPW変更API、LocalServer管理者PW変更API、例外記録API、監査ログ実装、`securityState.json` 永続化）。理由: フェーズBクローズへ向けて「変更API + 監査ログ」の実装証跡を todo 管理へ反映するため。
- 2026-03-13: `002-0020`〜`002-0022` の実装進捗を更新（`/api/admin/ap/batch/start`・`/api/admin/ap/batch/:batchId` 追加、順次処理化、`publicId/configVersion` を含む再起動後 `status` 最終判定実装）。理由: AP一括メンテナンス系の未完了タスクをフェーズBクローズへ向けて前進させるため。
- 2026-03-13: フェーズBクローズ判定ゲート `002-0099` を追加し、`002-0023` を完了化。理由: 未完了項目と必要試験ID（7027/7064/7065/7066/7067）を固定し、フェーズBを段階的にクローズできる状態へ整理するため。
- 2026-03-11: `002-0024`〜`002-0028`（AP遷移条件、AP中タスク停止、LED/LCD表示、ロール認証、初期パスワード変更運用）を追加。理由: APモード要件詳細を実装タスクへ分解し、取りこぼしを防ぐため。
- 2026-03-11: APメンテナンス一括運用の実装準備として `002-0019`〜`002-0023`（ログイン必須、USB Wi-Fi探索、順次更新、再起動後`status`判定、試験追加）を追加。理由: 複数台の現地更新を安全に進める実装単位へ分解するため。
- 2026-03-10: 4-5 強化として、画像更新 `fileSync` の HMAC/署名必須タスクと `imagePackageApply`（ZIP一括更新）タスクを追加。理由: OTA同等のセキュリティ要件で画像更新を実現するため。
- 2026-03-10: `002-0015` を追加。理由: `common.h` へ追加した `network` キー5項目（`*UrlName` と `keyDevice`）の実装追従を明示管理するため。
- 2026-03-10: `001-0001` を完了へ更新し、`beta.x` 自動インクリメント必須運用を廃止。理由: 版数更新は必要な作業のみ都度指示で実施する方針へ変更したため。
- 2026-03-10: `001-0003` を完了へ更新。理由: rollback 前提実装後に 7025 の `未確定起動失敗` 条件を実機再現し、旧面継続起動を確認できたため。
- 2026-03-10: `001-0002` を完了へ更新し、`001-0004` を問題点記録書 `#0005` 連携で完了へ更新。`001-0003` は 7025 のうち `未確定起動失敗` 条件を rollback 前提未整備として継続管理へ変更。理由: 実機試験結果を反映しつつ、未成立前提を明示して完了/未完了を分離するため。
- 2026-03-09: 未完了タスクへ固定ID（`章番号3桁-項目番号4桁`）を付与し、欠番許容ルールを追加。理由: 並び替えや統合後も他文書や会話から安定参照できるようにするため。
- 2026-03-09: `NVS` / `LittleFS` の保存区分、証明書の `LittleFS` 移行、`fileSync` 実装、ファイルログ実装と関連試験タスクを追加。理由: 合意済みの保存方針と局所更新方式を未完了タスクへ反映するため。
- 2026-03-09: `todo.md` の実装タスク名と API URL 表記を `/api/workflows/...` 前提へ更新し、`createPairingBundle` を内部 helper 扱いへ整理。理由: 実装着手時に公開 API と内部秘密処理の境界がブレないようにするため。
- 2026-03-09: `SecretCore` の高リスクワークフロー実行器化、進捗状態、`runPairingSession()` / `runKeyRotationSession()` / `runProductionSecureFlow()` / `runSignedOtaCommand()` の実装タスクを追加。理由: TS は進捗表示中心、Rust は完了判定まで責任を持つ構成へ実装計画を合わせるため。
- 2026-03-21: `003-0012` に KeyRotation / Production workflow の REST・IPC・Rust workflow 骨格追加を反映。理由: Pairing/OTA に続き未着手だった workflow 境界を固定し、残課題を「本体未実装」へ切り分けるため。
- 2026-03-09: AP 共通トップ画面認証、`runPairingSession()` / `createPairingBundle` 内部処理、`public_id` 初期値変更、ESP32 current/previous key 2 スロット運用のタスクを追加。理由: AP モード投入と逐次再ペアリングの設計確定内容を未完了タスクへ反映するため。
- 2026-03-09: シリアル機能停止を eFuse ではなく FW 制御で管理する方針と、インターネット接続時の最低限セキュリティ基準、関連試験タスクを追加。理由: 現地解析余地を残しつつ、4-8 / 4-9 / 4-10 を未完了タスクとして具体化するため。
- 2026-03-08: ESP32 内部設定の NVS 永続化と、メンテナンス用 Wi-Fi AP モードからの設定修正を直近フェーズへ追加。理由: Wi-Fi AP や MQTT ブローカー変更時に現地メンテナンスで復旧できることを、当面の最優先課題へ反映するため。
- 2026-03-08: 直近ゴール順に `todo.md` を再編成し、完了済みの「仕様書へ追加する」系タスクを整理。理由: ESP32 と LocalServer の当面の到達目標（2面OTA確認、鍵/証明書本番化、Secure Boot/ROM暗号化、eFuse後OTA継続）に沿って着手順を明確化するため。
- 2026-03-08: eFuse 本番前リハーサル、専用処理タスクのスタックサイズ確保、開始前メモリ余裕確認のタスクを追加。理由: eFuse 失敗防止には手順確認だけでなく実行時資源の安全余裕も必要であるため。
- 2026-03-08: `ProductionTool` の隠し入口、専用パスワード、段階実行ウィザードの設計タスクを追加。理由: 高リスク機能の入口制御と操作確認を未完了タスクとして具体化するため。
- 2026-03-08: `ProductionTool`、ESP32内部専用セキュア化フロー、中断時の読戻し判定タスクを追加。理由: 過去のシリアル焼込み中断経験を踏まえて、eFuse 最終有効化の安全設計を未完了タスクへ具体化するため。
- 2026-03-08: OTA標準運用を「メーカー署名 + 顧客実行」として実装タスクへ反映。理由: 正式採用した運用モデルを作業項目へ直結させるため。
- 2026-03-08: `日程表_ローカル運用まで_50日.md` の未完了事項と進捗ルールを `todo.md` へ統合する前提で、試験・統合タスクと運用ルールを追加。理由: タスク管理文書の冗長化を解消し、更新先を一本化するため。
- 2026-03-08: `k-iot` 系の実キー生成タスク（試験用/本番用の生成時期、保存場所、eFuseリハーサル、文書追記）を追加。理由: ESP32 セキュリティ鍵の作成タイミングと保管責任を未完了タスクとして具体化するため。
- 2026-03-08: 鍵管理タスクを `TPM + wrapped_secret + k-user / k-device` 方式へ更新。理由: 鍵設計の正式決定に合わせて未完了タスクを具体化するため。
- 2026-03-07: OTA実機試験完了（beta.5 OTA成功、done通知確認）を受けて、OTA関連タスクを `todo_old20260307.md` へ退避。
- 2026-03-07: SecretCore（Rust別層）Phase1～4の実装タスクを追加。IPC傍受対策・raw key非返却・難読化・DPAPI/Keychain方針。理由: TS+Rust分離アーキテクチャを確定したため。
- 2026-03-07: OTAセキュア化タスク（OTA開始コマンドHMAC署名/ESP32検証）を新規追加。理由: SecretCore完成後の必須セキュリティ強化のため。
- 2026-03-07: 全タスクをカテゴリ別に再整理（OTA残課題/SecretCore/ネットワーク/鍵管理/ESP32セキュア化/文書管理）。
- 2026-03-07: ESP32書込み前に `beta.x` を更新する継続運用タスクを追加（`bump-esp32-beta-version.ps1` 運用）。
- 2026-03-07: OTA詳細仕様化に合わせ、進捗通知・リトライ制御・SHA256検証の実装タスクを追加。
- 2026-03-07: LocalServer初期実装反映に伴い、実機E2E試験・ESP32受信検証・LocalSoft/LocalSoftForCloud実装タスクを追加。
- 2026-03-07: `(-9984) X509` 解消結果の記録タスクと、新ルータ導入（期限: 2026-03-14）向けの実施/確認/フォールバック廃止タスクを追加。
- 2026-03-07: 新ルータ導入計画の具体値（GW `172.17.1.1` / DNS `172.17.1.100` / DHCP `172.17.1.200+`）と、導入前の暫定フォールバック運用タスクを追加。
- 2026-03-07: `(-9984) X509` 対策として、SAN再発行後のTLS再接続試験タスクを追加。
- 2026-03-07: 専用Wi-Fiルーター導入計画に合わせ、`mqtt.esplab.home.arpa -> 172.17.1.100` 固定割当てタスクを追加。
- 2026-03-07: 暫定運用として「方法① ESP32だけDNS指定（172.16.1.59）」を採用したため、固定依存を廃止する将来タスクと再試験タスクを追加。
- 2026-03-07: ネットワーク運用仕様の検討結果に合わせ、ログイン前自動起動（Windows/macOS）とAWS移行時接続方式、保存期間/削除APIの仕様化タスクを追加。
- 2026-03-06: DNSタスクをCoreDNS前提へ具体化し、外部DNSとの両立確認と公開ドメイン上書き禁止ルールを追加。
- 2026-03-06: `K_device` によるMQTT payload暗号化/復号の必須タスクを追加。
- 2026-03-06: ローカルDNSサーバー導入検討タスクを追加。
- 2026-03-03: ローカルサーバー設計検討の未確定事項（署名検証状況、鍵管理方式、UIスコープ、開発順序、クラウドBroker候補）をTODOへ追加。
- 2026-02-24: 新規作成。第3段階中心のタスクを初期登録。
