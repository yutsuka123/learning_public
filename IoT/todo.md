---
marp: true
---

# IoT TODO

### タスク進捗統計 [2026-05-24]
| 計測日 | 残り個数 / 全個数 | 進捗率 | 備考 |
| :--- | :--- | :--- | :--- |
| **現在 (05-24 セッション④)** | **0 / 90** | **100% 🎉** | **DPAPI 選択モード実装完了 + 両方式 MQTT/OTA E2E テスト PASS**。`key_protect.rs` デュアルモード（`PROTECT_MODE=dpapi` / デフォルト AES-256-GCM）。`020-0026`/`020-0027` 新規追加。 |
| 前回 (05-24 セッション①〜③) | 0 / 90 | 100% | `010-0011`/`010-0012`/`012-0003`/`010-0014`/`020-0022`/`020-0021` 完了・DPAPI→ファイルベース移行・MQTT PASS。OTA は次回へ。 |
| 前々回 (05-23) | 11 / 90 | 約88% | 🎉`012-0008`/`012-0009` 全件クリア。cloud OTA S3 PASS（7205/7206）・タグ `v1.1.0-cloud-ota`。 |
| 前々々回 (05-21) | 13 / 90 | 約85〜86% | `012-0008` 7200〜7209 大部分クリア。CoreDNS bind 限定。 |
| 前々々々回 (05-20) | — | — | 詳細は `todo_old20260520.md` / `todo_old20260523.md` / `todo_old20260524.md` 参照。 |

[運用ルール][2026-05-19] 同日の複数回更新は **1 行に集約**、保持は **直近 5 日分** まで。詳細経緯は `## 変更履歴` 節と各 `todo_oldYYYYMMDD.md` を参照。

[重要] 未完了タスクのみを管理する。  
理由: 現在の実施対象を明確化するため。

[厳守] 各未完了タスクには固定IDを付与し、形式は `章番号3桁-項目番号4桁` とする。  
理由: 他文書や会話でタスクを安定して参照できるようにするため。

[厳守] タスクの削除、統合、後順化が発生しても既存IDは再採番せず、欠番を許容する。  
理由: 後から番号がずれて、他文書や試験記録、会話ログの参照修正が大量発生することを防ぐため。

## 未完了
### 再開インデックス（本書の読む順）[2026-05-24 最終]
- [正] **いまの作業の正**: **全 90 件完了 🎉**（アクティブタスク 0 件）。次の着手は 020 章将来対応（`020-0001` 等）から選択する。
- [最重要][2026-05-24] **`010-0011` / `010-0012` / `012-0003` 完了**（CoreDNS ゾーン管理ルール §2.4.2 / DHCP DNS 廃止計画 §2.4.3 / ドキュメント概要↔設計概要 相互参照）。残 **0/90**（**100% 完了**）。
- [最重要][2026-05-24] **`010-0014` 完了**（IoT LAN Firewall サブネット限定：`iotLanSubnet` 設定化・`applyIoTFirewallRules.ps1`・`GET/POST /api/admin/network/*`・`firewallConfigCard` UI・仕様書 §7.4.4c・§3.11・試験仕様書 7110）。[注] Firewall 実適用はスクリプトを管理者 PowerShell で手動実行要。
- [最重要][2026-05-24] **`020-0022` 完了**（LocalServer cloud/local mode 切替 UI：`brokerMode` 永続化・`disconnect()` I/F・`buildGateway()`・`GET/POST /api/admin/broker-mode`・`brokerModeCard` UI・管理画面仕様書 §3.10・試験仕様書 7109）。残 **4/90**（約 96%）。
- [最重要][2026-05-24] **`020-0021` 完了**（`mqttFallbackIp` NVS 保存・AP REST API・`pingBrokerHost` NVS 優先・試験仕様書 7089 追加・AP 仕様書 / MQTT 仕様書更新）。残 **5/90**（約 94%）。
- [最重要][2026-05-24] **`020-0020` 完了退避**（#0047 試験記録化）＋ **`020-0016/0017/0019/0025` 正式退避完了**（`todo_old20260524.md`）。残 **6/90**（約 93%）。
- [最重要][2026-05-23] **`012` 章全件完了**（`012-0001`〜`012-0009`）。Git タグ `v1.1.0-cloud-ota`。クラウド OTA S3 presigned URL PASS（7205/7206）・dual-partition 確認・文書整合完了。
- [重要] **鍵・将来対応**: `k-iot` は当面試験用を本番にも流用し、鍵新規発行は行わない。製品鍵分離は **`020-0001`**。クラウドは **`012` 章**（全件完了済み）。
- [重要] **文書引継ぎ**: 完了タスクは `todo_old20260430.md`（2026-04-30 クローズ群）および過去の `todo_old*.md` を参照。
- [重要] **中長期の正**（ローカル第3段階完了後）: `007-A` → `007-B` → `007-C`（4点証跡・安定性セットは `本番セキュア化出荷準備試験計画書.md` を正とする）。
- [厳守] 詳細な日別経緯の一次記録は `試験記録書.md` / `問題点記録書.md` を正とし、本節は判断と着手順の索引に留める。

### 007入口・鍵・手順索引（一式）[2026-04-28 起稿 / 2026-04-29 改訂]
- [目的] `007-A`・`7099` / `007-0009` を**同一作業日内で欠落なく**進めるためのチェックリスト。
- [重要][2026-04-29] **`k-iot-secure-boot` / `k-iot-flash-encryption` は当面、試験用（`コマンド仕様書.md` **§10.2.5**）と同一**で本番にも用いる。旧 `004-0006` / `004-0007`（別生成）は **`todo_old20260429.md` へ方針変更退避済み**。本番専用へ分離するときは **`020-0001`** を実施し **§10.2.5a** を正とする。**2026-04-29 12:27 方針変更: 今回の `007-B` では鍵新規発行なし。**
- [重要][2026-04-29] **判断基準**: 鍵を製品用へ切り替えても**仕組み（実装・手順・設定・証跡・判定条件）が変わらない**間は、当面この方針を維持する。**仕組みが変わる場合のみ**、`020-0001` の一環として追加試験を行う。
1. **`007-0011`**: **`[完了]`** — 試験記録は `試験記録書.md` **`7099`** の **2026-04-28** ブロック（`DEV-002`実測）および文書進捗 [進捗][2026-04-29]。**不可逆着手当日は** **`007-A.1`** で `7099` 同日可否を判断。
2. **`007-0004` / `007-0006`**: **`[完了]`** — `試験記録書.md` **`7095`**。本番（`DEV-002`）は **別表複写**。
3. **`7099` / `007-0009`**：最新の記録・クローズ状況は `試験記録書.md` の **`7099`** 欄を正とする。**本番1台目（`007`）の各段階**では計画書所定の安定性セットを省略しない。短時間の切り分けに限り、同欄の **「当日次回計画 2026-04-04b」**（AP5分 / STA5分 / OTA×2）を簡易ゲートとして索引できる。**10分派生や正式な所要・合否**は `本番セキュア化出荷準備試験計画書.md` を正とする。
4. **`007-B` 以降**は、`007-A` 完了・案A・ゲーターを満たした場合のみ着手する。

### 直近ゴール・現在地サマリ（2026-05-23 セッション終了時点）
- [🎉最重要][2026-05-21 続³] **ESP32 → AWS IoT Core 接続成功！** `connectToMqttBroker success. state=0` / `Certificate verified` / `publishStatusNotice success. topic=esp32lab/notice/status/IoT_04CEF94EB580 onlineState=Online`。**`012-0008` 7200 試験クリア**。`#0048` の真因は当初推測の DNS だけではなく、複合要因：
  1. `pingBrokerHost` のローカル fallback IP 強制使用バグ（`#0047`）→ 修正済
  2. ESP32 の AP `AP-IoTESP32Test` は閉域 AP（インターネット未接続）。クラウドモードでは `Buffalo-G-41E0`（WAN 接続あり）へ切替が必要 → `.env` に `CLOUD_WIFI_SSID`/`CLOUD_WIFI_PASSWORD` 追加・回復スクリプトで JSON へ書き出し・`ensureDefaultFileExists` を wifi 移行対応に拡張・`SENSITIVE_DATA_USE_HEADER_VALUES` を cloud で wifi 上書きしないようガード
  3. `MqttTlsClient::connect(IPAddress, port)` override で client cert/key を nullptr 固定 → AWS が mTLS で TCP リセット → `setClientAuthContext` 追加し IP 接続経路でも cert/key を渡すよう修正
  4. `willRetain=true` で MQTT CONNECT 拒否（AWS IoT Core は retained will message に `iot:RetainPublish` Policy 権限要求） → cloud mode のみ `willRetain=false` に固定（local モードは無変更）
- [最重要][2026-05-21 続²] **PC 側 DNS インフラ修正完了**：`C:\coredns\Corefile` を `.:53 { bind 172.17.1.100; ... }` に変更（bind plugin 追加、ゾーン header は `.:53` のまま）/ CoreDNS 再起動（172.17.1.100:53 専用 bind）/ PC からの nslookup で内部・外部 FQDN とも解決成功。**ICS（SharedAccess）停止は管理者権限でも失敗**（Cannot open service、SYSTEM-only ACL 推定）が、CoreDNS bind 限定で実害なし。
- [最重要][2026-05-21 続] **`#0048` 真因特定**（他ツール調査連携）：(a) ルータ Aterm 172.17.1.1 が DNS Query refused（外部・内部 FQDN とも） / (b) PC 上で CoreDNS（PID 13176）と ICS / SharedAccess（PID 5392）が UDP/53 競合 / (c) CoreDNS 設定 `C:\coredns\Corefile` 自体に ACL 制限なし。**Corefile bind を 172.17.1.100:53 へ限定 + ICS 停止 + ルータ DNS フィルタ OFF** が解消手順。**`020-0021` 新規追加**（ESP32 ローカル broker IP の AP 設定化）。未完了 **14 / 87**（約 84%）。
- [最重要][2026-05-21] **5 件進捗 + 2 件新規バグ修正 + 1 件未解決ブロッカー発見**。
- [最重要] **本セッションの作業**：
  1. ✅ **SecretCore 再ビルド**（`cargo build`）→ ENOENT 解消。前提：LocalServer + 旧 SecretCore プロセス停止後に再ビルド。
  2. ✅ **回復スクリプト実行成功**：`createSensitiveDataJsonForRecovery.cjs IoT_04CEF94EB580 cloud` → k-device fingerprint=`2445cc99fcc4c4e7` 復元 + `mqtt.brokerMode=cloud` + `cloudEndpoint` 同時付加した sensitiveData.json 生成。
  3. ✅ **LittleFS uploadfs**（COM4）→ 証明書 4 ファイル + sensitiveData.json 反映。
  4. ✅ **FW upload**（COM4、`esp32s3_secure`）→ 020-0017A/B 修正版を反映。
  5. ✅ **020-0017A/B 効果確認**：シリアルで `secureNvsInit::initializeSecureNvs: plaintext NVS initialized successfully (fallback)`、再フラッシュ後も k-device 保持。
  6. ✅ **`#0047` 新規発見 → 修正済**：cloud mode で `pingBrokerHost` がローカル broker IP（172.17.1.100）を強制使用し AWS TLS が `(-9984) X509` で失敗していたバグ。`mqtt.cpp:pingBrokerHost` を `mqttBrokerMode=="cloud"` 時に DNS のみ使用するよう分岐追加（local は完全に既存挙動維持）。
  7. ✅ **`mqtt.cpp:connectToMqttBroker` に cloud 専用 DNS 再設定追加**：cloud のみ `primary=172.17.1.100 (CoreDNS), secondary=8.8.8.8` を明示。local は無変更。
  8. ❌ **`#0048` 新規発見・未解決ブロッカー**：上記修正後も `hostByName(): DNS Failed for ...amazonaws.com` が約 8 秒タイムアウトで継続。PC からは CoreDNS / 8.8.8.8 とも解決可。**ESP32 → DNS サーバの経路問題**（AP クライアント分離 or CoreDNS ACL or ルータ設定）。
- [最重要] **ファームウェア現在状態（COM4 ESP32）**：
  - 最新 FW（`#0047` 対応 + cloud DNS 明示設定）反映済み
  - brokerMode = cloud（NVS + sensitiveData.json）
  - k-device 保持確認済み
  - DNS 経路問題で MQTT 接続まで到達できず（再起動毎に `MQTT RECON FAIL` ループ）
- [重要] **`#0048` ESP32 → DNS 経路** 切り分け候補（次セッション）：
  - (a) 物理 AP（`AP-IoTESP32Test`）のクライアント分離設定確認
  - (b) PC 上 CoreDNS の bind / ACL 設定確認（CoreDNS 設定ファイル所在を特定）
  - (c) PC ホストファイアウォール 53 ポートの実効ブロック有無
  - (d) ルータ（172.17.1.1）の DNS forwarding / NAT 設定
- [最重要] **`007-B` 不可逆本体**: 依然 **No-Go 継続**。
- [重要] **AWS・コスト管理**：`CLOUD_MQTT_ENABLED=false`（LocalServer 側）。ESP32 単独で AWS への接続試行はしているが、DNS 解決不可のため AWS 到達なし → 課金なし。次セッションで DNS 経路解消後、AWS 接続成立時に試験用 `CLOUD_MQTT_ENABLED=true` を確認。

#### 旧直近ゴール（2026-05-20 セッション③終了時点・参考用）
- [到達点][2026-05-20 ③] **`020-0017A/B` コード修正完了**（build SUCCESS）。未完了 **11 / 84**（約 87%）。SecretCore health check 失敗で回復スクリプトブロック中。

#### 旧直近ゴール（2026-05-20 セッション②終了時点・参考用）
- [到達点][2026-05-20 ②] **`020-0016` 根本原因特定完了**。Bug #3（`missingMqttConfig` X.509 対応）修正。`020-0017`/`020-0018` 追加。未完了 **11 / 84**（87%）。

#### 旧直近ゴール（2026-05-20 セッション①終了時点・参考用）
- [到達点][2026-05-20 ①] **`012-0001` / `012-0002` / `012-0004` / `012-0005` / `009-0004` / `012-0007` / `012-0006` 完了**。未完了 **9 / 82**（約 89%）。**AWS 環境構築完了**（X.509 cert 2種発行 ACTIVE / IoT Policy 2件・アタッチ / Thing `esp32s3-dev001` / LittleFS 証明書配備 / `.env` 7項目追記 / `.gitignore` 追加）。実施記録: `クラウド構築手順書.md §10`。退避先: `todo_old20260520.md`。

#### 旧直近ゴール（2026-05-19 セッション終了時点・参考用）
- [到達点][2026-05-19] **`009-0008` 〜 `009-0024` / `009-1020` / `010-0001` 完了**＋ `009-0014` 不採用＋ `007→020` 3 件移管＋ `013` 章 5 件全件完了（`todo_old20260519.md`）。**TPM→DPAPI 整合 19 文書 / HKDF→HMAC 整合 8 文書**。未完了 **15 / 81**（81%）。Git タグ `v1.0.0-local` / `local-env-complete-20260519` 付与。

#### 次セッション開始メモ（2026-05-24 セッション④終了時点）[最新・正]
- [最重要][2026-05-24④] **DPAPI 選択モード実装 + 両方式 MQTT/OTA E2E テスト完了 🎉**
  - `SecretCore/src/key_protect.rs` デュアルモード実装（`PROTECT_MODE=dpapi` / デフォルト AES-256-GCM）
  - ファイルベース AES-256-GCM: `data/keys/master_key.bin`（32B）+ `wrapped_secret.bin`（280B）
  - DPAPI: `PROTECT_MODE=dpapi` を `.env` に追加すると有効化、`wrapped_secret.bin`（552B）
  - **両方式テスト結果**:
    - DPAPI: MQTT PASS + OTA PASS（partition 1→0）
    - ファイルベース AES-256-GCM: MQTT PASS + OTA PASS（partition 0→1）
  - 現在: ファイルベースモードで復元済み（`.env` に `PROTECT_MODE` 未設定）
- [最重要] **実環境の現在状態（2026-05-24④セッション終了時点）**：
  - ESP32: local mode（brokerMode=local, AP-IoTESP32Test, 172.17.1.200, FW=1.1.0-beta.40, partition=1）
  - k-device=`9a57504897a75c5c`（ファイルベースモード、LittleFS + NVS 移行済み）
  - LocalServer: **起動中**（local mode, CLOUD_MQTT_ENABLED=false, PROTECT_MODE 未設定 = ファイルベース）
  - SecretCore: `data/keys/` 使用中（master_key.bin=32B, wrapped_secret.bin=280B）
  - Mosquitto: 稼働中 / CoreDNS: Corefile bind 172.17.1.100 限定
  - AWS: 無接続（課金なし）。S3 バケット空
- [最重要] **次の本線選択肢**：
  1. `020-0026`: PROTECT_MODE 鍵作成時対話選択 UX（`.env` 手書き不要化）
  2. `020-0001`: 本番鍵分離
  3. `020-0011`/`020-0012`: 不可逆処理 ProductionTool 実装
  4. 020 章その他将来対応（`020-0004`/`020-0007`〜`020-0010`/`020-0023`〜`020-0024`）
- [重要] **クラウド OTA 再試験手順**（次回試験時）:
  1. FW ビルド: `cd IoT/ESP32 && pio run -e esp32s3_secure`
  2. S3 アップロード: `aws s3 cp .pio/build/esp32s3_secure/firmware.bin s3://esp32lab-firmware-603480426819/firmware/<version>/firmware.bin`
  3. `クラウド構築手順書.md §11` 以降の手順に従う
- [重要] **AWS 認証情報**: `IoT/LocalServer/.env`（Git 除外済み）に保存済み
- [Claude Code / Cursor 共通入口] **新セッション開始時は `.cursorrules`（0番）→ `CLAUDE.md`（自動読込）→ `IoT/todo.md` の順で読む**（`CLAUDE.md §4` 参照）。

#### 次セッション開始メモ（2026-05-24 セッション③終了時点・旧版・参考用）
- [到達点] DPAPI → ファイルベース AES-256-GCM 移行完了。MQTT PASS。OTA は次回へ（→ セッション④で完了）。

#### 次セッション開始メモ（2026-05-23 最終・旧版・参考用）
- [最重要][2026-05-23] **`012` 章全件完了**。進捗 11/90（約 88%）。020-0016/0017/0019/0025 は 012-0008 作業中に実質対処済み → 2026-05-24 正式退避完了（`todo_old20260524.md`）。

#### 次セッション開始メモ（2026-05-21 セッション終了時点・旧版・参考用）
- [最重要][2026-05-21] 未完了 **13 / 86**（約 85%）。本セッション: SecretCore 再ビルド・回復スクリプト・FW フラッシュ・`#0047` cloud DNS バイパス修正・`#0048` DNS 経路ブロッカー発見。
- [最重要] **⚠️ 次回開始時の ESP32 現在状態**：
  - k-device 保持確認済み（020-0017A 効果）
  - brokerMode = cloud（NVS + LittleFS sensitiveData.json）
  - LittleFS に証明書 4 件 + sensitiveData.json あり
  - 最新 FW（`#0047` 対応 + cloud DNS 明示設定）反映済み
  - DNS 経路問題で MQTT 接続まで到達できず（`hostByName Failed → MQTT RECON FAIL` ループ）
- [最重要] **⚠️ 次回の最優先着手順序**：
  1. **`020-0019` DNS 経路調査**（`012-0008` 着手前提）：
     - PC 上 CoreDNS の設定ファイル所在を特定（`Corefile` 検索、Windows サービス・Docker・直接バイナリのいずれか）
     - CoreDNS の bind アドレス / ACL plugin / hosts plugin を確認し、ESP32（172.17.1.200）からの forward を許可するよう設定変更
     - もしくは物理 AP（`AP-IoTESP32Test`）の管理画面で「クライアント分離（AP isolation / wireless isolation）」設定を解除
     - 検証方法: ESP32 を再フラッシュせずに、シリアルで `connectToMqttBroker: cloud DNS configured. primary=172.17.1.100 secondary=8.8.8.8` の直後の `hostByName` が成功するかを確認
  2. **DNS 解決成功後**、`012-0008` クラウド統合試験 `7200`〜`7209` を順次実施
  3. **LocalServer 側で `CLOUD_MQTT_ENABLED=true`** に設定し、サブスクライバを起動（`クラウド構築手順書.md §9` 参照）
  4. 試験合格後 **`012-0009`**（まとめ・文書整合・`v1.0.0-cloud-test` タグ）
- [重要] **本セッションのコード修正（未コミット）**：
  - `IoT/ESP32/src/MQTT/mqtt.cpp`: `pingBrokerHost` cloud mode 分岐追加 / `connectToMqttBroker` cloud DNS 明示設定追加（local mode はデグレなし）
  - `IoT/問題点記録書.md`: `#0047`（cloud DNS バイパスバグ）/ `#0048`（ESP32 → DNS 経路不通）追加
  - `IoT/todo.md`: 進捗統計 05-21 行・サマリ・次セッション開始メモ・`020-0019` / `020-0020` 追加
- [重要] **アーキテクチャ確定（Option B）・実装完了・AWS 環境構築完了**：
  - ESP32 → AWS IoT Core（X.509, TLS 8883）← LocalServer（subscriber, X.509）
  - 証明書正本: `C:\mydata\project\myproject\certs\esp32\` / `localserver\`（Git 管理外）
  - LittleFS 配備先: `IoT/ESP32/data/certs/`（`.gitignore` 除外済み）
  - `.env` に `CLOUD_MQTT_ENABLED=false`（本日停止済み）/ `AWS_IOT_ENDPOINT` / 証明書パス 設定済み
- [重要] **FW バグ修正状況**（未コミット・次回セッションでコミット要）：
  - `IoT/ESP32/src/mqtt.cpp`: Bug #1 cloudEndpoint スコープ修正
  - `IoT/ESP32/src/main.cpp`: Bug #2 cloudModeActive ガード / Bug #3 missingMqttConfig X.509 対応
  - `IoT/LocalServer/scripts/createSensitiveDataJsonForRecovery.cjs`: `cloud` 引数追加
- [重要] **コスト管理（厳守）**：試験終了後は必ず `CLOUD_MQTT_ENABLED=false` に戻すこと（本日実施済み）。試験開始前に `true` に戻す手順: `クラウド構築手順書.md §9`。
- [重要] **AWS リソース一覧**（`クラウド構築手順書.md §10` に詳細記録）：
  - Endpoint: `***-ats.iot.ap-northeast-1.amazonaws.com`（実値は `.env` の `AWS_IOT_ENDPOINT`）
  - ESP32 cert: `cert_id=0a38a0c5...`（`certs\esp32\cert_meta.json`）・Policy `esp32lab-esp32-policy`
  - LocalServer cert: `cert_id=5311eb1a...`（`certs\localserver\cert_meta.json`）・Policy `esp32lab-localserver-policy`
  - Thing: `esp32s3-dev001`
- [重要] **AWS 認証情報**：`IoT/LocalServer/.env`（Git 除外）に `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` / `AWS_DEFAULT_REGION=ap-northeast-1` 保存済み。
- [Claude Code / Cursor 共通入口] **新セッション開始時は `.cursorrules`（0番）→ `CLAUDE.md`（自動読込）→ `IoT/todo.md` の順で読む**（`CLAUDE.md §4` 参照）。

#### 旧次セッション開始メモ（2026-05-19 セッション終了時点・参考用）
- [最重要][セッション終了][2026-05-19] **ローカル環境構築完了マイルストーン**。Git タグ **`v1.0.0-local`** / **`local-env-complete-20260519`** 付与済み。未完了 **15 / 81**（約 81%）。本日累計 **21 件完了退避 + 1 件不採用 + 3 件 007→020 移管 + 020 章 10 件追加 + 013 章 5 件全件完了**。
- [最重要] 次回着手は `012-0004`（責務分界表初版）→ **完了（2026-05-20）**。
- [重要] **AWS 認証情報**：`IoT/LocalServer/.env`（Git 除外、`.gitignore:10` で確認済み）に保存済み。
- [重要] **TPM → DPAPI 整合**（19 文書）／**`k-device` HKDF → HMAC 整合**（8 文書）／**MQTT topic 正本 `esp32lab/<kind>/<sub>/<name>`** 確定／**「Dual バンク」= OTA Rollback** 正式名称確定。設計仕様書の用語ぶれは全解消済み。

#### 旧次セッション開始メモ（2026-05-19 続² 時点、参考用）
- [セッション終了][2026-05-19] 未完了 **32 / 70**（約54%）。本日完了: **`009-0008` / `009-0009` / `009-0012`**（退避済み `todo_old20260519.md`）。**TPM → DPAPI 整合**（19 文書、`SecretCore/src/dpapi.rs` + `key_manager.rs:84-115 load_or_create_s_random`）。**`k-device` HKDF → HMAC 整合**（8 文書、`key_manager.rs:454-465 get_k_device` の HMAC-SHA256 直接導出に揃える）。Mac/Linux 汎用化は **`020-0004`** を新規追加。`007-B` は **No-Go 継続**。
- [最重要] 次回は 5 件のクローズ候補を順次確認・退避する。順序案: **`009-0014`**（MQTT topic 統一: `mqttProtocol.cpp:67,71`）→ **`009-0017`**（AES-256-GCM 復号: `maintenanceApServer.cpp:902-922` / `MQTT/mqtt.cpp:614-632`）→ **`009-0020`**（current/previous + 48h/240h: `sensitiveData.cpp:38-39,1313`）→ **`009-0018`**（初回ペアリング AP モード物理操作: `input.cpp:112,118`）→ **`009-0021`**（AP 復旧モード: `maintenanceApServer.cpp` + `APメンテナンス画面仕様書.md`）。各タスクで証跡を最終確認したうえで退避する。
- [残作業] クローズ候補退避後の残り 5 件: `009-0010`（Phase2 UI/CLI 手順確定）/ `009-0011`（認証ロジック公開範囲）/ `009-0015`（NVS 移行）/ `009-0016`（MQTT 最終運用値）/ `009-0023`（Wi-Fi state machine）/ `009-0024`（時刻サーバー NVS 優先）。それぞれ個別判断で進める。
- [注意] **Node.js バージョン**: `better-sqlite3` は **v22 でビルド済み**（NODE_MODULE_VERSION=127）。`nvm use 22` で v22.x に切り替えてから `npm run start` すること。v20 のまま起動すると `ERR_DLOPEN_FAILED` で即終了する。
- [最重要] **`007-B` 不可逆本体は No-Go 継続**。`ProductionTool` 最終形実ランナー（`precheck -> stage_execute -> readback -> evidence -> stability`）の実装・検証完了まで `espefuse burn_*` / `espsecure` 本番書込み / `write_flash --force` を実行しない。
- [ESP32 書込み注意] 標準書込みは `IoT/ESP32/platformio.ini` の `board_upload.offset_address=0x30000` を正とする（`ESP32_APP_OFFSET=0x30000`）。`IoT/ESP32/data/sensitiveData.json` は削除済み。raw `k-device` は文書・ログに記載しない。
- [証跡索引] `7001`（Task Scheduler 自動起動）/ `7102`〜`7107`（SQLite 履歴・定期エクスポート）/ `7108`（双方向テスト OK）。
- [正本索引] `試験記録書.md`: `7001` / `7102`〜`7108`。`todo_old20260517.md`: `009-0001`〜`009-0003`。`todo_old20260518.md`: `009-0006`。`設計議事録20260518.md`: 2026-05-18 作業要約。

#### 直近ゴールの沿革（念のため保存・要約）
- [歴史][2026-04-04〜05] NIC 未接続による broker 経路断、`7099` / MQTT オンライン維持、`DEV-002` パーティション最適化と保全の経緯は `試験記録書.md` / `問題点記録書.md`（`#0023` 等）に記録済み。
- [歴史][2026-04-05〜06] `DEV-002` 16MB パーティション候補A適用、`SecretCore` MQTT 常駐接続化、`7041` AP 到達ブロッカー（`Wi-Fi_lab` 等）の切り分けは `008-0052` 本文および `#0026` に集約。
- [歴史][2026-04-04c] `esp32s3_secure_final` のスタック・ログ強化・`APP_SECURE_FINAL_BUILD` 導入は `platformio.ini` / FW 履歴を参照。
- [補足][2026-04-17] 上記沿革の「次に何をするか」はすべて **案A 評価**へ統合済み。旧「次回手順」で `007-B` 直前まで列挙していた項目のうち未完了のものは、`007-0005` / `7095` / `PC-012`〜`014` など **可逆範囲の整理**として実施可否を都度判断する（不可逆は不可）。

### 手動確認待ち（2026-03-24整理）
- [最重要] 現時点の手動確認待ちはなし（2026-03-24時点）。
- [重要] 手動確認時の主参照先は以下とする。
  - `試験仕様書.md`: `7090` 本試験手順、`7041` 再開条件
  - `試験記録書.md`: `7090` 再開テンプレート、`7041` 再開テンプレート
  - `コマンド仕様書.md`: `ProductionToolSetup.exe` 生成コマンド、`7041` 実行コマンド
  - `問題点記録書.md`: `#0010`（AP未到達）、`#0011`（`ISCC.exe` 未導入）
- [厳守] 手動確認が終わるまで、不可逆処理本体（eFuse / Secure Boot / Flash Encryption の実行）へ進まない。

### セッション引継ぎ・可逆作業のチェックリスト（正）[2026-04-29 更新]
- [最重要][2026-04-28] 本日の終了時点で、`DEV-002`（`IoT_04CEF94EB580`）の `007-0011` / `7099` 当日再確認は文書・実機とも追記済み。`OTA x2`、`STA 10分`、`AP` 約 `10` 分相当、`STA` 復帰 `onlineState=online` の結果は `試験記録書.md` `7099` の 2026-04-28 追記を正とする。
- [最重要][2026-04-29] 次回開始時は、**`todo.md` 冒頭** → **`試験記録書.md` `7095` [固定]**（4点証跡の保存先・go/no-go）→ **`試験記録書.md` `7099` 最新** → **案A 直前 Go/No-Go**（`最終セキュア化方式選定メモ.md`）の順で再開する。条件が欠ける場合は `007-B` へ進まない。
- [最重要][2026-04-07] **案B No-Go**。次の実装判断は **案A**（`最終セキュア化方式選定メモ.md` 8章）。
- [重要][2026-04-07] **`008-0052` 完了済み**（`7041:pipeline:strict=FULL_COMPLETED`、`7101=OK` 等）。2026-04-06 時点の「7041 未回復なら次段へ進むな」指示は **目的は達成済み**（同日以降の追記・`008-0052` 完了欄を参照）。
- [厳守][2026-04-23] **`007-B` は No-Go のまま**。案A（または案C への分岐）の Go/No-Go が確定するまで不可逆に入らない。
- [重要][2026-04-04] `7041` の `expectedMac` は **AP 表示 MAC** を正とする（例: `F0:D0:F9:4E:B5:80`。SoC 表記と逆順にならないよう注意）。
- [重要][2026-04-04] MQTT/broker 側が不安定なときは、**NIC と broker 経路**（例: `172.17.1.100:8883`）を先に確認する（過去の主因: NIC 未接続）。
- [重要][2026-05-14] `app0=0x30000` の間は、`IoT/ESP32/platformio.ini` の `board_upload.offset_address=0x30000` と `scripts/ensure_app0_upload_offset.py` を正とし、標準 `platformio -t upload` でも `0x30000` へ書く。`0x10000` へ戻る設定変更は禁止。
- [重要][2026-04-06] `7087:normal` / `7087` / `7041` で `ProductionTool` を使う場合は **既定 Pipe** `\\\\.\\pipe\\iot-secret-core-ipc` の `SecretCore` 待受が必要。
- [重要][2026-04-07] `7041` で AP 経路が詰まる場合は `LocalServer` の **AP 接続アダプタ名**（例: `wifiUsbInterfaceName=Wi-Fi_lab`）と `7041-apdiag-*` 証跡を優先する。
- [重要][2026-04-04] `NVS patternA/patternC` 方針（開発時 plain fallback 可・製品時 secure 固定）は `008-0040` / `本番セキュア化出荷準備試験計画書.md` / `製品化成果概要書.md` を正とする。
- [厳守][2026-04-29] 不可逆の前に `006` 完了条件、**`k-iot` が試験用（§10.2.5）で足りる旨の確認**（`todo_old20260429.md`）、**自動検証観点**（`005-0008`・`ProductionTool画面仕様書.md` 9.4〜9.4.6 および `試験記録書.md` 突合）、**4点証跡は `試験記録書.md` `7095` で試験機分まで集済み（本番は別表）** とすること。NVS暗号化関連は旧仕様へ退避済み。
- [厳守] まず読むファイル（案A・`007` 準備時）: `IoT/todo.md`、`IoT/最終セキュア化方式選定メモ.md`、`IoT/本番セキュア化出荷準備試験計画書.md`、`IoT/試験仕様書.md`、`IoT/試験記録書.md`、`IoT/セキュリティ方針_認証情報取扱い.md`、`IoT/問題点記録書.md`（`#0025`/`#0026`）、`IoT/デバイス管理表.md`。
- [推奨] `7099` / `007-0005` / `PC-012`〜`PC-014` / `7095` は **可逆範囲の整理**として、案A実装と並行で欠落がないか確認する（`試験記録書.md` の最新追記を正とする）。
- [旧コピペ][2026-04-17] 2026-04-04〜06 の「次セッション用コピペ指示」3種は **状況が前進したため集約**した。原文が必要な場合は **git 履歴**（本ファイルの 2026-04-17 より前の版）を参照する。

#### 次セッション用コピペ指示（2026-04-30 続・正）
- [重要] 次セッション開始時は、以下をそのまま貼ってよい。

```text
IoT プロジェクトの継続です。回答は日本語でお願いします。

現在地（2026-04-30 続）:
- `007-0004`/`007-0006`/`007-0011`/`007-0013` の退避記録・監査経路は `todo_old20260430.md` と、`試験記録書.md` **`7095`** / **`7099`**（[`更新`][2026-04-28]）、**`機能仕様書.md` §1.5**（出荷後 OTA 主）を参照。
- **`007-A` の準備は一段完了** とするが、**`007-B`（不可逆）は `7095` [固定]** のとおり **自動 Go にならない**（案A直前ゲート・本番 **`7095` 別表**・当日 **`007-A.1`** 等）。
- `005`/`006` 完了済み、`DEV-002` の `7099` 実測（2026-04-28）の正本は `試験記録書.md`。

次の依頼:
1. `最終セキュア化方式選定メモ.md` の **案A 直前 Go/No-Go** を確認する。
2. 不可逆を始める**作業日**なら、**`007-A.1`** に従い **`7099` を同日に再実行するか**判断し、実施したら `試験記録書.md` と（本番なら）`7095` 別表へ追記する。
3. 上記が揃ったうえでのみ **`007-B` 進入可否** を判断する。
```

#### 次セッション用コピペ指示（2026-04-17・集約版）
- [重要] 次セッション開始時は、以下をそのまま貼ってよい。

```text
IoT プロジェクトの継続です。回答は日本語でお願いします。

現在地（2026-04-23 時点の todo.md 集約）:
- `008-0042` は退避済み。最終運用のシリアル方針は `008-0001` の上位方針として残し、`7100` 証跡は `todo_old20260423.md` 側を正とする。
- `007-0012`、`008-0050`〜`008-0052` も退避済み。`007-B` 不可逆はまだ No-Go のまま。
- `008` 章の残件は、`008-0007`、`008-0010`〜`008-0049` の未完了群とする。`008-0001`〜`008-0006`、`008-0008`、`008-0009` は完了退避済み。重複や完了済みのものがあれば、根拠を確認して退避側へ移してください。
- [補足][2026-05-07] `008-0006`〜`008-0009` の HMAC は `OTA開始` / `重要設定変更` のコマンド署名であり、NVS暗号化 HMAC 旧仕様とは別物。`7042` と `7083` により OTA 開始側の HMAC は試験済みで、残件は **`008-0007`** のみとする。
- `009-0004` は将来の `012-0004` / `012-0005` と責務分界を合わせる前提のため、単独完了扱いにしない。
- 文書タスクの直近スコープは `006-0011` の再試行条件確認。`011-0002` は完了退避済みで、クラウド化は `012` 章（`012-0001` 以降）のみで追う。

依頼:
1. 上記の正として IoT/todo.md の「再開インデックス」「直近ゴール・現在地サマリ」「セッション引継ぎ・可逆作業のチェックリスト」を読み、現在の未完了タスクが完了済みと矛盾していないか確認してください。
2. `008` 章では、完了済み・証跡完了済みのものを優先して退避候補にしてください。特に `008-0042` のように上位方針と重複しているものは、退避側を正にしてください。
3. `007-B` に入る判断を求められたら拒否し、案A と 006 ゲートを先に満たすよう促してください。
4. 文書更新時は変更理由・根拠・出典を残してください。完了済みタスクを残す場合は、残す理由を明示してください。
```
- [変更理由][2026-04-17] セッション引継ぎが日付重複で矛盾しやすかったため、**正の一覧**と **単一コピペ**に集約し、クラウド第4段階を同一ファイル内で参照可能にするため。

### 001. 直近フェーズA: OTA安全性の確認（最優先）
- [重要] [念のため保存] 001章の完了タスクは `todo_old20260312.md` へ退避済み。

### 002. 直近フェーズB: ESP32設定永続化とAPメンテナンス
- [重要] [念のため保存] 002章の完了タスクは `todo_old20260313.md` へ退避済み。
- [フェーズB 実施状況][2026-03-14]
  - 完了条件: 002-0001〜002-0004、002-0019〜002-0022、002-0027、002-0028 は文書・コード確認で完了済み。詳細は `todo_old20260313.md`。
  - [完了][2026-03-14] 002-0099 ゲート完了。7027/7064/7065/7066/7067 全 OK、試験で変更したパスワードはデフォルトへ復元済み。退避先: `todo_old20260313.md`。

## 直近実行順序（2026-03-25再整理・2026-04-28改訂）
0. [最重要][2026-04-27] `007` 進入判断は `最終セキュア化方式選定メモ.md` の当日状態を必ず併読し、`006-0011` 完了証跡を前提にする。
1. [完了][2026-04-28] 自動検証観点（旧 `005-0008`）は `ProductionTool画面仕様書.md` 9.4〜**9.4.6** と `試験記録書.md` で突合済み（`todo_old20260428.md` 参照）。以後は `007-A` -> `007-B` -> `007-C` の順で進入条件を満たす。
  - 対象の主軸: `007-A`, `007-B`, `007-C`, `7019`〜`7024`, `007-0009` / `7099`
2. [重要][2026-04-29] `k-iot` は当面**試験用と同一**（`コマンド仕様書.md` §10.2.5、根拠 `todo_old20260429.md`）。本番専用分離は **`020-0001`**。
3. [参照][2026-04-28] `007` の自動検証手順は 9.4 の `AV-001`〜`006` を正とし、本番1台目でも同観点で 4点証跡を取る。
4. [最重要] フェーズG（本番1台目）は `007-A` -> `007-B` -> `007-C` の順で実施する。
   - 段階1: 実行前最終確認（鍵ID、対象機識別、電源、通信、監査開始）
   - 段階2: 不可逆処理の段階実行（`Flash Encryption` -> `Secure Boot v2` -> `JTAG/DL封鎖`）
   - 段階3: 各段階の読戻し検証と再起動健全性確認（失敗時は即停止）
   - 段階4: `7025` による OTA 継続更新可否の確認
   - 段階5: `7100` による JTAG/シリアル安全確認
   - 段階6: レビューゲートまたは2台目切替条件判定
5. [禁止] 1台目の段階完了と記録確定前に、複数台展開や同時並行実行を行わない。
6. [厳守][2026-03-25] 1台目で `NG` の場合は「即2台目へ進む」のではなく、先に1台目を隔離判定して失敗理由を4点証跡で確定し、その後に2台目着手可否を判断する。

### 直近の章別整理（005 -> 006 -> 007 の見え方整理）
- [重要][2026-03-25] 章番号の並びと、**いま実際に進める順番**は完全一致しない。
- `005` 章:
  - [重要] `005-0001`〜`005-0007` は完了済み。
  - [完了][2026-04-28] `005-0008` は `todo_old20260428.md` へ退避済み。本章に未完了タスクはない。
- `006` 章:
  - [最優先] `006-A` / `006-B` は完了済み。`006-0011` の完了証跡を正とし、以後は `007` 着手条件の整理へ移る。
  - [厳守] `006-C` の出口条件を満たした後で `007` 準備へ進む（自動検証観点は `ProductionTool画面仕様書.md` 9.4.6 で試験記録と突合済み）。`k-iot` は当面試験用と同一（`todo_old20260429.md`）。
- `007` 章:
  - [厳守] `006-0001`〜`006-0011` 完了後のみ着手する。
  - [厳守] 実行順は `007-A` -> `007-B` -> `007-C` 固定。
  - [厳守][2026-04-29修正] `007-B` の現行段階順は `FE -> NVS暗号化なし確認（非焼込みゲート） -> SB -> 封鎖` とし、`RD_DIS` は FE鍵とSBダイジェスト投入後に扱う。NVS HMAC鍵（EF-009b）は本番不可逆として焼かない。
- [最重要][2026-04-29] したがって、**現時点の実行順**は次の 2 ステップで整理する。
  1. `007-A`（事前準備・4点証跡・`007-0011` 等）を満たす。
  2. `007-B` -> `007-C` へ進む（`007-B` は案A Go・ゲート充足後のみ）。
7. [厳守][2026-03-25] 各段階の成功可否は「判定ログセット」で判定する（`ProductionTool` 判定ログ / ESP32 シリアルログ / `espefuse summary before-after` / `試験記録書.md` 反映）。
- [厳守] 既存IDは固定のため再採番しない。実施順は本節を正とし、章番号は歴史的なまとまりとして扱う。

### 003. 直近フェーズC: LocalServer OTA実行面の整備
- [重要] [念のため保存] 003-0001, 003-0002, 003-0003, 003-0008, 003-0011, 003-0015, 003-0018 は todo_old20260314.md へ退避済み。
- [フェーズC クローズ][2026-03-19] フェーズC の実施可能な試験は一通り完了した。003-0020〜003-0023（7081/7082/7083/7084）は完了済み。003-0024（7085）は総合進入判定ゲートであり、前提条件（004-0010, 009-0019, 005-0001〜005-0005, 004-0001〜004-0005）が未達のため「未達・進入不可」のまま。フェーズC としての試験実施は完了とする。次はフェーズD（004-0008〜004-0010, 005-0001〜005-0005）の推進で効率的に進める。
- [完了タスク退避][2026-03-24] `003-0004`, `003-0013`, `003-0014`, `003-0019`, `003-0020`, `003-0021`, `003-0022`, `003-0023`, `003-0024` は `todo_old20260324.md` へ退避済み。
### 004. 直近フェーズD: ProductionTool 基本機能の安定化と試験用鍵・署名素材・証明書の整備
- [実行順序][2026-03-16] 本章は前半の `004-0008`〜`004-0010` で `ProductionTool` 基本機能の実装・試験・安定化を先行し、その後に後半の `004-0001`〜`004-0005` で試験用鍵・署名素材・証明書手順を固定する。理由: `ProductionTool` の基本導線が固まる前に鍵手順だけを先行確定すると、後で実装都合の戻りが出やすいため。[補足][2026-04-29] 旧 `004-0006` / `004-0007` は `todo_old20260429.md` へ方針退避済み（本番専用分離は **`020-0001`**）。
- [変更方針][2026-03-22][重要] `004-0006` / `004-0007` は当初「フェーズF実行直前の本番専用生成」として置いていた。[方針変更][2026-04-29] **当面は試験用 `k-iot` と同一**とし、当該2タスクは `todo_old20260429.md` へ退避。本番専用分離は **`020-0001`**。
- [完了タスク退避][2026-03-24] `004-0001`, `004-0002`, `004-0003`, `004-0004`, `004-0005`, `004-0008`, `004-0009`, `004-0010` は `todo_old20260324.md` へ退避済み。

#### 004-A. 実施順固定
- [重要][2026-04-29] **`004` 章に未完了タスクはない**（`004-0006` / `004-0007` は `todo_old20260429.md` へ方針変更退避済み）。`k-iot` は当面 **`コマンド仕様書.md` §10.2.5** の試験用鍵を本番にも流用する。本番専用生成が必要になったら **`020-0001`** を実施し **§10.2.5a** を参照する。

### 005. 直近フェーズE: ProductionTool と AP 画面分離の実装
- [実行順序][2026-03-16] 本章は前半 `005-0001`〜`005-0005` をフェーズDの `ProductionTool` 基本機能整備として先行し、後半 `005-0006`〜`005-0008` を `004-0001`〜`004-0005` の試験用鍵手順固定後に進める。理由: 先に UI / 認証 / 対象機確認 / ウィザードを固め、その後に不可逆処理の中身を差し込む方が手戻りが少ないため。[補足][2026-04-29] 本番専用 `k-iot` 分離は **`020-0001`**。
- [変更方針][2026-03-22][重要] 後半は `005-0006` / `005-0007` の設計確定を先行し、`005-0008` は実機実施を伴う有効確認としてフェーズF側で扱う方針とした。理由: フェーズDでは「前準備固定」までを完了条件とし、実行後の有効確認はフェーズFのリハーサルと一体で扱うため。[追記][2026-04-28] `005-0008` は `todo_old20260428.md` へ退避済み（9.4.6 突合）。
- [完了タスク退避][2026-03-24] `005-0001`, `005-0002`, `005-0003`, `005-0004`, `005-0005`, `005-0006`, `005-0007` は `todo_old20260324.md` へ退避済み。

#### 005-A. 実施順固定
- [厳守][2026-03-25] `005` の残タスクはない（`005-0008` は `todo_old20260428.md` へ退避済み）。
- [重要] `006-0011` 完了後の自動検証観点は `ProductionTool画面仕様書.md` **9.4.6** で `試験記録書.md` と突合済み（旧 `005-0008`）。
- [厳守][2026-04-28] `005-0008` を退避済みとしても、**本番1台目（`007`）**では `ProductionTool画面仕様書.md` **9.4** の AV を都度実施し、4点証跡欠落のまま次段へ進まない。
- [完了退避][2026-04-28] `005-0008` は `todo_old20260428.md` へ退避済み。観点・試験記録書との突合は `ProductionTool画面仕様書.md` **9.4.6** を正とする。
- [フェーズE 整理結果][2026-03-22] `005-0001`〜`005-0007` は完了済み。`005-0008` はフェーズFリハーサルと一体で扱う前提だったが、**2026-04-28** に設計・試験記録突合まで完了し `todo_old20260428.md` へ退避した。

### 006. 直近フェーズF: Secure Boot / ROM暗号化 / Flash Encryption / eFuse試験
- [厳守][2026-03-22] 本章のうち `006-0002` 以降の着手条件は `003-0024` の進入判定条件充足と `7085` の進入可判定、および `005` 章の ProductionTool 導線と `004` 章の試験用鍵手順が揃っていること。`006-0001` のチェックリスト整備自体はこの差し止め対象に含めない。理由: 進入判定ゲートの固定と不可逆処理本体の開始を分離し、準備文書だけを先行完了できるようにするため。
- [完了タスク退避][2026-03-24] `006-0001` は `todo_old20260324.md` へ退避済み。
- [厳守][2026-03-24] `006` 章では「試験機での段階実行手順の確定」までを目的とし、本番対象機の最終有効化は `007` 章へ分離する。
- [進捗][2026-03-25] `7085` は `FULL_COMPLETED` 証跡で進入可へ更新済み。非不可逆範囲として `7019` / `7020` / `7021` / `7023` を先行完了し、`7022` / `7024` は保留。
- [厳守][2026-04-28] **本番1台目（`007-B`）**の不可逆処理は、案A・ゲート・4点証跡を満たしたうえで実施する。試験機で `006-0011` リハーサル済みでも省略しない。なお `7022` / `7024` 等の非不可逆整理が未整理のまま **`007` 本番に入らない**。

#### 006-A. 実施順固定（不可逆リハーサル開始前）
- [厳守][2026-03-25] `006` は以下の順番で進める。  
  `準備` -> `実行保護` -> `開始判定` -> `非不可逆試験完了` -> `試験機で不可逆リハーサル開始` -> `試験結果記録`
- [厳守][2026-04-23] `006` 章の着手順は次の順で固定する。  
  1. `006-0009`
  2. `006-0003`
  3. `006-0004`
  4. `006-0011`
- [理由][2026-04-23] 先に実行保護と開始判定を固め、その後に試験結果を反映し、最後に不可逆リハーサルへ進めるため。`006-0010` と `006-0015` / `006-0016` は完了退避済みのため、この順には含めない。
- [禁止] `7022` / `7024` 未完了のまま、試験機の不可逆リハーサルへ進まない。理由: 非不可逆で潰せる不安要素を残したまま試験機を消費しないため。

#### 006-B. [注意][試験機で不可逆リハーサル開始] ここから下は試験機に対する不可逆工程を含む
- [注意][不可逆][2026-03-25] ここから先は試験機であっても `Secure Boot` / `Flash Encryption` / eFuse 実書込みを含むため、対象機の切替不能リスクを伴う。
- [注意][不可逆][2026-03-25] 実行前に「対象機識別」「読戻し手順」「停止条件」「試験記録書の記入場所」を確認すること。
- [完了済みタスク退避][2026-04-29] `006-0003`, `006-0004`, `006-0009`, `006-0010`, `006-0011` は `todo_old20260429.md` へ退避済み。証跡は `試験記録書.md` を参照。
  - [厳守] `006` 章で得た復旧素材と証跡は `007` 章の再利用判定に引き継ぐ。

#### 006-C. `007` 着手前の出口条件
- [厳守][2026-03-25] `006` の完了条件は、試験機リハーサル結果と読戻し結果が揃い、`007` へ安全に進めると判断できること。
- [重要] `006` 完了後に `007` 章へ進む（`k-iot` は当面試験用と同一。実行後検証手順は 9.4 を正とする）。

### 007. 直近フェーズG: eFuse最終化とOTA再更新
- [厳守][2026-03-16] 本章は `006-0001`〜`006-0011` 完了後のみ着手する。理由: リハーサル未了のまま本番 fuse 適用へ進むことを禁止するため。
- [厳守][2026-04-25] `007` 着手前の最終確認は `006-0011` の試験機リハーサル完了を必須とする。理由: `006-A` の出口条件が閉じていないまま `007` に進むと、不可逆工程の前提が崩れるため。

#### 007-A. 実施順固定（不可逆開始前）
- [厳守][2026-03-25] `007` は以下の順番で進める。  
  `事前準備` -> `4点証跡準備` -> `007-B進入前安定性/連携確認` -> `不可逆開始` -> `FE` -> `SB` -> `封鎖` -> `最終OTA確認` -> `JTAG/シリアル安全確認` -> `レビューゲート` -> `007クローズ判定`
- [参照][2026-04-29] **入口の作業順・`7099` 索引**は、冒頭「### 007入口・鍵・手順索引（一式）」を正とする。**`k-iot`** は当面 **§10.2.5**（試験用と同一）。本番専用分離後は **`020-0001`** と **§10.2.5a** を参照する。
- [禁止] 順番を前後させない。理由: 不可逆工程の順序誤りは復旧不能または原因不明化につながるため。
- [完了][2026-04-30 続] **`007-0011`**（不可逆直前文書・STA/AP/OTA）は **`試験記録書.md` `7099` の [更新][2026-04-28]** および文書 [進捗][2026-04-29] で充足。不可逆**着手当日**は **`007-A.1`** を再適用。**退避** `todo_old20260430.md`。
- [完了][2026-04-30]**[007-0013]** 方針は **`機能仕様書.md` §1.5** を正。**出荷後の更新は OTA が主**。製造側の標準クローズは **`ProductionTool`**。**シリアル開放は任意オプション**とし適用時は注意喚起。詳細・退避 `todo_old20260430.md`。

#### 007-A.0 入口チェック短縮索引[2026-04-28 / 2026-04-29 改訂]
- [参照] 手順の全文・順序は「### 007入口・鍵・手順索引（一式）」を正とする。
- [要点] **準備完了** — `007-0011` は一次記録済（`試験記録書.md` **`7099`**/`[更新][2026-04-28]`）。チェーン: **4点証跡（`7095`／試験機集済）** → **`7099` / `007-0009`**（同書 **7099** 欄）→ **`007-B`**（ゲートのみ）。本番開始時は **`7095` 別表**。**`k-iot`** は当面試験用（§10.2.5）。

#### 007-A.1 当日実行ミニ手順[2026-04-29 追加／2026-04-30 続]
- [目的] 不可逆着手**当日**、`7099` と同レベルの文書・実機確認を **同一作業日内** に行い、`007-B` 進入だけを判断できる状態へ持ち込む（過去の一次充足に**代替しない**）。
- 1. 文書再確認: `本番セキュア化出荷準備試験計画書.md` / `試験仕様書.md` / `eFuse適用チェックリスト.md` / `試験記録書.md` を開き、`BLOCK_KEY0/1`、`RD_DIS`、`DIS_DOWNLOAD_MODE`、`ENABLE_SECURITY_DOWNLOAD`、`DIS_USB_SERIAL_JTAG` 方針が一致していることを確認する。
- 2. 証跡準備: **`試験記録書.md` `7095` [固定]** の表・当日テンプレ・（本番用）別表複写方針に沿い、ログディレクトリの書込可否を確認する。
- 3. 実機再確認: `STA` 通常モード疎通、`AP` 到達性、`OTA` 継続更新可否を確認する。ここで不整合や到達不能が出たら `007-B` へ進まない。
- 4. 安定性確認: `7099` の当日手順に従い、`OTA x2`、`AP 10分`、`STA 10分` の観測を行い、異常があれば即停止して `問題点記録書.md` へ記録する。
- 5. 当日判定: 本条 1〜4 と `7099` 当日欄が埋まってから、`007-B` に進めるかのみを判断する（基準証跡例: **`7099`** 2026-04-28 追記）。

#### 007-B. [注意][不可逆開始] ここから下は eFuse 書込みを含む不可逆工程
- [正][2026-04-30][修正 2026-04-29] 本番1台目で当日作業するときのチェック列は **`IoT/007_本番1台目_実行順チェックリスト.md`**。一次定義は **`本番セキュア化出荷準備試験計画書.md` §6.4**（現行順序:**FE→NVS暗号化なし確認（非焼込みゲート）→SB→封鎖**）。進入経路が `最終セキュア化方式選定メモ.md` 第8章 PoCのみで止めないことを **§1.1（2026-04-30）** で明示した。
- [注意][不可逆][2026-03-25] ここから先は、各項目の実行前に「対象機確認」「4点証跡開始」「NVS秘密バックアップ取得済み」を確認すること。
- [注意][不可逆][2026-03-25] 各段階の直後に `読戻し -> 再起動 -> 4点証跡確定 -> 安定性確認セット` を完了しない限り、次段階へ進まないこと。
- [020 章移管][2026-05-19] `007-0001` は **`020-0011`** へ移管。本番1台目の不可逆処理本体（FE → NVS暗号化なし確認 → SB → 封鎖）の段階実施タスク。`007-B` No-Go 継続中、`ProductionTool` 最終形実ランナー実装後に着手。詳細は `020-0011` を参照。`todo_old20260519.md` に詳細退避。
- [020 章移管][2026-05-19] `007-0014` は **`020-0012`** へ移管。`ProductionTool` を不可逆工程の責任主体として実装へ収束させる本実装タスク。`PT-005z` precheck / execute / readback / evidence / stability の段階ランナーは骨格まで実装済み、本格運用は本番展開時。詳細は `020-0012` を参照。`todo_old20260519.md` に詳細退避。
- [020 章移管][2026-05-19] `007-0009` は **`020-0013`** へ移管。各段階完了判定前の安定性確認セット（OTA 2回連続 + APモード10分 + 通常モード10分）を実施するタスク。`020-0011`（不可逆段階実施）の各段階ゲートとして組み込む。詳細は `020-0013` を参照。`todo_old20260519.md` に詳細退避。

### 008. 直近ゴール後のセキュリティ拡張
#### 008-A. 方針・文書整理
- [x] [008-0001] [2026-03-09][2026-05-02完了][厳守] シリアル機能停止は eFuse ではなく FW 制御で管理する方針を実装へ反映する。
  - 量産通常FWでは高詳細ログと危険な保守コマンドを停止または最小化する
  - シリアル停止のみを目的に eFuse を焼かない
  - 問題解析時のみ署名済み診断用FWで一時的にシリアル出力を有効化できるようにする
  - [重要][2026-05-02整理] 本番最終クローズでは「シリアルを止めたバージョンへ上げる」ことを正とし、シリアル出力停止は量産通常FW更新で実現する。`DIS_USB_SERIAL_JTAG` は必要時のみ書換え不能化として別扱いにする。
  - [完了][2026-05-02] `IoT/ESP32/platformio.ini` に通常運用FW/診断用FWのビルド差分を反映し、`firmwareMode.h` で切替を一元化した。`esp32s3_secure_final` は高詳細ログを抑止し、`maintenanceApServer.cpp` で `pairing` / `production` 系APIを通常運用FWでは `403` 拒否する実装へ更新した。
- [x] [008-0002] [2026-03-09][2026-05-02完了][重要] 通常運用FWと診断用FWの差分方針を `機能仕様書.md` または専用仕様へ整理する。
  - 出力可能ログ種別
  - 許可する保守コマンド
  - 切替条件
  - 顧客環境で恒久運用しない条件
  - [完了][2026-05-02] `通常運用FW・診断用FW差分仕様書.md` を新規作成し、通常運用FWと診断用FWの差分、切替条件、最終クローズ時の量産通常FW更新方針、`7030` / `7031` との対応を固定した。
- [x] [008-0003] [2026-03-09][2026-05-02完了][重要] インターネット接続時の最低限セキュリティ基準を確定する。
  - 不要待受停止
  - 未使用ポート閉鎖
  - 管理ポート非公開
  - Topic ACL / API 認可
  - 認証失敗バックオフ / 一時遮断
  - 監査ログ / アラート
  - 本プロジェクト要件外の汎用プロキシ / 中継 / ファイル共有 / ポートフォワード禁止
  - [完了][2026-05-02] `ネットワーク運用仕様書.md` 7.4 を正本として、ESP32 / LocalServer / Broker / ProductionTool / SecretCore ごとの最低限基準、判定観点、試験対応（`7032` / `7033` / `7034`）を固定した。

#### 008-B. 実装・統合
- [完了タスク退避][2026-05-10] `008-0007` は `todo_old20260510.md` へ退避済み。
- [完了タスク退避][2026-05-17] `008-0010` / `008-0011` / `008-0012` / `008-0013` / `008-0014` / `008-0015` は `todo_old20260512.md` へ退避済み（IPC stdin/stdout ブートストラップ・nonce 暗号乱数・秘密ファイル保存なし確認・wrapped_secret 破損検知・payload 最短スコープ・監査ログ 0600+OSログ。2026-05-11 完了）。
- [完了タスク退避][2026-05-07] `008-0006` は `todo_old20260507.md` へ退避済み。
- [完了タスク退避][2026-04-29] `008-0017`, `008-0018` は `todo_old20260429.md` へ退避済み。
- [完了タスク退避][2026-04-29] `008-0019`〜`008-0022` は実装済み（SecretCore 連携、ワークフロー型 API 移行完了）のため `todo_old20260429.md` へ退避済み。
- [完了タスク退避][2026-03-24] `008-0023`, `008-0024` は `todo_old20260324.md` へ退避済み。
- [完了タスク退避][2026-05-17] `008-0026` は `todo_old20260512.md` へ退避済み（`SecretCore` に `runSignedOtaCommand()` 実装確認。2026-05-11 完了）。
- [完了タスク退避][2026-05-17] `008-0038` / `008-0039` / `008-0041` / `008-0043` / `008-0029` は `todo_old20260512.md` へ退避済み（AP ロールパスワード変更・工場出荷状態復帰要件定義・ProductionTool GUI 化要件定義・AP 共通トップ画面拡張。2026-05-12 完了）。
#### 008-C. 画面・評価・将来
- [完了タスク退避][2026-05-14] `008-0031` は `todo_old20260514.md` へ完了退避済み。
  - 鍵再発行
  - HMACチェーン付き監査ログ
  - `k-device` 導出とペアリング連携
  - [方針][2026-05-13] macOS 対応は `020` 章へ移管し、TPM初期化検知は本件の対象外とする。Phase4 の本線は `runKeyRotationSession()` / `runProductionSecureFlow()` / 障害時再登録フローへ集中する。
  - [進捗][2026-05-13] `LocalServer/src/server.ts` と `LocalServer/public/admin.html` に障害時再登録フロー案内 API / UI と `restore` 実行 API / `execute` 一括実行 API を追加し、既存の `device_db` 復元・`k-user` 復元・`pairing` / `key-rotation` 導線へ繋げた。
- [完了タスク退避][2026-05-14] `008-0033` は `todo_old20260514.md` へ完了退避済み。
- [完了タスク退避][2026-05-07] `008-0005` は `todo_old20260507.md` へ退避済み。
### 009. ESP32 / LocalServer の後続整備
#### 009-A. 起動・保存基盤
- [完了タスク退避][2026-05-17] `009-0001` は `todo_old20260517.md` へ完了退避済み。Task Scheduler **`IoT_LocalServer_AutoStart`**（既定 **`AtLogOn` + `Interactive`**）、`Start-ScheduledTask` 後 **`/api/health` OK** を `試験記録書.md` `7001` に記録した。
- [完了タスク退避][2026-05-17] `009-0002` は `todo_old20260517.md` へ退避済み（SQLite 統合・`7102`〜`7106`）。
- [完了タスク退避][2026-05-17] `009-0003` は `todo_old20260517.md` へ退避済み（Task Scheduler + 定期エクスポート組込み・`7107` OK）。`.env` に `LOCAL_HISTORY_SCHEDULED_EXPORT_ENABLED=true` / `INTERVAL_MS=86400000` を追記し、`コマンド仕様書.md` §5.1 / `試験仕様書.md` `7107` / `試験記録書.md` `7107` を整備した。
#### 009-1000番台: LocalServer SQLite統合（009-0005〜009-0006 完了済み）
- [012章移管][2026-05-17] `009-0004` は `012` 章へ移管済み（クラウド連携は `012` 章で一括管理する方針のため）。
- [完了][2026-05-17] [009-0005] [2026-03-07][推奨] `LocalServer` のデータ保存期間設定（30/90/365日）と削除API仕様を `機能仕様書.md` に追記する。
  - [完了理由][2026-05-17] `機能仕様書.md` §7.1 を新設し、`localHistoryRetentionDays` 推奨プリセット（30/90/365/0=無期限）、`DELETE /api/admin/local-history` 範囲削除、`POST /api/admin/local-history/delete-database` 全消去（`confirm` 必須・不可逆）、監査ログ記録義務を明文化した。実装の技術詳細は `DB仕様書.md` §2〜§3 に記載済みのため参照索引を設けた。
- [完了タスク退避][2026-05-18] `009-0006` は `todo_old20260518.md` へ退避済み。`scripts/test7108BidirectionalE2E.mjs` 実機実行 → `commandHistory` +1・`deviceStatusHistory` +2・`lastSeenAt` 更新を確認（`試験記録書.md` `7108` OK）。

#### 009-B. 秘密管理・ペアリング
- [完了タスク退避][2026-05-19] `009-0008` は `todo_old20260519.md` へ完了退避済み。`鍵管理初期セットアップ_実装たたき台.md` §2 にて Phase1〜4（および Phase5）へ担当（責任モジュール）と期限（相対順序）を明記。
- [完了タスク退避][2026-05-19] `009-0009` は `todo_old20260519.md` へ完了退避済み。`SecretCore/src/key_manager.rs:84-115 load_or_create_s_random()` で CSPRNG 生成 → DPAPI ラップ → `wrapped_secret.bin` 保存まで実装済み。`機能仕様書.md` F3-091 にも明文化済み。文言上の TPM → DPAPI 整合は `鍵管理および初期セットアップ設計仕様書.md` / `LocalServer秘密処理別層仕様書.md` / `鍵管理初期セットアップ_実装たたき台.md` を 2026-05-19 に同期更新。
- [完了タスク退避][2026-05-19] `009-0010` は `todo_old20260519.md` へ完了退避済み。Phase2「LocalServer 起動時鍵導出」の秘密処理側（`SecretCore` `load_or_create_s_random` / `get_k_user` / `get_k_device`）は実装済み。`runPairingSession()` の AP モード `/api/auth/login`〜`POST /api/pairing/transport-handshake` までは `LocalServer秘密処理別層仕様書.md:118-124` で確認済み。**LocalServer TS 側の REST 公開・進捗表示・UI フロー（`admin.html`/`settings.html` の pairingSession 開始画面・進捗表示・完了確認）の詳細化は `020-0009` へ移管**。
- [完了タスク退避][2026-05-19] `009-0011` は `todo_old20260519.md` へ完了退避済み。判定基準は **`LocalServer秘密処理別層仕様書.md` §7.1**（許可 API 15 個 + REST 公開経路 4 個）／ **`:7.2`**（禁止 API 4 個：`getUserKey()` / `getDeviceKey()` / `exportPlainSecret()` / `getPairingSessionKey()`）／ **`セキュリティ方針_認証情報取扱い.md` §3〜§7**（基本方針・鍵管理・バックアップ・k-device・改ざん対策）／ **`鍵管理および初期セットアップ設計仕様書.md` §7.1** に明文化済み。raw key 非返却ルールと IPC 保護（DH 鍵交換 + AES-GCM + nonce）も完全仕様化済み。実装は `SecretCore/src/main.rs:352` で IPC 公開経路が既に限定されており、設計文書との齟齬なし。
- [完了タスク退避][2026-05-19] `009-0012` は `todo_old20260519.md` へ完了退避済み。`SecretCore/src/key_manager.rs:454-465 get_k_device` で **既実装**（`HMAC-SHA256(key=k-user, message=target_device_name=publicId)`）。旧 HKDF 仕様式は撤回し、設計仕様書群（`鍵管理および初期セットアップ設計仕様書.md` §3 / §6 / §16、`LocalServer秘密処理別層仕様書.md` §3.1、`IF仕様書.md` / `WiFi設定更新_詳細設計書.md` / `セキュリティ方針_認証情報取扱い.md` / `要件定義書.md` FR-009 / `鍵一覧仕様書.md` / `鍵管理初期セットアップ_実装たたき台.md`）を実装側へ正本更新済み。
- [完了タスク退避][2026-04-25] `009-0013` は `todo_old20260425.md` へ退避済み。
- [不採用クローズ][2026-05-19] `009-0014` は `todo_old20260519.md` へ退避済み（**不採用クローズ**）。実装と `MQTTコマンド仕様書.md` §2.1 は `esp32lab/<kind>/<sub>/<name>` で統一されており（Wi-Fi 設定更新は `esp32lab/network/...`、実装: `mqtt.cpp:3298 subscribeTopicNetwork`）、`device/<public_id>/wifi/update` への切替は逆に統一性を崩すため不採用。`mqttProtocol.cpp:62-72` の未使用ヘルパー（`buildTopicWifiUpdate/Confirm/DeviceBoot`）は参考実装として残置し、ヘッダにコメント追記済み。関連文書（`WiFi設定更新_詳細設計書.md` §5 / `Mosquitto_TLS_IDパスワード設定手順書.md`）も整合更新済み。
- [完了タスク退避][2026-05-19] `009-0015` は `todo_old20260519.md` へ完了退避済み（**`020-0008` へ移管**）。実装側は `sensitiveData.cpp` 経由で `sensitiveData.json` から読込んでおり、`sensitiveData.h` 静的ヘッダー直接依存は既に解消されている。完全な NVS 永続化（平文 JSON 運用の廃止）は **`020-0008`** で `009-0024`（時刻サーバ NVS 優先化）と統合実施。
- [完了タスク退避][2026-05-19] `009-0016` は `todo_old20260519.md` へ完了退避済み。最終運用値（TLS 有効 + 8883 + ID/Password 必須 + `allow_anonymous false`）は **設定・実装・試験で完了確認済み**：`ESP32/data/sensitiveDataSample.json:16-21`（`mqttPort=8883` / `mqttTls=true`）／`ESP32/src/MQTT/mqtt.cpp:857-868`（TLS CA 証明書設定実装）／`Mosquitto_TLS_IDパスワード設定手順書.md:94-108`（`listener 8883` / `password_file` / `allow_anonymous false` / `acl_file` 4 点必須化）／**`試験記録書.md` `7032` OK**（`8883` 待受のみ、`1883` 待受なし、2026-05-07）／**`7033` OK**（管理者ログイン 3 回連続失敗で 5 分ロック、2026-05-07）。`7034`（踏み台化防止：Mosquitto ACL / ルータ WAN 管理等）は **`020-0002`/`020-0003`** へ移管済み。
- [完了タスク退避][2026-05-19] `009-0017` は `todo_old20260519.md` へ完了退避済み。ESP32 側で **3箇所** に `mbedtls_gcm_auth_decrypt`（GCM tag 検証付き）の AES-256-GCM 復号を実装済み: `maintenanceApServer.cpp:881-927 decryptAes256GcmForAp`（AP pairing bundle・AAD あり）／`MQTT/mqtt.cpp:604-639 decryptAesGcm`（MQTT payload）／`MQTT/mqttPayloadSecurity.cpp:92-131 decryptAesGcm`（MQTT payload security 層・AAD あり）。仕様書側も 13 文書で AES-256-GCM 採用を明記済み。
- [完了タスク退避][2026-05-19] `009-0018` は `todo_old20260519.md` へ完了退避済み。「物理操作必須・短時間有効」は **5 層セキュリティ**（① 物理操作必須・② Wi-Fi WPA2 認証・③ Web UI ロール認証・④ 通常通信からの隔離・⑤ 単発有効/持ち越さない）で実装済み。「短時間有効」は **単発セッション**として `APメンテナンス画面仕様書.md` §1.1 に明文化し、N 分無操作の自動切断タイマーは要件としない方針を確定。実装は `main.cpp:494-533 startMaintenanceApModeAndHold` / `main.cpp:539 detectStartupMaintenanceLongPress` / `maintenanceMode.cpp:51-64 consumeMaintenanceModeRequest` / `maintenanceApServer.cpp` 全体。
- [完了タスク退避][2026-03-24] `009-0019` は `todo_old20260324.md` へ退避済み。
- [完了タスク退避][2026-05-19] `009-0020` は `todo_old20260519.md` へ完了退避済み（**試験仕様書整備によるクローズ**）。鍵ローテーションは必須運用ではなく、`k-user` を変更した時のみ発生する任意機能。本実装（runtime カウンタ・state 遷移判定・自動削除・認証拒否切替）と加速試験は **`020-0005`** で扱う方針。`試験仕様書.md` `7038` に通常運用試験と加速試験（120 倍スケール・2h 完了）の手順・期待結果・試験後復旧を詳細化済み。実装あり：NVS キー定義（`previousKeyState` / `graceActiveRuntimeMinutes`、[sensitiveData.cpp:38-39](IoT/ESP32/src/sensitiveData.cpp:38)）、pairing/key-rotation 直後の grace 設定（[sensitiveData.cpp:1313-1320](IoT/ESP32/src/sensitiveData.cpp:1313)）、AP モード設定画面表示（[maintenanceApServer.cpp:505](IoT/ESP32/src/maintenanceApServer.cpp:505)）。
- [完了タスク退避][2026-05-19] `009-0021` は `todo_old20260519.md` へ完了退避済み。タスク文言スコープ（WPA2 / 固有パスワード / Web UI）は `009-0018` と同一コードパス（`main.cpp:494-533 startMaintenanceApModeAndHold` / `maintenanceApServer.cpp` 全体 / `iotCommon::apConfig::kMaintApPass`）で実装済み。仕様書 `WiFi設定更新_詳細設計書.md` §9.1 にあった「Wi-Fi 接続失敗回数しきい値超過による自動 AP 遷移」（フェイルセーフ）は実装側に存在せず、**`020-0006`** へ将来対応として移管。
- [完了タスク退避][2026-05-19] `009-0022` は `todo_old20260519.md` へ完了退避済み（**`020-0007` へ移管**）。Wi-Fi 設定更新ステートマシン本体（`009-0023`）と試験仕様 7XXX 追加（`009-0022`）はセットで扱うため、`020-0007` に統合。設計は `WiFi設定更新_詳細設計書.md` §7-8 で確定済み。
- [完了タスク退避][2026-05-19] `009-0023` は `todo_old20260519.md` へ完了退避済み（**`020-0007` へ移管**）。`mqttProtocol.cpp:132-137` で `wifiConfigUpdate`/`wifiConfigConfirm` コマンド定義とパース関数（`parseWifiUpdatePayload`）は存在するが、`009-0014` の精査で判明したように実 active path は `esp32lab/network/...` であり、これらは未使用ヘルパー。完全な state machine（未確定保存 → 接続試行 → CONFIRM → ロールバック）の active 実装と試験仕様 7XXX 追加は **`020-0007`** で `009-0022` と統合実施。
- [完了タスク退避][2026-05-19] `009-0024` は `todo_old20260519.md` へ完了退避済み（**`020-0008` へ移管**）。実装側は `sensitiveDataService::loadTimeServerConfig`（[sensitiveData.cpp:431-434](IoT/ESP32/src/sensitiveData.cpp:431)）で `sensitiveData.json` 経由で読込んでおり、`sensitiveData.h` 直接依存は既に解消されている。完全な NVS 永続化は **`020-0008`** で `009-0015` と統合実施。

#### 009-C. インストーラ/アンインストーラ EXE 化（本番・客先環境の要件最小化）
- [完了タスク退避][2026-05-19] `009-1020` は `todo_old20260519.md` へ完了退避済み。**設計仕様確定までは完了**：`インストーラEXE化設計仕様書.md` §1〜§4 で LocalServer / ProductionTool の仕様、Node.js 実行形式選定、UAC・Task Scheduler 自動化、安全消去ポリシー対応（§3.2）が文書化済み。ProductionTool 側は `ProductionTool/installer/ProductionTool.iss` と `ProductionTool/scripts/build-production-tool-installer.ps1` の骨格あり。**LocalServer 用 `.iss` スクリプト / Node.js 戦略確定 / ビルド・試験は `020-0010` へ移管**。

### 010. ネットワーク・インフラ
- [完了済みタスク退避][2026-04-29] `010-0002`〜`010-0009`, `010-0013`, `010-0015` は `todo_old20260429.md` へ退避済み。
- [完了タスク退避][2026-05-19] `010-0001` は `todo_old20260519.md` へ完了退避済み。`ネットワーク運用仕様書.md` §2.4 / §6 で「新ルータは GW `172.17.1.1` で運用する（2026-04-21 時点で現行運用中）」「現行ローカルLANの基準値は GW=172.17.1.1、サーバー系=172.17.1.100、DHCP=172.17.1.200+」を運用確定。`MQTT_TLS環境構築まとめ.md:394` でも「新ルータ導入後も `mqtt.esplab.home.arpa -> 172.17.1.100` を正規経路」と運用文書化。関連 `010-0002`〜`0009`, `0013`, `0015` は `todo_old20260429.md` へ退避済み。
- [x] [010-0014] [2026-05-24 完了] ブローカ運用ネットワークを IoT専用LAN（Private）へ分離し、FirewallのRemoteAddressをサブネット限定する。→ 退避: `todo_old20260524.md`

### 011. 文書・設計管理
- [重要][2026-04-24] 完了済みの `011-0001` / `011-0002` / `011-0003` / `011-0004` / `011-0005` / `011-0006` / `011-0007` は `todo_old20260424.md` に退避済み。`011` 章の未完了はなし。  
  ただし `005`〜`010` 章には後続 backlog が残っているため、`todo.md` 全体としては未完了が存在する。
- [補足][2026-04-28] `006-0011` および `005-0008`（観点・突合）は完了。`007` 章の残件は `007-0009` / `7099` を含む試験記録・現地確認の更新と整合させる。

### 005〜010 の扱い（2026-04-29 整理）
- [重要] ローカル環境のセキュア化は一段落したため、`005`〜`010` 章は「今すぐの本線」ではなく「後続 backlog」として扱う。
- [重要] 並び順は次を正とする。  
  1. `007-A` / `007-0011` / **4点（`7095`）** / `007-0009`（安定性継続）
  2. `008` 章の残り（拡張実装・試験）
  3. `009` 章の残り（LocalServer / 秘密管理）
  4. `010` 章の残り（インフラ分離）
- [補足] `005` / `006` 章の完了済みタスク、`008-0048` / `008-0049`、および `008-0044`〜`008-0047`（NVS旧仕様）は退避済み。

### 012. クラウド連携・第4段階[2026-04-18]
[最重要] クラウド化に関する **未完了タスクは本章（`012-xxxx`）以降にのみ** 追加する。理由: 直近は `011-0007` までを片付ける方針とし、章境界をブレさせないため。

[重要] クラウド関連の将来対応タスクは `012` 章で扱う。`020` 章はそれ以外の将来対応を集約する。

[重要] `設計概要.md` の第4段階（Cloud で認証・書換管理）に向け、**ローカル第3段階（案A確定・`007` 完了）の後**で順に着手する。理由: ブローカ接続・デバイス秘密・不可逆手順が未固定のままクラウドへ移すと、試験不能・巻き戻し不能な不整合が増えるため。

#### ロードマップ（D4-0〜D4-4）
- **D4-0（着手ゲート）**: 案A または案C の結論が文書で確定しており、`007-C` の扱いも文書で確定していること。高リスクワークフロー（`SecretCore` / `ProductionTool`）をクラウドに置くかどうかは `012-0004` の責務分界表で判断材料を固定する。
- **D4-1（マネージド MQTT）**: AWS IoT Core または Google Cloud 側の MQTT 相当（TLS・デバイス認証・ポリシー/ACL）へ、**既存トピック設計**（`設計概要.md` の `serverId` / `brokerId` 前提）を接続する。関連: `010-0013`（NAT 下 outbound 等の試験観点・試験仕様書への追記）、`012-0001`（Broker 候補比較）。
- **D4-2（アイデンティティ）**: デバイス登録・証明書ローテーション・失効。現行 `k-user` / `k-device` / OS 暗号化サービス（現行: Windows DPAPI）境界との対応は `012-0004` の成果物へ反映する。
- **D4-3（コントロールプレーン＝LocalServer のクラウド相当）**: 運用 UI・REST・OTA メタ・監査のうち、PC 非依存に移せる範囲を切り出す。関連: `009-0004`（双方向同期の最小 I/F、本章に移管済み）。**不可逆 eFuse 等は当面ローカル/工場ツールに残す**方針を維持する。
- **D4-4（文書）**: `012-0002`（AWS/Google 比較表）、`012-0003`（`ドキュメント概要.md` / `設計概要.md` との相互参照同期）。

#### 012 の未完了タスク
- [x] [012-0001] [2026-03-03→**完了 2026-05-20**][重要] Broker 候補比較完了 → **AWS IoT Core 採用確定**。比較: AWS IoT Core / EMQX Cloud / HiveMQ Cloud。AWS IoT Core が日本リージョン・Cognito/Lambda ネイティブ統合・フルマネージド・無難さの全要件で最優。退避: `todo_old20260520.md`。証跡: `設計議事録20260520.md` §3.2。
- [x] [012-0002] [2026-03-08→**完了 2026-05-20**][重要] AWS vs Google 比較完了 → **AWS IoT Core 一択**（Google Cloud IoT Core は 2023-08-16 サービス終了済み）。退避: `todo_old20260520.md`。証跡: `設計議事録20260520.md` §3.1。
- [x] [012-0003] [2026-04-17→**完了 2026-05-24**][将来対応][重要] 本章ロードマップおよび `012-0001` / `012-0002` の結論を、`ドキュメント概要.md` および `設計概要.md` と一方通行参照にならないよう、相互参照（節番号・ファイル名・更新日）で同期する。退避: `todo_old20260524.md`。
  - [移管][2026-04-18] 旧ID `011-0010` から移管。
  - [完了証跡][2026-05-24] `ドキュメント概要.md` の `CloudServer/readme.txt` 項目に `設計概要.md §3`・`設計議事録20260520.md §3.1/§3.2` 参照追加。`設計概要.md §3` の第4段階完了項目に `ドキュメント概要.md` ナビゲーション参照追加（双方向整合確認済み）。
- [x] [012-0004] [2026-04-17→**完了 2026-05-20**][重要] `IF仕様書.md §5`（クラウド連携（第4段階）責務分界表）を初版化。ローカル鍵階層（DPAPI→S_random→k-user→k-device）とクラウド側（X.509/JITP）の責務分界・禁止事項（raw k-device 禁出境界等）を表形式で定義済み。`012-0005` / `009-0004` の前提充足。退避: `todo_old20260520.md`。
  - [完了証跡][2026-05-20] `IF仕様書.md §5.1`（概念図）/ `§5.2`（責務分界表）/ `§5.3`（禁止事項 6 件）/ `§5.4`（AWS IoT Core X.509 初版）/ `§5.5`（`009-0004` 前提制約）
- [x] [012-0005] [2026-04-18→**完了 2026-05-20**][重要] LocalServer の AWS IoT Core subscriber 実装（クラウド連携モードの I/F 設計・実装）完了。`012-0004` の前提充足済み。退避: `todo_old20260520.md`。
  - [完了証跡][2026-05-20] `config.ts`（7 cloud fields 追加・validation）/ `cloudMqttSubscriber.ts`（新規・X.509 mutual TLS factory）/ `mqttGateway.ts`（`clientOverride?: MqttClient` 追加）/ `server.ts`（cloud/local gateway 切替）。TypeScript `tsc --noEmit` OK。
- [x] [009-0004] [2026-03-07→**完了 2026-05-20**][重要][012章移管 2026-05-17] LocalServer のクラウド連携モード I/F 実装完了（`012-0005` と一体）。退避: `todo_old20260520.md`。
  - [移管理由] クラウド連携は `012` 章で一括管理する方針のため、`009` 章から移管した（2026-05-17）。
- [x] [012-0006] [2026-05-20→**完了 2026-05-20**][重要] **AWS 環境構築完了**（自動構築・手順記録付き）。退避: `todo_old20260520.md`。
  - [完了証跡][2026-05-20] エンドポイント取得（`***-ats.iot.ap-northeast-1.amazonaws.com`）/ ESP32 用 X.509 cert 発行（ACTIVE・`cert_id: 0a38a0c5...`）/ LocalServer 用 X.509 cert 発行（ACTIVE・`cert_id: 5311eb1a...`）/ `esp32lab-esp32-policy` 作成・アタッチ（Connect `IoT_*` / Pub+Sub+Recv `esp32lab/*`）/ `esp32lab-localserver-policy` 作成・アタッチ（Connect `localserver-*` / Sub+Recv `esp32lab/*` / Pub `esp32lab/call/*`）/ Thing `esp32s3-dev001` 作成・証明書アタッチ / `IoT/ESP32/data/certs/` に 3 ファイル配置（`.gitignore` 除外済み）/ `IoT/LocalServer/.env` に 7 項目追記（`CLOUD_MQTT_ENABLED=false` でデフォルト OFF）/ `.env.example.sample.txt` に構造追記。詳細実施記録: `クラウド構築手順書.md §10`。
  - [注意] 試験終了後は `CLOUD_MQTT_ENABLED=false` + `brokerMode=local` に戻し課金を止める（`クラウド構築手順書.md §9`）
  - [残作業] ESP32 への LittleFS アップロード（`pio run --target uploadfs`）は USB 接続後に手動実行が必要
- [x] [012-0007] [2026-05-20→**完了 2026-05-20**][重要] **ESP32 クラウドモード実装完了**。退避: `todo_old20260520.md`。
  - [完了証跡][2026-05-20] `IoT/shared/include/common.h`（`kBrokerMode` / `kCloudEndpoint` キー追加）/ `IoT/ESP32/header/sensitiveDataService.h`（`saveBrokerModeConfig` / `loadBrokerModeConfig` API 追加）/ `IoT/ESP32/src/sensitiveData.cpp`（NVS 読み書き実装）/ `IoT/ESP32/src/MQTT/mqtt.cpp`（X.509 mutual TLS・LittleFS `/certs/` cert ロード・cloud clientId `IoT_<mac>`）/ `IoT/ESP32/src/main.cpp`（cloud endpoint override block）/ `IoT/ESP32/src/maintenanceApServer.cpp`（AP 設定 UI `brokerMode` / `cloudEndpoint` 追加）。
- [x] [012-0008] 退避済み → `todo_old20260523.md` 参照（完了 2026-05-23、コミット `260523-1`、タグ `v1.1.0-cloud-ota`）
- [x] [012-0009] [2026-05-20→**完了 2026-05-23**][重要] **まとめ・文書整合**（試験合格後）。全件完了。退避: `todo_old20260523.md`。
  - ✅ `試験記録書.md` に `7200`〜`7209` の試験結果を記録（2026-05-23）
  - ✅ ローカルモードへ完全復帰（`CLOUD_MQTT_ENABLED=false`）
  - ✅ `ドキュメント概要.md` / `設計概要.md` に 012 章クラウド連携完了の相互参照を同期（旧 `012-0003`）
  - ✅ `todo_old20260523.md` に 012-0008 を退避
  - ✅ Git コミット `260523-1` + Git タグ `v1.1.0-cloud-ota` 付与
  - ⏭ `設計議事録20260520.md` 詳細追記・`クラウド構築手順書.md` 更新 → 必要に応じ次セッション

### 013. 文書整備・コードコメント充実化[2026-05-19 新設]
[重要] `012` クラウド連携に着手する前の整備フェーズ。既存文書 12 件のセキュア関連内容は充実しているが、**統合的なノウハウ集** と **設計書↔実装マッピング表** が不足。コードコメントは ESP32 セキュア系は良好だが LocalServer / SecretCore TypeScript/Rust 側は弱い。本章でこれらを段階的に整備する。

[厳守] 本章タスクは「機密値（パスワード・raw key・eFuse 実値）は文書・コード・コメントに記載しない」前提を維持する。

#### 013-A. 統合ノウハウ文書とマッピング表
- [完了][2026-05-19] [013-0001] **セキュア全般ノウハウ統合文書** `セキュア全般ノウハウ_設計から実装まで.md` を新規作成 → **完了**（行数約 780、全 18 章 + 関連文書索引）。`ドキュメント概要.md` 文書一覧に索引追加。各技術（DPAPI / Flash Encryption / Secure Boot v2 / OTA Rollback / TLS / MQTT 認証 / AES-256-GCM / HMAC / HKDF / 鍵導出 / 初回ペアリング / eFuse 操作）を「目的 → 仕組み → 設計判断 → 実装場所 → 手動実行例 → 関連文書」の統一フォーマットで深掘り。初心者向け基本概念解説（CIA + 認証、暗号種類）、機密情報取扱方針、トラブルシュート観点、設計書↔実装マッピング簡易版を含む。
  - [目的] 設計方法・実装方法・手動実行手順を初心者でもわかるレベルで一本化。技術選定の目的と仕組みの説明を充実させる。
  - [対象技術]（各セクションで「目的 → 仕組み → 設計判断 → 実装場所（ファイル:行）→ 手動実行例 → 関連文書」を統一フォーマットで記述）
    - **ROM 暗号化（Flash Encryption）**: ESP32-S3 の暗号化フラッシュ、`SPI_BOOT_CRYPT_CNT`、`espsecure`、暗号化済みバイナリ書込み
    - **Secure Boot v2**: 署名鍵ダイジェスト、`SECURE_BOOT_EN`、未使用 slot revoke、`espefuse` / `espsecure` の使い分け
    - **OTA Rollback（dual app partition、旧称「Dual バンク」）**: `BOOTLOADER_APP_ROLLBACK_ENABLE`、`app0`/`app1`（`0x30000`/`0x430000`）、`otadata` 管理、`esp_ota_*` API
    - **TLS 通信**: MQTT TLS（8883）、HTTPS OTA、証明書配置、CA バンドル
    - **MQTT 通信暗号化**: AES-256-GCM payload 暗号化（`k-device` 鍵、nonce/tag 検証付き）
    - **MQTT 認証**: TLS + ID/Password + `acl_file`、`allow_anonymous false`
    - **DPAPI**（LocalServer 側 OS 暗号化サービス、`wrapped_secret`、`WRAPPED_SECRET_ALG="DPAPI"`）
    - **HMAC-SHA256 / HKDF**（command 署名 / k-user 導出 / k-device 直接導出）
    - **鍵階層 `OS 暗号化サービス → S_random → k-user → k-device`** の生成式・利用箇所
    - **NVS 永続化**（pattern A / sensitiveData.json 経由）
  - [機密事項禁止] パスワード実値、raw `k-user` / `k-device` / `S_random`、eFuse 実値、証明書秘密鍵は記載しない
  - [関連] `セキュリティ方針_認証情報取扱い.md` / `鍵管理および初期セットアップ設計仕様書.md` / `本番セキュア化出荷準備試験計画書.md` / `Mosquitto_TLS_IDパスワード設定手順書.md` / `OTA仕様書.md`
- [完了][2026-05-19] [013-0002] **設計書↔実装マッピング表** `設計書実装マッピング表.md` を新規作成 → **完了**。全 12 章（鍵階層・鍵管理 / 暗号アルゴリズム / ESP32 セキュリティ機能 / TLS・MQTT / 初回ペアリング・AP モード / ESP32 タスク構成 / LocalServer-SecretCore 連携 / ProductionTool 段階ランナー / 機能仕様書対応 / 試験仕様書対応 / 機密情報サンプル対応 / 関連文書索引）を表形式で網羅。`ドキュメント概要.md` 文書一覧に索引追加。
  - [目的] 再設計や移管時に「今回使用した技術が設計書のどこに書いてあるか／実装のどこに書いてあるか」を一覧で確認できるようにする。コード抜粋も含める。
  - [形式] 表形式（技術項目 × 設計書ファイル §節 × 実装ファイル:行番号 × コード抜粋 × 関連試験 ID）
  - [対象] `013-0001` と同じ全技術項目に加え、ESP32 タスク構成・LocalServer / SecretCore IPC・ProductionTool 段階ランナーも含める

#### 013-B. 既存文書充実化
- [完了][2026-05-19] [013-0003] **`ソース概要.md` / `ドキュメント概要.md` / `設計概要.md` の充実化** → 完了。`ソース概要.md` に **§3.1「セキュア技術別の実装場所索引」** を新規追加（10 サブセクション：PC 側鍵管理 / 暗号化・署名 / ESP32 セキュリティ機能 / TLS・MQTT 通信 / AP モード・初回ペアリング / IPC / 製造・eFuse / 認証・認可 / NVS・LittleFS / 関連文書）。`設計概要.md` に **§4.1「OTA Rollback（旧称 Dual バンク）正式名称」** を新規追加（partition 設計・ビルドフラグ・実装インターフェース・実装本体・動作仕組み・関連文書）。`ドキュメント概要.md` は `013-0001` / `013-0002` / `013-0005` の新規索引を既に追加済み。
  - `ソース概要.md`：「セキュア関連実装詳細」セクション追加（`secureNvsInit.cpp` → FE/NVS 初期化 patternA/B/C、`maintenanceApServer.cpp` → AP 認証ロール、`mqttPayloadSecurity.cpp` → AES-256-GCM フロー、`dpapi.rs` / `key_manager.rs` → DPAPI と鍵階層）
  - `設計概要.md`：「Dual Bank」正式用語（**OTA Rollback / `BOOTLOADER_APP_ROLLBACK_ENABLE` / app0/app1 dual partition**）と対応ファイル（`otaRollback.h` / `platformio.ini` パーティション定義）を明記
  - `ドキュメント概要.md`：`013-0001` / `013-0002` の新規文書を索引に追加

#### 013-B+. システム構成・モジュール機能説明
- [完了][2026-05-19] [013-0005] **システム全体構成・モジュール機能・通信・つながりの説明文書** を整備する → **`システム構成と通信フロー.md` を新規作成**。`ドキュメント概要.md` 文書一覧先頭に索引追加。ASCII 構成図 / 各モジュール解説（ESP32 / LocalServer / SecretCore / ProductionTool / Mosquitto / CoreDNS / ルータ / HTTPS OTA 配布）/ 通信経路一覧 5 種（MQTT TLS / HTTPS OTA / AP モード / IPC / CLI）/ 主要シーケンス 5 種（初回ペアリング / 通常運用 / OTA / Wi-Fi 更新 / 鍵ローテーション）/ モジュール × 機能マトリクス / 鍵階層 / 用語ミニ辞典 / 関連文書索引を含む。
  - [目的] 初心者でも各モジュールの役割・機能・通信経路が一目で分かる文書を提供する。
  - [対象モジュール]
    - **ESP32**（デバイス側ファームウェア）
    - **LocalServer**（PC 側 TypeScript / Node.js 本体 + Web UI + REST API）
    - **SecretCore**（PC 側 Rust 秘密処理層）
    - **ProductionTool**（PC 側 Rust 工場ツール）
    - **MQTT ブローカー**（Mosquitto）
    - **CoreDNS**（ローカル名前解決、`172.17.1.100`）
    - **ルータ**（GW `172.17.1.1`、DHCP `172.17.1.200+`）
    - **HTTPS OTA 配布**（LocalServer 内、ポート `4443`）
  - [記述内容]（各モジュールごと）
    - **役割**: そのモジュールが何を担当するか（1〜2 行で初心者向け）
    - **責任範囲**: 何を保持・処理し、何をしないか
    - **機能一覧**: 主要機能の箇条書き（コードとの対応付き）
    - **使用技術**: 言語・主要ライブラリ・対応バージョン
    - **入出力**: 受け取るデータ / 出力するデータ / 永続化先
    - **依存モジュール**: どのモジュールと通信するか
  - [通信フロー]
    - **MQTT (TLS) `8883`**: ESP32 ↔ Mosquitto ↔ LocalServer（topic 体系・暗号化・認証）
    - **HTTPS OTA `4443`**: ESP32 → LocalServer（manifest / firmware 配布、SHA256 / 署名検証）
    - **AP モード HTTP**: ESP32 ← LocalServer / ProductionTool（`192.168.4.1`、初回ペアリング / 復旧）
    - **IPC (Named Pipe)**: LocalServer ↔ SecretCore（ランダム Pipe 名 + AES-256-GCM + nonce）
    - **ローカル CLI**: ユーザー ↔ ProductionTool（不可逆処理段階実行）
  - [図示推奨] テキスト ASCII 図 or Mermaid 図で構成図と通信フローを記載
  - [機能マトリクス] モジュール × 機能（MQTT publish / subscribe・OTA・ペアリング・鍵管理・eFuse 等）のマトリクス表
  - [関連] `013-0001`（セキュア全般ノウハウ）／`013-0002`（マッピング表）と相互参照
  - [既存文書との整合] `設計概要.md` / `モジュール仕様書.md` / `ソース概要.md` / `ネットワーク運用仕様書.md` / `LocalServer秘密処理別層仕様書.md` / `MQTTコマンド仕様書.md` の内容を統合し、初心者向けの導入文書として位置づける

#### 013-C. コードコメント充実化
- [完了][2026-05-19] [013-0004] **ESP32 / LocalServer / ProductionTool / SecretCore のコードヘッダー・関数コメント充実化** → 完了。`SecretCore/src/dpapi.rs` にモジュールヘッダー（目的・仕組み・設計判断・限界・関連文書）を新規追加。`SecretCore/src/key_manager.rs` の主要 4 関数（`get_k_device` / `sign_by_k_device` / `encrypt_by_k_device` / `decrypt_by_k_device`）に詳細 rustdoc（用途・引数・戻り値・注意・関連文書・ESP32 側対向実装・試験 ID）を追加。`ESP32/src/MQTT/mqtt_network.cpp` / `mqtt_set.cpp` / `mqtt_get.cpp` / `mqtt_call.cpp` / `ESP32/src/mqttProtocol.cpp` のヘッダーを目的・主要関数・トピック構造・関連文書込みで充実化。LocalServer TS 主要ファイル（`server.ts` / `secretCoreFacade.ts` / `secretCoreManager.ts` / `secretCoreIpcClient.ts` / `keyService.ts` / `mqttGateway.ts` / `localHistoryStore.ts` / `historyRedaction.ts`）と ProductionTool / SecretCore 他ファイルは確認の上、既に充実したヘッダーが整備済みと確認。
  - [厳守] 自作コードはすべてファイルヘッダーに「説明・目的・概要・機能・構成」を記述
  - [厳守] 関数にもできるだけ説明コメント（Doxygen / JSDoc / rustdoc）を追加
  - [重要] 主要技術（セキュア・MQTT・書き換え（OTA）・NVS アクセス）は詳細コメントを書く
  - [優先対象]
    - **LocalServer**（TypeScript）: ファイルヘッダー充実度が低い。`server.ts` / `secretCoreManager.ts` / `mqttGateway.ts` / `secretCoreFacade.ts` / 各 ts ファイルへ JSDoc 追加
    - **SecretCore**（Rust）: `pairing_workflow.rs` / `mqtt_transport.rs` / `ota_workflow.rs` / `generic_workflow.rs` の rustdoc 充実
    - **ProductionTool**（Rust）: 段階ランナー系（`irreversible_command_runner.rs` / `irreversible_stage_plan.rs` / `pc004_check_state.rs`）の rustdoc 充実
    - **ESP32**（C++）: セキュア関連は概ね充実だが、`MQTT/mqtt_*.cpp` 系の詳細コメント追加
  - [機密事項禁止] パスワード実値・raw key・eFuse 実値・証明書秘密鍵を含むコメントを書かない

### 020. 将来対応
[重要] `014`〜`019` は予約枠として空ける。`013` 章は文書整備・コードコメント充実化のために使用中。本章では将来対応だけを集約し、現行着手の邪魔をしない。

- [完了タスク退避][2026-05-24] `020-0016` は `todo_old20260524.md` へ完了退避済み。NVS 消失原因（secureNvsInit.cpp の nvs_flash_erase() 自動呼び出し）究明完了・短期対処（回復スクリプト cloud 引数対応）済み・根本修正は `020-0017` 完了済み。012-0008 全件クリア（2026-05-23）で動作確認完了。

#### 020-A. 後続技術・インフラ
- [完了タスク退避][2026-05-24] `020-0017` は `todo_old20260524.md` へ完了退避済み。secureNvsInit.cpp の nvs_flash_erase() 自動呼び出し削除・sensitiveData.cpp の LittleFS.remove() 廃止（build SUCCESS）。回復スクリプト・uploadfs・FW flash・k-device 保持確認完了（2026-05-21）。012-0008 全件クリア（2026-05-23）で S3 OTA 後も k-device 保持確認済み。

- [ ] [020-0018] [2026-05-20][将来対応] **AP モード経由での OTA ファームウェア更新対応**。現在の AP モード HTTP サーバ（`maintenanceApServer.cpp`）は設定変更 API のみ提供し、OTA（ファームウェアバイナリアップロード）エンドポイントを持たない。現在のファームウェア更新手段: (1) LocalServer 経由 local MQTT OTA、(2) LocalServer 経由 cloud MQTT OTA、(3) USB シリアル直接書込み。
  - [背景][2026-05-20] ユーザー要求: 「AP モード経由 OTA も今後の課題にする」。USB 接続なしで AP モードからもファームウェアを更新できると、現地でのメンテナンスが容易になる。
  - [実装案] `maintenanceApServer.cpp` に `/api/system/ota` POST エンドポイントを追加し、バイナリを受け取って `Update.h`（ESP32 Arduino OTA ライブラリ）で書込む。認証は既存の Bearer Token 方式を流用。
  - [前提] `020-0007`（Wi-Fi ステートマシン）/ AP モード認証フロー整備後が望ましい。
  - [着手条件] `012-0008` クラウド統合試験完了後の将来対応フェーズ。

- [完了タスク退避][2026-05-24] `020-0019` は `todo_old20260524.md` へ完了退避済み。CoreDNS Corefile を `bind 172.17.1.100` 限定に変更 + cloud mode は Buffalo-G-41E0（WAN 接続あり）AP を使用するよう変更で解消。7200 PASS（ESP32 → AWS IoT Core 接続成功）で確認済み（2026-05-21）。

- [完了タスク退避][2026-05-24] `020-0021` 完了済み。`mqttFallbackIp` NVS 保存（`sensitiveDataService::saveMqttFallbackIp` / `loadMqttFallbackIp`）・AP REST API `/api/settings/network` GET/POST 対応・`mqtt.cpp:pingBrokerHost` NVS 優先・マクロ fallback・試験仕様書 `7089` 追加・`APメンテナンス画面仕様書.md` §5.2 / §7 更新・`MQTTコマンド仕様書.md` §3.3 将来対応注記追加。`SENSITIVE_MQTT_FALLBACK_IP` コメント変更（初期値マクロ明記）。cloud モードのデグレなし（`pingBrokerHost` の cloud 分岐は変更なし）。

- [完了タスク退避][2026-05-24] `020-0025` は `todo_old20260524.md` へ完了退避済み。S3 presigned URL OTA 実装（generateCloudOtaFirmwareUrl()・TTL=600s）・ota.cpp cloud mode AmazonRootCA1.pem 切替・IoT Policy esp32lab/* 拡張。7205/7206 PASS（dual-partition OTA サイクル完全確認）済み（2026-05-23）。コミット: 260523-1・タグ: v1.1.0-cloud-ota。

- [ ] [020-0024] [2026-05-21][重要] **LocalServer デバイス一覧にデバイス毎のクラウド/ローカル接続経路表示列を追加**。
  - [背景][2026-05-21] 現状の LocalServer デバイス一覧（`index.html`）には接続経路（cloud / local）の表示がない。本セッションでは LocalServer と ESP32 は同一ローカルネットワーク（Buffalo-G-41E0 内）に**たまたま**居るが、クラウド運用ではデバイスごとに：
    - (a) **ローカル直結**（同一 LAN・Mosquitto 経由）
    - (b) **クラウド経由**（AWS IoT Core 経由・異なる LAN / 顧客先など）
    の 2 経路があり、運用者は経路を識別できる必要がある。
  - [設計方針]
    - device 一覧テーブルに新規列「接続経路」を追加（例：`local` / `cloud` / `cloud (AP-XXX)` / `unknown`）
    - 判定ロジック：受信メッセージのソース broker（LocalServer の cloud subscriber 経由か local mqttGateway 経由か）を device 状態に付与
    - LocalServer 自体が cloud subscriber と local mqttGateway の両方を持つ場合の前提（`mqttGateway.ts:clientOverride` 経路に応じて区別）
    - 既存 `connectionMode: "mqtt"` を `connectionMode: "local-mqtt" | "cloud-mqtt"` に拡張案、もしくは別フィールド `mqttBrokerSource: "local" | "cloud"` を追加
    - WebSocket 経由でブラウザへ即時反映
  - [更新範囲]
    - `IoT/LocalServer/src/mqttGateway.ts`: メッセージソース識別ロジック追加
    - `IoT/LocalServer/src/deviceRegistry.ts`（or 該当箇所）: device state に経路フィールド
    - `IoT/LocalServer/public/index.html` / `admin.html`: 列追加・色分け表示
    - `IoT/LocalServer管理画面仕様書.md` 更新
    - `IoT/機能仕様書.md` 関連節更新
  - [試験仕様] `7XXX`: cloud / local 両モードで同時に device が見えるとき、各 device の経路が正しく表示されることを確認
  - [関連] `020-0022`（cloud/local mode UI 切替）/ `012-0008` cloud 試験成果 / `機能仕様書.md`

- [ ] [020-0023] [2026-05-21][将来対応] **ローカルモード（Mosquitto）でも MQTT publish の `retain` 必要性を検討**。
  - [背景][2026-05-21] cloud mode 修正で `iot:RetainPublish` 権限不足を回避するため、cloud のみ `retain=false` とした（`mqttBrokerMode != "cloud"` のとき従来通り `retain=true`）。これは cloud / local の挙動差を生む。
  - [検討観点]
    - (a) **retain=true（現状ローカル）の利点**: LocalServer 再起動・新規 subscriber 接続時に Mosquitto から最新ステータスが即時 push される
    - (b) **retain=false（cloud と統一）の利点**: 設計シンプル化、ステイルなオフライン状態をマスクしない、subscriber は fresh 状態を能動的に問い合わせる前提
    - (c) **ハイブリッド案**: status 系は retain、command 系は no-retain など使い分け
  - [判断材料] ローカル運用で「LocalServer 再起動後の即時 device 表示」がどれだけ重要か、設計仕様書の意図（`機能仕様書.md` / `MQTTコマンド仕様書.md`）はどちら寄りか
  - [更新範囲]
    - `IoT/ESP32/src/MQTT/mqtt.cpp:3548` 付近 (`useRetain` 条件)
    - `IoT/MQTTコマンド仕様書.md` 該当節
    - 試験追加：retain=false で LocalServer 再起動 → fresh state 取得の動線確認
  - [関連] `問題点記録書.md #0047` / cloud mode `iot:RetainPublish` 制約 / `012-0008` 7200 系試験

- [完了タスク退避][2026-05-24] `020-0022` は `todo_old20260524.md` へ完了退避済み。LocalServer cloud/local mode 切替 UI 実装完了（`brokerModeType`・`disconnect()`・`buildGateway()`・`GET/POST /api/admin/broker-mode`・`brokerModeCard`・`settings.json` 永続化）。管理画面仕様書 §3.10・試験仕様書 7109 追加済み。TypeScript コンパイルエラーなし確認済み。

- [完了タスク退避][2026-05-24] `020-0020` は `todo_old20260524.md` へ完了退避済み。`#0047` 試験記録化完了（試験仕様書 7200 手順追加・試験記録書 7200 #0047 修正確認追記・問題点記録書 #0047 試験結果確認セクション追記・#0048 状態「解決済み」更新）。

- [ ] [020-0001] [2026-04-29][将来対応][重要] `k-iot-secure-boot` / `k-iot-flash-encryption` を**本番専用**に分離する（オフライン生成、`鍵一覧仕様書.md` / `コマンド仕様書.md` **§10.2.5a** に沿った運用、試験用との保管・適用境界の明文化、`004-0006` / `004-0007` 相当の再導入）。
  - [着手条件][2026-04-29] 量産・鍵失効・試験/本番の分離要件、または**製品鍵への切替で仕組み（実装・手順・設定・証跡・判定条件）が変わる**と判断した場合に着手する。
  - [試験方針][2026-04-29] **仕組み変更あり**: `020-0001` の一部として追加試験を行う。**仕組み変更なし**: 鍵差し替え・適用確認・運用確認の最小確認を行い、全面再試験は要求しない。
  - 理由: **当面（2026-04-29〜）は試験用鍵を本番にも流用**（`todo_old20260429.md`）とするが、製品化時に鍵変更そのものと仕組み変更を混同しないため。
- [完了タスク退避][2026-05-14] `020-0002` は `todo_old20260514.md` へ完了退避済み。Mosquitto `acl_file` は導入方針とし、device / role 単位 Topic ACL の正本は `ネットワーク運用仕様書.md` 7.4.4 と `Mosquitto_TLS_IDパスワード設定手順書.md` を参照する。
- [完了タスク退避][2026-05-16] `020-0003` は `todo_old20260516.md` へ完了退避済み。Router `WAN 管理` / `UPnP` / `DMZ` / `port forward` は不採用方針とし、正本は `ネットワーク運用仕様書.md` 7.4.4 / 7.4.4a / 7.4.4b を参照する。
- [完了タスク退避][2026-05-14] `008-0037` は `todo_old20260514.md` へ完了退避済み。耐量子暗号移行計画の正本は `耐量子暗号移行計画書.md` とし、2030年以降の将来対応として扱う。
- [ ] [020-0013] [2026-05-19→旧 `007-0009`][最重要] 各段階（FE / NVS暗号化なし確認 / SB / 封鎖）の完了判定前に、**安定性確認セット**（OTA 2回連続 + AP モード 10 分 + 通常モード 10 分）を実施する。
  - [厳守] いずれかで `abort` / 再起動ループ / AP 到達不能 / MQTT 再接続失敗 / OTA 失敗を検知した場合は即停止する
  - [厳守] 停止時は `問題点記録書.md` と `試験記録書.md`（`7099`）へ記録し、原因確定まで次段へ進まない
  - [重要] 文鎮化リスクの早期検知を目的とし、成功証跡は 4 点証跡（旧 `007-0006`）に追加で添付
  - [`020-0011` ゲート組込] 本タスクは `020-0011` の各段階ゲートとして組込み、`ProductionTool` `PT-005z stability` で自動収集する
  - [移管理由][2026-05-19] `007` 章を空にして `012` クラウド連携前に着手前タスクを整理するため、`020` 章へ移管
- [完了タスク退避][2026-05-20] `020-0015` は `todo_old20260520.md` へ完了退避済み。Wi-Fi_lab で ESP32 AP 接続成功を確認（2026-05-20）し、`configureEsp32CloudMode.ps1` の `$AP_IFACE` を `'Wi-Fi_lab'` に戻した。根本原因は (1) AP Wi-Fi パスワード不一致（`#0040`）と (2) Wi-Fi_lab GCMP 選択（`#0045`）の複合。詳細は `問題点記録書.md #0045`。
- [ ] [020-0014] [2026-05-20][将来対応] **ESP32 AP モード移行の自動化**（MQTT 暗号化対応）。`esp32lab/call/maintenance/<deviceName>` は kStrict モードで AES-256-GCM 暗号化が必須のため、平文の `mosquitto_pub` では受け付けられない。LocalServer が local モードで動作中であれば `/api/admin/commands/maintenance-reboot` API（暗号化済み）で自動移行できるが、cloud モード時は手動 GPIO 4 ボタン長押しが唯一の方法。
  - [発見日] 2026-05-20（012-0008 試験中）
  - [暫定対処] GPIO 4 ボタン 3 秒長押し（起動時）で手動 AP モード遷移
  - [改善案] LocalServer に `/api/admin/esp32/trigger-ap-mode` エンドポイントを追加し、cloud/local 両モードで一時 local 接続 → 暗号化 publish → cloud 復帰の自動フローを実装する（`020-0007` の Wi-Fi ステートマシンと同期要）
- [ ] [020-0012] [2026-05-19→旧 `007-0014`][重要] **`ProductionTool` を不可逆工程の責任主体として実装へ収束**する。
  - [進捗（移管時点）] `ProductionTool/src/irreversible_stage_plan.rs`（段階計画固定）／`irreversible_command_runner.rs`（`espefuse`/`espsecure`/`esptool` テンプレート + Windows CLI 最小実ランナー + 二重ゲート `PRODUCTION_TOOL_ENABLE_IRREVERSIBLE_COMMAND_RUNNER=1` + `PRODUCTION_TOOL_ALLOW_IRREVERSIBLE_EXECUTION=1`）／`pc004_check_state.rs`（PC-004 鍵ID・署名素材ID・ロット・作業指示番号照合）／`PT-005z precheck/run/段階指定実行/前段 stability ゲート/Stage1→4 逐次実行/段階4 USB シリアル切替`は実装済み。
  - [未了] `espefuse` / `espsecure` / `esptool` を `ProductionTool` 管理下で実行する完全な段階ランナーは未実装（`precheck → stage_execute → readback → evidence → stability` を各段階で閉じる、秘密値をログへ出さない）
  - [厳守] 実ランナーを追加するまで、既存の本番1号機実績手順（計画書 §6.4 / 007 チェックリスト）を崩さない。デグレと誤焼込み防止
  - [移管理由][2026-05-19] `007` 章を空にして `012` クラウド連携前に着手前タスクを整理するため、`020` 章へ移管
- [ ] [020-0011] [2026-05-19→旧 `007-0001`][重要] `ProductionTool` から **本番対象機へ不可逆段階を順次実施**し、各段階の出荷可否判定ログを保存する（**詳細手順順序は `本番セキュア化出荷準備試験計画書.md` §6.4**）。
  - [段階1][不可逆] **Flash Encryption 系**：FE 鍵投入、`SPI_BOOT_CRYPT_CNT`、関連 eFuse、暗号化済みバイナリ書込み
  - [段階2][非焼込みゲート] **NVS 暗号化なし確認（案A）**：`NVS Encryption` / `HMAC 鍵 burn-key（EF-009b）` は本番不可逆として実施しない。`patternA` ログ、NVS 読書き、AP HTTP 全ロール認証を確認
  - [段階3][不可逆] **Secure Boot v2 系**：SB 署名鍵ダイジェスト投入、`SECURE_BOOT_EN`、未使用 slot revoke、`RD_DIS` write-protect（順序・EF 番号は計画書）、署名済みバイナリ書込み
  - [段階4][不可逆] **JTAG/DL 封鎖**：`DIS_PAD_JTAG`、`DIS_USB_JTAG`、必要時のみ `DIS_USB_SERIAL_JTAG`、※`ENABLE_SECURITY_DOWNLOAD` は最後（計画書 段階4）。シリアル出力停止は段階4の最終クローズ判断としてシリアル抑止済みの量産通常 FW へ更新する形で扱う
  - [前提] **`007-B` No-Go 継続中**。`020-0012` の `ProductionTool` 最終形実ランナー実装・検証完了後に着手
  - [鍵方針] `k-iot-secure-boot` / `k-iot-flash-encryption` は試験用素材を本番でも流用（鍵新規発行なし）。本番専用鍵分離は `020-0001`
  - [移管理由][2026-05-19] `007` 章を空にして `012` クラウド連携前に着手前タスクを整理するため、`020` 章へ移管
- [ ] [020-0010] [2026-05-19][将来対応][重要] **LocalServer 用インストーラ/アンインストーラ EXE 化**（Inno Setup `.iss` スクリプト作成、Node.js 戦略確定、ビルド、本試験）を実施する。
  - [背景][2026-05-19] `009-1020` の設計仕様（`インストーラEXE化設計仕様書.md`）は完成度高。ProductionTool 側骨格（`ProductionTool/installer/ProductionTool.iss` + `ProductionTool/scripts/build-production-tool-installer.ps1`）も存在。**LocalServer 用は `.iss` スクリプト未作成、Node.js バンドル戦略未確定、EXE 生成・試験未実施**。
  - [着手条件] 客先配布要件が確定した時、または本番1台目（`007`）完了後の出荷準備フェーズに進む時。
  - [実装範囲]
    - Node.js 戦略確定: 方針 A（ポータブル同梱）/ 方針 B（Node.js SEA 単体 EXE）の選定
    - `LocalServer/installer/LocalServer.iss`（Inno Setup スクリプト）新規作成
    - `LocalServer/scripts/build-local-server-installer.ps1`（ビルドスクリプト）新規作成
    - UAC（管理者権限）必須化、Task Scheduler 登録自動化、ファイアウォール許可ウィザード
    - アンインストール時の安全消去（`安全消去ポリシー.md` 準拠）・ファイアウォール解除・Task Scheduler 解除
    - `試験仕様書.md` `7090` 系（EXE インストール・アンインストール本試験）追加
  - [関連] `009-1020`（設計仕様確定までで完了退避、本実装は本タスクへ移管）／`インストーラEXE化設計仕様書.md` §1〜§4／`ProductionTool/installer/`（参考実装）
- [ ] [020-0009] [2026-05-19][将来対応][重要] **LocalServer TS 側の初回ペアリング UI/CLI 詳細化と実装**（`admin.html` / `settings.html` の pairingSession 開始画面・進捗表示・完了確認）を実施する。
  - [背景][2026-05-19] `009-0010` の精査で、Phase2 秘密処理側（`SecretCore` `load_or_create_s_random` / `get_k_user` / `get_k_device` / `runPairingSession()` AP モード `/api/auth/login`〜`transport-handshake`）は実装済みと確認。**LocalServer TS 側の REST 公開・進捗表示・UI フローの詳細化が未完成**。
  - [着手条件] 初回ペアリング運用が実機で必要になった時、または客先導入時の現地作業フローを確定する必要がある時。
  - [実装範囲]
    - `admin.html` / `settings.html` の pairingSession 開始画面（対象機選択・Wi-Fi/MQTT/OTA/認証情報入力フォーム・必須項目検証）
    - 進捗表示: `queued → running → waiting_device → verifying → completed/failed` の状態遷移を UI で可視化
    - 完了確認画面: 成功時の確認、失敗時のエラー要約とリトライ導線
    - CLI 経路: `LocalServer/scripts/run-pairing-session.ps1` 等（バッチ実行・無人運用想定）
    - 試験仕様書 `7XXX`（初回ペアリング UI/CLI 端末試験）追加
  - [関連] `009-0010`（秘密処理側完了で退避、UI/CLI 詳細化は本タスクへ移管）／`009-0018`（5 層セキュリティ）／`LocalServer秘密処理別層仕様書.md` §4.1（pairing/start 進捗）／`APメンテナンス画面仕様書.md`
- [ ] [020-0008] [2026-05-19][将来対応][重要] **機密設定（Wi-Fi / MQTT / HTTPS / 時刻サーバ）の NVS 永続化**（平文 JSON 運用の完全廃止）を実施する。
  - [背景][2026-05-19] `009-0015`（機密設定の NVS 移行）と `009-0024`（時刻サーバ設定 NVS 優先化）を統合。現状は `sensitiveDataService::load*Config` が `sensitiveData.json` 経由で読込んでいる（`sensitiveData.h` 静的ヘッダー直接依存は既に解消）。完全な平文 JSON 運用廃止と NVS 永続化への移行は将来対応。
  - [着手条件] 客先環境での運用要件として「設定ファイルの平文化を許容しない」基準が確定した時、または本番出荷準備の一環として実施する時。
  - [実装範囲]
    - Wi-Fi（SSID / Password）／MQTT（host / port / username / password / TLS）／HTTPS OTA（host / port / CA 参照）／時刻サーバ（URL / Port / TLS）の各設定を NVS へ移行
    - 既存 `sensitiveData.json` からの自動マイグレーション（初回起動時に NVS へ取り込み、後続は NVS のみ参照）
    - `sensitiveData.json` 平文ファイルの削除または読込専用化
    - NVS 暗号化との整合（`secureNvsInit.cpp` の HMAC 鍵投入は `007-B` 案A で不採用 → patternA NVS 暗号化なしで運用）
  - [関連] `009-0015`（NVS 移行・本タスクへ統合・退避済み）／`009-0024`（時刻サーバ NVS 優先化・本タスクへ統合・退避済み）／`鍵管理および初期セットアップ設計仕様書.md` §6.1 設定保存方針／`WiFi設定更新_詳細設計書.md` §6.1
- [ ] [020-0007] [2026-05-19][将来対応][重要] **ESP32 Wi-Fi 設定更新ステートマシン本実装 + 誤設定ロールバック試験仕様 7XXX 追加**。
  - [背景][2026-05-19] `009-0023`（state machine 本実装）と `009-0022`（誤設定ロールバック試験仕様追加）を統合。設計は `WiFi設定更新_詳細設計書.md` §7-8 で確定済み。実装側は `wifiConfigUpdate`/`wifiConfigConfirm` コマンド名定義とパース関数（`mqttProtocol.cpp:132-137 / parseWifiUpdatePayload`）は存在するが、`009-0014` 精査で判明したように実 active path は `esp32lab/network/...` であり、これらは未使用ヘルパー。
  - [着手条件] Wi-Fi 設定変更の運用要件が顕在化した時、または客先設置時の Wi-Fi 変更需要が発生した時。
  - [実装範囲]
    - state machine: 未確定保存 → 接続試行 → CONFIRM 受信 → 確定 / タイムアウト → 旧設定ロールバック
    - 未確定 / 確定の NVS スロット 2 系統管理（または既存 sensitiveData.json の `pending` フィールド追加）
    - 接続試行タイムアウト（例: 30 秒）、CONFIRM 受信猶予（例: 60 秒）の設計値確定
    - MQTT トピックは `esp32lab/network/...` 系を使用（`009-0014` 不採用クローズに整合）
    - 試験仕様書 `7XXX`（Wi-Fi 誤設定試験：誤った SSID/PSK 投入 → タイムアウト → 旧設定ロールバック確認）を新規追加
  - [試験方針]
    - 正常系: 正しい新 SSID 投入 → 接続成功 → CONFIRM 受信 → 確定保存
    - 異常系1: 誤った SSID 投入 → タイムアウト → 旧設定ロールバック → 旧 SSID で再接続
    - 異常系2: 正しい SSID で接続成功するが CONFIRM 受信前にタイムアウト → ロールバック
    - 試験後は元の Wi-Fi 設定へ戻す
  - [関連] `009-0022`（試験仕様追加・本タスクへ統合・退避済み）／`009-0023`（state machine 本実装・本タスクへ統合・退避済み）／`WiFi設定更新_詳細設計書.md` §7-8／`機能仕様書.md` F3-028
- [ ] [020-0006] [2026-05-19][将来対応][重要] ESP32 の **Wi-Fi 接続失敗回数しきい値超過による自動 AP モード遷移**（フェイルセーフ）を実装する。
  - [背景][2026-05-19] `009-0021` の精査で、`WiFi設定更新_詳細設計書.md` §9.1 にトリガーとして記載があるものの実装側に存在しないと判明。AP モードのコア機能（WPA2 / 固有パスワード / Web UI / 4 ロール認証）は実装済みで、本タスクは「自動遷移トリガーの追加」のみが対象。
  - [着手条件] 客先環境で Wi-Fi 誤設定からの自動復旧が必要になった時、または Wi-Fi 接続失敗時の手動 AP モード遷移が運用負荷として顕在化した時。
  - [実装範囲]
    - 連続失敗カウンタ: Wi-Fi 接続失敗時にカウントアップ、成功時にリセット（メモリ上のみ／NVS 永続化の要否を設計で判断）
    - しきい値定義: 例 `WIFI_FAIL_THRESHOLD_FOR_AP=5`（連続 5 回失敗で AP 遷移）／時間窓のあわせ持ち（例: 30 分以内の連続失敗で判定）の要否は設計時に検討
    - 自動 AP モード遷移: しきい値超過時に `maintenanceMode::requestMaintenanceModeOnNextBoot()` を呼んで再起動 → AP モード入り
    - 誤検出回避: 一時的な切断（ルータ再起動・電源瞬断）と恒久的な誤設定の判別ロジック（時間窓のリセット条件等）
    - 監査ログ: AP 自動遷移イベントを記録（誰がいつ何故 AP に入ったかを追跡可能にする）
  - [試験方針]
    - 連続失敗 → AP 自動遷移の確認（実機: Wi-Fi SSID を誤値に変更して N 回失敗を観測）
    - 一時切断時に誤発動しないことの確認（ルータ瞬断時に AP に入らない）
    - 試験後は元の Wi-Fi 設定へ戻す
  - [関連] `009-0021`（精査結果に基づき本タスクへ移管・退避済み）／`WiFi設定更新_詳細設計書.md` §9.1（将来対応として明文化済み）／`機能仕様書.md` F3-012 / F3-067
- [ ] [020-0005] [2026-05-19][将来対応][重要] ESP32 の `current/previous` key 2 スロット運用において、**previous key 稼働時間管理のコア実装**（runtime カウンタ・state 遷移判定・自動削除・認証拒否切替）を行い、加速試験（2h 完了）→ 試験結果合格 → 設計時間（48h/240h）へ復帰、までを実施する。
  - [背景][2026-05-19] `009-0020` の精査で、現状は NVS キー定義と pairing 直後の grace 設定のみ実装で、コア処理（runtime カウンタ等）が未実装と判明。鍵ローテーションは必須運用ではない（`k-user` 変更時のみ発生）ため、本実装は将来対応へ移管。
  - [着手条件] 鍵ローテーション運用を実機で行う必要が出てきた時、または `runKeyRotationSession()` の end-to-end 実試験を完成させる時。
  - [実装範囲]
    - 時間定数の定義: `KEY_ROTATION_GRACE_MINUTES`（既定 2880=48h）／`KEY_ROTATION_RETAINED_MINUTES`（既定 14400=240h）／`RUNTIME_LOG_INITIAL_DELAY_MIN`（既定 5）／`RUNTIME_LOG_INTERVAL_MIN`（既定 60）
    - 加速試験用ビルドフラグ: `-DKEY_ROTATION_TIME_SCALE=1`（通常）または `=120`（加速、48h→24min、240h→120min）
    - runtime カウンタ実装: 起動後 `RUNTIME_LOG_INITIAL_DELAY_MIN` で 1 回、その後 `RUNTIME_LOG_INTERVAL_MIN` 間隔で `graceActiveRuntimeMinutes` を NVS 加算
    - state 遷移判定: `graceActiveRuntimeMinutes >= KEY_ROTATION_GRACE_MINUTES` で `grace → expired-retained`、`>= KEY_ROTATION_RETAINED_MINUTES` で `expired-retained → deleted` かつ `previousKDevice` を NVS から削除
    - 認証拒否切替: `expired-retained` 以降は旧鍵での署名検証・復号を拒否し、新鍵のみ受理
    - 監査ログ: 全 state 遷移と自動削除を監査ログに記録、UI には出さない
  - [試験方針]
    - 加速試験（2h）: `試験仕様書.md` `7038` の手順 1〜8 を実施
    - 通常運用試験（実 240h）: 別個体または実 10 日間運用で実施（運用上の都合次第）
    - **試験後の必須措置**: 元の鍵へ戻す（`runKeyRotationSession()` で試験前の `currentKDevice` を再度 current 化）／設計時間に戻す（`-DKEY_ROTATION_TIME_SCALE=1` で再ビルド・配信）／結果を `試験記録書.md` `7038` 欄へ追記
  - [関連] `009-0020`（精査結果に基づき本タスクへ移管・退避済み）／`試験仕様書.md` `7038`／`機能仕様書.md` F3-035・F3-036／`要件定義書.md` FR-047／`IF仕様書.md:353`／`コマンド仕様書.md:528`／`SecretCore/src/key_manager.rs:454-465 get_k_device`（k-device 導出）
- [ ] [020-0027] [2026-05-24][将来対応] **管理画面への鍵保護モード表示**（現在どちらのモードで動いているかを UI に表示する）。
  - [背景][2026-05-24] `PROTECT_MODE` 環境変数でファイルベース AES-256-GCM と Windows DPAPI を切替可能になったが、現在の管理画面（`admin.html`）には active な保護モードが表示されない。
  - [実装案]
    - `SecretCore` に `active_protection_mode()` 関数がある（`key_protect.rs:172`）。LocalServer 側の health/info API または SecretCore facade 経由でモードを返す。
    - `GET /api/admin/secret-core/info` に `protectionMode: "file" | "dpapi"` フィールドを追加。
    - `admin.html` の SecretCore ステータスカードに保護モードを表示（例: `AES-256-GCM (file)` / `Windows DPAPI`）。
    - `data/keys/master_key.bin` の有無・`wrapped_secret.bin` のサイズ（280 vs 552 bytes）でも簡易判定可能だが、API 経由の方が正確。
  - [関連] `key_protect.rs:active_protection_mode()` / `020-0026`（対話選択）/ `LocalServer秘密処理別層仕様書.md`

- [ ] [020-0026] [2026-05-24][将来対応][重要] **PROTECT_MODE 鍵作成時の対話選択 UX**（鍵生成スクリプト/セットアップ時にユーザーがモードを選択できるようにする）。
  - [背景][2026-05-24] 現在は `PROTECT_MODE=dpapi` 環境変数を事前に設定してから `createSensitiveDataJsonForRecovery.cjs` を実行しないとモードが切り替わらない。設定し忘れると復号に失敗するため UX が悪く、次回セッションでの引継ぎが分かりにくい。
  - [ユーザー要望] 「DPAPI と非 DPAPI はビルドでなく、鍵を作るとき、その場でユーザーが作るときに選択できるといい」
  - [実装案]
    - `createSensitiveDataJsonForRecovery.cjs` に `--protect-mode dpapi|file` オプションを追加し、指定がない場合は対話プロンプト（`readline`）でモードを選択させる。
    - または LocalServer の管理画面（`admin.html`）に「SecretCore 初期セットアップ」ウィザードを追加し、モード選択 → 鍵生成 → LittleFS 書き出しまでをブラウザから実行できるようにする。
    - `SecretCore` のヘルスチェック API に `currentMode` を含め、LocalServer 起動時にログ出力する。
    - `PROTECT_MODE` は `.env` への永続化（`dotenv` 書き戻し）も検討。
  - [着手条件] LocalServer のセットアップフローを改善したい時、または複数 PC 環境への展開が必要になった時。
  - [関連] `key_protect.rs:active_protection_mode()` / `020-0027`（UI 表示）/ `createSensitiveDataJsonForRecovery.cjs`

- [ ] [020-0004] [2026-05-19][将来対応][重要] `SecretCore` の OS 暗号化サービス（現行: Windows DPAPI）を Mac / Linux でも汎用対応できる方式へ抽象化する。
  - [背景][2026-05-19] 現実装は Windows DPAPI 単独（`SecretCore/src/dpapi.rs` / `SecretCore/src/key_manager.rs`、`WRAPPED_SECRET_ALG="DPAPI"`）。チップ非依存（TPM 不要）だが OS 依存（Windows 限定）。Mac / Linux 配布要件が出た時点で着手する。
  - [着手条件][2026-05-19] macOS または Linux 向けの LocalServer 配布要件が確定したとき、または現行 Windows 単独運用方針が変更されたとき。
  - [候補方式][2026-05-19] macOS: Keychain Services（`security` framework）。Linux: libsecret（GNOME Keyring / KWallet バックエンド）。Rust では `keyring` クレート等の抽象層を採用候補とする。
  - [試験方針][2026-05-19] 各 OS 上で `S_random` ラップ / アンラップ、`wrapped_secret` 整合性、別ユーザー復号拒否、ユーザープロファイル再作成時の挙動を確認する。`WRAPPED_SECRET_ALG` の値設計（OS 別エイリアス）と既存 DPAPI ファイルの後方互換読込を併せて整理する。
  - [関連文書] `鍵管理および初期セットアップ設計仕様書.md` §2 / §16、`LocalServer秘密処理別層仕様書.md` §5.0 / §11 Phase4、`鍵管理初期セットアップ_実装たたき台.md` §1 / §8。
- [x] [010-0011] [2026-03-06→**完了 2026-05-24**][将来対応][厳守] CoreDNSではローカル専用ゾーンのみ管理し、公開ドメインの上書きを禁止する運用ルールを文書化する。退避: `todo_old20260524.md`。
  - [完了証跡][2026-05-24] `ネットワーク運用仕様書.md §2.4.2` 追加（CoreDNS ゾーン管理ルール・`fallthrough` 必須・公開ドメイン上書き禁止・現行 Corefile 構造を明文化）
- [x] [010-0012] [2026-03-07→**完了 2026-05-24**][重要][将来対応] DHCP配布DNSまたはルーターDNSフォワーダ設定を見直し、ESP32側の固定DNS指定（方法①）を廃止しても `mqtt.esplab.home.arpa` が解決できる状態へ移行する。退避: `todo_old20260524.md`。
  - [完了証跡][2026-05-24] `ネットワーク運用仕様書.md §2.4.3` 追加（現状・目標・移行条件・ルータ DHCP DNS 設定手順・移行タイミングを定義）。実際のルータ設定変更はユーザーが手動で実施する（LocalServer から自動変更不可）。

## 運用ルール
- [厳守] 中期計画・未完了タスク・進捗メモは `todo.md` に統合し、別の日程表へ分散させない。
- [厳守] 毎作業日の終了時に `todo.md` の状態と必要なメモを更新する。
- [厳守] タスクを完了にした同日中に、関連仕様書・試験書・問題点記録書も更新する。
- [厳守] 完了タスクは `todo_oldYYYYMMDD.md` へ退避し、本書から削除する。

## 変更履歴
- 2026-05-24（**セッション④: DPAPI 選択モード実装 + 両方式 E2E テスト完了**）: `key_protect.rs` デュアルモード実装（`PROTECT_MODE=dpapi` / デフォルト AES-256-GCM）。DPAPI: MQTT PASS + OTA PASS（partition 1→0）。ファイルベース AES-256-GCM: MQTT PASS + OTA PASS（partition 0→1）。`020-0026`（PROTECT_MODE 対話選択）/`020-0027`（管理画面モード表示）新規追加。設計仕様書 §4 デュアルモード対応更新。コミット `260524-N`。
- 2026-05-24（**020-0021 完了**）: `020-0021`（AP/NVS fallback IP 統合化）完了。実装：`common.h` kMqttFallbackIp 追加・`sensitiveDataService` `saveMqttFallbackIp`/`loadMqttFallbackIp` 追加・`maintenanceApServer.cpp` POST/GET 対応・`mqtt.cpp:pingBrokerHost` NVS 優先・マクロ fallback。文書：`APメンテナンス画面仕様書.md` §5.2 / §7・`MQTTコマンド仕様書.md` §3.3・`試験仕様書.md` `7089` 追加。進捗 **5/90**（約 94%）。
- 2026-05-24（**020-0020 完了退避 + 020-0016/0017/0019/0025 正式退避**）: `020-0020`（`#0047` 試験記録化）完了。試験仕様書 7200 手順に `pingBrokerHost` cloud DNS 確認ステップ追加・試験記録書 7200 に `#0047 修正確認` エントリ追加・問題点記録書 `#0047` 試験結果確認セクション追記・`#0048` 状態「解決済み」更新。合わせて `012-0008` 作業中に実質対処済みだった 4 件（020-0016/0017/0019/0025）を正式クローズ。`todo_old20260524.md` へ退避。進捗 **6/90**（約 93%）。
- 2026-05-23（🎉**012-0008 全件クリア・7205/7206 OTA PASS**）: **クラウドモードでの OTA S3 配信試験完了**。020-0025（S3 OTA）実装済み環境で 7205/7206 を正式実施し PASS。
  - **7205** OTA via S3 presigned URL: LocalServer が `generateCloudOtaFirmwareUrl()` で presigned URL 生成 → AES-256-GCM + HMAC-SHA256 コマンドを AWS IoT Core 経由で ESP32 へ送信 → ESP32 が `AmazonRootCA1.pem` で S3 TLS 接続・ダウンロード・SHA256 検証・フラッシュ・再起動 → `runningPartition: "1"`（app0→app1）。`otaPhase: "done"` / `otaProgressPercent: 100` 確認。
  - **7206** OTA × 2 dual-partition: 2 回目 OTA で `runningPartition: "0"` / `bootPartition: "0"`（app1→app0）確認。dual-partition（`BOOTLOADER_APP_ROLLBACK_ENABLE`）OTA サイクル完全動作確認。
  - **バグ修正 2 件**:（1）IoT Policy `esp32lab-localserver-policy` の Publish リソースを `esp32lab/call/*` → `esp32lab/*` に拡張（`esp32lab/get/*` publish で AWS が強制切断するバグ修正）。（2）`ota.cpp:resolveOtaTlsCaCertificate` を cloud mode 時 `AmazonRootCA1.pem` 使用に修正（従来 `mqtt-ca.pem`=Mosquitto CA → S3 TLS ハンドシェイク失敗していたバグ修正）。
  - **012-0008** を完了マーク（`[x]`）。**進捗 12 / 90（約 87%）**。`012-0009` まとめ進行中。`CLOUD_MQTT_ENABLED=false` で課金停止済み。
- 2026-05-21（続⁴・🎉**012-0008 7200/7201/7203/7204/7207/7208/7209 クリア**）: 本セッションでクラウド連携の主要試験項目を完了。
  - **7207** 第三者アクセス拒否: AWS IoT Core エンドポイントへ (a) 無証明書 → ECONNRESET / (b) 自己署名偽証明書 → silent close、両方拒否確認。
  - **7208** cloud→local フォールバック: ESP32 を `brokerMode=local` JSON で uploadfs → ESP32 再起動 → brokerMode mismatch 検出して再移行 → `wifi loaded. ssid=AP-IoTESP32Test` / `connectToMqttBroker start. mode=local host=mqtt.esplab.home.arpa` / `state=0`。
  - **7209** ローカル復帰後通常通信: LocalServer を `CLOUD_MQTT_ENABLED=false` で再起動 → local Mosquitto 経由で device 認識 (`onlineState=online`, `wifiSsid=AP-IoTESP32Test`, `ipAddress=172.17.1.200`)。
  - **新規バグ修正**: `sensitiveData.cpp:ensureDefaultFileExists` が brokerMode 切替時の再移行を扱わない設計だったため、brokerMode mismatch 検出 + `migrateNeeded` 条件拡張で解消（cloud↔local 双方向移行に対応）。
  - **OTA 試験 7205/7206 は延期**: 現行 OTA はコマンド経由 cloud + バイナリ経由 LocalServer:4443 ハイブリッド構成のため、本番想定（ESP32 と LocalServer が異ネットワーク）で動かない。S3/CloudFront 経由配信に改修してから正式試験 → `020-0025` 新規追加。
  - **新規 todo 4 件**: `020-0022`（cloud/local UI 切替）/ `020-0023`（local retain 検討）/ `020-0024`（接続経路表示列）/ `020-0025`（OTA S3 化）。
- 2026-05-21（続³・🎉**ESP32 → AWS IoT Core 接続成功 / 012-0008 7200 クリア**）: TLS handshake 成功（TLSv1.2 / TLS-ECDHE-RSA-WITH-AES-128-GCM-SHA256 / Certificate verified）→ `connectToMqttBroker success. state=0` → `publishStatusNotice success. topic=esp32lab/notice/status/IoT_04CEF94EB580 onlineState=Online`。本セッションで判明・修正した連鎖根本原因 4 件：
  1. **`pingBrokerHost` ローカル fallback 強制使用**（`#0047`）→ cloud mode で hostByName のみに分岐（local 完全維持）
  2. **AP-IoTESP32Test 閉域 AP（WAN なし）でクラウド到達不可** → ESP32 を `Buffalo-G-41E0`（WAN 接続あり）へ切替。`.env` に `CLOUD_WIFI_SSID` / `CLOUD_WIFI_PASSWORD` 追加（Git 除外）、`createSensitiveDataJsonForRecovery.cjs` cloud 指定時に wifi セクションも書き出し（health check リトライも追加）、`main.cpp` の `SENSITIVE_DATA_USE_HEADER_VALUES` ブロックを cloud で wifi/mqtt 上書きしないようガード、`sensitiveData.cpp:ensureDefaultFileExists` の legacy JSON 移行トリガを「keyDevice 欠落」だけでなく「wifi 欠落」も含むよう拡張
  3. **`MqttTlsClient::connect(IPAddress, port)` override の client cert/key nullptr 固定** → `setClientAuthContext(cert, key)` を新設し IP 接続経路でも mTLS cert/key を提示するよう修正。AWS が mTLS で TCP リセット（`MBEDTLS_ERR_NET_CONN_RESET -0x0050`）していたのが解消
  4. **MQTT CONNECT の willRetain=true で AWS 拒否**（retained will には `iot:RetainPublish` Policy 権限が必要） → cloud mode のみ `willRetain=false` に固定（local mode は既存 retain=true 維持）
- 2026-05-21（続・**PC 側 DNS 修正完了 / ルータ操作待ち**）: 他ツール調査で `#0048` 真因確定（CoreDNS ACL 仮説は除外）。**PC 側修正実施**：`C:\coredns\Corefile` を `.:53 { bind 172.17.1.100; ... }` に変更（backup: `Corefile.backup-20260521`） / CoreDNS 再起動（PID 24744）/ PC nslookup で内部・外部 FQDN とも解決成功（AWS = 13.158.166.76）。ICS（SharedAccess）停止は管理者権限でも `Cannot open service` で失敗（SYSTEM-only ACL）。**PC 側修正後も ESP32 → 172.17.1.100:53/UDP は依然失敗**（UDP/123 NTP は成功 / UDP/53 DNS のみ失敗）→ **AP/ルータ Aterm 172.17.1.1 の UDP/53 横取り**で確定。**`#0047`/`#0048` 追記更新**。**ユーザー操作待ち**：Aterm 管理画面で DNS フィルタ / DNS お預かり / ワイヤレス分離 OFF。**`020-0021` 新規追加**（ESP32 ローカル broker IP の AP 設定化）。**未完了 14 / 87（約 84%）**。
- 2026-05-21（**セッション終了・`012-0008` 着手前ブロッカー発見**）: **SecretCore 再ビルド成功**（`cargo build`、LocalServer + 旧 SecretCore プロセス停止後に実施）→ ENOENT 解消。**回復スクリプト成功**（`createSensitiveDataJsonForRecovery.cjs IoT_04CEF94EB580 cloud`、k-device fingerprint=`2445cc99fcc4c4e7` 復元 + cloud endpoint 同時付加）。**LittleFS uploadfs + FW upload 成功**（COM4、020-0017A/B 反映、k-device NVS 保持確認）。**新規バグ修正 `#0047`**：cloud mode で `pingBrokerHost` がローカル broker IP（172.17.1.100）を強制使用し AWS TLS が `(-9984) X509` で失敗するバグ発見。`mqtt.cpp:pingBrokerHost` に `mqttBrokerMode=="cloud"` 分岐を追加（local 完全維持）、`connectToMqttBroker` に cloud 専用 DNS 明示設定追加（`primary=172.17.1.100, secondary=8.8.8.8`）。**新規未解決ブロッカー `#0048`**：上記修正後も ESP32 から DNS 解決失敗（`hostByName Failed`、約 8 秒タイムアウト）。PC からは CoreDNS / 8.8.8.8 とも解決可。ESP32 → DNS サーバの経路問題（AP クライアント分離 / CoreDNS ACL / ルータ NAT のいずれか）。**新規タスク 2 件追加**：`020-0019`（DNS 経路調査・`012-0008` 着手前提）/ `020-0020`（mqtt.cpp 修正の試験記録化）。**未完了 13 / 86（約 85%）**。`012-0008` は `020-0019` 解消後に着手。本セッションのコード修正は未コミット、次セッション開始時にコミット要。
- 2026-05-20（続³）: **`020-0016` 新規追加**（NVS 消失原因究明・`012-0008` 着手前に実施）。`012-0008` 試験中に k-device が NVS から消失（`loadKDeviceBytes failed. actual=0`）した件について、`createSensitiveDataJsonForRecovery.cjs` で復旧済みだが根本原因は未特定のため、クラウド試験着手前に調査する方針に変更。調査候補: パーティション CSV アドレス設定誤り / `uploadfs` 副作用 / ファームウェア書込み時 chip erase / NVS 初期化コード。次の着手順: **`020-0016`**（原因究明）→ **`012-0008`**（クラウド統合試験）。未完了 **9 / 82**（約 89%）。
- 2026-05-20（続²・**セッション終了**）: **`012-0006` 完了退避**。**AWS 環境構築完了**：エンドポイント取得 / ESP32 用 X.509 cert 発行（`cert_id=0a38a0c5...` ACTIVE）/ LocalServer 用 X.509 cert 発行（`cert_id=5311eb1a...` ACTIVE）/ `esp32lab-esp32-policy`（Connect `IoT_*` / Pub+Sub `esp32lab/*`）/ `esp32lab-localserver-policy`（Connect `localserver-*` / Sub `esp32lab/*` / Pub `esp32lab/call/*`）/ Thing `esp32s3-dev001` / LittleFS `data/certs/` 配備（`.gitignore` 除外追加）/ `.env` 7 項目追記（`CLOUD_MQTT_ENABLED=false` 既定）/ `.env.example.sample.txt` 構造追記 / `クラウド構築手順書.md §10` に実施記録追加（コマンド・説明・伏せ字・手動再現手順付き）。**TypeScript `tsc --noEmit` OK**。未完了 **8 / 81**（約 90%）。次の本線は **`012-0008`**（クラウド統合試験 7200〜7209）。⚠️ 要手動: ESP32 USB 接続 → `pio run --target uploadfs` → AP モードで `brokerMode=cloud` 設定。
- 2026-05-20（続・セッション継続）: **`012-0005` / `009-0004` / `012-0007` 完了退避**（`todo_old20260520.md`）。**ESP32 クラウドモード実装完了**：`IoT/shared/include/common.h`（`kBrokerMode`/`kCloudEndpoint`）/ `sensitiveDataService.h`（`saveBrokerModeConfig`/`loadBrokerModeConfig` API）/ `sensitiveData.cpp`（NVS 読み書き）/ `mqtt.cpp`（X.509 mutual TLS・LittleFS `/certs/` cert ロード・cloud clientId `IoT_<mac>`）/ `main.cpp`（cloud endpoint override）/ `maintenanceApServer.cpp`（AP UI `brokerMode`/`cloudEndpoint` 追加）。**LocalServer クラウドサブスクライバ実装完了**：`config.ts`（cloud 7 フィールド追加・validation）/ `cloudMqttSubscriber.ts`（新規・X.509 mutual TLS factory / `rejectUnauthorized: true` 維持）/ `mqttGateway.ts`（`clientOverride?: MqttClient` 追加）/ `server.ts`（cloud/local gateway 切替）。**TypeScript `tsc --noEmit` OK**。未完了 **9 / 81**（約 89%）。設計ルール遵守：ローカル技術スタック非破壊 / いつでもローカル復帰可 / `rejectUnauthorized: true` 維持 / raw k-device クラウド送信なし。
- 2026-05-19（続¹⁶・**セッション終了・`v1.0.0-local` 確定**）: **ローカル環境構築完了マイルストーン**。Git タグ **`v1.0.0-local`** / **`local-env-complete-20260519`** 付与。**Cursor + Claude Code 併用方針を明文化**：`.cursorrules` 冒頭と `CLAUDE.md` 冒頭に相互参照ブロックを追加（判断ルール優先順、ツール切替時の手順、最終整合確認日）。**AWS 認証情報を `IoT/LocalServer/.env` に保存**（Git 除外確認済み、`.gitignore:10` で除外、5 観点で漏洩なし確認：① `.env` 自体が追跡外 ② Access Key 文字列が Git 履歴に無い ③ Secret Key 文字列が Git 履歴に無い ④ check-ignore で除外確認 ⑤ git status に出ない）。**変更履歴 195 行を `todo_history_until_20260512.md` へ退避**。**`CLAUDE.md` を新規作成**（152 行、Claude Code・Cursor 共通の引継ぎノート）。次セッション開始は `CLAUDE.md` → `IoT/todo.md` → `IoT/システム構成と通信フロー.md` の順で読む。次の本線は **`012` 章 AWS クラウド連携**（`012-0004` 責務分界表初版から着手）。残件 **15 / 81**（約 81%）。本日累計 **21 件完了 + 1 件不採用 + 3 件 007→020 移管 + 020 章 10 件追加 + 013 章 5 件全件完了 + 新規文書 4 本（システム構成 645 行 / セキュア全般ノウハウ 1196 行 / 設計書実装マッピング表 680 行 / CLAUDE.md 152 行 = 計約 2,700 行）+ 既存文書拡張 + コードヘッダー強化**。
- 2026-05-19（続¹⁵）: **`013-0003` / `013-0004` 完了**で **`013` 章全 5 件完了**。`013-0003`：`ソース概要.md` に §3.1「セキュア技術別の実装場所索引」（10 サブセクション）を新規追加、`設計概要.md` に §4.1「OTA Rollback（旧称 Dual バンク）正式名称」を新規追加。`013-0004`：`SecretCore/src/dpapi.rs` にモジュールヘッダー新規追加（目的・仕組み・設計判断・限界・関連文書）、`key_manager.rs` 主要 4 関数（`get_k_device` / `sign_by_k_device` / `encrypt_by_k_device` / `decrypt_by_k_device`）に詳細 rustdoc 追加（用途・引数・戻り値・注意・ESP32 側対向実装・試験 ID）、`ESP32/src/MQTT/mqtt_network.cpp` / `mqtt_set.cpp` / `mqtt_get.cpp` / `mqtt_call.cpp` / `mqttProtocol.cpp` のヘッダーを目的・主要関数・トピック構造・関連文書込みで充実化。LocalServer TS 主要 8 ファイル + ProductionTool 全 Rust ファイル + SecretCore 他ファイルは確認の上既に充実したヘッダー整備済みと確認。残件 **15 / 81**（約81%）。本日累計 **21 件完了 + 1 件不採用 + 3 件 007→020 移管**。
- 2026-05-19（続¹⁴）: **`013-0002` 完了**。`設計書実装マッピング表.md` を新規作成（全 12 章、横串リファレンス表形式）。全技術項目について設計書 § / 実装ファイル:行 / コード抜粋 / 試験 ID の対応を表形式で網羅：鍵階層・鍵管理 / 暗号アルゴリズム実装 / ESP32 セキュリティ機能 / TLS・MQTT / 初回ペアリング・AP モード / ESP32 タスク構成 / LocalServer-SecretCore 連携 / ProductionTool 段階ランナー / 機能仕様書 F3-XXX 対応 / 試験仕様書 7XXX 対応 / 機密情報サンプル対応 / 関連文書索引。`ドキュメント概要.md` 文書一覧に索引追加。残件 **17 / 81**（約79%）。理由: 再設計・移管・新規参画者の onboarding・設計実装齟齬チェック・試験範囲漏れ確認を、文書横断 grep せず一つの表で参照可能にするため。
- 2026-05-19（続¹³）: **`013-0001` 完了**。`セキュア全般ノウハウ_設計から実装まで.md` を新規作成（行数約 780、全 18 章 + 関連文書索引）。各技術（DPAPI / Flash Encryption / Secure Boot v2 / OTA Rollback / TLS / MQTT 認証 / AES-256-GCM / HMAC / HKDF / 鍵導出 / 初回ペアリング / eFuse 操作）を「目的 → 仕組み → 設計判断 → 実装場所 → 手動実行例 → 関連文書」の統一フォーマットで深掘り。初心者向け基本概念解説（CIA + 認証、暗号種類）、機密情報取扱方針、トラブルシュート観点、設計書↔実装マッピング簡易版を含む。`ドキュメント概要.md` 文書一覧に索引追加。残件 **18 / 81**（約78%）。理由: セキュア技術が複数文書に分散しており、初心者が「なぜこの方式を選んだか」「コードのどこにあるか」を横断的に把握できる文書が不足していたため。
- 2026-05-19（続¹²）: **`013-0005` 完了**。`システム構成と通信フロー.md` を新規作成（行数約 380、初心者向け統合ガイド）。ASCII 構成図 / 各モジュール解説（ESP32 / LocalServer / SecretCore / ProductionTool / Mosquitto / CoreDNS / ルータ / HTTPS OTA 配布）/ 通信経路一覧 5 種 / 主要シーケンス 5 種（初回ペアリング / 通常運用 / OTA / Wi-Fi 更新 / 鍵ローテーション）/ モジュール × 機能マトリクス / 鍵階層 / 用語ミニ辞典 / 関連文書索引を含む。`ドキュメント概要.md` の文書一覧先頭に新規索引を追加。残件 **19 / 81**（約77%）。理由: 既存文書は責務定義や詳細仕様に重点があり、初心者向けの「システム全体像と通信フロー統合ドキュメント」が不足していたため。
- 2026-05-19（続¹¹）: **`013-0005` 新規追加**（システム全体構成・モジュール機能・通信フロー説明文書整備）。対象モジュール: ESP32 / LocalServer / SecretCore / ProductionTool / MQTT ブローカー（Mosquitto）/ CoreDNS / ルータ / HTTPS OTA 配布。各モジュールの役割・責任範囲・機能一覧・使用技術・入出力・依存関係を記述し、MQTT/HTTPS OTA/AP モード HTTP/IPC（Named Pipe）/ローカル CLI の通信フローを図示する。**進行順序を明示**：`011` ローカル確立 → `013` 文書整備 → `012` クラウド連携。**進捗統計テーブルを集約**：同日複数回更新を 1 行に集約、保持は直近 5 日分まで（運用ルール明文化）。`013` 章タスクは 4 件 → 5 件へ。残件 **20 / 81**（約75%、`013-0005` 追加で全タスク数 +1）。
- 2026-05-19（続¹⁰）: **`007` 章 3 件を `020` 章へ移管**（`007-0001` → `020-0011` / `007-0014` → `020-0012` / `007-0009` → `020-0013`）。`007-B` No-Go 継続中、`ProductionTool` 最終形実ランナー実装後に着手のため `020` 章で管理。`007` 章は本作業で**空に**なる。**`013` 章を新設**し、文書整備タスク 4 件（`013-0001` セキュア全般ノウハウ統合文書 / `013-0002` 設計書↔実装マッピング表 / `013-0003` 既存文書充実化 / `013-0004` コードコメント充実化）を起票。`012` クラウド連携着手前の整備フェーズとして実施。Explore 調査で既存セキュア関連文書 12 件は充実しているが統合ノウハウとマッピング表が不足、コードコメントは ESP32 セキュア系良好だが LocalServer/SecretCore は弱いと確認。残件 **20 / 80**（約75%、`013` 章 4 件 + `020` 章 3 件追加で全タスク数 +4）。「Dual バンク」の正式名称は **OTA Rollback / `BOOTLOADER_APP_ROLLBACK_ENABLE` / app0 (0x30000) + app1 (0x430000) dual partition** で確定。
- 2026-05-19（続⁹）: **`009-0010` / `009-0011` / `009-0016` / `009-1020` を退避**（`todo_old20260519.md`）。`009-0011`（認証ロジック公開範囲）と `009-0016`（MQTT 最終運用値・TLS+8883+認証必須）はコード・文書・試験（`7032`/`7033`）の証跡が揃い完了確認。`009-0010`（Phase2 UI/CLI）は秘密処理側完了で退避、LocalServer TS UI/CLI 詳細化は **`020-0009`** へ移管。`009-1020`（EXE 化）は設計仕様完了で退避、LocalServer 用 `.iss` 作成・Node.js 戦略確定・本試験は **`020-0010`** へ移管。残件 **19 / 76**（約75%）。**009 章のクローズ可能タスクは本日全て処理完了**。残るは `009-0004`（012 章移管済み）のみ。本日累計 **16 件完了退避 + 1 件不採用 + 020 章 7 件追加**。
- 2026-05-19（続⁸）: **`010-0001` 完了退避 / `009-0015` `009-0022` `009-0023` `009-0024` を `020` 章へ統合移管**（`todo_old20260519.md`）。`010-0001`（新ルータ 172.17.1.x 運用）は `ネットワーク運用仕様書.md` §2.4/§6 で運用確定済み。`009-0022`+`009-0023` を **`020-0007`**（Wi-Fi 設定更新 state machine 本実装＋誤設定ロールバック試験仕様 7XXX 追加）に統合。`009-0015`+`009-0024` を **`020-0008`**（機密設定 NVS 永続化）に統合。残件 **23 / 74**（約69%、`020-0007` / `020-0008` 追加で全タスク数 +2）。理由: ユーザー方針「020 への移行を優先検討、実装済みならクローズ」に従い、現状の部分実装と完全実装の差分を 020 章で段階的に扱う方針へ整理。
- 2026-05-19（続⁷）: **`009-0021` 完了退避**（`todo_old20260519.md`）。AP 復旧モードのコア機能（WPA2 / 固有パスワード / Web UI / 4 ロール認証）は `009-0018` と同一コードパス（`main.cpp:494-533 startMaintenanceApModeAndHold` / `maintenanceApServer.cpp`）で実装済み。`WiFi設定更新_詳細設計書.md` §9.1 にあったトリガー「Wi-Fi 接続失敗回数しきい値超過による自動 AP 遷移」（フェイルセーフ）は実装側に存在せず、**`020-0006`** を新規追加して将来対応へ移管。`WiFi設定更新_詳細設計書.md` §9.1 にトリガーを現状（起動時長押し / `call/maintenance` / 救済専用ビルド）と将来対応（Wi-Fi 失敗超過自動遷移 → `020-0006`）に分離して明文化。残件 **28 / 72**（約61%、`020-0006` 追加で全タスク数 +1）。理由: タスク文言（括弧内）のコア機能は実装済み、付随トリガーは仕様書のみに存在していたため、将来対応化が妥当と判断。
- 2026-05-19（続⁶）: **`009-0020` 完了退避**（`todo_old20260519.md`、**試験仕様書整備によるクローズ**）。鍵ローテーションは任意運用（`k-user` 変更時のみ発生）。精査の結果、NVS キー定義と pairing 直後の grace 設定のみ実装で、コア処理（runtime カウンタ・state 遷移判定・自動削除・認証拒否切替）が未実装と判明。本実装と加速試験は **`020-0005` を新規追加**して将来対応へ移管。`試験仕様書.md` `7038` を通常運用試験と加速試験（120 倍スケール・2h 完了）の手順・期待結果・試験後復旧（元の鍵へ戻す／設計時間に戻す）込みで詳細化。残件 **29 / 71**（約59%、`020-0005` 追加で全タスク数 +1）。理由: 「実装済みか／既検討済みか／差分実装の要否」精査により、本実装が必要だが任意機能のため将来対応化が妥当と判断、現時点では試験仕様の詳細化までを `009-0020` のクローズ条件とする。
- 2026-05-19（続⁵）: **`009-0018` 完了退避**（`todo_old20260519.md`）。初回ペアリング AP モードは **5 層セキュリティ**（① 物理操作必須 = ボタン3秒長押し ② Wi-Fi WPA2 認証 ③ Web UI ロール認証 ④ 通常通信からの隔離 ⑤ 単発有効/持ち越さない）で実装済みを確認。「短時間有効」は **単発セッション**として `APメンテナンス画面仕様書.md` §1.1 を新設して明文化し、`機能仕様書.md` F3-068 / `要件定義書.md` FR-073 にも参照を追加。N 分無操作の自動切断タイマーは要件としない方針を確定。実装: `main.cpp:494-533 startMaintenanceApModeAndHold` / `main.cpp:539 detectStartupMaintenanceLongPress` / `maintenanceMode.cpp:51-64 consumeMaintenanceModeRequest`。残件 **29 / 70**（約59%）。理由: 「実装済みか／既検討済みか／差分実装の要否」精査により、5 層で攻撃面が十分縮小されていると確認し、強制切断タイマーはペアリング作業中断リスクで運用に悪影響と判断。
- 2026-05-19（続⁴）: **`009-0017` 完了退避**（`todo_old20260519.md`）。ESP32 側 3 箇所で `mbedtls_gcm_auth_decrypt`（GCM tag 検証）付き AES-256-GCM 復号を実装済みを確認: `maintenanceApServer.cpp:881-927` / `MQTT/mqtt.cpp:604-639` / `MQTT/mqttPayloadSecurity.cpp:92-131`。仕様書側も 13 文書で AES-256-GCM 採用を明記済み。残件 **30 / 70**（約57%）。理由: 「実装済みか／既検討済みか／差分実装の要否」精査により、タスク文言と完全一致した実装と仕様書記述を確認できたため。
- 2026-05-19（続³）: **`009-0014` 不採用クローズ**（`todo_old20260519.md`）。実装は `esp32lab/network/+/<deviceNodeName>` 系で安定動作（`mqtt.cpp:3298`）、`MQTTコマンド仕様書.md` §2.1 も `esp32lab/<kind>/<sub>/<name>` で統一済み。Wi-Fi 設定更新だけ `device/<public_id>/wifi/update` へ切替する必然性が薄く、逆に統一性を崩すため不採用。`WiFi設定更新_詳細設計書.md` §5 / `Mosquitto_TLS_IDパスワード設定手順書.md` を整合更新。`mqttProtocol.cpp:62-72` の未使用ヘルパーは参考実装として残置し、`mqttProtocol.h` に「現行運用では未使用」コメント追記。残件 **31 / 70**（約56%）。理由: 「実装済みかどうか／既に検討済みか／差分実装の要否」の精査により、Wi-Fi update topic は実装と MQTT 全体設計の双方で `esp32lab/...` に統一されていると確認できたため。
- 2026-05-19（続²）: **`009-0012` 完了退避**（`todo_old20260519.md`）。`SecretCore/src/key_manager.rs:454-465 get_k_device` で **HMAC-SHA256(key=k-user, message=target_device_name) 直接導出**が既実装。設計仕様書群（`鍵管理および初期セットアップ設計仕様書.md` §3 / §6 / §16 / `LocalServer秘密処理別層仕様書.md` §3.1 / §13 / `IF仕様書.md` / `WiFi設定更新_詳細設計書.md` / `セキュリティ方針_認証情報取扱い.md` / `要件定義書.md` FR-009 / `鍵一覧仕様書.md` / `鍵管理初期セットアップ_実装たたき台.md`）を旧 HKDF 式から HMAC 式へ正本更新（8 文書）。残件 **32 / 70**（約54%）。理由: command 署名（`sign_by_k_device`）／OTA 開始署名（`ota_workflow.rs:344`）／ESP32 側 `mbedtls_md_*` も HMAC-SHA256 で統一されており、`k-device` 導出だけ HKDF にしてもライブラリ・実装が分岐するだけで安全性向上に寄与しないため。
- 2026-05-19（続）: **TPM 前提を全面撤回**し、`鍵管理および初期セットアップ設計仕様書.md` / `LocalServer秘密処理別層仕様書.md` / `鍵管理初期セットアップ_実装たたき台.md` を **Windows DPAPI（OS 暗号化サービス・チップ非依存）** で正本化。これに合わせて **`009-0009`**（Phase1 最小実装）を既実装として **完了退避**（`SecretCore/src/key_manager.rs:84-115 load_or_create_s_random()` + `機能仕様書.md` F3-091）。Mac/Linux 汎用化のため **`020-0004`**（OS 暗号化サービスの汎用化）を新規追加。再開インデックス・直近ゴール・次セッション開始メモを **`009-0010`** 起点へ更新。未完了 **33 / 70**（約53%）。理由: 実装は最初から DPAPI で固定されていたのに、設計文書のみ 2026-03-08 改訂で TPM 前提に置き換わったまま実装が追従しなかった。`todo.md` `009-0008` 着手時に齟齬が判明したため、正本を実装側へ揃え、将来の汎用化を `020` 章へ正しく逃がす。
- 2026-05-19: **`009-0008` 完了退避**（`todo_old20260519.md`）。`鍵管理初期セットアップ_実装たたき台.md` §2 へ Phase1〜4（および Phase5）の **担当（責任モジュール）** と **期限（相対順序）** を明記し、§10 変更履歴へ反映。再開インデックス・直近ゴール・次セッション開始メモを **`009-0009`** 起点へ更新。未完了 **33 / 69**（約52%）。理由: 単独運用のソロ開発前提で空振りの絶対日付を作らず、`009-0009` 以降の着手判断を依存関係のみで行えるようにするため。
- 2026-05-18（続）: **`009-0006` 完了退避**（`todo_old20260518.md`）。`npm run test:7108` 実機実行 OK（commandHistory +1・deviceStatusHistory +2・lastSeenAt 更新）。`試験記録書.md` `7108` を **OK** へ更新。再開インデックス・直近ゴール・次セッション開始メモを **`009-0008`** 以降へ更新。未完了 **34 / 69**（約51%）。理由: 実機テスト完了の証跡を文書全体に整合させるため。
- 2026-05-18: `009-0006` に進捗追記（`scripts/test7108BidirectionalE2E.mjs` 作成・`npm run test:7108` 追加・`試験仕様書.md` `7108` 定義済み）。05-17 未コミット分（40 ファイル）をコミット（`260517-1`）、todo 日付を 05-18 へ更新。理由: 試験スクリプト・仕様の整備完了と実機確認ギャップを文書で明示するため。
- 2026-05-17（続¹²）: **`009-0004` を `012` 章へ移管**し、**`008` 完了項目（008-0010〜008-0015 / 008-0026 / 008-0029 / 008-0038 / 008-0039 / 008-0041 / 008-0043）を `todo_old20260512.md` へ退避**。**`009-0005` 完了**（`機能仕様書.md` §7.1 新設）。`ドキュメント概要.md` を同期。理由: クラウド以外を先に片付ける方針のため。
- 2026-05-17（続¹¹）: **`009-0003` を完了退避**し `todo_old20260517.md` へ追記。残 **36/69**。次本線を **`009-0004`** へ更新。`コマンド仕様書.md` §5.1 / `試験仕様書.md` `7107` / `試験記録書.md` `7107` を整備し、`.env` に `LOCAL_HISTORY_SCHEDULED_EXPORT_ENABLED=true` を追記済み。Node.js v22/v20 バージョン不一致（`ERR_DLOPEN_FAILED`）は `nvm use 22` で解消。理由: Task Scheduler 常駐と定期エクスポートの組込み完了証跡を固定するため。
- 2026-05-17（セッション終了整理）: **セッション交代**に備え、進捗表見出しを **セッション終了時点** とし、**「前回」** を **`009-0002` 直前（38/69）** と明示。**次セッション開始メモ** 先頭に **交代一行**（37/69・次 `009-0003`・退避先・試験記録済）を追加。**今日の正本** 箇条に **`009-0002` 完了** の文書群を追記。理由: 次セッションで索引の読み違えを防ぐため。
- 2026-05-17（続⁹）: **7106 手順 3** を **`npm run test:7106:disable`** で自動化し **`試験記録書.md`** を **OK** に更新したため、同日 **`続⁵`** にあった「**ENABLED=false はスクリプト外・手動**」の記述は事実上 **置換済み** とする。理由: 退避ログと試験正本の矛盾を解消するため。
- 2026-05-17（続⁸）: **`009-0002` を完了退避**し `todo_old20260517.md` へ追記。残 **37/69**。次本線を **`009-0003`** へ更新。`test:7102:live` / `test:7106:disable` を追加実施し `試験記録書.md` を同期。理由: SQLite 履歴の試験仕様 7102/7106 を実機・自動の両方で閉じるため。
- 2026-05-17（続⁷）: **`009-0002`** 進捗に **`npm run verify:bootstrap`**（#0044）と **`test:7102 -- --skipLive`** 回帰を追記。参照順に §4 `verify:bootstrap` を明示。理由: 引き続き SQLite 本線を進めつつ IPC 前提を毎回固定するため。
- 2026-05-17（続⁶）: SecretCore **`secret_core.exe` 未更新時の pipe 不一致**（`問題点記録書.md` **#0044**）へ対し、`secretCoreManager` の **stdout bootstrap ack 待ち**、`waitForSecretCoreReady` 延長、`npm run verify:bootstrap` / `secret-core:build` を追加。理由: LocalServer 起動の自動修復と検証をコマンド化するため。
- 2026-05-17（続⁵）: `009-0002` 試験 **7106** を **`npm run test:7106`**（`scripts/test7106ScheduledHistoryExport.mjs`）で自動検証、`試験記録書.md` を **OK（スクリプト範囲）** へ更新。**ENABLED=false での新規定期出力停止**はスクリプト外・手動と明記。理由: 定期エクスポートの観測証跡を固定するため。
- 2026-05-17（続⁴）: `009-0002` 試験 **7104** を再起動後 **OK** へ更新（`試験記録書.md` / `#0043` 解決済）。理由: 停止・`npm run start`・`test:7102` の証跡を todo と一致させるため。
- 2026-05-17（続³）: `009-0002` 試験 `7102`/`7103`/`7105` を `npm run test:7102` で自動記録、`7104` 環境 404 を `問題点記録書` **#0043** へ追記。理由: 作業後検証の証跡を固定するため。
- 2026-05-17（続²）: **`settings.html`** / **`admin.html`** にローカル履歴 UI（保持日数・DB 削除）を追加。理由: `009-0002` の運用導線を画面から辿れるようにするため。
- 2026-05-17（続）: **`009-0001` を完了退避**し、`todo_old20260517.md` を新設。未完了件数を **`38 / 69`** に更新。再開インデックス・直近ゴール・次セッション開始メモを **`009-0002`** 基準へ繰り上げた。理由: 実機で再登録・`Start-ScheduledTask`・`/api/health` OK まで確認し、`7001` / `#0042` と整合したため。
- 2026-05-17: **`009-0001`** の途中経過として、`install-task-scheduler.ps1` 既定を **`AtLogOn` + `Interactive`** とし、`267011` 切り分けを文書へ反映した（同日完了・`todo_old20260517.md` へ退避）。
- 2026-05-16: `009-0001` の進捗を更新し、Task Scheduler 登録復旧、`run-local-server.ps1` の `node dist/server.js` 直起動化、`schtasks /run` 後も `LastRunTime` 未更新 / `LastTaskResult=267011` が残る現状を反映した。理由: 次セッションで「管理者権限不足」ではなく `S4U` / 実行コンテキスト切り分けから再開できるようにするため。
- 2026-05-16: `020-0003` を完了退避し、Router `WAN 管理` / `UPnP` / `DMZ` / `port forward` を不採用方針として `ネットワーク運用仕様書.md` / `試験仕様書.md` / `試験記録書.md` へ反映した。理由: ルータ公開面の扱いを将来検討のまま残さず、確認方法と手動確認手順まで固定して未完了管理から外せる状態にしたため。
- 2026-05-14: `020-0002` を完了退避し、Mosquitto `acl_file` を導入方針として `ネットワーク運用仕様書.md` / `Mosquitto_TLS_IDパスワード設定手順書.md` / `試験仕様書.md` / `試験記録書.md` へ反映した。理由: device / role 単位 Topic ACL の目的、適用範囲、禁止事項、確認観点を文書化でき、検討タスクとして完了したため。
- 2026-05-14: `008-0037` を完了退避し、`耐量子暗号移行計画書.md` を新規作成、`ドキュメント概要.md` と `セキュリティ方針_認証情報取扱い.md` へ参照を追加した。理由: post-quantum アルゴリズム移行を 2030年以降の将来対応として正本化でき、未完了タスクとして残す必要がなくなったため。
- 2026-05-13: `008-0031` の Phase4 方針を再整理し、macOS 対応は `020` 章へ移管、TPM初期化検知は対象外とした。理由: Phase4 の本線を `runKeyRotationSession()` / `runProductionSecureFlow()` / 障害時再登録フローへ絞り、将来対応と対象外を明確化するため。
- 2026-05-14: `008-0031` を完了退避し、同一PC復旧と別PC/機材交換の障害時再登録フローを `settings.html` / `機能仕様書.md` / `秘密処理別層仕様書.md` まで反映済みの状態へ整理した。理由: Phase4 の本線が UI・API・仕様で揃い、次の試験タスク `008-0033` に進める状態になったため。
- 2026-05-13: `008-0032` を `todo_old20260513.md` へ完了退避し、`7040` / `7041` / `7042` の高リスクワークフロー結合試験が試験仕様・試験記録へ揃っている状態へ整理した。理由: `runKeyRotationSession()`、`runProductionSecureFlow()`、`runSignedOtaCommand()` の受け皿と実施証跡が確認できたため。
- 2026-05-13: `008-0034` を `todo_old20260513.md` へ完了退避し、`SecretCore/Cargo.toml` の release profile に `codegen-units=1` / `lto=true` / `panic=abort` / `strip=symbols` を反映した。理由: 配布時の解析面と余計な巻き戻し面を縮小する方針をコードへ固定したため。
- 2026-05-13: `008-0035` を `todo_old20260513.md` へ完了退避し、`7072` / `7073` の試験仕様・試験記録が IPC 境界固定と保護付き通信の受け皿として成立していることを確認した。理由: IPC 傍受・戻り値保護の観点を試験で追跡できる状態になっていたため。
- 2026-05-13: `008-0030` を `todo_old20260513.md` へ完了退避し、残件数を `47 / 69` へ更新した。理由: `wrapped_secret` / `device_db` / `k-user` の復旧・バックアップ導線が `LocalServer` / `SecretCore` / `settings.html` / 仕様書まで揃い、Phase3 を次段へ進める状態になったため。

[退避済み][2026-05-19] 2026-05-12 以前の変更履歴（194 行）は **`todo_history_until_20260512.md`** へ退避済み。ローカル環境構築完了 v1.0.0-local の節目で実施。詳細経緯は同ファイルおよび各 `todo_oldYYYYMMDD.md` を参照。
