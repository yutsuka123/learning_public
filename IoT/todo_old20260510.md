# IoT TODO 完了済み（退避: 2026-05-10）

[補足] 本書は `todo.md` から退避した完了済みタスクの記録である。  
理由: `todo.md` を未完了タスク中心に保ち、現在の確認対象を必要最小限に絞るため。

## 退避済みタスク

- [x] [008-0007] [2026-03-07][2026-05-10完了][重要] 重要設定変更コマンドに HMAC 署名を付与し、ESP32側で検証する実装を追加する（SecretCore Phase1完了後）。
  - [進捗][2026-05-10] `set/keyDeviceSet` と `set/fileLogSet` に `HMAC-SHA256` + `signature` を付与し、ESP32 側で未署名・改ざん payload を拒否する実装へ更新した。`IoT/LocalServer/scripts/test7052KeyDeviceHmacVerification.mjs` と `npm run test:7052` を追加し、`keyDeviceSet` の改ざん署名 publish 後も既存 `k-device` で `securePing` が継続成功することを確認する試験導線を整備した。
  - [進捗][2026-05-10] `npm run check`（`IoT/LocalServer`）および `python -m platformio run -e esp32s3_secure`（`IoT/ESP32`）でビルド整合を確認済み。
  - [完了][2026-05-10] 実機 `IoT_04CEF94EB580` で `npm run test:7052 -- --targetDeviceName IoT_04CEF94EB580 --observeMilliseconds 5000` を実施し、正規 `keyDeviceSet` 後の `securePing` と改ざん署名 publish 後の `securePing` がともに成功することを確認した。レポート: `IoT/LocalServer/logs/test-reports/test7052-2026-05-10T05-13-34-118Z.json`。
  - [追加確認][2026-05-10] `beta.38` / `beta.39` の2回連続 OTA を実施し、両回とも workflow 完了、版数更新、A/B面切替、`securePing` 成功を確認した。理由: `008-0007` の重要設定 HMAC 実装後も OTA 経路と通常暗号通信にデグレがないことを確認するため。

## 変更履歴
- 2026-05-10: 新規作成。理由: `008-0007` が実装・ビルド・実機 `7052`・追加 OTA 疎通確認まで完了し、`todo.md` から退避できる状態になったため。
