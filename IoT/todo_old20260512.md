# IoT TODO 完了済み（退避: 2026-05-12）

[補足] 本書は `todo.md` から退避した完了済みタスクの記録である。  
理由: `todo.md` を未完了タスク中心に保ち、現在の確認対象を必要最小限に絞るため。

## 退避済みタスク

- [x] [008-0029] [2026-03-09][2026-05-12完了][重要] AP 共通トップ画面へ `shortId`、`public_id`、Production対応/非対応、eFuse、Secure Boot、Flash Encryption、シリアル状態を表示する。
  - [完了理由][2026-05-12 10:55] 起動中の旧面が `app1=0x430000` 由来であることを readback で切り分け、正規 OTA slot の `app1=0x430000` へ最新 FW を反映後、AP モードで再確認した。未認証 `GET /` は `401 unauthorized`、認証済み `GET /` は `AP 共通トップ画面` を返し、`shortId=4EB580`、`public_id=IoT_04CEF94EB580`、Production 対応、eFuse、Secure Boot、Flash Encryption、シリアル状態の表示を確認したため完了扱いとする。
  - [注意][2026-05-12 10:55] `shared/include/common.h` の AP Wi-Fi パスワードがプレースホルダーのまま FW に含まれており、LocalServer 標準プロファイルでは接続できなかった。`008-0029` の表示・認証条件は満たしたが、AP 接続設定の不整合は `問題点記録書.md` `#0040` で別管理する。

- [x] [008-0038] [2026-03-26][2026-05-12完了][重要] AP ロールパスワード「変更可能ON」の実装可否を、工場出荷状態復帰機能とセットで評価する。
  - [完了理由][2026-05-12] `7064` の実機確認で AP admin ロールパスワード変更とデフォルト復元の往復を検証でき、`008-0039` で復帰範囲も確定したため、変更可能ON の恒久運用可否を判断できる状態になった。
  - [実施内容][2026-05-12] `LocalServer/src/server.ts` に `POST /api/admin/ap/password/change` が実装済みであることを確認。`機能仕様書.md` と `要件定義書.md` に変更可能ON の恒久化可否と工場出荷状態復帰の境界を追記。`試験仕様書.md` / `試験記録書.md` に `7062` / `7063` を追加し、実機 `7064` で AP admin ロールパスワード変更・`credentials/rotation`・監査ログ確認・デフォルト復元まで完了した。

- [x] [008-0039] [2026-03-26][2026-05-12完了][重要] 工場出荷状態へ戻す機能（認証情報・設定復帰）の要件定義を作成し、`008-0038` と同時にレビューする。
  - [完了理由][2026-05-12] 要件定義の境界を確定し、`008-0038` との同時レビュー前提も文書へ反映したため完了扱いとした。復帰対象は AP ロール認証・ネットワーク設定・運用フラグに限定し、不可逆 eFuse 巻き戻しは対象外とする。
  - [実施内容][2026-05-12] `機能仕様書.md` と `要件定義書.md` に復帰対象を AP ロール認証・ネットワーク設定・運用フラグへ限定する方針を追記。`試験仕様書.md` / `試験記録書.md` に `7062` / `7063` を追加した。

- [x] [008-0041] [2026-03-27][2026-05-12完了][重要] `ProductionTool` GUI 化の要件定義と方式選定を行い、CLI 骨格との対応表を作成する。
  - [完了] 起動画面、追加認証、対象機確認、工程ロック、dry-run、安全停止、監査ログ確認の GUI 写像を要件定義し、`ProductionTool画面仕様書.md` へ反映した。
  - [厳守] GUI 化は安全条件の緩和を意味しない。

- [x] [008-0043] [2026-03-27][2026-05-12完了][重要] `ProductionTool` GUI に、`製造・セキュア化手順書.md` どおりの工程ロック、対象機再確認、4点証跡導線、`デバイス管理表.md` 連携を実装設計する。
  - [完了] 工程ロック・対象機再確認・4点証跡導線・`デバイス管理表.md` 連携を `ProductionTool画面仕様書.md` に実装設計として反映した。

- [x] [008-0010] [2026-03-07][2026-05-11完了][厳守] SecretCore の IPC ハンドシェイクは、子プロセス spawn + stdin/stdout 渡しを優先実装する（Pipe への外部割り込み排除）。
  - [実施][2026-05-11] `LocalServer/src/secretCoreManager.ts` と `SecretCore/src/main.rs` を更新し、起動時ブートストラップを stdin/stdout へ寄せた。業務 IPC 本体は named pipe + AES-256-GCM のまま維持。`cargo check` と `tsc --noEmit src/secretCoreManager.ts` が通過した。

- [x] [008-0011] [2026-03-07][2026-05-11完了][厳守] IPC の nonce は必ず `crypto.randomBytes(16)` で生成する。`Math.random()` の使用を絶対に禁止する。
  - [実施][2026-05-11] `LocalServer/src/secretCoreIpcClient.ts` と `SecretCore/src/main.rs` を更新し、IPC nonce を 16 byte の暗号学的乱数へ変更した。AES-GCM 実IV は nonce から導出する形へ調整した。`cargo check` と `tsc --noEmit src/secretCoreIpcClient.ts` が通過した。

- [x] [008-0012] [2026-03-08][2026-05-11完了][厳守] `wrapped_secret` 以外の秘密（`S_random` / `k-user` / `k-device`）をファイル保存しないことを実装レビューで確認する。
  - [確認][2026-05-11] `SecretCore/src/key_manager.rs` を確認し、`S_random` は `wrapped_secret.bin` として DPAPI 保護、`k-user` は `wrapped_k_user.bin` と `k_user_backup.enc.json` として暗号化保存、`k-device` はファイル保存なしで都度導出であることを確認した。`SecretCore/src/main.rs` / `pairing_workflow.rs` / `mqtt_transport.rs` も横断確認し、平文の `S_random` / `k-user` / `k-device` を書き出す経路は見つからなかった。

- [x] [008-0013] [2026-03-08][2026-05-11完了][重要] `wrapped_secret` 破損検知（version / createdAt / 整合性チェック）を実装する。
  - [実施][2026-05-11] `SecretCore/src/key_manager.rs` に `wrapped_secret` envelope を追加し、`version` / `createdAt` / SHA-256 整合性チェック付きで読み込むようにした。旧 raw DPAPI blob は警告付きで互換読み込みを残した。`cargo check` が通過した。

- [x] [008-0014] [2026-03-07][2026-05-11完了][重要] TS 層で IPC から受け取った署名/暗号 payload は即座に送信し、変数保持時間を最短にする。
  - [実施][2026-05-11] `LocalServer/src/server.ts` の `secure-ping` と不正署名試験 2 箇所を更新し、受け取った暗号 payload / 署名 payload を IIFE の最短スコープで扱うようにした。IDE linter 確認で新規エラーなし。

- [x] [008-0015] [2026-03-07][2026-05-11完了][重要] 監査ログは Phase1 からパーミッション制限（0600）+ OS ログ（Windows イベントログ / macOS syslog）への二重書き込みを実装する。
  - [実施][2026-05-11] `LocalServer/src/server.ts` の `appendSecurityAuditLog()` を更新し、監査ファイルを 0600 相当に寄せたうえで、Windows は eventcreate、macOS は logger を使って OS ログへも書き込むようにした。`security-audit.log` のディレクトリ作成時にも 0700 相当を試行するようにした。`tsc --noEmit` が通過した。

- [x] [008-0026] [2026-03-09][2026-05-11完了][重要] `SecretCore` に `runSignedOtaCommand()` を実装する。
  - [確認][2026-05-11] `SecretCore/src/ota_workflow.rs` と `SecretCore/src/main.rs` で、署名付き OTA の開始・送達確認・検証結果確認・完了判定までを Rust 側へ集約していることを確認した。`LocalServer/src/secretCoreFacade.ts` の `runSignedOtaCommand()` と `/api/workflows/signed-ota/start` からの呼び出し経路を確認した。`cargo check` と `tsc --noEmit secretCoreFacade.ts` が通過した。

## 変更履歴
- 2026-05-17: 新規作成。理由: `008-0010`〜`008-0015`（2026-05-11 完了）、`008-0026`（2026-05-11 完了）、`008-0029` / `008-0038` / `008-0039` / `008-0041` / `008-0043`（2026-05-12 完了）を `todo.md` から退避するため。
