# 設計議事録 2026-05-17

[重要] 本書は `IoT` プロジェクトの日次引継ぎ記録である。  
理由: セッションを跨いでも判断の一次情報を欠落させないため。

## 実施内容（要約）
- **`009-0001`（Task Scheduler 自動起動）** のブロッカー `LastTaskResult=267011`（`0x41303` = `SCHED_S_TASK_HAS_NOT_RUN`）を、**`AtStartup` + `S4U` ではユーザー文脈の実プロセスが起動しない**系の切り分けとして整理した。
- `IoT/LocalServer/scripts/install-task-scheduler.ps1` の**既定**を **`AtLogOn` + `Interactive`** とし、**`AtStartup` + `S4U`** は **`-TriggerMode AtStartup`** 明示時のみとした。
- `invoke-install-task-scheduler-admin.ps1`（UAC 昇格）、`install-task-scheduler.ps1` の管理者チェックを追加した。
- `install-local-server.ps1` に **`-TaskSchedulerTriggerMode`** を追加し、`コマンド仕様書.md` §5、`LocalServer/README.md` §6、`問題点記録書.md` `#0042`、`試験記録書.md` `7001`、`IoT/todo.md` を同期更新した。

## 続報（2026-05-17 以降・009-0002）
- **`009-0002` 第1段**: `LocalServer` に `better-sqlite3` 基盤の `localHistoryStore.ts` を追加。`deviceStatusHistory` / `commandHistory` / `exportHistory` スキーマを `DB仕様書.md` 4章に一致させ、`mqttGateway` から status 受信および `publishCommand` 成功時に記録。保持日数・DBパス・パージ間隔は `LOCAL_HISTORY_*` 環境変数。`コマンド仕様書.md` §4、`DB仕様書.md` 変更履歴、`todo.md` を同期した。
- **`009-0002` 第3段（プロセス内定期）**: `LOCAL_HISTORY_SCHEDULED_EXPORT_ENABLED` / `INTERVAL_MS` / `FILTER_JSON`。`runLocalHistoryExportToDisk` を手動 API と共有。`exportHistory.triggerType=scheduled`、`executedBy=LocalServer/scheduledExport`。無効 JSON は定期のみ開始せずログに理由を出す。
- **`009-0002` (4) 試験番号固定**: `試験仕様書.md` に **`7102`〜`7106`**（SQLite 起動・`status` 記録／コマンド記録・マスク／手動エクスポート＋監査／purge／定期エクスポート）、`試験記録書.md` に同一番号の **未実施テンプレ** を追加。`009-0002` 全体クローズは **実機で合否を埋めた後**。

## 完了（2026-05-17 終了時点）
- 実機でタスクを **`MSFT_TaskLogOnTrigger` / `LogonType=Interactive`** に再登録し、`Start-ScheduledTask` 後 **`LastRunTime` 更新**、**`GET /api/health` OK** を確認した。
- **`009-0001`** を **`todo_old20260517.md` へ退避**（未完了 **`38 / 69`**）。**`#0042` は解決済**とした。

## 関連ID
- `009-0001`、`7001`、`問題点記録書` `#0042`、`todo_old20260517.md`
- `009-0002`、`7102`〜`7106`（SQLite 履歴試験）

## 変更理由
- [2026-05-17] ログオン前に LocalServer が必須でない運用では、**ログオン後起動が信頼性とセキュリティのバランスで有利**であり、再現できた「受理されるが実行されない」状態をコード既定で避けるため。
