# IoT TODO 退避版（2026-04-26）

[重要] 本書は完了済みタスクの退避先とする。  
理由: `todo.md` を未完了中心に保ち、再開判断を簡潔にするため。

## 退避済みタスク

- [完了][2026-04-26] [008-0027] `SecretCore` に `createPairingBundle` 内部 helper を実装する。
  - [完了理由] `SecretCore/src/pairing_workflow.rs` に `create_pairing_bundle()` が実装され、`runPairingSession()` 配下で AP 到達 precheck、`/api/pairing/session`、`/api/pairing/bundle-summary`、`/api/pairing/transport-session`、`/api/pairing/transport-handshake`、`/api/pairing/secure-bundle` までの流れが接続済みであるため。
  - [備考] `runPairingSession()` 以外から内部 helper を呼ばない境界は維持する。

- [完了][2026-04-26] [008-0028] AP 共通トップ画面の共通パスワード認証と、ProductionTool 追加認証を実装する。
  - [完了理由] `ESP32/src/maintenanceApServer.cpp` に `/api/auth/login` の共通認証入口があり、`IoT/ProductionTool/src/auth_screen_state.rs` と `IoT/ProductionTool/src/main.rs` に `PT-002` 追加認証と安全停止までの導線が実装済みで、`7086` / `7087` の試験記録でも正常系・異常系とも確認できているため。
  - [備考] 共通トップ画面と `ProductionTool` の認証境界は分離を維持する。

- [完了][2026-04-26] [008-0025] `SecretCore` に `runProductionSecureFlow()` を実装する。
  - [完了理由] `LocalServer/src/types.ts` / `server.ts` の `precheckSnapshot` 入力型と検証、`SecretCore/src/generic_workflow.rs` の dry-run workflow 骨格、`ESP32/src/maintenanceApServer.cpp` の `POST /api/production/precheck` / `GET /api/production/state` 追加により、`runProductionSecureFlow()` の事前条件確認・段階実行・読戻し検証・隔離判定の導線が実装済みで、`7041` の `FULL_COMPLETED` 証跡でも確認できているため。
  - [備考] `dryRun=false` の本実行は現行フェーズの対象外で、`7041` は dry-run と precheck の確認に限定する。

- [完了][2026-04-26] [008-0047] `DEV-001` 問題を即参照できるクイックリファレンスを整備し、全関連文書から参照可能にする。
  - [完了理由] `DEV-001問題点クイックリファレンス.md` が既に存在し、`問題点記録書.md`、`デバイス管理表.md`、`設計概要.md`、`ドキュメント概要.md` から参照導線も張られているため、即参照用途の文書整備と索引反映が揃っている。
  - [備考] 原本は `問題点記録書.md` / `試験記録書.md`、要約はクイックリファレンスで分担する。

- [完了][2026-04-26] [008-0046] `DEV-002` の「0から設定して動作した実績」を定常運用手順として固定する。
  - [完了理由] `生デバイス初期設定手順書.md` に `3.1` / `5.4` の正式ルートが固定され、`試験記録書.md` の `7101` 当日テンプレートと合わせて `LocalServer` -> `SecretCore` -> `ProductionTool` -> 対象機接続の順序ぶれを防ぐ運用手順が揃っているため。
  - [備考] `COM/MAC/deviceName`、`k-device` 発行、`AP5/STA5/OTA2`、`7101` 判定、停止条件は同一導線で参照する。

- [完了][2026-04-26] [008-0048] ROM使用率 99.6%（`#0026`）の対策として、16MB前提パーティション再設計案を作成し、`DEV-002` で安全検証する。
  - [完了理由] 最新の `esp32s3_secure_final` 実測で `Flash: 31.1% (used 1305185 bytes from 4194304 bytes)` を確認でき、`DEV-002` 側でも候補案Aの実適用と `setup completed` / `Wi-Fi STA` / `NTP` / `MQTT` の復帰が記録済みのため、ROM 逼迫の対策と再検証は完了扱いにできる。
  - [備考] `DEV-001` へは適用せず、`#0026` の最新値は別途監視対象として残す。

- [完了][2026-04-26] [008-0049] パーティション変更前提で fallback 可能な減量策（ログレベル・機能フラグ）を整理し、案B/A/C判定条件へ反映する。
  - [完了理由] `ROM容量分析資料.md` で `CORE_DEBUG_LEVEL`、`secure_final` 専用ログ抑制、`maintenanceApServer.cpp` / `MQTT/mqtt.cpp` の分割候補を整理済みで、`最終セキュア化方式選定メモ.md` に ROM マージン条件と案A/C 判定条件も反映済みのため。
  - [備考] 可視性が必要な rollback 前後ログや原因コードは削除対象にしない。
