# 設計議事録 2026-05-18

[重要] 本書は `IoT` プロジェクトの日次引継ぎ記録である。  
理由: セッションを跨いでも判断の一次情報を欠落させないため。

## 実施内容（要約）

### 1. 05-17 未コミット分のコミット（260517-1）
- 前セッション（2026-05-17）で蓄積していた 40 ファイルを `260517-1` としてコミット。
  - `localHistoryStore.ts`（better-sqlite3 ベースの SQLite 履歴ストア）
  - `historyRedaction.ts`（マスク処理）、`historyTimestamps.ts`（タイムスタンプ正規化）
  - `localHistoryRetentionDays.ts`（保持日数パージ）
  - `test7102`〜`test7107` 各スクリプト（SQLite 履歴・定期エクスポート試験）
  - `todo_old20260512.md`、`todo_old20260517.md` 退避ファイル
  - `設計議事録20260517.md` 等

### 2. 009-0006: LocalServer ↔ デバイス双方向テスト（7108 OK）
- `scripts/test7108BidirectionalE2E.mjs` を新規作成し `npm run test:7108` を追加。
- `試験仕様書.md` に `7108`（双方向テスト手順）を追加。
- 実機テスト実行: デバイス `IoT_04CEF94EB580`（Node.js v22.14.0）で OK。
  - `commandHistory` +1（43 → 44）
  - `deviceStatusHistory` +2（690 → 692）
  - `lastSeenAt` が `2026-05-18T02:59:31.610183500+00:00` → `02:59:56.852289600+00:00` へ更新
- `試験記録書.md` `7108` を OK 記録。
- `009-0006` を `todo_old20260518.md` へ退避。

### 3. バグ修正: test7108 token 設定誤り
- `loginAsAdmin()` は `{ token, config }` オブジェクトを返すが、スクリプト内で `const token = await loginAsAdmin()` と誤代入していた。
- `const { token } = await loginAsAdmin()` へ修正し、401 エラーを解消した。

### 4. ドキュメント整理
- `todo.md`: 再開インデックス・直近ゴール・次セッション開始メモを `009-0008` 以降へ更新。古い終了メモ（05-16 / 05-14 由来）を圧縮・削除。
- `試験記録書.md`: `7108` を OK として実測値を記録。
- `todo_old20260518.md`: `009-0006` 退避ファイル新規作成。
- `設計議事録20260518.md`（本書）: 新規作成。

## 完了タスク
- **`009-0006`** → `todo_old20260518.md`（`試験記録書.md` `7108` OK）

## 次セッションの着手点
- **`009-0008`**: `鍵管理初期セットアップ_実装たたき台.md` の Phase1〜4 に担当・期限を割り当てる。
- **`009-0009`**: Phase1（`S_random` 生成 + TPM ラップ + `wrapped_secret` 保存）の最小実装先行着手（SecretCore Phase1 と統合）。
- 詳細は `todo.md` の **`#### 009-B. 秘密管理・ペアリング`** 節を参照。

## 関連ID
- `009-0006`、`7108`、`todo_old20260518.md`
- `009-0008`、`009-0009`（次セッション）

## 技術的注意事項（次セッション引継ぎ）
- **Node.js v22 必須**: `better-sqlite3` は v22 でビルド済み（NODE_MODULE_VERSION=127）。v20 では `ERR_DLOPEN_FAILED`。
- **`007-B` No-Go 継続**: `ProductionTool` 最終形実ランナー未完了につき不可逆コマンド禁止。
- **worktree ブランチ** `claude/frosty-bardeen-4d6740` は conflicting commits を持つため **マージ不要**。main ブランチが正本（最新 commit: `260518-2`）。セッション終了後に `git worktree remove --force` + `git branch -D` で削除可能。

## 変更理由
- 09-0006 実機テスト完了（commandHistory / deviceStatusHistory の双方向増分を確認）により、LocalServer ↔ ESP32 間の基本通信品質を証跡として固定できたため。
- 次セッション開始メモの旧情報が 3 セッション分蓄積していたため圧縮し、次の着手点（`009-0008`）へ索引を統一した。
