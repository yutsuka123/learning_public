---
marp: true
---

# IoT TODO 変更履歴（〜2026-05-12 までの古い履歴）

[重要] 本ファイルは `todo.md` の変更履歴のうち、**2026-05-12 までの古いエントリ**を退避したもの。  
理由: `todo.md` の変更履歴が肥大化したため、直近 1 週間（05-13 以降）のみを `todo.md` に残し、それ以前を本ファイルへ移管した（ローカル環境構築完了 v1.0.0-local の節目で実施）。

[読み方] 各 `todo_oldYYYYMMDD.md` には**完了退避された個別タスクの詳細**が記録されている。本書はそれらを縦串でつなぐ**作業進捗の日次サマリ**である。

---

## 退避エントリ（2026-03-03 〜 2026-05-12、計 194 行）

- 2026-05-10: `008-0007` を `todo_old20260510.md` へ完了退避し、残件数を `56 / 69` へ更新した。理由: 重要設定変更 command HMAC 実装、`7052=OK`、`beta.38` / `beta.39` の2回連続 OTA と `securePing` 確認まで完了したため。
- 2026-05-10: `008-0007` の進捗を追記。`set/keyDeviceSet` / `set/fileLogSet` に `HMAC-SHA256` + `signature` を付与し、ESP32 側で改ざん拒否する実装、`test:7052` 実機試験導線、`npm run check` / `python -m platformio run -e esp32s3_secure` のビルド確認結果を反映した。理由: 重要設定変更 command の未署名受理を防ぎつつ、次セッションで実機証跡取得へ直行できるようにするため。
- 2026-05-07: `008-0008` / `008-0009` を完了退避対象として整理し、残件数を `57 / 69` へ更新した。理由: `7042` 試験仕様具体化、不正署名拒否確認、`beta.36` / `beta.37` の 2 回連続 OTA 実測まで完了したため。
- 2026-05-07: `008-0006` を完了退避し、`otaStart` に `HMAC-SHA256` + `signature` を付与、ESP32 側で署名不一致時に開始拒否する実装を追加した。理由: 高リスク OTA 開始 command の改ざん検知を code / 文書 / ビルド確認まで一貫させるため。
- 2026-05-07: `acl_file` とルータ `WAN 管理` / `UPnP` / `DMZ` / `port forward` を **`020-0002` / `020-0003`** へ移管し、`008-0005` を完了退避扱いへ整理した。理由: ユーザー指示により、これらを現行の本線完了条件ではなく将来検討課題として扱うため。
- 2026-05-07: `008-0005` の進捗を更新。`7033` を **OK** まで進め、`LocalServer` 管理者ログインの **3回連続失敗で5分ロック**を実装・検証し、`#0032` を解決済みに更新した。理由: ユーザー指示に基づく固定値をコード・試験・問題記録へ一貫反映するため。
- 2026-05-07: `008-0005` の進捗を追記。`7032=OK`、`7033=NG`、`7034=NG` を `試験記録書.md` に反映し、未解決要因を `問題点記録書.md` `#0032` / `#0033` へ追加した。理由: 文書整備だけでなく実測に基づく残課題を次作業へ直結できるようにするため。
- 2026-05-07: `008-0004` を完了として `todo_old20260507.md` へ退避し、再開インデックス、直近ゴール・現在地サマリ、次セッション開始メモを `008-0005` 起点へ更新。理由: Mosquitto / `LocalServer` / ルータ / Firewall の設定具体化を `ネットワーク運用仕様書.md` 7.4.4 と `試験仕様書.md` `7032`〜`7034` へ反映し、次作業を試験実施へ絞るため。
- 2026-05-02: 再開インデックス、直近ゴール・現在地サマリ、次セッション開始メモを更新。理由: `008-0001`〜`008-0003` 完了後の状態、残件数 `60 / 67`、次着手点 `008-0004`、および `007-B` No-Go 継続を次セッションで即参照できるようにするため。
- 2026-05-02: 冒頭の「タスク進捗統計」を `現在` / `前回` / `前々回` の3行構成へ整理し、`04-28` の `67 / 67` を追加。理由: セッション再開時に直近3回分の残件推移を冒頭だけで追えるようにするため。
- 2026-04-29 12:27: `007-B` 方針を「鍵新規発行なし」「不可逆本体は `ProductionTool` 最終形実ランナー実装・検証後」へ変更。`007-0001` の残条件を `PC-004` 照合と `ProductionTool` 最終形実装へ更新。理由: 手動既存手順ではなく `ProductionTool` を実行責任主体として収束させるため。
- 2026-04-30（続⁴）: **`007`** 章へ **`IoT/007_本番1台目_実行順チェックリスト.md`** をリンク（`007-B` **[正]**）。**`007-0001`/`007-0009`** の段階表記を **`FE→NVS(HMAC)→SB→封鎖`** に **`本番セキュア化出荷準備試験計画書.md` §6.4** へ整合。**`最終セキュア化方式選定メモ.md` §1** を **`§1.1`** と整合。理由: 不可逆の開始条件説明ぶれ・旧誤順序を解消するため。**[修正 2026-04-29]** 現行案Aでは NVS Encryption / HMAC eFuse 投入を実施しないため、現行順は **`FE→NVS暗号化なし確認→SB→封鎖`** とする。
- 2026-05-02: **`007-0014`** に `ProductionTool` 最小実ランナーの進捗を追記。`PRODUCTION_TOOL_ENABLE_IRREVERSIBLE_COMMAND_RUNNER` + CLI `run` + `PRODUCTION_TOOL_ALLOW_IRREVERSIBLE_EXECUTION` の二重ゲートで、証跡ディレクトリ作成、コマンド展開、stdout/stderr 保存、手動確認待ち停止、安全ゲート停止を `ProductionTool` 管理下へ移した。理由: 完全版の `precheck -> stage_execute -> readback -> evidence -> stability` には未達でも、責任主体を `ProductionTool` 側へ段階的に寄せるため。
- 2026-05-02: **`007-0014`** に `PT-005z precheck` の進捗を追記。Windows 対応、証跡保存先、`python`、鍵ファイルパスの存在確認を `runner-precheck-summary.json` と CLI へ出すようにした。理由: `ProductionTool` が `stage_execute` 前の停止条件を自前で説明できるようにし、手動手順との差分を減らすため。
- 2026-05-02: **`007-0014`** に `runner-execution-summary.json` の進捗を追記。段階ごとの `before/after readback`、失敗有無、停止理由を `ProductionTool` 側で要約できるようにした。理由: `readback -> evidence` を段階単位で追跡し、4点証跡の整理に近づけるため。
- 2026-05-02: **`007-0014`** に `runner-stability-summary.json` の進捗を追記。各段階の `OTA x2 / AP 10分 / STA 10分`、証跡パス、次段可否を `ProductionTool` 側で回収できるようにした。理由: `stability` も `ProductionTool` で一貫して扱う方針に合わせるため。
- 2026-05-02: **`007-0014`** に `PC-004` 照合の進捗を追記。`ProductionTool` が鍵ID・署名素材ID・ロット・作業指示番号を CLI で照合し、`pc004-check-summary.json` を保存し、未照合なら `PT-005z` を安全停止させるようにした。理由: `PC-004` も `ProductionTool` の責任で一貫して扱う方針へ寄せるため。
- 2026-05-02: **`007-0014`** に `PT-005z precheck` の `PC-004` 期待値確認を追記。`PRODUCTION_TOOL_FE_KEY_ID` / `PRODUCTION_TOOL_SB_SIGNING_MATERIAL_ID` / `PRODUCTION_TOOL_TARGET_LOT_ID` の設定漏れを `runner-precheck-summary.json` と CLI へ出すようにした。理由: 不可逆直前ではなく precheck 段階で準備不足を止めるため。
- 2026-05-02: **`007-0014`** に `PT-005z` の段階指定実行を追記。`run 1`〜`run 4` で対象段階だけを実行し、`runner-execution-summary.json` に `selectedStageNumber` を残すようにした。理由: 本番時に段階単位で証跡と安定性確認を閉じられるようにするため。
- 2026-04-30（続³）: **`008-0016`** 完了（ユーザー向け明記のみ）。MQTT／OTA と信頼ネットワークの注意として **`取扱説明書.md` §2.2**。退避 **`todo_old20260430.md`**。統計 **62/67**。理由: `SecretCore Phase1`/ HMAC〜**`008-0006`** 群が全経路締まるまでの運用ガードレールを取説で明示するため（実装残は `008-0006`〜`009`）。
- 2026-04-30（続²）: **`007-0013`**（出荷後の更新経路）をユーザー決定に基づき **`機能仕様書.md` §1.5** とし、`todo_old20260430.md` へ退避。統計 **63/67**。理由: OTA 主経路／`ProductionTool` 基準／シリアルをオプションで注意を促す方針を仕様へ固定するため。
- 2026-04-30（続）: **`007-0011`** を **`試験記録書.md` `7099`** の [更新][2026-04-28] / 文書 [進捗][2026-04-29] を正として **`todo_old20260430.md`** へ退避。統計 **64/67**。理由: 準備系IDを一段進め、`007-A` と `007-B` の間の判断材料のみを残すため。
- 2026-04-30: **`007-0004`** / **`007-0006`** を **`試験記録書.md` `7095`** への試験機（`DEV-001`）4点証跡の **集約転記・go/no-go 整合**で完了として `todo_old20260430.md` へ退避。統計は **65/67**。理由: ID をクローズ可能な監査経路へ落としたため。**本番 `DEV-002`** は **`7095` 空白ブロック複写後**に同一形式で記入すること。
- 2026-04-29: `007-0004` / `007-0006` に対応する **4点証跡・保存先・go/no-go** を `試験記録書.md` **`7095`** **[固定]** とし、`ドキュメント概要.md` と本書の再開インデックス・007入口索引・007-A の `007-0004`/`007-0006` 行へ反映した。**実段階の証跡採取または `007-B` は未着手**とする。理由: ユーザー依頼（`todo.md` を一つずつ片付ける）における準備項目のクローズ。
- 2026-04-28: `DEV-002` で `007-0011` / `7099` の当日再観測結果を反映し、`todo.md` 冒頭の再開インデックス・直近サマリ・引継ぎ文・次セッション用コピペを現状態へ更新した。残件数は `67 / 67` で再計測し、実機確認は進んだが未完了ID数は変動なしと整理した。理由: セッションを跨いでも次回の開始順と不可逆進入条件を誤らないようにするため。
- 2026-04-29（追記）: `k-iot` の将来切替条件を明確化し、「**仕組みが変わる場合のみ追加試験**、変わらない鍵差し替えは**最小確認**」を追記。理由: 個人開発段階では試験鍵継続で進め、製品化時の判断基準だけを先に固定するため。
- 2026-04-29: **`k-iot` は当面試験用と本番で同一**とする方針を反映。`004-0006` / `004-0007` を `todo_old20260429.md` へ退避、**`020-0001`**（将来の本番専用分離）を追加。「007入口」を「鍵・手順索引」へ改称し、§10.2.5 / §10.2.5a の役割を整理。理由: ユーザー方針に合わせ、当面の本線から本番専用鍵生成を外すため。
- 2026-04-28（続）: 「### 007入口・本番鍵：作業順索引（一式）」と **`007-A.0`** を追加し、`004-A` に `006-0011` 後も前倒し禁止＋**§10.2.5a** 参照を追記。`007-0009` に `7099` / 試験記録書・5分ゲート索引を追記。理由: `004-0006`/`004-0007` と `007-A`・`7099` を1経路で辿れるようにするため。
- 2026-04-28: `005-0008` を `IoT/todo_old20260428.md` へ完了退避。`ProductionTool画面仕様書.md` に **9.4.6**（`試験記録書.md` 突合表）を追加。`todo.md` の直近実行順・章別整理・`005` 章・`005〜010` 索引・`eFuse適用チェックリスト.md` の G-006/R-004 を整合。理由: 試験機証跡に基づき AV-001〜006 の相互参照を確定し、本線を `007` 入口へ進められるようにするため。
- 2026-04-28: `005-0008` の設計を `ProductionTool画面仕様書.md` 9.4（AV-001〜006）へ初版化し、`機能仕様書.md` / `本番セキュア化出荷準備試験計画書.md` / `eFuse適用チェックリスト.md` / `ドキュメント概要.md` / `設計概要.md` と突合。`todo.md` の `005-0008` に進捗・残作業を追記。再開インデックスを「設計固定済み・実測でタスク完了」へ更新。理由: `006-0011` 完了後の優先タスクであったため。
- 2026-04-27: `005`〜`011` の再点検を実施し、追加で退避できる項目がないことを確認した。理由: `006-0011` の再開条件が未達で、`005-0008` / `007` 章以降の残件は試験記録と現地確認の更新待ちのため。
- 2026-04-24: 直近の章次ゴールと文書スコープを `006-0010` / `006-0011` の再開条件へ戻し、`011` 章は完了退避済みとして整理した。理由: `011-0001`〜`011-0007` の退避完了後に、現時点の未完了タスクへ視線を戻し、再開順を `006` に固定するため。
- 2026-04-26: `020` へ逃がしていたクラウド第4段階の未完了タスクを `012` 章へ戻し、`020` はクラウド以外の将来対応に限定した。理由: ユーザー方針としてクラウド化は `012` で扱うのが正であり、章の責務分担をそこに揃えるため。
- 2026-04-25: `006-0002` を完了退避対象へ移し、`006` 章の着手順を `006-0008` 起点へ更新した。理由: `003-0024` / `7085` / `004` / `005` / `009` の準備ゲートが揃い、`006-0002` は本線の未完了管理から外せる状態と判断したため。
- 2026-04-25: `006-0005` を完了退避対象へ移し、`mainTaskStackSize` を 15KB へ整理した。理由: `7023` の stack margin 証跡と `mainTask` の拡張後運用値が整合し、15KB のきりのよい余裕として閉じられると判断したため。
- 2026-04-25: `006-0007` を完了退避対象へ移し、`mainTaskStackSize` と `7023` の stack margin 証跡を仕様書へ固定した。理由: `eFuse` 開始前の余裕確認値が 15KB 運用で十分であることを文書・試験・実装で揃えられたため。
- 2026-04-25: `006-0008` を完了退避対象へ移し、`stackMarginOk` / `heapMarginOk` の NG 時は `failed` で停止する実装を確認した。理由: スタック不足・ヒープ逼迫時の開始拒否が `SecretCore` の `production precheck` により既に担保されているため。
- 2026-04-23: `007-0012`、`008-0050`〜`008-0052`、`008-0042` を退避し、`011-0002` の前段整理を反映した。理由: 完了済み・証跡完了済みの細部は `todo_old20260423.md` に寄せ、`todo.md` には今後の再開判断に効く節目だけを残すため。
- 2026-04-18: クラウド関連を **`011-0008`〜`011-0011` から `012-0001`〜`012-0005` へ移管**し、`011` 章は **`011-0007` で打ち止め**と明記。冒頭の「クラウド化ロードマップ」独立節を廃止し、内容は `### 012` へ統合。再開インデックス・直近ゴールに「直近は011まで」を追記。移管記録を `todo_old20260418.md` に退避。理由: ユーザー方針（クラウド化は012以降、直近は011まで片付ける）に章構造を合わせるため。
- 2026-04-17: 冒頭に「再開インデックス」「直近ゴール・現在地サマリ（案A主軸）」「沿革要約」を追加し、`セッション引継ぎ` を 2026-04-17 正として集約（旧コピペ3種は git 履歴参照へ）。「クラウド化ロードマップ（第4段階）」を新設し、`011-0010` / `011-0011` を追加（後に 2026-04-18 に012へ移管）。「直近実行順序」に `008-0044` を `007-B` より前のステップとして明示。理由: 久々の再開でも着手順がぶれないようにし、クラウド化までの道筋を同一ファイルで追えるようにするため。
- 2026-04-07c: `008-0044` に PlatformIO の Arduino 3.x 対応観点（official PlatformIO ではなく community fork `pioarduino` が案内されていること）を追記し、案A PoC の次アクションを具体化した。理由: 「Arduino v3.x に上げれば案Bが成立するのか」を PlatformIO platform 基盤の観点も含めて評価し、次に何を案Aで確認すべきかをセッション跨ぎでも誤らないようにするため。
- 2026-04-07: `008-0045`（案B PoC）を **No-Go 確定** として完了化。`008-0044` に案A 移行の次アクションを追記。セッション引継ぎを案A 評価開始へ更新。理由: 現行 Arduino-ESP32 v2.0.17（IDF v4.4.7）には `nvs_sec_provider` API が存在せず、Arduino v3.x メジャー更新は Breaking Change が広範で `007-C` 計画を大幅に阻害するため、案B を不採用として確定し、次の判定段階（案A）へ即移行できるようにするため。
- 2026-04-06: セッション引継ぎ欄と次セッション用コピペ指示を更新し、`DEV-002` の現状を「MQTT/OTA/ProductionTool は回復済み、残ブロッカーは `AP` 到達のみ」として固定した。理由: セッションを跨いでも次回の開始点と停止条件を誤らず、`7041` 回復を最優先で再開できるようにするため。
- 2026-04-05: `008-0051` を `otadata=0x2000` 維持、`coredump=0x20000` 採用の確定内容へ更新。理由: `DEV-002` へ切り替えるパーティションサイズを `todo.md` 側でも固定し、次の書込み手順へ進めるようにするため。
- 2026-04-05: `008-0050` / `008-0051` に保全整理結果（`DEV-002` ベースライン、論理復元方針、raw NVS の位置づけ、候補案Aの実CSVオフセット）を追記。理由: セッションを跨いでも「どこまで整理済みか」と「次に何を実機で確認すべきか」を `todo.md` だけで再開判断できるようにするため。
- 2026-04-05: `008-0050`〜`008-0052` と、`DEV-002` 保全 -> パーティション最適化 -> 最適化後確認 の次セッション順序、ならびに新しいコピペ指示を追加。理由: セッションを跨いでも `DEV-002` の安全保全とパーティション最適化から再開できるようにするため。
- 2026-04-05: `008-0048` / `008-0049` を追加。理由: ROM 使用率 99.6% の差し止め要因（`#0026`）を次セッションで即再開できるよう、パーティション再設計と減量策の二段構えでタスク化するため。
- 2026-04-05: `008-0044`〜`008-0047` を追加。理由: ユーザー方針（案B優先、失敗時案A、最終的に案C）を次セッションでも同一判断で再開できるよう、分岐条件・証跡文書・停止条件を `todo.md` へ固定するため。
- 2026-04-04: セッション引継ぎ・直近ゴール・次セッション用コピペ指示を更新し、`7101` 回復、NIC 未接続が停止主因だったこと、次回は `7099` / `007-0005` / `PC-012`〜`PC-014` 整理を優先する方針を反映。理由: 本日の終了時点で「再調査不要な論点」と「次回最初にやること」を `todo.md` だけで判断できるようにするため。
- 2026-04-04: `008-0040` に build 方針の実装進捗を追記。`secureNvsInit` を `CONFIG_NVS_ENCRYPTION` マクロ依存から外し、`esp32s3_secure`（fallback許可）/`esp32s3_secure_final`（fallback禁止）を追加した。理由: 開発継続性を保ちつつ、最終版の secure 固定ビルドをコードとコマンドの両面で明示できるようにするため。
- 2026-04-05: `008-0042`（シリアル最終運用方針）に `DEV-001` 実測の禁止事項（`write_flash --force` を本番復旧手段にしない）を追記し、重複していた GUI タスク番号を `008-0043` へ整理。理由: 番号衝突を解消しつつ、「ログは残す/危険読書きは封じる」方針を実機知見で固定するため。
- 2026-03-27: `007-0012` にセッション再開後の再現確認結果を追記。固定 Pipe `SecretCore` 起動、AP 再接続、`7041:pipeline:strict` / `7087:normal` / `7087` / `7083(iterations=2)` の再確認を反映した。理由: `007-B` 進入前ゲートが現セッションでも成立することと、`ProductionTool.exe` 実行時の固定 Pipe 前提を `todo.md` 側にも明記するため。
- 2026-03-26: `006-0014` の段階1(FE)後停止問題を **解決済** へ更新。対策A（NVS HMAC鍵投入 + secureNvsInit FW修正 + OTA beta.29）実施後、AP HTTP 全4ロール認証成功・`BLOCKED_AP_REACHABILITY` 解消を確認。問題点記録書 #0020 解決済。理由: 段階2(NVS)の不可逆処理と FW 安全化が完了し、AP モード復旧を実機証跡付きで確認したため。
- 2026-03-26: `007-0013`（出荷後更新経路を OTA 主経路へ一本化する最終条件確認）を追加。理由: 「OTAのみ」方針の採否を、不可逆設定と復旧性の両面で判断できるようにするため。
- 2026-03-26: `006-0013`（ProductionTool 2モード運用）と `006-0014`（三段階実行ゲート）を追加。理由: 本番同等の運用手順で `006-0011` を安全側に実施するため。
- 2026-03-26: `006-0012`（鍵運用の最終方式 A/B 判定）を追加。理由: `006-0011` の不可逆リハーサル実行前に、鍵生成主体・鍵粒度・復旧責任を固定し、実行判断を文書ベースで行えるようにするため。
- 2026-03-25: 「不可逆以外を先行完了」の方針に合わせ、`7019` / `7020` / `7021` / `7023` の実施結果を反映し、`006` 章へ非不可逆優先の進捗メモを追記。理由: 実書込み前に完了可能な検証を先に閉じ、残課題を明確化するため。
- 2026-03-25: 1台目/2台目で共通適用する製品版セキュリティプロファイル `production-hmac-v1` を固定し、`007-0008` へ `.env` / `.env.example.sample.txt` / `sensitiveData.h` 同期タスクを追加。理由: 1台目失敗時でも設定判断をやり直さず、2台目試験を即開始できるようにするため。
- 2026-03-25: NVS Encryption HMAC方式を1台目から採用する方針へ変更し、`007-0008` を追加。公式再照合により `nvs_keys` 追加案を取り下げ、`nvs` 現状維持 + sdkconfig/FW実装 + eFuse書込み + 事前バックアップ運用へ更新。詳細は `本番セキュア化出荷準備試験計画書.md` 5.6c 参照。
- 2026-03-25: `007-0009` を追加し、各段階の完了判定前に安定性確認セット（OTA2回 + AP10分 + 通常10分）を必須化。理由: セキュア化過程での `abort`・再起動ループ・通信異常を早期検知し、文鎮化リスクを段階的に抑えるため。
- 2026-03-25: `Wi-Fi_lab` 再接続と `LocalServer` 起動復旧後に `7041:pipeline:strict` を再実行し `FULL_COMPLETED` を確認。`7085` を進入可へ更新し、次アクションを `006-0002` 以降の段階実行へ切替した。理由: フェーズG進行可否を実証跡で確定するため。
- 2026-03-25: AP未到達の再切り分け結果（`Wi-Fi_lab` 未接続、`ping 192.168.4.1` timeout、`BLOCKED_AP_REACHABILITY` 継続）を引継ぎへ追記し、次回開始コマンドへ `test:7041:diag` を追加。理由: 7085 再判定の前提を実装状態と接続状態で切り分け、再開判断の誤りを防ぐため。
- 2026-03-24: `7085` 再判定結果（`BLOCKED_AP_REACHABILITY`）を受け、`006-0002`〜`007-0005` の停止継続を追記。あわせて `test0091022FactoryTerminalFinalValidation.ps1` を 3フェーズ明示実行へ統一した。理由: 不可逆処理ゲート未通過時の誤着手防止と、工場端末最終検証コマンドの再現性確保のため。
- 2026-03-24: セッション終了引継ぎとして「次セッション開始手順（7085再判定優先）」を追記。理由: 別セッション再開時に不可逆処理へ誤着手しないよう、開始順を固定するため。
- 2026-03-24: 次ゴールを `007` 章（フェーズG）へ更新し、不可逆処理を段階ゲート方式（読戻しOK時のみ次段、NG時即停止）で進める実行順序へ再整理した。あわせて `007-0003`〜`007-0005` を追加し、1台目完了前の複数台展開禁止を明記した。理由: `Secure Boot` / `ROM暗号化` / `Flash Encryption` / `eFuse` を安全側で段階実行するため。
- 2026-03-24: `todo.md` に残っていた完了タスク（`003/004/005/006/008/009` 章の `[x]`）を追加で `todo_old20260324.md` へ退避し、`todo.md` を未完了タスク中心へ整理した。理由: 現在の作業対象を明確化し、運用ルール「未完了のみ管理」を厳守するため。
- 2026-03-24: `009-1022` を完了として `todo.md` から退避し、`todo_old20260324.md` を追加。理由: 工場専用端末向け最終再検証（`test0091022` 3フェーズ OK）まで完了し、未完了タスク一覧を整理するため。
- 2026-03-24: `003-0012` を完了化。内蔵 `Wi-Fi` 経路で `npm run test:7041:pipeline:strict` の `FULL_COMPLETED` を確認し、同時に `AP_ROLE_MFG_*` 読込不足修正、`mainTaskStackSize` 拡張、`7041` 状態履歴判定の誤検知修正を反映した。理由: AP 到達性・認証・stack margin・判定ロジックの連鎖ブロッカーを解消し、strict 完了条件を実機証跡付きで満たしたため。
- 2026-03-24: `003-0012` の再判定として `npm run test:7041:pipeline:strict` を再実行し、`BLOCKED_AP_REACHABILITY` 継続を確認。あわせて `test7041Pipeline.mjs` の gate 前レポート保存を反映し、strict 実行時の「pipeline レポート未生成」偽失敗を解消した。理由: 未達理由を AP 到達性へ限定し、次の実機確認条件を明確化するため。
- 2026-03-24: `003-0017` を完了化。`uninstall-legacy-localsoft.ps1` と `test7092LegacyLocalSoftUninstallSmoke.ps1` を追加し、旧 `LocalSoft` / `LocalSoftForCloud` 資産の安全消去（上書き消去 + 削除 + 監査ログ）を 7092 で確認した。理由: LocalSoft 廃止時の機密残留を防ぐ実装・証跡を揃えるため。
- 2026-03-24: TODO 整理として `003-0016`（インストーラ/アンインストーラ整備）と `009-1021`（EXE化）を完了化し、受入環境での最終確認のみを `009-1022` へ分離した。あわせて「手動確認待ち」と直近実行順序の主軸を新IDへ更新。理由: 実装完了タスクと受入検証タスクを分け、未完了一覧の解像度を上げるため。
- 2026-03-24: `ProductionTool/.env.example.sample.txt` を追加し、`README.md`・`install-production-tool.ps1`・`ProductionTool.iss` へ反映。理由: 共有時に実値を配布しない運用を実装と文書で一致させるため。
- 2026-03-22: `003-0016` / `009-1021` の進捗として、`ProductionTool` 用の安全消去付きアンインストーラ骨格（`uninstall-production-tool.ps1`）と Inno Setup 骨格（`ProductionTool.iss`、`build-production-tool-installer.ps1`）を追加。理由: フェーズE完了後の前倒し実装として、工場端末配布と安全消去の実装入口をコード化するため。
- 2026-03-22: `003-0024` を完了化し、`7085` の親定義・必須条件・差し止め対象を `todo.md` と `試験仕様書.md` で固定した。あわせて `006-0001` として `eFuse適用チェックリスト.md` を新規作成し、総合進入判定ゲートと作業単位チェックリストを分離した。理由: フェーズF準備完了の境界を機械判定できるようにしつつ、不可逆処理の本実行開始とは明確に切り分けるため。
- 2026-03-22: `008-0025` の進捗として、ESP32 AP に `production/precheck` / `production/state` を追加し、`SecretCore` から AP ログイン経由で firmware/MAC/heap/stack の実測値を取得して dry-run precheck へ反映できるようにした。`npm run check`、`cargo check`、`python -m platformio run -e esp32s3_secure` を通過。理由: 不可逆処理をまだ解放しないまま、ESP32 側専用受け口と実測 precheck 経路をフェーズDの範囲で固定するため。
- 2026-03-22: `005-0006` / `005-0007` を完了化。`ProductionTool画面仕様書.md` 第9節と `機能仕様書.md` に、ProductionTool 専用セキュア化フロー、事前チェック8項目、読戻し検証、隔離判定を反映した。あわせて `005-0008` は実機実施を伴う有効確認として保留明記し、直近実行順序と `003-0024` の前提条件を `005-0001`〜`005-0007` ベースへ更新。理由: フェーズDでは実行前準備までを完了とし、実行後確認はフェーズFへ分離するため。
- 2026-03-22: 直近ゴールと実行順序を再整理し、「フェーズDクローズ完了」「フェーズFは準備完了まで」「本番用 `k-iot` 実キー生成は実行直前のみ」を明記。あわせて `004-0006` / `004-0007` を前倒ししない運用方針を追加。理由: eFuse / Secure Boot / ROM暗号化の不可逆処理を急がず、準備完了と実行開始の境界を明確にするため。
- 2026-03-21: `003-0004` を完了化。`ESP32/src/ota.cpp` の CA 解決・ホスト名検証付き TLS 接続実装と、`試験記録書.md` 7011 の `Certificate verified.` / OTA成功実機証跡を突合し、タスク条件「証明書検証有効状態で OTA 成功」を満たすことを確認した。理由: `7085` 差し止め要因から 003-0004 を外せる状態かを、実装・試験・文書の3点で整合させるため。
- 2026-03-21: `009-0019` を完了化。実機 AP `AP-esp32lab-F0D0F94EB580` 接続下で `npm run test:7082 -- --targetDeviceId "CF-FV_1"` を実施し、workflow `completed/OK` と `GET /api/pairing/state` の `state=applied`、`savedCurrentKeyVersion=v1` を確認。理由: 「コード実装済み」ではなく「実機証跡付き完了」へ進め、`7085` 再判定の材料を確定するため。
- 2026-03-16: `009-0019` に実機確認の「次アクション」（FW 書込み、`.env` の AP 到達設定、`npm run test:7082` 証跡、`pairing/state` の `applied` 記録）を追記。理由: 実装済みと試験完了の切り分けを運用手順レベルで固定するため。
- 2026-03-19: `004-0001`〜`004-0005` を完了化。`コマンド仕様書.md` へ試験用 `k-iot` 鍵生成手順、`OTA仕様書.md` へ `k-iot-ota-sign` 別鍵運用方針、`試験仕様書.md` へ 004-0004 検証条件（7016/7017/7018）を反映。理由: 006 進入前の鍵手順を再現可能な形で固定するため。
- 2026-03-19: `003-0016` / `009-1021` の配布前提を `インストーラEXE化設計仕様書.md` 3.4 へ確定（対象端末、インストール先、監査ログ先、混在インストール禁止、安全消去監査ログ）。これに伴い `004-0010` を完了化。理由: 006 着手前ゲートの未達要因を解消するため。
- 2026-03-19: `005-0002` を完了化。AP 共通トップ画面と ProductionTool 画面の分離仕様（eFuse 導線混在禁止、追加認証境界）を3文書で整合し、AP 側の実装タスクは `008-0028` へ切り分け。あわせて `004-0010` 進捗文を更新。理由: 005 フェーズEの設計完了範囲を確定し、残課題を 003-0016/009-1021 に集約するため。
- 2026-03-19: `005-0001` と `005-0004` を完了化。あわせて `APメンテナンス画面仕様書.md` へ AP/ProductionTool 分離方針（eFuse導線混在禁止）を反映し、`005-0002` の進捗を更新。理由: 005 フェーズE の設計完了範囲を確定し、未達を AP 側最終実装へ限定するため。
- 2026-03-24: `005-0005` の方針を入力簡素化仕様へ更新。`PT-002` は操作者ID/作業指示番号を任意入力化し、空の作業指示番号は `YYYYMMDDHHMMSS` 自動採番へ変更。`PT-003` は確認文言/シリアル/MAC再入力を廃止して一覧行選択方式へ変更。理由: 過剰入力を減らし、MAC未指定でも対象機選択できる運用へ合わせるため。
- 2026-03-19: `005-0003` を完了化。メーカーモード専用パスワード管理方式（責任者、配布、更新、失効、緊急停止、開発値から本番値移行条件）を `セキュリティ方針_認証情報取扱い.md` と `ProductionTool画面仕様書.md` に反映。理由: 006 着手前の高権限認証管理方式を確定するため。
- 2026-03-19: `004-0010` の必須条件へ三者整合レビュー（LocalServer/ProductionTool/ESP32）と再判定順序（7088->004-0010->7085）を追加し、`005-0001`〜`005-0005` に完了条件・証跡・未達項目を追記。理由: 005 完了判定の曖昧さを解消し、006 着手前の安全ゲートを機械的に判断できるようにするため。
- 2026-03-16: `009-0019` の進捗へ encrypted bundle 本体送達（`/api/pairing/secure-bundle`）、AES-256-GCM 復号、NVS 保存、workflow `completed(OK)` 遷移を追記。理由: 006 着手前の重大残課題だった「本体送達/復号/NVS保存」の到達状況を `todo.md` 上で明示し、実機試験での最終証跡反映ポイントを固定するため。
- 2026-03-19: `004-0010` の進捗へ `8001` 異常系試験 OK を反映。理由: 7088 判定と `todo.md` の進捗表示を一致させるため。
- 2026-03-18: `004-0009` 進捗: PT-002/PT-003/PT-005 骨格（`auth_screen_state.rs`・`device_select_screen_state.rs`・`dry_run_screen_state.rs`）を追加し、`main.rs` をウィザードフロー対応に拡張して `cargo check` 通過。理由: スモーク試験実施に向けて追加認証・対象機確認・dry-run 導線の実装を先行完了させるため。
- 2026-03-18: `004-0008` を完了化し、`SecretCore` reachable 到達確認結果（7086 OK）を記録。理由: `SecretCore` 起動状態で `ProductionTool cargo run` を実行し、`secretCorePreflightResult=reachable`・監査ログ出力を確認したため。
- 2026-03-16: フェーズC/D を「`LocalServer` 基本安定化 -> `ProductionTool` 基本安定化 -> 耐久/過負荷 -> 鍵/不可逆導線 -> 総合ゲート -> eFuse」の順へ再整理し、`004-0008`〜`004-0010` を追加。理由: `LocalServer` と `ProductionTool` の基本機能を先に実装・試験・安定化させてから鍵手順や不可逆処理へ進む方針を `todo.md` 全体へ反映するため。
- 2026-03-16: `todo.md` に「直近実行順序（2026-03-16再整理）」と `003-0020`〜`003-0024` の安定化/耐久/過負荷/総合進入判定ゲートを追加し、`005`→`004`→`006`→`007` の着手順を明示。理由: `LocalServer` とセキュア機能を先に固め、OTA耐久試験と長時間過負荷試験を通してから `ProductionTool` / eFuse へ進む方針を明文化するため。
- 2026-03-15: `003-0014` を完了化。理由: ランダム Pipe 名、32byte IPC セッション鍵、AES-256-GCM、requestId リプレイ拒否を実装し、E2E 動作確認まで完了したため。
- 2026-03-15: `003-0012` を [部分完了] へ見直し、`003-0013` と `003-0019` を完了化。理由: `SecretCoreFacade` による境界IF固定と k-user バックアップ実装は完了した一方、通信まで Rust 主導の最終要件は未達のため。
- 2026-03-15: `003-0019`（SecretCore `export_k_user` / `import_k_user_backup`）を追加。理由: `k-user` のパスワード暗号化バックアップ受け渡しを Rust 側で完結させるフェーズC課題を明示するため。
- 2026-03-13: Rust/TS 併用切り替え可能、`ProductionTool`（Rust共通部再利用・GUI=Windowsアプリ・`LocalServer` とは別ソフト）、002-1000番台・009-1000番台セクション、新ルータ `172.17.1.x` 運用反映。理由: セキュア部移行方針、`ProductionTool` 設計、タスク順序、ネットワーク現状を整合させるため。
- 2026-03-13: `LocalSoft` 廃止・`LocalServer` 一体化方針を反映し、`003-0012`/`003-0013`/`003-0017` と `009-0002`〜`009-0006` を統合前提へ更新。理由: 保存・同期・運用機能の実装先を単一化し、以後のタスクを新方針と一致させるため。
- 2026-03-13: `7048/7049` を完了化。理由: 7048 最終620回異常0件に加え、7049 で `pruneFileLogsIfNeeded removed` の実機証跡を取得し、試験後に本番しきい値FWへ復元したため。
- 2026-03-13: 7048/7049 に最終620回再試験の進捗を追記。理由: バッチflush版FWで異常0件を再確認し、#0008 の再現性評価を最新化するため。
- 2026-03-13: 7048/7049 にバッチflush・廃止判断基準・完全停止フラグの進捗を追記。理由: 実装と運用判断基準を同時に管理して、段階廃止の判断を可能にするため。
- 2026-03-13: 7048/7049 に寿命試算の進捗を追記。理由: フラッシュ書換回数への懸念に対し、レート上限と10年試算を定量で管理するため。
- 2026-03-13: 7048/7049 の進捗を再々々々更新。理由: 未作成ファイルへの `exists/open(r)` 回避で `open(): ... no permits for creation` が 0 件化し、#0008 の根因解釈を更新する必要があるため。
- 2026-03-13: 7048/7049 の進捗を再々々更新。理由: 単一ファイルログ書込みタスク化で stack canary が停止し、負荷起因の安定化効果と `open()` 失敗残課題を分離して管理する必要があるため。
- 2026-03-13: 7048/7049 の進捗を再々更新。理由: `fileLogTrace` 追加FWで `LittleFS Already Mounted` + `open(): ... no permits for creation` + `timeServerTask` stack canary を採取し、#0008 の調査観点（`i2cTask` 固定ではない）を反映する必要があるため。
- 2026-03-13: 7048/7049 の進捗を再更新。理由: 最小1行固定（one-shot）再試験で `open(): /littlefs/logs/...` は再現したが `i2cTask` stack canary は未再現となり、#0008 の切り分け結果を反映する必要があるため。
- 2026-03-13: 7048/7049 の進捗を更新。理由: 実機でローテーション処理入口の実行を確認した一方、`i2cTask` stack canary 再発が発生し、先に #0008 の解消が必要になったため。
- 2026-03-13: `002-0026` を完了化。理由: 7055 実機で APモード表示（LED/LCD）が仕様どおりであることを確認できたため。
- 2026-03-13: `002-0025` を完了化。理由: 7056 実機で APモード中の MQTT 接続開始抑止を確認し、混在起動禁止の受入条件を満たしたため。
- 2026-03-13: `002-0014` を完了化。理由: 7028 実機で誤設定による接続不能再現から AP 経由復旧、再起動後 `online/mqtt` 復帰と `status` 応答再開まで確認できたため。
- 2026-03-13: `002-0009` の実機試験を完了化。理由: LittleFS 初期化対策FW反映後に 7063 を再試験し、`/images`・`/certs` の局所更新と権限制御の合格を確認できたため。
- 2026-03-13: `002-0009` の進捗を更新。理由: 7063 で `file write failed` の主因を LittleFS 初期化不足と特定し、`ensureLittleFsReadyForAp` 実装まで完了したため（再書込み確認は `COM9` 解放待ち）。
- 2026-03-13: `002-0017`/`002-0018` を完了化。理由: 方針A（画像更新は `fileSync` 標準、ZIP経路は [新規利用禁止] の限定保守）を正式確定し、試験ID運用を 7061/7062 主体へ整理したため。
- 2026-03-13: `002-0009` の残課題メモを更新。理由: 7063 実機で RBAC/パス拒否は成立したが `upsert=file write failed` を再現し、FW再反映後の再試験が必要になったため。
- 2026-03-13: `002-0010` の追加修正を実施。理由: 実機で `wifiTask` の stack canary を再現したため、`log.cpp` を軽量化して `writeFileLogLine` 呼出しを `fileLogEnabled` 有効時のみに制限したため。
- 2026-03-13: `002-0012` を完了化。理由: 1KBローテーションと 30日/100KB しきい値による古いログ削除を `log.cpp` へ実装し、ビルド確認まで完了したため。
- 2026-03-13: `002-0011` を完了化。理由: ファイルログ保存先を同期状態で分岐するパス規約（同期済み: `/logs/<年>/<月日>/<年月日時分秒ms-連番>.log`、未同期: `/logs/<起動回数>/00001.log`）を実装し、起動回数NVS管理とビルド確認まで完了したため。
- 2026-03-13: `002-0010` を完了化。理由: ファイルログをシリアルログ経路から分離し、`setFileLogEnabled()` による既定OFF/必要時ONの制御を実装してビルド確認できたため。
- 2026-03-13: `002-0009` を完了化。理由: APモード向けに `/images` `/certs` 局所更新API（ESP32側 `/api/files/upsert` `/api/files/delete`、LocalServer側 `/api/admin/ap/files/upsert` `/api/admin/ap/files/delete`）を実装し、ビルド整合を確認できたため。
- 2026-03-13: `002-0013` を完了化。理由: APメンテナンスモードのネットワーク設定 API（ESP32側 `/api/settings/network`、LocalServer側 `/api/admin/ap/settings` `/api/admin/ap/configure`）が実装済みで、保存層連携まで接続されたため。
- 2026-03-13: `002-0015` を完了化。理由: `mqttUrlName` / `serverUrlName` / `otaUrlName` / `timeServerUrlName` / `keyDevice` が ESP32保存層・AP設定API・LocalServer中継APIまで追従済みで、ESP32/LocalServer のビルド成功で整合確認できたため。
- 2026-03-13: `002-0005` を完了化。理由: `fileSync targetArea=certs` で `/certs/mqtt-ca.pem` を更新/削除した際、NVS メタ（`issueNo`/`setAt`/`sha256`/`active`）を LittleFS 実体へ追従同期する実装を追加し、ビルド確認まで完了したため。
- 2026-03-13: フェーズBの進捗整理を更新。理由: 実装/試験済みの `002-0006` `002-0007` `002-0008` `002-0016` を完了チェック化し、未完了タスクを「直近残課題」として再整理してフェーズB完了までの見通しを明確化するため。
- 2026-03-13: `002-0018` の進捗を更新。理由: 画像 `fileSync` を MQTT送信可能サイズに追従する動的分割へ改修し、`chunkSizeBytes=102400` 指定でも `totalChunks=76` で `completed` を確認できたため。
- 2026-03-13: `002-0018` の進捗を再更新。理由: 100KB分割方針で正常系（7061）と異常系（7062）を実施し、固定小チャンク運用（`chunkSizeBytes=1024`）を暫定適用したため（後続で動的分割へ改修済み）。
- 2026-03-13: `002-0018` の進捗を更新。理由: `testImageFileSyncE2E.mjs` により ZIP非使用の画像更新E2Eを実施し、`4096byte` ダミー画像で分割転送（`splitFiles=1` / `totalChunks=7` / `completed`）を確認できたため。
- 2026-03-13: `002-0017` の進捗を更新。理由: `imagePackageApply` 失敗ログを再精査した結果、`IPKG_HTTP_CONNECT_FAILED` の一部が `hostByName(): DNS Failed` 起因であることを確認し、`mqtt.cpp` へ DNS失敗時のTLS検証維持IPフォールバック実装を追加したため（FW反映は `COM9` 解放待ち）。
- 2026-03-13: `002-0017` の進捗を更新。理由: `connection refused` の内訳を再調査した結果、実体は `X509` 検証失敗および `mqttTlsCertSha256` メタ不整合（`cert sha256 mismatch`）であることを確認し、再開手順を「COM9で復旧FW反映」優先へ改めたため。
- 2026-03-13: `002-0017` の進捗を更新。理由: OTA反映後の実機試験で `IPKG_HTTP_CONNECT_FAILED`（`reason=connection refused`）を確認し、阻害点が `SHA-256` ではなく HTTPS 配布口 `:4443` の接続拒否であることを確定できたため。
- 2026-03-12: `002-0017` の進捗を更新。理由: `imagePackageApply` 実機再試験で `connect(): failed connect` を確認し、失敗原因を遠隔で判別できるよう `IPKG_HTTP_CONNECT_FAILED` など詳細エラーコード通知を `mqtt.cpp` へ追加したため。
- 2026-03-12: `003-0012`〜`003-0018` を追加。理由: `LocalServer` / `ProductionTool` / `LocalSoft` の Rust主導セキュア化、UI/セキュア分離、境界IPC保護、インストーラ/アンインストーラ整備（機密安全消去必須）を直近フェーズCの実装タスクとして固定するため。
- 2026-03-12: `002-0017` / `002-0018` の進捗を更新。理由: `imagePackageApply` 実送信で `planning=OK` / `downloading=NG(IPKG_DOWNLOAD_OR_HASH_FAILED)` まで確認し、OTA HTTPS 側 `/assets` 公開対応・`mqttTask` スタック最適化（12288 + 分割静的バッファ）を反映、翌日再開手順を固定するため。
- 2026-03-12: `002-0017` の進捗を再更新。理由: `imagePackageApply` の ZIP 展開本体（安全パス検証・`overwrite` 制御・`tmp` + `rename`）を実装し、`IPKG_ZIP_NOT_IMPLEMENTED` 状態を解消したため（現時点は ZIP `stored` 方式のみ対応）。
- 2026-03-12: `002-0017` の進捗を更新。理由: `imagePackageApply` の受信・署名検証・HTTPS取得・SHA-256検証・スコープ検証・進捗通知（`imagePackageStatus`）を追加し、残課題を ZIP 安全展開に限定したため。
- 2026-03-12: `002-0018` の進捗を更新。理由: `試験仕様書.md` に `imagePackageApply` 正常系/異常系（7059/7060）を追加し、`試験記録書.md` に受け皿を反映して文書整合を取ったため。
- 2026-03-12: `002-0006` へ `fileSyncStatus` 通知進捗を追記。理由: `phase/result/errorCode` 通知を追加し、`FSYNC_SIG_INVALID` など失敗理由を運用側で判別できるようにしたため。
- 2026-03-12: `002-0016` の進捗を更新。理由: `fileSyncPlan` / `fileSyncChunk` / `fileSyncCommit` に `signature` 必須化と `HMAC-SHA256(k-device)` 検証を実装し、未署名/検証失敗時の実行拒否をコードで担保したため。
- 2026-03-12: `002-0006` / `002-0007` / `002-0008` の進捗を更新。理由: MQTT第一経路の `fileSync*` 受信実装、`SHA-256` 検証反映、`tmp` + `rename` 適用を実装反映し、残課題を試験・署名検証へ絞るため。
- 2026-03-12: `002-0005` の進捗へ OTA HTTPS 証明書経路の `/certs` 主経路化を追記。理由: MQTT/HTTPS の証明書実体を FW 埋め込み優先から `LittleFS:/certs` 優先へ段階移行した実装状態を明確化するため。
- 2026-03-12: `002-0005` の進捗を更新。理由: MQTT CA 証明書の `/certs` 実体保存（`tmp`+`rename`）と NVS メタ（`SHA-256` / active）管理の実装を反映し、残りを HTTPS(OTA) 系証明書へ明確化するため。
- 2026-03-12: `002-0015` / `009-0015` の実装進捗を更新（完了判定待ち）。理由: `network` 追加5項目の保存読書き実装と、`sensitiveDataService` の NVS 主保存 + 旧 `LittleFS:/sensitiveData.json` 移行実装を反映し、残りは実機試験結果の反映へ絞るため。
- 2026-03-12: 完了済み `001-0001`〜`001-0004`、`003-0005`、`003-0006`、`003-0007`、`003-0009`、`003-0010`、`009-0007`、`009-0025` を `todo_old20260312.md` へ退避し、`todo.md` から削除。理由: [厳守] ルールに従い「未完了タスクのみ管理」へ整合させるため。
- 2026-03-12: `009-0025` を完了へ更新。理由: strict 実機試験（7057/7058）合格に加えて、LocalServer/ESP32 の既定値 strict 化と、`コマンド仕様書.md` への切替/ロールバック手順固定まで完了したため。
- 2026-03-12: `009-0025` に `7057/7058 OK` の進捗を追記。理由: strict モード試験の完了状態と、残作業（本番既定値化・手順書固定）を分離して追跡するため。
- 2026-03-12: `009-0025`（MQTT payload暗号化 `strict` 本番移行タスク）を追加。理由: `compat` 実装完了後の次アクションを未完了タスクとして固定し、平文拒否条件を明確化するため。
- 2026-03-12: `009-0007` を完了へ更新し、`k-device` による MQTT payload全文暗号化モジュール化（LocalServer/ESP32）と実機往復試験（7053 OK）を反映。理由: 本日実施した暗号化実装と試験結果を未完了管理へ同期するため。
- 2026-03-12: `003-0005` / `003-0006` / `003-0007` / `003-0009` / `003-0010` を完了へ更新。理由: 管理者認証、鍵発行、AP自動設定、統合一覧、`call/maintenance` の実装済み項目を未完了一覧から整理するため。
- 2026-03-11: `003-0005`〜`003-0011`（LocalServer管理者認証、鍵発行、AP自動設定、AP Web遷移、統合一覧、`call/maintenance`、eFuse分離）を追加。理由: 管理画面の新要件を実装タスクへ分解するため。
- 2026-03-13: `002-0028` の方針を「初期パスワード固定運用禁止」から「初期/各種パスワードの変更推奨」へ更新。理由: 現場運用での例外対応余地を残しつつ、初回変更手順・変更API・監査ログでセキュリティ担保を維持するため。
- 2026-03-26: `008-0028` に「`007` クローズまでは `sensitiveData.h` 定義値を固定運用」「変更可能ONは `008` で工場出荷状態復帰機能とセット検討」を追記し、`008-0038` / `008-0039` を追加。理由: 不可逆フェーズ直前で認証運用を安定化しつつ、将来拡張の判断軸を先に固定するため。
- 2026-03-26: `006-0014` の3段目進捗を「着手済み（precheck停止）」へ更新。理由: `dryRun=false` workflow は起動したが AP login timeout で `stage_execute` 未到達だったため、不可逆未実施を明確化して誤進行を防ぐため。
- 2026-03-13: `002-0028` の実装進捗を更新（APロールPW変更API、LocalServer管理者PW変更API、例外記録API、監査ログ実装、`securityState.json` 永続化）。理由: フェーズBクローズへ向けて「変更API + 監査ログ」の実装証跡を todo 管理へ反映するため。
- 2026-03-13: `002-0020`〜`002-0022` の実装進捗を更新（`/api/admin/ap/batch/start`・`/api/admin/ap/batch/:batchId` 追加、順次処理化、`publicId/configVersion` を含む再起動後 `status` 最終判定実装）。理由: AP一括メンテナンス系の未完了タスクをフェーズBクローズへ向けて前進させるため。
- 2026-03-13: フェーズBクローズ判定ゲート `002-0099` を追加し、`002-0023` を完了化。理由: 未完了項目と必要試験ID（7027/7064/7065/7066/7067）を固定し、フェーズBを段階的にクローズできる状態へ整理するため。
- 2026-03-11: `002-0024`〜`002-0028`（AP遷移条件、AP中タスク停止、LED/LCD表示、ロール認証、初期パスワード変更運用）を追加。理由: APモード要件詳細を実装タスクへ分解し、取りこぼしを防ぐため。
- 2026-03-11: APメンテナンス一括運用の実装準備として `002-0019`〜`002-0023`（ログイン必須、USB Wi-Fi探索、順次更新、再起動後`status`判定、試験追加）を追加。理由: 複数台の現地更新を安全に進める実装単位へ分解するため。
- 2026-03-10: 4-5 強化として、画像更新 `fileSync` の HMAC/署名必須タスクと `imagePackageApply`（ZIP一括更新）タスクを追加。理由: OTA同等のセキュリティ要件で画像更新を実現するため。
- 2026-03-10: `002-0015` を追加。理由: `common.h` へ追加した `network` キー5項目（`*UrlName` と `keyDevice`）の実装追従を明示管理するため。
- 2026-03-10: `001-0001` を完了へ更新し、`beta.x` 自動インクリメント必須運用を廃止。理由: 版数更新は必要な作業のみ都度指示で実施する方針へ変更したため。
- 2026-03-10: `001-0003` を完了へ更新。理由: rollback 前提実装後に 7025 の `未確定起動失敗` 条件を実機再現し、旧面継続起動を確認できたため。
- 2026-03-10: `001-0002` を完了へ更新し、`001-0004` を問題点記録書 `#0005` 連携で完了へ更新。`001-0003` は 7025 のうち `未確定起動失敗` 条件を rollback 前提未整備として継続管理へ変更。理由: 実機試験結果を反映しつつ、未成立前提を明示して完了/未完了を分離するため。
- 2026-03-09: 未完了タスクへ固定ID（`章番号3桁-項目番号4桁`）を付与し、欠番許容ルールを追加。理由: 並び替えや統合後も他文書や会話から安定参照できるようにするため。
- 2026-03-09: `NVS` / `LittleFS` の保存区分、証明書の `LittleFS` 移行、`fileSync` 実装、ファイルログ実装と関連試験タスクを追加。理由: 合意済みの保存方針と局所更新方式を未完了タスクへ反映するため。
- 2026-03-09: `todo.md` の実装タスク名と API URL 表記を `/api/workflows/...` 前提へ更新し、`createPairingBundle` を内部 helper 扱いへ整理。理由: 実装着手時に公開 API と内部秘密処理の境界がブレないようにするため。
- 2026-03-09: `SecretCore` の高リスクワークフロー実行器化、進捗状態、`runPairingSession()` / `runKeyRotationSession()` / `runProductionSecureFlow()` / `runSignedOtaCommand()` の実装タスクを追加。理由: TS は進捗表示中心、Rust は完了判定まで責任を持つ構成へ実装計画を合わせるため。
- 2026-03-21: `003-0012` に KeyRotation / Production workflow の REST・IPC・Rust workflow 骨格追加を反映。理由: Pairing/OTA に続き未着手だった workflow 境界を固定し、残課題を「本体未実装」へ切り分けるため。
- 2026-03-09: AP 共通トップ画面認証、`runPairingSession()` / `createPairingBundle` 内部処理、`public_id` 初期値変更、ESP32 current/previous key 2 スロット運用のタスクを追加。理由: AP モード投入と逐次再ペアリングの設計確定内容を未完了タスクへ反映するため。
- 2026-03-09: シリアル機能停止を eFuse ではなく FW 制御で管理する方針と、インターネット接続時の最低限セキュリティ基準、関連試験タスクを追加。理由: 現地解析余地を残しつつ、4-8 / 4-9 / 4-10 を未完了タスクとして具体化するため。
- 2026-03-08: ESP32 内部設定の NVS 永続化と、メンテナンス用 Wi-Fi AP モードからの設定修正を直近フェーズへ追加。理由: Wi-Fi AP や MQTT ブローカー変更時に現地メンテナンスで復旧できることを、当面の最優先課題へ反映するため。
- 2026-03-08: 直近ゴール順に `todo.md` を再編成し、完了済みの「仕様書へ追加する」系タスクを整理。理由: ESP32 と LocalServer の当面の到達目標（2面OTA確認、鍵/証明書本番化、Secure Boot/ROM暗号化、eFuse後OTA継続）に沿って着手順を明確化するため。
- 2026-03-08: eFuse 本番前リハーサル、専用処理タスクのスタックサイズ確保、開始前メモリ余裕確認のタスクを追加。理由: eFuse 失敗防止には手順確認だけでなく実行時資源の安全余裕も必要であるため。
- 2026-03-08: `ProductionTool` の隠し入口、専用パスワード、段階実行ウィザードの設計タスクを追加。理由: 高リスク機能の入口制御と操作確認を未完了タスクとして具体化するため。
- 2026-03-08: `ProductionTool`、ESP32内部専用セキュア化フロー、中断時の読戻し判定タスクを追加。理由: 過去のシリアル焼込み中断経験を踏まえて、eFuse 最終有効化の安全設計を未完了タスクへ具体化するため。
- 2026-03-08: OTA標準運用を「メーカー署名 + 顧客実行」として実装タスクへ反映。理由: 正式採用した運用モデルを作業項目へ直結させるため。
- 2026-03-08: `日程表_ローカル運用まで_50日.md` の未完了事項と進捗ルールを `todo.md` へ統合する前提で、試験・統合タスクと運用ルールを追加。理由: タスク管理文書の冗長化を解消し、更新先を一本化するため。
- 2026-03-08: `k-iot` 系の実キー生成タスク（試験用/本番用の生成時期、保存場所、eFuseリハーサル、文書追記）を追加。理由: ESP32 セキュリティ鍵の作成タイミングと保管責任を未完了タスクとして具体化するため。
- 2026-03-08: 鍵管理タスクを `TPM + wrapped_secret + k-user / k-device` 方式へ更新。理由: 鍵設計の正式決定に合わせて未完了タスクを具体化するため。
- 2026-03-07: OTA実機試験完了（beta.5 OTA成功、done通知確認）を受けて、OTA関連タスクを `todo_old20260307.md` へ退避。
- 2026-03-07: SecretCore（Rust別層）Phase1～4の実装タスクを追加。IPC傍受対策・raw key非返却・難読化・DPAPI/Keychain方針。理由: TS+Rust分離アーキテクチャを確定したため。
- 2026-03-07: OTAセキュア化タスク（OTA開始コマンドHMAC署名/ESP32検証）を新規追加。理由: SecretCore完成後の必須セキュリティ強化のため。
- 2026-03-07: 全タスクをカテゴリ別に再整理（OTA残課題/SecretCore/ネットワーク/鍵管理/ESP32セキュア化/文書管理）。
- 2026-03-07: ESP32書込み前に `beta.x` を更新する継続運用タスクを追加（`bump-esp32-beta-version.ps1` 運用）。
- 2026-03-07: OTA詳細仕様化に合わせ、進捗通知・リトライ制御・SHA256検証の実装タスクを追加。
- 2026-03-07: LocalServer初期実装反映に伴い、実機E2E試験・ESP32受信検証・LocalSoft/LocalSoftForCloud実装タスクを追加。
- 2026-03-07: `(-9984) X509` 解消結果の記録タスクと、新ルータ導入（期限: 2026-03-14）向けの実施/確認/フォールバック廃止タスクを追加。
- 2026-03-07: 新ルータ導入計画の具体値（GW `172.17.1.1` / DNS `172.17.1.100` / DHCP `172.17.1.200+`）と、導入前の暫定フォールバック運用タスクを追加。
- 2026-03-07: `(-9984) X509` 対策として、SAN再発行後のTLS再接続試験タスクを追加。
- 2026-03-07: 専用Wi-Fiルーター導入計画に合わせ、`mqtt.esplab.home.arpa -> 172.17.1.100` 固定割当てタスクを追加。
- 2026-03-07: 暫定運用として「方法① ESP32だけDNS指定（172.16.1.59）」を採用したため、固定依存を廃止する将来タスクと再試験タスクを追加。
- 2026-03-07: ネットワーク運用仕様の検討結果に合わせ、ログイン前自動起動（Windows/macOS）とAWS移行時接続方式、保存期間/削除APIの仕様化タスクを追加。
- 2026-03-06: DNSタスクをCoreDNS前提へ具体化し、外部DNSとの両立確認と公開ドメイン上書き禁止ルールを追加。
- 2026-03-06: `K_device` によるMQTT payload暗号化/復号の必須タスクを追加。
- 2026-03-06: ローカルDNSサーバー導入検討タスクを追加。
- 2026-03-03: ローカルサーバー設計検討の未確定事項（署名検証状況、鍵管理方式、UIスコープ、開発順序、クラウドBroker候補）をTODOへ追加。
- 2026-02-24: 新規作成。第3段階中心のタスクを初期登録。
