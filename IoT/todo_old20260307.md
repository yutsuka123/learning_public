# IoT TODO 完了済み（退避: 2026-03-07）

[補足] 本書は 2026-03-07 時点で完了とみなしたタスクの退避記録である。

---

## 完了済みタスク

### OTA実装・実機試験（2026-03-07 完了）
- [x] [2026-03-07][重要] LocalServer初期実装（Web UI + MQTT status/OTA command + OTA HTTPS）の実機E2E試験を完了し、`試験記録書.md`へ結果を記録する。
  - 結果: beta.5 OTA成功。done通知（phase=done, progress=100）をESP32からMQTT受信確認。FW書換日時NVS保存確認。
- [x] [2026-03-07][重要] ESP32側で `esp32lab/call/status/<name>` と `esp32lab/call/otaStart/<name>` の受信処理結果を検証し、不足実装があれば修正する。
  - 結果: call/status→notice/statusの返答確認済み。call/otaStart→HTTPS OTA実行確認済み。
- [x] [2026-03-07][重要] OTA進捗通知（`esp32lab/notice/otaProgress/<name>`）をESP32とLocalServer双方で実装し、デバイス別進捗表示を有効化する。
  - 結果: write/verify/done/error の各phaseをMQTTで通知。LocalServerブラウザにて進捗列表示確認済み。
- [x] [2026-03-07][厳守] OTAリトライ制御を実装する（同一進捗率: 5秒間隔3回、最初から再試行: 3回）。
  - 結果: ESP32 ota.cpp にリトライ制御実装済み。同率5s×3回、初回再試行3回。
- [x] [2026-03-07][厳守] OTA前後のSHA256検証を実装し、一致時のみ更新確定・次回起動領域切替を行う。
  - 結果: LocalServerがSHA256をmanifestに含めて配信。ESP32側でSHA256検証実装済み。
- [x] [2026-03-07][重要] ESP32側のOTA署名検証の実装状況を確定する（実装済/未実装/要改修を判定）。
  - 結果: SHA256検証を実装済みとして確定。HMAC署名検証はSecretCore Phase1完了後に追加予定。
- [x] [2026-03-03][重要] Web UIの初期スコープを確定する（管理者向け最小機能 / 運用ダッシュボード込み）。
  - 結果: デバイス一覧（名前/IP/status/FWバージョン/FW書換日時/最終受信/OTA進捗）+ status更新ボタン + OTA開始ボタン + 設定画面で確定。

### LocalServer設定・UI（2026-03-07 完了）
- [x] [2026-03-07][重要] ブラウザの時刻表示をサーバーの現地時刻で表示する。設定画面でタイムゾーン変更できるようにする。
  - 結果: settingsStore.ts, settings.html 実装済み。`/api/settings` GET/PUT 確認済み。
- [x] [2026-03-07][重要] ファームウェアファイルをブラウザからアップロードまたはローカルパスで指定できるようにする。
  - 結果: `firmwareSource: localPath | uploadedFile` 切替、multer対応、`/api/settings/firmware-upload` 実装済み。
- [x] [2026-03-07][重要] OTA完了時に FW書換日時（firmwareWrittenAt）をNVSに保存し、status通知でOTA成功時刻を返す。
  - 結果: firmwareInfo.cpp（Preferences/NVS）実装済み。OTA成功後にresolveFirmwareWrittenAtForStatusで正確な時刻を返すことを確認。
- [x] [2026-03-07][重要] ESP32からOTA終了時に `done` または `error` を明示送信し、LocalServerが受信後に表示を更新する。
  - 結果: publishOtaProgress + waitForTerminalPublishAck 実装済み。ESP32からdone受信をLocalServerブラウザで確認。
- [x] [2026-03-03][推奨] 開発順序を確定する（設計書先行 / 最小実装先行）。
  - 結果: 最小実装先行で進めることで確定。

### セキュリティ設計・文書化（2026-03-07 完了）
- [x] [2026-03-07][重要] `セキュリティ方針_認証情報取扱い.md` を全面改訂する（別PCコピー・ロジック改ざん脅威モデルへ更新）。
- [x] [2026-03-07][重要] `鍵管理および初期セットアップ設計仕様書.md` に TS + Rust 秘密処理別層設計を反映する。
- [x] [2026-03-07][重要] `LocalServer秘密処理別層仕様書.md` を新規作成する（TS+Rust構成、IPC保護、raw key非返却、難読化方針）。
- [x] [2026-03-07][重要] `使用技術仕様書.md` に Rust SecretCore・IPC暗号化方針を反映する。

---

## 退避ルール
- [厳守] 完了タスクは `todo_old<YYYYMMDD>.md` へ退避し、`todo.md` 本体から削除する。
- [補足] 本ファイルは参照専用。再開タスクがあれば `todo.md` へ移動する。
