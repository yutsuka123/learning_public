# IoT 試験仕様書

[重要] 本書は LocalServer 初期実装の疎通・機能試験仕様を定義する。  
理由: 実装後の合否判定を再現可能にするため。

## 1. 試験方針
- 試験番号は4桁（上1桁: 分類、下3桁: 連番）で管理する。
- 7000番台をネットワーク/サーバー系試験として利用する。
- [厳守] 実施結果は `試験記録書.md` に1対1で記録する。

## 2. 試験項目
- 7001: LocalServer 起動試験
  - 手順: `cd IoT/LocalServer && npm install && npm run build && npm run start`
  - 期待結果: `HTTP started` が出力され、`/api/health` が `status=ok` を返す。
- 7002: MQTT 接続試験
  - 手順: `cd IoT/LocalServer && npm run test:connect`
  - 期待結果: MQTTへ接続し `esp32lab/call/status/all` publish成功を出力する。
- 7003: Web GUI 一覧表示試験
  - 手順: `http://localhost:3100/` を開き、`/api/devices` 応答確認。
  - 期待結果: テーブル描画ができ、操作ボタンが押下可能。
- 7004: statusコマンド送信試験
  - 手順: `POST /api/commands/status` に `{"targetNames":"all"}` を送信。
  - 期待結果: `result=OK` が返り、MQTT publishされる。
- 7005: OTAコマンド送信試験
  - 手順: `POST /api/commands/ota` に `{"targetNames":"all"}` を送信。
  - 期待結果: `result=OK` が返り、`otaStart` publishされる。
- 7006: OTA配布エンドポイント試験
  - 手順: `/ota/manifest.json` と `/ota/firmware.bin` を取得。
  - 期待結果: manifest取得成功。firmware未配置時は404を返す。
- 7007: OTA進捗通知試験（デバイス別）
  - 手順: OTA実行中に `esp32lab/notice/otaProgress/+` を購読し、LocalServer画面を確認。
  - 期待結果: デバイス別に進捗率（例: 30%）が表示される。
- 7008: OTA同一進捗率リトライ試験
  - 手順: 通信を劣化させ、同一進捗率で停止する状況を作る。
  - 期待結果: 5秒間隔で最大3回まで同一進捗率リトライを行う。
- 7009: OTA最初から再試行試験
  - 手順: 7008で回復不可条件を維持する。
  - 期待結果: 最初から再試行を最大3回実施し、上限超過でNG通知する。
- 7010: SHA256一致試験
  - 手順: 正常な `firmware.bin` と一致するSHA256を配布指令へ設定する。
  - 期待結果: SHA256一致後のみ更新確定し、次回起動先が更新領域へ切替わる。
- 7011: SHA256不一致試験（異常系）
  - 手順: 意図的に誤SHA256を `otaStart` に設定する。
  - 期待結果: `shaMismatch` としてNG終了し、更新確定しない。
- 7012: OTA再認証試験
  - 手順: Web UI または API 経由で OTA実行前に管理者パスワードを再入力する。
  - 期待結果: 再認証成功後のみ OTA開始操作が許可される。
- 7013: OTA再認証セッション有効期限試験
  - 手順: 7012 成功後、同一ブラウザセッション内で 3時間未満と 3時間超過後の双方で OTA開始可否を確認する。
  - 期待結果: 3時間以内は再入力不要、3時間超過後は再認証が再度要求される。
- 7014: OTA HTTPS証明書検証試験
  - 手順: 正常証明書と異常証明書（ホスト名不一致、信頼外CA等）で OTA取得を試行する。
  - 期待結果: 正常証明書のみ取得成功し、異常証明書は拒否される。
- 7015: OTAデュアルパーティション交互切替試験
  - 手順: 連続2回以上 OTA を実施し、各回の書込み先パーティションを確認する。試験中は `status` 通知の一時項目 `runningPartition` / `bootPartition` / `nextUpdatePartition` を参照する。
  - 期待結果: 書込み先が 0 / 1 の2面で交互に切り替わる。
  - [廃止の方針] `runningPartition` / `bootPartition` / `nextUpdatePartition` は 7015 / 7025 確認用の一時項目とし、試験完了後は `status` 通知から削除する。理由: 常設の運用通知へ試験専用項目を残さないため。
- 7016: メーカー署名済み配布物実行試験
  - 手順: メーカー署名済み `firmware.bin` を LocalServer へ配置し、顧客環境相当の LocalServer から OTA を実行する。
  - 期待結果: 署名済み配布物のみ OTA が開始・完了できる。
- 7017: 未署名配布物拒否試験
  - 手順: 未署名または署名情報欠落の `firmware.bin` を用意し、OTA 実行を試みる。
  - 期待結果: LocalServer または ESP32 側で実行拒否し、更新処理へ進まない。
- 7018: 改ざん配布物拒否試験
  - 手順: 署名済み配布物の一部バイトを改変した `firmware.bin` を用意し、OTA 実行を試みる。
  - 期待結果: 署名検証または整合性検証で NG となり、更新確定しない。
- 7019: AP 共通トップ画面認証試験
  - 手順: AP 接続後に共通トップ画面へアクセスし、未認証、誤パスワード、正パスワードの各条件で表示可否を確認する。
  - 期待結果: 正しい共通パスワード成功時のみ共通トップ画面が表示される。
- 7020: ProductionTool 追加認証試験
  - 手順: 共通トップ画面から ProductionTool 画面へ遷移し、通常管理者パスワード、誤メーカーモード専用パスワード、正しいメーカーモード専用パスワードの各条件で遷移可否を確認する。
  - 期待結果: ProductionTool 画面は追加認証成功時のみ表示され、通常管理者パスワードでは代替できない。
- 7021: ProductionTool 画面段階実行試験
  - 手順: ProductionTool 画面を起動し、事前確認、対象機確認、実行内容確認、最終確認、段階実行、完了判定の各画面遷移を確認する。
  - 期待結果: 各段階で確認操作が要求され、最終確認完了前には eFuse 処理が開始されない。
- 7022: eFuse 書込み中断後の再判定試験
  - 手順: ProductionTool 側の eFuse 処理中に電源断、通信断、内部例外を模擬し、再起動後に再判定フローを確認する。
  - 期待結果: eFuse 読戻し結果から残工程または隔離判定へ進み、通常顧客モードの継続処理へ戻らない。
- 7023: eFuse 専用処理タスクのスタック余裕試験
  - 手順: eFuse 専用処理タスクについて、平常時と最悪条件時の `uxTaskGetStackHighWaterMark` 相当値、空きヒープ、開始可否判定を確認する。
  - 期待結果: 定義した安全閾値以上のスタック余裕があり、閾値未達またはヒープ不足時は eFuse 開始を拒否する。
- 7024: eFuse 本番前リハーサル完了確認試験
  - 手順: 本番対象の手順・ファームウェア系統・鍵IDに対応する試験機リハーサル記録の有無を確認し、未完了状態で本番開始を試みる。
  - 期待結果: 対応するリハーサル記録が存在する場合のみ本番開始可能とし、未完了時は開始拒否する。
- 7025: OTA途中失敗時の旧パーティション継続起動試験
  - 手順: OTA の途中失敗、整合性不一致、未確定起動失敗の各条件を作り、再起動後の起動パーティションと稼働版数を確認する。試験中は `status` 通知の一時項目 `runningPartition` / `bootPartition` / `nextUpdatePartition` を参照する。
  - 期待結果: 旧パーティションから継続起動し、直前まで正常稼働していた版数で運用継続できる。
  - [重要] `未確定起動失敗` 条件は rollback 前提（`BOOTLOADER_APP_ROLLBACK_ENABLE` と確定処理実装）が成立している場合のみ実施可能とする。
  - [重要] `未確定起動失敗` 再現時は `POST /api/commands/rollback-test` (`mode=enable`) を実行してから OTA を開始し、試験後は `mode=disable` へ戻す。
- 7026: eFuse 最終化後の OTA バージョンアップ試験
  - 手順: eFuse / Secure Boot / Flash Encryption 有効化後の個体に対し、証明書検証有効のまま署名済み `firmware.bin` を OTA 配布し、版数更新を確認する。
  - 期待結果: eFuse 最終化後も OTA による更新が成功し、更新後版数で正常起動する。
- 7027: ESP32 設定永続保存試験
  - 手順: Wi-Fi / MQTT / HTTPS / 時刻同期 / その他設定を更新し、再起動後に NVS から復元されることを確認する。
  - 期待結果: 設定値が再起動後も保持され、主要運用設定の主保存先が NVS であることを確認できる。
- 7028: APメンテナンスモード設定修正試験
  - 手順:
    - Wi-Fi AP または MQTT ブローカー設定へ意図的に誤値を投入し、通常運用で接続不能状態を再現する。
    - ESP32 をメンテナンス用 Wi-Fi AP モードへ移行する。
    - LocalServer 管理API（`/api/admin/ap/configure`）経由で正しい設定値を再投入し、再起動を要求する。
    - 再起動後に `status` topic 応答と通常運用復帰を確認する。
  - 期待結果:
    - メンテナンスモード経由で設定修正・保存できる。
    - 再起動後に MQTT 復帰し、`status` 応答が再開する。
- 7029: 新ルータ運用開始後の DNS / MQTT 起動前確認試験
  - 手順: `WiFi AP=172.17.1.1` `DNS=172.17.1.100` 環境で ESP32 を書き込み起動し、`mqtt.esplab.home.arpa` と `ntp.esplab.home.arpa` の名前解決、NTP 同期、MQTT 接続開始条件をログで確認する。
  - 期待結果: `hostByName()` が DNS 名を解決し、NTP 同期後に MQTT 接続処理へ進める。`SENSITIVE_MQTT_FALLBACK_IP` に依存しない。
- 7030: 通常運用FWのシリアル出力制約試験
  - 手順: 量産通常FWで起動し、シリアル高詳細ログ、危険な保守コマンド、秘密値出力の有無を確認する。
  - 期待結果: 通常運用に不要な詳細シリアル出力や危険な保守コマンドは停止または最小化され、秘密値が出力されない。
- 7031: 診断用FWの一時シリアル有効化試験
  - 手順: メーカー管理下の署名済み診断用FWを書き込み、一時的にシリアル出力で障害解析できることと、通常FWへ戻した後に再び制約されることを確認する。
  - 期待結果: 診断時のみ必要なシリアル出力が可能で、通常FWへ戻すと恒久的な開放状態が残らない。
- 7032: 未使用ポート閉鎖確認試験
  - 手順: ESP32、LocalServer、Broker の稼働状態で利用予定外の待受ポートを列挙し、閉鎖状態を確認する。
  - 期待結果: 設計上必要なポート以外は待受しておらず、開発用・管理用ポートが本番相当構成で露出していない。
- 7033: 認証失敗時のバックオフ・一時遮断試験
  - 手順: MQTT 認証や管理系 API 認証を連続失敗させ、バックオフ、一定回数超過時の一時遮断、監査ログ記録を確認する。
  - 期待結果: 連続失敗に応じて抑止動作が入り、総当たり的な試行がそのまま継続できない。
- 7034: インターネット接続時の踏み台化防止設定確認試験
  - 手順: 本番相当ネットワークで、管理ポートの非公開、不要待受停止、汎用中継機能無効、送信先制限、監査ログ記録の有無を確認する。
  - 期待結果: 本プロジェクト要件外の中継機能が有効になっておらず、不要な公開面が残っていない。
- 7035: `createPairingBundle` 必須設定事前検証試験
  - 手順: Wi-Fi / MQTT / OTA / 認証情報のいずれかを欠落させた入力で bundle 作成を要求し、TS 側の事前検証結果を確認する。
  - 期待結果: `SecretCore` 呼出し前にエラーとなり、bundle 生成が開始されない。NTP 欠落のみは許容される。
- 7036: `createPairingBundle` AP 初回投入試験
  - 手順: AP モード接続した未ペアリング個体へ `public_id`、`k-device`、`keyVersion`、Wi-Fi / MQTT / OTA / 認証情報を含む bundle を投入する。
  - 期待結果: ESP32 が署名検証、ECDH セッション復号、NVS 保存を行い、通常運用へ移行する。
- 7037: `k-user` 再発行後の current/previous 2 スロット試験
  - 手順: `k-user` 再発行後に 1 台を再ペアリングし、ESP32 側の `currentKeyVersion/currentKDevice` と `previousKeyVersion/previousKDevice` の更新状態を確認する。
  - 期待結果: 再ペアリング直後に `previousKeyState=grace` となり、新旧 2 スロットが正しく更新される。
- 7038: 48h / 240h 稼働時間による previous key 状態遷移試験
  - 手順: 並存期間中に runtime 積算を進め、48h 到達時と 240h 到達時の状態遷移と NVS 更新タイミングを確認する。
  - 期待結果: 48h で `expired-retained` へ遷移して旧鍵認証を拒否し、240h 後に previous key を自動削除し、結果は監査ログのみに残る。
- 7039: `runPairingSession()` workflow 状態遷移試験
  - 手順: AP モード初回投入を実施し、`workflowId` に対する状態遷移を取得する。
  - 期待結果: `queued` -> `running` -> `waiting_device` -> `verifying` -> `completed` の順で遷移し、TS 側は進捗と結果のみを表示する。
- 7040: `runKeyRotationSession()` 完了判定試験
  - 手順: `k-user` 再発行後の再ペアリングを workflow 経由で実施し、個体単位の完了判定を確認する。
  - 期待結果: `SecretCore` が再ペアリング開始から完了判定まで実行し、TS 側は `completed` または `failed` の結果のみを扱う。直後の AP `GET /api/pairing/state` では `savedCurrentKeyVersion=<指定値>`、旧鍵が存在した場合 `previousKeyState=grace` を返す。
- 7041: `runProductionSecureFlow()` 段階実行試験
  - [重要][2026-03-22] 現フェーズでは dry-run のみを対象とし、`stage_execute` 直前で停止する。
  - 手順:
    1. `npm run test:7041:pipeline` を実行する（`preflight -> full -> summary -> gate`）。
    2. preflight が失敗した場合、`failureCategory` / `failureRecommendation` / `diagnosis.routeHint` / `diagnosis.arpHint` を確認し、AP 接続性を復旧して再実行する。
    3. AP 復旧待ち中は `npm run test:7041:pipeline:wait` で preflight 再判定を継続する。
    4. preflight 成功後に full が実行され、`waiting_device` / `verifying` / `completed` への遷移と AP `production/state` を確認する。
    5. 自動判定が必要な場合は `npm run test:7041:pipeline:strict` または `npm run test:7041:gate:strict` を実行し、`READY_FOR_FULL` または `FULL_COMPLETED` 以外で NG とする。
    6. 最後に `test7041-summary-*.json` を確認し、直近比較で改善有無を判定する（比較対象は `test7041-YYYY...json` の実行レポートのみ）。
  - [重要] AP 復旧後の再開は `npm run test:7041:pipeline:strict` を標準とし、単体 `test:7041` や `test:7041:gate:strict` のみで合否判定を完結させない。理由: 実行順、比較、ゲート判定を同一証跡系列で残すため。
  - [推奨] 再開前に `ping -n 2 192.168.4.1` と `curl.exe ... /api/auth/login` を実施し、PC が `192.168.4.x` セグメントにいることを確認してから `pipeline:strict` を実行する。
  - 記録必須項目:
    - `test7041-*.json`（成功/失敗とも）
    - `test7041-pipeline-*.json`（`preflight/full/summary` 各 step 判定）
    - `test7041-gate-*.json`（ゲート判定結果）
    - `failureCategory` / `failureRecommendation`（失敗時）
    - `diagnosis.targetHost/localIpv4/routeHint/arpHint/nextAction`（失敗時）
    - `test7041-summary-*.json`（比較結果）
  - 期待結果: `SecretCore` が段階進行と AP `production/state` 照合を担当し、TS 側は進捗状態と結果のみを表示する。AP 側では `runId` 一致、`state=precheck_collected`、観測 `firmwareVersion` / `MAC` / `freeHeap` / `stackMargin` が確認できる。preflight NG 時は full を実行せず summary まで到達する。
- 7042: `runSignedOtaCommand()` 署名付き OTA 開始試験
  - 手順: OTA 開始を workflow 経由で実施し、署名付き開始指示、送達、検証、完了判定を確認する。
  - 期待結果: `SecretCore` が OTA 開始ワークフローを実行し、TS 側は中間秘密を扱わず進捗と結果のみを扱う。
- 7043: workflow 異常系 `failed` 遷移試験
  - 手順: AP 未接続、署名検証失敗、NVS 保存失敗などの異常条件を作り、workflow の失敗遷移を確認する。
  - 期待結果: `queued` / `running` / `waiting_device` / `verifying` のいずれかから `failed` へ遷移し、TS 側は内部手順を再構成せずエラー表示のみを行う。
- 7044: `fileSync` 画像差分更新試験
  - 手順: `/images` に既存ファイルと未更新ファイルを配置した状態で、`fileSyncPlan` / `fileSyncChunk` / `fileSyncCommit` を `MQTT` で実行する。
  - 期待結果: `SHA-256` 差分があるファイルのみ更新され、未変更ファイルは再受信されない。
- 7045: `fileSync` 削除 / 全削除時の `/logs` 保持試験
  - 手順: `/images` と `/logs` にファイルを配置し、`deleteMode=listed` および `deleteMode=all` をそれぞれ実行する。
  - 期待結果: `/images` 側だけが削除対象となり、`/logs` は保持される。
- 7046: `fileSync` `SHA-256` 不一致時の旧ファイル維持試験
  - 手順: 意図的に誤った `sha256` を指定して画像または証明書更新を実行する。
  - 期待結果: `failed` または `NG` となり、旧ファイルが維持される。
- 7047: 証明書 `LittleFS` 移行更新試験
  - 手順: `/certs` に MQTT / HTTPS 用証明書を更新し、`NVS` の証明書メタ情報更新後に再接続を実施する。
  - 期待結果: `LittleFS` の新証明書と `NVS` メタ情報が整合し、再接続で新証明書が利用される。
- 7048: ファイルログ ローテーション試験
  - 手順: 時刻同期済みと未同期の両条件でログ出力を行い、再起動時および 1KB 到達時のファイル切替を確認する。
  - 期待結果: 時刻同期済み時は `/logs/<年>/<月日>/...`、未同期時は `/logs/<起動回数>/...` 形式となり、再起動時または 1KB 到達時に新ファイルへ切り替わる。
- 7049: ファイルログ 自動削除試験
  - 手順: 30 日超過ログ、または総量 100KB 超過となるログを事前配置してログ整理処理を実行する。
  - [重要] 実機で短時間再現が必要な場合のみ、試験用に総量しきい値を一時的に縮小（例: `8KB`）して `pruneFileLogsIfNeeded removed` 証跡を採取してよい。理由: 100KB条件の自然到達待ちを避け、試験時間内に 7049 の成立確認を完了するため。
  - [厳守] 上記の一時縮小を使用した場合、試験完了後は必ず本番値（`100KB`）へ戻したFWを再書込みして復元完了を記録する。理由: 試験条件のまま運用へ残す事故を防止するため。
  - 期待結果: 古いログから順に削除され、保持条件を満たすまで整理される。
- 7050: LocalServer 管理者認証 API 試験
  - 手順: `POST /api/admin/auth/login` へ正しい `username/password`、誤った `username/password`、欠落パラメータの3条件で送信する。
  - 期待結果: 正常条件のみ `result=OK` とトークンを返し、異常条件は `401` または `NG` を返す。
- 7051: `k-user` 発行 API 試験
  - 手順: 管理者ログイン後、`POST /api/admin/keys/k-user/issue` を実行する。
  - 期待結果: `result=OK` を返し、`keyFingerprint` が返却される。raw `k-user` 値は応答へ含まれない。
- 7052: `k-device` 発行・ESP32 投入 API 試験
  - 手順: 管理者ログイン後、`POST /api/admin/keys/k-device/issue` を `pushToDevice=true` で実行し、ESP32 受信ログで `set/keyDeviceSet` を確認する。
  - 期待結果: API は `result=OK` / `pushedToDevice=true` を返し、ESP32 側は `keyDevice` を保存する。
- 7053: `k-device` 暗号化 MQTT 相互通信試験（`securePing`）
  - 事前条件: 対象ESP32が MQTT 接続済みで、`set/keyDeviceSet` 受信後であること。
  - 手順: 管理者ログイン後、`POST /api/admin/commands/secure-ping` を実行し、LocalServer で復号結果と `requestId` 一致を確認する。
  - 期待結果: `result=OK` を返し、ESP32 応答の暗号化 payload が `k-device` で復号できる。
- 7054: APモード遷移条件試験
  - 手順: `起動時3秒長押し`、`MQTT call/maintenance`、`Wi-Fi/MQTT未設定` の3条件で起動し、APモード遷移を確認する。
  - 期待結果: 3条件すべてで AP モードへ遷移し、通常モードとの切替は再起動を伴う。
- 7055: APモード表示試験（LED/LCD）
  - 手順: APモード中に LED と LCD を目視確認する。
  - 期待結果: LED は `0.3s ON -> 0.3s OFF -> 0.3s ON -> 0.3s OFF -> 1.0s ON -> 0.3s OFF` を繰返し、LCD は 1行目 `MaintenanceMode`、2行目 AP名（16文字以内）を表示する。
- 7056: APモード時タスク抑止試験
  - 手順: APモード中のログで `MQTT connect` や通常モード専用タスク起動を監視する。
  - 期待結果: APモード中は MQTT タスクと通常モード専用タスクが起動しない。
- 7057: MQTT payload暗号化 strict モード受信拒否試験（平文拒否）
  - 手順:
    - LocalServer を `MQTT_PAYLOAD_ENCRYPTION_MODE=strict` で起動する。
    - 平文 payload の `notice/status` 相当メッセージを test client から publish する。
    - LocalServer 側ログと `/api/devices` 反映有無を確認する。
  - 期待結果: 平文 payload は復号対象外として拒否され、`/api/devices` が更新されない。
- 7058: MQTT payload暗号化 strict モード E2E 試験（暗号化のみ許可）
  - 手順:
    - LocalServer を `MQTT_PAYLOAD_ENCRYPTION_MODE=strict` で起動する。
    - ESP32 を `MQTT_PAYLOAD_SECURITY_MODE=2`（strict）で書込み起動する。
    - `POST /api/admin/commands/secure-ping` を実行し、往復応答を確認する。
  - 期待結果: `result=OK` を返し、シリアルログで `encrypted=1` を確認できる。平文メッセージは双方で受理されない。
- 7059: `imagePackageApply` 正常系試験（展開・同名上書き・ディレクトリ指定）
  - 事前条件:
    - `imagePackageApply` 実装済みであること。
    - 対象ESP32が MQTT / HTTPS 到達可能であること。
    - 展開先 `/images` 配下に既存ファイルを配置しておくこと。
  - 手順:
    - `destinationDir=/images/top`、`overwrite=true` で同名ファイルを含むZIPを適用する。
    - `destinationDir=/images/campaign/2026` のようにサブディレクトリ指定でZIPを適用する。
    - 適用後に `fileSyncStatus` もしくは更新結果APIで反映結果を確認する。
  - 期待結果:
    - ZIP展開が完了し、`destinationDir` 配下へファイルが配置される。
    - `overwrite=true` 指定時は同名ファイルが上書きされる。
    - `/images` 配下以外へは展開されない。
- 7060: `imagePackageApply` 異常系試験（署名/HMAC不一致・SHA不一致・不正ディレクトリ）
  - 事前条件:
    - 署名/HMAC付き `imagePackageApply` コマンド送信手段があること。
  - 手順:
    - `signature` または HMAC を改ざんしたコマンドを送信する。
    - ZIP本体の `SHA-256` と payload 指定値を意図的に不一致にして送信する。
    - `destinationDir=/logs` や `../` を含む不正ディレクトリ指定で送信する。
  - 期待結果:
    - 署名/HMAC不一致は実行拒否される。
    - SHA不一致は展開されず、既存ファイルは維持される。
    - 不正ディレクトリ指定は実行拒否される。
- 7061: `fileSync` 画像更新標準経路試験（ZIP非使用・1ファイル更新・100KB以上分割）
  - 事前条件:
    - ESP32 側 `fileSyncPlan` / `fileSyncChunk` / `fileSyncCommit` と署名検証が有効であること。
    - `IoT/LocalServer/scripts/testImageFileSyncE2E.mjs` が実行可能であること。
  - 手順:
    - `imagesDir` 配下へ 100KB 未満ファイルと 100KB 以上ファイルを混在配置する。
    - `node scripts/testImageFileSyncE2E.mjs --targetName <device>` を実行する（必要に応じ `--splitThresholdBytes 102400 --chunkSizeBytes 102400 --mqttPacketSizeBytes 4096` を指定）。
    - 実行ログで `files` / `splitFiles` / `totalChunks` を確認する。
    - 実機通知で `fileSyncStatus: planning -> receiving -> completed` を確認する。
    - `/images` 配下の更新結果（上書き含む）を確認する。
  - 期待結果:
    - ZIP を使わず 1ファイルずつ更新が完了する。
    - 100KB 以上ファイルは複数 chunk で送信される。
    - [重要] `chunkSizeBytes` は上限希望値として扱い、実送信は MQTT パケット上限（`mqttPacketSizeBytes`）を超えないサイズへ自動縮小される。
    - `SHA-256` 検証成功時のみ反映され、失敗時は旧ファイルを維持する。
- 7062: `fileSync` 画像更新異常系試験（SHA不一致・chunk欠損）
  - 事前条件:
    - 7061 の通常送信経路が実行可能であること。
  - 手順:
    - `--abnormalMode shaMismatch` で送信し、commit失敗を確認する。
    - `--abnormalMode chunkMissing` で送信し、commit失敗を確認する。
  - 期待結果:
    - いずれも `fileSyncStatus` が `phase=failed` となり、`errorCode=FSYNC_SHA_MISMATCH` で終了する。
    - 失敗時は既存ファイルが維持される。
- 7063: APモード局所ファイル更新試験（`/images` `/certs`）
  - 事前条件:
    - ESP32 が AP メンテナンスモードで起動し、`/api/auth/login` が利用可能であること。
    - LocalServer から `POST /api/admin/ap/files/upsert` / `POST /api/admin/ap/files/delete` が呼び出せること。
  - 手順:
    - `targetArea=images` でダミー画像を `upsert` し、続けて `delete` を実行する。
    - `targetArea=certs` で証明書ファイルを `upsert` する（admin ロール）。
    - `targetArea=certs` を maintenance ロール相当で実行し、権限拒否を確認する。
    - `../` を含む不正パス、範囲外パス（例: `/logs/...`）を送信して拒否を確認する。
  - 期待結果:
    - `/images` は更新/削除が成功する。
    - `/certs` は admin 権限時のみ成功し、非adminは拒否される。
    - 不正パスは拒否され、管理領域外へ書込みされない。
- 7064: AP/管理者/サービスパスワード変更推奨運用試験（初期値変更 + 監査ログ）
  - 事前条件:
    - AP Web UI ロール別パスワード、LocalServer 管理者パスワード、サービス認証情報（MQTT/DB/外部連携）の変更手段（UI または API）が実装済みであること。
    - 監査ログ取得手段が利用可能であること。
  - 手順:
    - 初期値のままログインできる状態を確認した後、`POST /api/admin/auth/password/change` で LocalServer 管理者パスワードを変更する。
    - `POST /api/admin/ap/password/change` で APロール（`user` / `maintenance` / `admin` / `mfg`）のいずれかを変更する。
    - 必要に応じて `POST /api/admin/credentials/rotation` で例外運用（固定値継続）の記録を登録する。
    - 旧値での認証失敗と新値での認証成功を確認する。
    - 例外運用として固定値継続フラグまたは同等申告を行い、理由・対象・期間の記録を登録する。
    - 監査ログで「変更実施」および「例外継続」の双方が記録されることを確認する。
  - 期待結果:
    - 各パスワードで変更処理が成功し、旧値は利用できず新値のみ有効となる。
    - 固定値継続を選択した場合でも、監査ログへ理由・対象・期間が必須記録される。
    - 監査ログに平文パスワードは出力されない。
- 7065: AP メンテナンス Web UI ログイン必須・RBAC 到達制御試験
  - 事前条件:
    - AP モード機体へ接続できること。
    - `user` / `maintenance` / `admin` / `mfg` のロールが有効であること。
  - 手順:
    - 未認証で設定参照、設定更新、FW更新、画像更新APIへアクセスする。
    - 各ロールで API 到達可否（読取/更新/証明書更新/eFuse操作）を確認する。
  - 期待結果:
    - 未認証は高リスク API へ到達できない。
    - 各ロールは定義済み権限範囲でのみ成功し、越権は拒否される。
- 7066: AP 探索・順次一括メンテナンス試験（複数台）
  - 事前条件:
    - LocalServer / ProductionTool から USB Wi-Fi で AP 探索可能であること。
  - 手順:
    - `POST /api/admin/ap/batch/start`（`targetSsids` 未指定）で `AP-esp32lab-<MAC(no colon)>` を複数台探索する。
    - 「探索 -> 接続 -> ログイン -> 設定更新 -> 再起動要求 -> 次個体」を 1 台ずつ順次実行する。
    - `GET /api/admin/ap/batch/{batchId}` で処理対象/処理済/失敗の状態遷移を確認する。
  - 期待結果:
    - 複数台を同時多重ではなく順次処理できる。
    - 個体ごとの結果（成功/失敗理由）が追跡できる。
- 7067: AP 更新後の最終成功判定試験（再起動後 `status`）
  - 事前条件:
    - 7066 の更新フローが実行可能であること。
  - 手順:
    - AP更新を実行後、再起動前の AP 切断状態と再起動後 `status` topic 応答を比較する（`/api/admin/ap/batch/{batchId}` の結果を参照）。
    - `public_id` / `fwVersion` / `configVersion` を収集して成否判定する。
  - 期待結果:
    - AP切断のみでは成功扱いにしない。
    - 再起動後 `status` 応答で所定項目が収束した場合のみ成功と判定する。
- 7068: `get/trh` 応答試験（BME280・I2C共有）
  - 事前条件:
    - `BME280` を `I2C` モード（`CSB=3.3V`）で配線済みであること。
    - `LCD` は従来どおり `SDA/SCL` 共有接続されていること。
  - 手順:
    - LocalServer または MQTT クライアントから `esp32lab/get/trh/<receiverName>` を publish する。
    - ESP32 シリアルログで `detectBme280Address` と `readEnvironmentSnapshot` の結果を確認する。
    - `esp32lab/notice/trh/<senderName>` を subscribe し、`temperatureC` / `humidityRh` / `pressureHpa` / `sensorAddress` を確認する。
  - 期待結果:
    - `sensorAddress` が `0x76` または `0x77` で検出される。
    - `notice/trh` に温度・湿度・気圧が含まれる。
    - `LCD` 表示処理と同時運用しても `I2C` 競合エラーが常時発生しない。
- 7069: LocalServer `notice/trh` 受信・画面表示・定期取得試験
  - 事前条件:
    - 7068 が合格していること（ESP32 側 `get/trh` → `notice/trh` が動作すること）。
    - LocalServer がビルド済みで起動していること。
  - 手順:
    1. ブラウザで LocalServer Web UI (`http://localhost:3100`) を開く。
    2. ESP32 がオンラインであることを確認し、`status更新` ボタンを押す。
    3. REST API `POST /api/commands/get` で `{"targetNames":["<deviceName>"],"subCommand":"trh"}` を送信する。
    4. Web UI のデバイス一覧テーブルに「温度」「湿度」「気圧」カラムが表示されることを確認する。
    5. ESP32 を再起動し、offline → online 遷移時に自動で `get/trh` が送信されることを LocalServer ログで確認する。
    6. 5分経過後、定期 `get/trh` が自動送信されることを LocalServer ログで確認する。
  - 期待結果:
    - Web UI テーブルに温度(℃)・湿度(%)・気圧(hPa)が表示される。
    - offline → online 遷移時のログに `get/trh sent. targets=<device> reason=offline-to-online` が出力される。
    - 5分間隔のログに `get/trh sent. targets=<device> reason=periodic-5min` が出力される。
    - `GET /api/devices` の応答に `temperatureC`、`humidityRh`、`pressureHpa`、`environmentUpdatedAt` が含まれる。
- 7090: 安全消去試験（003-0018）
  - 事前バックアップ（k-user 再発行回避用）:
    - [厳守] アンインストール前に LocalServer を停止し、`scripts/backup-k-user-for-7090.ps1` を実行する。
    - [厳守] keyStore.json（k-user のバックアップ用ファイル。暗号化済みであり、生の k-user ではない）をインストールルート外へ退避する。
    - バックアップ先: `%LOCALAPPDATA%\IoT-LocalServer-Backup-7090\YYYYMMDD-HHMMSS\keyStore.json`
    - 試験後復元: `scripts/restore-k-user-after-7090.ps1` で復元するか、手動で `data\keyStore.json` へコピーする。
  - 手順: LocalServer または ProductionTool のアンインストールを実行し、`安全消去ポリシー.md` 4.1 / 4.2 の手順に従う。消去前後で対象ファイルの存在と監査ログを確認する。
  - [重要][2026-03-22] ProductionTool 側では `scripts/uninstall-production-tool.ps1` と Inno Setup の `UninstallRun` を対象に、`C:\ProgramData\IoT\ProductionTool\keys\`、`work\`、`logs\audit\`、`C:\ProgramData\IoT\InstallerAudit\ProductionTool\` の成否を確認する。
  - [推奨][2026-03-23] 実インストール前の前段確認として、`ProductionTool/tests/testProductionToolInstallHelperSmoke.ps1` を実行し、`ProductionTool.exe`、設定、補助スクリプト、`ProgramData` 配下の `logs\audit` / `work` / `keys` が揃うことを確認してよい。
  - [推奨][2026-03-23] 実インストール前の前倒し確認として、`ProductionTool/tests/test7090ProductionToolUninstallSmoke.ps1` を実行し、一時ディレクトリ上の擬似 `installRoot` / 擬似 `ProgramData` に対して安全消去と監査ログ生成が成立することを確認してよい。
  - 期待結果:
    - 消去対象（keyStore、securityState、settings、logs、uploads、certs 等）が定義どおり消去される。
    - 機密対象が上書き消去されていること（通常削除のみでない）。
    - 監査ログに消去対象・結果・日時が記録されている。
- 7070: SecretCore k-user インポート・動作検証試験
  - 関連機能ID: F3-090〜F3-094
  - 前提条件:
    - TS版 `keyStore.json` に k-user が発行済み。
    - ESP32 に TS版 k-user から導出した k-device が書き込み済み。
  - 手順:
    1. LocalServer を停止する。
    2. SecretCore を単独で起動する。
    3. `node scripts/migrateKUserToSecretCore.mjs` を実行する。
    4. スクリプト出力で k-user 抽出成功、`import_k_user` 成功、k-device 一致検証成功を確認する。
    5. `server.ts` の `USE_SECRET_CORE = true` を確認して LocalServer を起動する。
    6. ESP32 が online になり、`notice/status` と `notice/trh` が復号エラーなく受信されることをログで確認する。
    7. `GET /api/devices` で `temperatureC`、`humidityRh`、`pressureHpa` が取得できることを確認する。
  - 期待結果:
    - マイグレーションスクリプトが `検証成功!` と表示される。
    - LocalServer ログに `Decryption error` が出力されない。
    - SecretCore 経由で暗号化/復号が正常に行われ、従来の TS版 keyService と同等の動作となる。
    - `wrapped_k_user.bin` が `data/` に生成されている。
- 7071: SecretCore k-user バックアップ出力/復元試験（パスワード暗号化）
  - 関連機能ID: F3-095〜F3-096
  - 前提条件:
    - SecretCore が起動中。
    - `wrapped_k_user.bin` に有効な k-user が保存済み。
  - 手順:
    1. `node scripts/exportKUserBackupFromSecretCore.mjs --password "<強固なパスワード>"` を実行する。
    2. 出力された `backupFilePath` にバックアップファイルが生成されることを確認する。
    3. `wrapped_k_user.bin` を退避したうえで SecretCore を再起動する。
    4. `node scripts/importKUserBackupToSecretCore.mjs --password "<同じパスワード>" --in "<バックアップファイル>"` を実行する。
    5. `get_k_device` の fingerprint が復元前と一致することを確認する。
  - 期待結果:
    - `export_k_user` が成功し、raw `k-user` がログ/応答へ出力されない。
    - バックアップファイル形式は `scrypt + AES-256-GCM` を含むJSONである。
    - `import_k_user_backup` 成功後、`source=imported (wrapped_k_user.bin)` となる。
    - 復元前後で同一 `targetDeviceName` の `k-device` が一致する。
- 7072: SecretCore 境界IF固定試験
  - 関連機能ID: F3-097〜F3-098
  - 前提条件:
    - `LocalServer` ソースが最新である。
  - 手順:
    1. `LocalServer/src` を確認し、業務コードから `SecretCoreIpcClient.sendRequest()` の直接呼出しがないことを確認する。
    2. `SecretCoreFacade` を経由して `issue_k_user` / `get_k_user_status` / `encrypt` / `decrypt` が呼ばれることを確認する。
    3. `GET /api/admin/keys/k-user/status` と `POST /api/admin/keys/k-user/issue` の応答に raw key が含まれないことを確認する。
  - 期待結果:
    - `SecretCoreIpcClient.sendRequest()` の直接利用は `SecretCoreFacade` 内に限定される。
    - `keyFingerprint` は返るが raw `k-user` は返らない。
    - UI / REST handler が low-level IPC コマンド名へ依存しない。
- 7073: SecretCore IPC 保護試験
  - 関連機能ID: F3-099〜F3-100
  - 前提条件:
    - `USE_SECRET_CORE = true`
    - LocalServer が SecretCore を子プロセス起動する構成である。
  - 手順:
    1. LocalServer を起動する。
    2. LocalServer ログで `SecretCore IPC protection mode=secure` が出力されることを確認する。
    3. ログでランダム Named Pipe 名（`iot-secret-core-<machineId>-<sessionId>`）が出力されることを確認する。
    4. `GET /api/devices` を実行し、ESP32 の `status` / `trh` が取得できることを確認する。
    5. LocalServer ログに `Failed to parse IPC response`、`Unsupported state or unable to authenticate data`、`IPC protection error` が出ていないことを確認する。
  - 期待結果:
    - LocalServer と SecretCore の通信が保護モードで成立する。
    - 保護付き IPC のまま MQTT strict 通信と BME280 受信が正常動作する。
    - 起動ごとに Pipe 名が固定値ではなく変化する。
- 7074: SecretCore MQTT command publish Stage1 試験
  - 関連機能ID: F3-101〜F3-102
  - 前提条件:
    - `USE_SECRET_CORE = true`
    - `LocalServer` が `mqttGateway: publish transport mode=rust` で起動している。
    - ESP32 実機が MQTT strict で接続可能である。
  - 手順:
    1. LocalServer を起動し、ログで `SecretCore ready. attempt=` が出た後に `MQTT connected.` となることを確認する。
    2. `POST /api/commands/status` body=`{"targetNames":"all"}` を実行する。
    3. `POST /api/commands/get` body=`{"targetNames":"all","subCommand":"trh","args":{}}` を実行する。
    4. `GET /api/devices` を実行し、対象デバイスの `onlineState=online` と `temperatureC` / `humidityRh` / `pressureHpa` / `environmentSensorAddress=0x76` を確認する。
    5. LocalServer ログに `mqtt_publish failed` や `connect ENOENT` が継続残留せず、起動後は Rust publish 経由で status / get/trh が成立することを確認する。
  - 期待結果:
    - command publish が TypeScript 直 publish ではなく `SecretCore` 経由で成立する。
    - `SecretCore` readiness 待機後に MQTT 接続が始まり、起動直後レースによる受信失敗が継続しない。
    - `GET /api/devices` で status と BME280 値が取得できる。
- 7075: SecretCore MQTT notice subscribe Stage2 起動試験
  - 関連機能ID: F3-103〜F3-104
  - 前提条件:
    - `USE_SECRET_CORE = true`
    - `LocalServer` が `mqttGateway: transport mode=rust` で起動している。
  - 手順:
    1. LocalServer を起動し、ログで `mqttGateway: rust receiver start result.` が出力されることを確認する。
    2. `GET /api/devices` を実行し、Rust subscribe で受けた retained notice または最新 notice が `DeviceRegistry` に反映されることを確認する。
    3. 必要に応じて `POST /api/commands/status` を実行し、受信待ち状態へ移行できることを確認する。
    4. LocalServer ログに TS `mqtt.connect()` 由来の subscribe 設定ログが不要な状態で、Rust bridge 経由の受信処理が継続することを確認する。
  - 期待結果:
    - `mqtt_start_receiver` により Rust 側 subscribe 常駐ループが開始される。
    - `mqtt_drain_events` 経由の受信イベントで `DeviceRegistry` が更新される。
    - TS 側は broker へ直接 subscribe せず、Rust bridge からのイベント drain で通常監視を継続できる。
- 7076: SecretCore MQTT 受信DTO正規化 Stage3 起動試験
  - 関連機能ID: F3-105〜F3-106
  - 前提条件:
    - `USE_SECRET_CORE = true`
    - `LocalServer` が `mqttGateway: transport mode=rust` で起動している。
  - 手順:
    1. LocalServer を起動し、`GET /api/devices` を実行する。
    2. retained notice または最新 notice により、`DeviceRegistry` が更新されることを確認する。
    3. LocalServer 側コードで Rust モード時に `handleIncomingTopicPayload()` を経由せず、Rust から返る `statusUpdated` / `trhUpdated` / `otaProgressUpdated` / `secureEchoUpdated` DTO で反映していることを確認する。
    4. 必要に応じて `POST /api/commands/status` / `POST /api/commands/get` を実行し、送信系と受信系がともに Rust bridge 中心で動作することを確認する。
  - 期待結果:
    - `SecretCore` が受信payloadを `k-device` で復号し、最小DTOへ正規化して返す。
    - TS 側は raw topic/payload の復号・正規化を行わず、DTO のみで通常監視を継続できる。
    - retained notice のみでも起動直後の状態一覧更新が成立する。
- 7077: SecretCore `deviceState` 完成スナップショット Stage4 実機試験
  - 関連機能ID: F3-107〜F3-108
  - 前提条件:
    - `USE_SECRET_CORE = true`
    - `LocalServer` が `mqttGateway: transport mode=rust` で起動している。
    - ESP32 実機が online 応答可能である。
  - 手順:
    1. LocalServer を起動し、`GET /api/devices` を確認する。
    2. `POST /api/commands/status` と `POST /api/commands/get`（`subCommand=trh`）を実行する。
    3. `GET /api/devices` で `onlineState=online`、`detail=Reply`、`lastStatusSub=reply`、`temperatureC`、`humidityRh`、`pressureHpa`、`environmentSensorAddress=0x76` を確認する。
    4. Rust モード時の `mqttGateway` が `deviceState` スナップショットを `registry.upsertDeviceState()` で保持していることをコード確認する。
  - 期待結果:
    - `SecretCore` が `status/trh` を統合した `deviceState` を返す。
    - TS 側は `updateByStatus/updateByTrh/updateByOtaProgress` に依存せず、完成スナップショット保持で一覧更新できる。
    - 実機 online + BME280 値が Rust スナップショット経由で一覧へ反映される。
- 7078: SecretCore `offline timeout` Stage5 実機試験
  - 関連機能ID: F3-109〜F3-110
  - 前提条件:
    - `USE_SECRET_CORE = true`
    - `MQTT_TRANSPORT_MODE = "rust"`
    - `STATUS_OFFLINE_TIMEOUT_SECONDS = 90`（または明示値）
    - ESP32 実機が一度 online 応答し、その後 90 秒超 notice/status を送らないこと
  - 手順:
    1. LocalServer を起動し、`GET /api/devices` で対象機が `online` になることを確認する。
    2. `lastSeenAt` を記録し、timeout 秒数を超えるまで待機する。
    3. `GET /api/devices` を再度実行し、対象機が `offline` へ遷移していることを確認する。
    4. `detail=Offline timeout`、`lastStatusSub=timeout`、`onlineState=offline` を確認する。
    5. TS 側 `DeviceRegistry` が Rust モードで `updateOfflineByTimeout()` を実行しないことをコード確認する。
  - 期待結果:
    - `SecretCore` の `mqtt_drain_events` が `deviceStateUpdated` を返し、TS 側はその snapshot を保持する。
    - TS 側ローカル timeout を無効化していても、一覧表示が `offline timeout` へ正しく遷移する。
- 7079: SecretCore `wait_for_status_recovery` Stage6 単体IPC試験
  - 関連機能ID: F3-111〜F3-112
  - 前提条件:
    - `SecretCoreManager` / `SecretCoreIpcClient` / `SecretCoreFacade` を利用可能である。
    - MQTT notice subscribe により `deviceState` が一度でも online 更新されていること。
  - 手順:
    1. 一時的な `SecretCoreManager` を起動する。
    2. `SecretCoreFacade.startMqttReceiver()` を呼ぶ。
    3. `SecretCoreFacade.waitForStatusRecovery("", 15)` を呼び、任意 online 機の復帰結果を待つ。
    4. `deviceName`、`publicId`、`firmwareVersion`、`configVersion` を確認する。
  - 期待結果:
    - `wait_for_status_recovery` が timeout せず成功する。
    - 応答は TS 側 `registry.listDevices()` polling に依存せず、Rust 側 `deviceState` から返る。
- 7080: SecretCore OTA workflow Stage7 起動・状態取得試験
  - 関連機能ID: F3-113〜F3-114
  - 前提条件:
    - `USE_SECRET_CORE = true`
    - 管理者認証済み
    - 単一対象機向け OTA 引数が解決できる
  - 手順:
    1. `POST /api/workflows/signed-ota/start` を単一 target で呼ぶ。
    2. 応答の `workflow.workflowId` と `workflow.state=queued` を確認する。
    3. `GET /api/workflows/{workflowId}` を呼び、`running` / `waiting_device` / `verifying` / `completed` / `failed` のいずれかへ遷移することを確認する。
  - 期待結果:
    - TS 側は OTA 開始後の逐次手順を保持せず、Rust workflow の状態だけを返す。
    - workflow 状態取得APIが `workflowId`、`state`、`result`、`errorSummary`、補助 detail の最小情報だけを返す。
- 7081: LocalServer 基本機能 連続安定試験
  - 関連タスク: `003-0020`
  - 前提条件:
    - LocalServer / ESP32 / MQTT / AP メンテナンス導線が通常起動可能であること。
    - `status`、`trh`、APログイン、AP設定、再起動、復旧導線が少なくとも単発では成立していること。
  - [推奨] `IoT/LocalServer/scripts/test7081BasicStability.mjs` を使って半自動実行可能。`npm run test:7081 -- --iterations 10`。実機未接続時は `--skipOta --skipAp` で最小セット（認証・一覧・status・trh）のみ実施。
  - 手順:
    1. 管理者認証、一覧表示、`status`、`trh`、単一対象 OTA workflow、APログイン、AP設定取得、AP設定反映、再起動、復旧確認を 1 セットとして定義する。
    2. 上記 1 セットを連続 10 回以上実施する。
    3. 各セットで API 応答、UI 反映、online/offline 復帰、ログ異常有無を確認する。
  - 期待結果:
    - 連続実行しても abort、watchdog、stack canary、復帰不能が発生しない。
    - 基本操作の成否が回ごとにぶれず、再起動後の復旧導線も維持される。
- 7082: LocalServer セキュア機能 end-to-end 固定試験
  - 関連タスク: `003-0021`
  - 前提条件:
    - `USE_SECRET_CORE = true`
    - signed OTA workflow、Pairing workflow、鍵バックアップ/復元、AP secure transport がコード上で到達可能であること。
  - 手順:
    1. `runSignedOtaCommand()` workflow を実行し、状態遷移と完了判定を確認する。
    2. `runPairingSession()` workflow について、precheck、session metadata、bundle summary、transport session、P-256 handshake、encrypted bundle 本体送達（`/api/pairing/secure-bundle`）、`state=applied`、workflow `completed(OK)` までの状態詳細を確認する。
    3. `export_k_user` / `import_k_user_backup` を実行し、復元後も継続利用できることを確認する。
    4. TS / REST 応答 / 画面表示に raw `k-device`、ECDH 共有秘密、`k-pairing-session` が出ていないことを確認する。
  - 期待結果:
    - 高リスク経路の主体が `SecretCore` に固定され、TS 側は開始要求・状態表示・結果表示だけを担当する。
    - 中間秘密や平文秘密値が TS や REST 応答へ露出しない。
- 7083: OTA耐久試験（10回連続）
  - 関連タスク: `003-0022`
  - 前提条件:
    - OTA HTTPS 配布、署名付き OTA workflow、版数同期手順が成立していること。
    - 試験ごとに投入版数、戻し版数、manifest 版数、実機版数を記録できること。
    - [推奨] `IoT/LocalServer/scripts/bump-esp32-beta-version.ps1`、`python -m platformio run -e esp32s3_secure`、`POST /api/workflows/signed-ota/start`、`GET /api/workflows/{workflowId}` の手順が再現可能であること。
    - [推奨] `IoT/LocalServer/scripts/test7083OtaDurability.mjs` を使って workflow 履歴と結果 JSON を採取できること。
  - 手順:
    1. 試験対象機を 1 台固定し、開始前に現在版数、`runningPartition`、`bootPartition`、`nextUpdatePartition`、`onlineState` を記録する。
    2. 各回で `bump-esp32-beta-version.ps1` により OTA 対象版数を 1 つ進め、`python -m platformio run -e esp32s3_secure` で `firmware.bin` を再生成する。
    3. `GET /api/settings` と `GET /ota/manifest.json` で配布版数・`sha256` を確認し、実機版数と不整合がないことを確認する。
    4. 管理者認証後、`POST /api/workflows/signed-ota/start` を単一 target で実行し、`workflowId` を取得する。  
       [推奨] `npm run test:7083 -- --iterations 10 --targetDeviceName <deviceName>` を使用し、各回の workflow 履歴を JSON で保存する。
    5. `GET /api/workflows/{workflowId}` を polling し、`queued` / `waiting_device` / `verifying` / `completed` または `failed` の遷移を記録する。
    6. `POST /api/commands/status` 実行後、`GET /api/devices` で `firmwareVersion`、`onlineState=online`、`otaPhase=done`、`otaProgressPercent=100`、パーティション状態を確認する。
    7. 上記を 10 回連続で実施し、各回の所要時間、失敗有無、復旧要否、シリアルログ要点を記録する。
  - 記録必須項目:
    - 実施回（1/10〜10/10）
    - 開始版数 / 配布版数 / 完了版数
    - `workflowId`
    - workflow 状態遷移
    - `runningPartition` / `bootPartition` / `nextUpdatePartition`
    - 所要時間
    - 異常ログ有無（abort / watchdog / stack canary / reboot loop / 復帰不能）
    - 復旧操作の有無
    - `LocalServer/logs/test-reports/test7083-*.json` 保存先
  - 停止条件:
    - 1 回でも `failed`、abort、watchdog、stack canary、reboot loop、復帰不能が出た時点で試験を中断し、`問題点記録書.md` へ転記する。
  - 期待結果:
    - 10 回すべて成功し、abort、watchdog、stack canary、reboot loop、復帰不能が発生しない。
    - 1 回でも失敗した場合は 006章/007章への進入条件を満たさない。
- 7084: 1時間過負荷試験（1時間×10セット）
  - 関連タスク: `003-0023`
  - 前提条件:
    - 負荷パターンを再現するスクリプトまたは手順があること。
    - メモリ使用量、stack 高水位、online/offline 復帰、AP復旧可否を観測できること。
    - [推奨] `IoT/LocalServer/scripts/test7084OneHourLoad.mjs`、`POST /api/commands/status`、`POST /api/admin/commands/secure-ping`、`GET /api/devices` を併用できること。
  - 手順:
    1. 1 セット 60 分とし、対象機 1 台または最小複数台構成を固定する。
    2. セット中は以下を繰返す。
       - `status` 取得
       - `get/trh` 応答確認
       - `secure-ping`
       - 一覧画面 polling
       - OTA配布口待受確認
       - AP 切替と復旧確認（必要回数を事前定義）
       - Rust workflow 起動可否確認（signed OTA または Pairing の到達確認）
      - [推奨] `npm run test:7084 -- --sets 10 --setDurationMinutes 60 --targetDeviceName <deviceName>` を使用し、`status` / `secure-ping` / `devices` snapshot の観測 JSON を保存する。
    3. 15 分ごとに `GET /api/devices`、必要ならシリアルログ、AP 復旧導線、MQTT 復帰を確認する。
    4. 各セット終了時に heap、stack 高水位、温湿度応答、online/offline 状態、再起動有無、異常ログ有無を記録する。
    5. 上記を 10 セット実施する。
  - 記録必須項目:
    - セット番号（1/10〜10/10）
    - 実施日時（開始/終了）
    - 負荷内容（実行した操作種別と回数）
    - `GET /api/devices` 観測結果
    - heap / stack 高水位の観測値
    - `trh` 応答有無
    - AP切替 / MQTT復帰 / workflow 起動可否
    - abort / watchdog / stack canary / deadlock / 復帰不能の有無
    - `LocalServer/logs/test-reports/test7084-*.json` 保存先
  - 停止条件:
    - 1 セットでも abort、watchdog、stack canary、deadlock、通信復帰不能が出た時点で試験を中断し、`問題点記録書.md` へ転記する。
  - 期待結果:
    - 10 セットすべてで abort、watchdog、stack canary、deadlock、通信復帰不能が発生しない。
    - 1 セットでも異常が出た場合は 006章/007章への進入条件を満たさない。
- 7085: Secure Boot / Flash Encryption / eFuse 進入判定ゲート試験
  - 関連タスク: `003-0024`
  - [厳守] 判定条件の親定義は `todo.md` の `004-0010` と `003-0024` を採用し、他文書はこの条件に追従する。
  - [重要][2026-03-22] 本試験の差し止め対象は `006-0002` 以降および `007` 章であり、`006-0001` のチェックリスト整備自体は差し止め対象に含めない。
  - 前提条件:
    - `003-0004`, `003-0012`, `003-0020`, `003-0021`, `003-0022`, `003-0023` の完了条件が揃っていること。
    - `004-0010`（`ProductionTool` 基本機能安定化ゲート）が完了していること（必須: `004-0008`, `004-0009`, `005-0001`〜`005-0005`, `003-0016`, `009-1021`）。
    - `009-0019` が encrypted bundle 本体送達 / 復号 / NVS 保存まで到達していること。
    - `005` 章の ProductionTool 実行導線に必要な設計が `005-0001`〜`005-0007` まで揃っていること。`005-0008` はフェーズFリハーサル時の保留項目として除外されていること。
    - `006-0001` として `eFuse適用チェックリスト.md` が整備済みであること。
    - `004-0004` に対応する `k-iot-ota-sign` 試験手順が `コマンド仕様書.md` へ記載され、`7016` / `7017` / `7018` で検証可能であること。
  - 手順:
    1. 上記前提の実施証跡を確認する。
    2. `eFuse適用チェックリスト.md` を参照し、文書・試験ゲート確認欄が未記入のまま運用開始されないことを確認する。
    3. `todo.md`、`試験記録書.md`、関連仕様書の更新漏れがないことを確認する。
    4. 未達項目がある状態で `006-0002` 以降または 007章へ進もうとした場合、開始しないことを確認する。
  - 期待結果:
    - 進入条件がすべて揃った場合のみ `006-0002` 以降および 007章へ進める。
    - 未達が 1 件でもある場合は、Secure Boot / Flash Encryption / eFuse 着手を禁止できる。
- 7086: `ProductionTool` 最小実行骨格 起動・分離試験
  - 関連タスク: `004-0008`
  - 前提条件:
    - `IoT/ProductionTool` の最小起動物が存在し、`LocalServer` と別プロセス・別配布物として起動できること。
    - `SecretCore` 共通部の利用有無、接続先、監査ログ出力先が文書化されていること。
  - 手順:
    1. `LocalServer` を通常起動した状態と、停止した状態の両方で `ProductionTool` を起動し、起動要件を確認する。
    2. `ProductionTool` が `LocalServer` と混在UIではなく、独立した起動画面、追加認証入口、対象機表示領域、監査ログ出力導線を持つことを確認する。
    3. `SecretCore` 共通部との接続初期化に失敗した場合、不可逆処理へ進まず安全停止することを確認する。
    4. [厳守] Rust 共通化が `LocalServer` と `ProductionTool` のソフト統合ではなく、共通部再利用として設計・実装されていることを確認する。
    5. [厳守] eFuse / Secure Boot / Flash Encryption の本実行ボタンや最終確定導線を、この試験では使用しない。
  - 記録必須項目:
    - 起動物名 / 版数
    - `LocalServer` 起動有無ごとの結果
    - `SecretCore` 共通部接続結果
    - 追加認証入口表示有無
    - 対象機一覧または識別情報表示有無
    - 監査ログ出力先
    - 安全停止挙動
  - 停止条件:
    - 起動不能、`LocalServer` と混在起動、`SecretCore` 接続失敗時の unsafe 継続、不可逆処理導線の露出が確認された時点で中断する。
  - 期待結果:
    - `ProductionTool` が `LocalServer` と分離した最小起動物として安定起動し、追加認証入口・状態表示・監査ログ導線まで確認できる。
    - Rust 共通化の前提が「共通部再利用」であり、両ソフトが独立動作していることを確認できる。
- 7087: `ProductionTool` 基本機能 スモーク試験
  - 関連タスク: `004-0009`
  - 前提条件:
    - `7086` が OK であること。
    - `005-0001`〜`005-0005` の設計が文書化され、通常系・異常系の観測観点が整理されていること。
  - 手順:
    1. 起動、追加認証、対象機選択、状態表示、監査ログ、dry-run 導線を順に実行する。
    2. 誤パスワード、対象機未選択、対象機取り違え防止、監査ログ出力失敗などの異常系で、安全停止と明確なエラー表示を確認する。
    3. 対象機確認、実行内容確認、最終確認までのウィザード遷移を確認し、不可逆処理は dry-run または最終確認直前で停止する。
    4. `PT-003` の `対象機確認文言` について、以下を確認する。
      - 許可文字外（記号/小文字/空白含む）を拒否すること。
      - 8文字未満、32文字超を拒否すること。
      - 表示文言と完全一致時のみ次画面へ進めること。
      - 3回連続不一致時に監査ログへ閾値イベントが記録されること。
    5. [厳守] raw key、中間秘密、平文認証情報、eFuse 実値を UI / ログ / REST 応答へ出さないことを確認する。
  - 記録必須項目:
    - 通常系の実施結果
    - 異常系の実施結果
    - 追加認証成功 / 失敗時の挙動
    - 対象機確認 / 再入力ルールの動作
    - 対象機確認文言ルール（許可文字・桁数・一致判定・不一致時挙動）
    - 監査ログの文言照合イベント記録有無（実値非保存）
    - 監査ログ記録有無
    - dry-run 停止点
    - 秘密値非露出確認結果
  - 停止条件:
    - 追加認証バイパス、対象機誤選択の防止失敗、監査ログ未記録、秘密値露出、dry-run を超えた不可逆処理到達が確認された時点で中断する。
  - 期待結果:
    - `ProductionTool` の通常系・異常系スモーク試験が成立し、不可逆処理本体へ入らずに基礎品質を確認できる。
- 7088: `ProductionTool` 基本機能安定化ゲート試験
  - 関連タスク: `004-0010`
  - [厳守] 判定条件の親定義は `todo.md` の `004-0010` を採用し、`7088` はその証跡確認として扱う。
  - 前提条件:
    - `7086`, `7087` が OK であること。
    - `8001`（MQTT認証失敗異常系）が OK であること。
    - `005-0001`〜`005-0005`、`003-0016`、`009-1021` の関連設計・配布方針が更新済みであること。
  - 手順:
    1. `7086`, `7087` の結果、関連仕様書、配布・インストーラ方針、監査ログ方針を突き合わせて更新漏れがないことを確認する。
    2. `ProductionTool` の基本機能が「起動できる」だけでなく、「試験項目が固定され、工場端末へ展開する前提が崩れていない」ことを確認する。
    3. 未達項目がある状態で `004-0001`〜`004-0007` や `005-0006`〜`005-0008` に進もうとした場合、着手しないことを確認する。
  - 記録必須項目:
    - `7086` 結果
    - `7087` 結果
    - `8001` 結果
    - `005-0001`〜`005-0005` 完了有無
    - `003-0016` / `009-1021` 方針確定有無
    - 文書更新有無
    - 判定
  - 期待結果:
    - `ProductionTool` 基本機能の安定化ゲートが OK となった場合のみ、試験用鍵・署名素材手順と不可逆処理導線の固定へ進める。
- 8001: 異常系（MQTT認証失敗）
  - 手順: `.env` の `MQTT_PASSWORD` を誤値にして起動。
  - 期待結果: 接続失敗ログが出力され、プロセスが異常終了しない。

## 3. 変更履歴
- 2026-03-22: 7041 strict 判定の合格条件を `READY_FOR_FULL` または `FULL_COMPLETED` に整理。理由: full 正常完了後も strict 成功として扱えるようにするため。
- 2026-03-22: 7041 pipeline の preflight/full レポート参照を実行レポート（`test7041-YYYY...json`）に限定。理由: gate 判定が非実行レポートを誤参照しないようにするため。
- 2026-03-22: 7041 の標準手順を gate 内蔵 pipeline 前提へ更新し、`pipeline:strict` による1コマンド strict 判定を追加。理由: 実行手順と判定手順を分離せずに一体運用するため。
- 2026-03-22: 7041 の標準手順を `pipeline` 内蔵 gate 前提（`preflight -> full -> summary -> gate`）へ更新し、strict 判定導線を `pipeline:strict` 優先へ整理。理由: 実行順と判定順を1コマンドへ集約し、運用ミスを減らすため。
- 2026-03-22: 7041 手順へ `test:7041:gate:strict` を追加。理由: 自動判定系で `READY_FOR_FULL` 到達を厳密に判定できるようにするため。
- 2026-03-22: 7041 手順へ `test:7041:gate` を追加し、記録必須項目へ `test7041-gate-*.json` を追加。理由: pipeline 実行後の進行可否を機械判定し、次作業への遷移基準を固定するため。
- 2026-03-22: 7041 の記録必須項目へ `test7041-pipeline-*.json` を追加。理由: 3段階実行の判定（preflight/full/summary）を1ファイルで追跡し、試験記録転記の漏れを防止するため。
- 2026-03-22: 7041 を `pipeline` / `pipeline:wait` / `summary` 前提の手順へ更新し、失敗分類・診断キー・比較レポートを記録必須項目へ追加。理由: AP未接続期間でも再判定と証跡比較を継続し、復旧後に同一手順で full 試験へ移行するため。
- 2026-03-22: `7085` に `006-0001` チェックリスト整備との関係、`005-0008` 保留扱い、差し止め対象を `006-0002` 以降および `007` 章へ限定する方針を追記。理由: `003-0024` を「進入判定条件の固定」として完了化しつつ、不可逆処理本体の開始条件は別途厳格に維持するため。
- 2026-03-16: `7082` の Pairing 観点を更新し、`/api/pairing/secure-bundle` による encrypted bundle 本体送達、`state=applied`、workflow `completed(OK)` の確認を追加。理由: `009-0019` の実装到達点を試験仕様へ同期するため。
- 2026-03-19: `7085` 前提条件へ `004-0004`（`k-iot-ota-sign` 試験手順と 7016/7017/7018 検証可能性）を追記。理由: 004-0004 の完了判定を試験仕様へ明確に接続するため。
- 2026-03-19: `7087` 手順/記録項目へ `PT-003` 対象機確認文言ルール（許可文字、桁数、一致判定、3回不一致時イベント）を追加。理由: 005-0005 の完了条件を試験仕様で判定可能にするため。
- 2026-03-19: `7085` / `7088` に判定条件の親定義（`todo.md` `004-0010`）を追記し、`7088` の前提条件・記録必須項目へ `8001` を追加。理由: 004-0010 ゲート判定と試験仕様の整合を固定するため。
- 2026-03-16: `IoT/ProductionTool` へ名称統一し、Rust 共通化は `LocalServer` 側共通部の再利用であってソフト統合ではないことを `7086` へ追記。理由: `ProductionTool` 名称統一と、独立動作前提の試験観点を固定するため。
- 2026-03-16: `7086`〜`7088`（`ProductionTool` 最小起動骨格、基本機能スモーク、基本機能安定化ゲート）を追加し、`7085` の前提条件へ `004-0010` を追記。理由: `todo.md` の `004-0008`〜`004-0010` と `003-0024` を試験仕様側でも 1 対 1 に近い形で受けられるようにするため。
- 2026-03-18: `7081` に `test7081BasicStability.mjs` の利用前提と実行例を追記。理由: `003-0020` の半自動試験導線を試験仕様から固定するため。
- 2026-03-16: `test7083OtaDurability.mjs` / `test7084OneHourLoad.mjs` の利用前提を 7083 / 7084 へ追記。理由: workflow 履歴、device snapshot、JSON レポート保存先を試験仕様から直接辿れるようにするため。
- 2026-03-16: 7081〜7085（LocalServer 基本機能連続安定、セキュア機能固定、OTA耐久10回、1時間過負荷10セット、Secure Boot / eFuse 進入判定ゲート）を追加。理由: `todo.md` の `003-0020`〜`003-0024` と 1 対 1 に近い形で試験ゲートを固定し、006章/007章へ進む前提を文書でも明確化するため。
- 2026-03-15: 7080（SecretCore OTA workflow Stage7 起動・状態取得試験）を追加。理由: `003-0012` の OTA workflow 移行開始を試験観点へ固定するため。
- 2026-03-15: 7079（SecretCore `wait_for_status_recovery` Stage6 単体IPC試験）を追加。理由: `003-0012` の完了判定監視責務移行を試験観点へ固定するため。
- 2026-03-15: 7078（SecretCore `offline timeout` Stage5 実機試験）を追加。理由: `003-0012` の状態遷移責務移行を試験観点へ固定するため。
- 2026-03-15: 7077（SecretCore `deviceState` 完成スナップショット Stage4 実機試験）を追加。理由: `003-0012` の状態統合責務移行を試験観点へ固定するため。
- 2026-03-15: 7076（SecretCore MQTT 受信DTO正規化 Stage3 起動試験）を追加。理由: `003-0012` の受信payload処理移行開始を試験観点へ固定するため。
- 2026-03-15: 7075（SecretCore MQTT notice subscribe Stage2 起動試験）を追加。理由: `003-0012` の subscribe Rust化開始を試験観点へ固定するため。
- 2026-03-15: 7074（SecretCore MQTT command publish Stage1 試験）を追加。理由: `003-0012` の最初の通信Rust化と readiness 待機の実動確認を試験で担保するため。
- 2026-03-15: 7073（SecretCore IPC 保護試験）を追加。理由: `003-0014` 実装の起動確認と E2E 動作を試験で担保するため。
- 2026-03-15: 7072（SecretCore 境界IF固定試験）を追加。理由: `003-0013` の実装結果をコード・APIの両面で固定するため。
- 2026-03-15: 7071（SecretCore k-user バックアップ出力/復元試験）を追加。理由: Rust 側の `export_k_user` / `import_k_user_backup` 実装を試験で担保するため。
- 2026-03-15: 7070（SecretCore k-user インポート・動作検証試験）を追加。理由: TS版 k-user の SecretCore 移植後に暗号化/復号が正常動作することを試験で担保するため（Phase2完了）。
- 2026-03-15: 7069（LocalServer `notice/trh` 受信・画面表示・定期取得試験）を追加。理由: LocalServer 側の受信→画面反映→定期送信→offline→online 自動送信を試験観点へ固定するため。
- 2026-03-15: 7068（`get/trh` 応答試験: BME280 / I2C共有 / `notice/trh` / `sensorAddress`）を追加。理由: ESP32 側の温湿度・気圧取得実装と配線前提を試験観点へ固定するため。
- 2026-03-15: 7090 に k-user バックアップ（keyStore.json 退避）・復元手順を追記。理由: 試験後の k-user 再発行を不要にするため。
- 2026-03-15: [003-0018] 7090（安全消去試験）を追加。理由: アンインストール時の機密消去を試験で担保するため。
- 2026-03-13: 7065〜7067（APログイン必須/RBAC、AP探索と順次一括メンテ、再起動後`status`最終判定）を追加。理由: `002-0019`〜`002-0023` の完了判定を試験IDで追跡可能にするため。
- 2026-03-13: 7064（初期/各種パスワードの変更推奨運用と監査ログ）を追加。理由: 初期パスワード方針を「禁止」から「推奨」へ変更しつつ、他パスワード類も同一運用で試験担保するため。
- 2026-03-13: 7049 に「短時間再現時の一時しきい値縮小」と「試験後の本番値復元必須」を追記。理由: 実機試験の再現性と運用安全性を同時に担保するため。
- 2026-03-13: 7063（APモード局所ファイル更新: `/images` `/certs`）を追加。理由: `002-0009` 実装（AP向け局所更新API）に対する正常系/権限制御/スコープ拒否の試験観点を固定するため。
- 2026-03-13: 7028 の手順を AP運用実装（`/api/admin/ap/configure`）に合わせて具体化。理由: `002-0014` の実機再現手順を固定し、次回実施時の解釈揺れを防ぐため。
- 2026-03-13: 7061 の分割パラメータ説明を MQTT 上限追従の動的分割へ更新。理由: 固定 `chunkSizeBytes=1024` ではなく「MQTT送信可能サイズに合わせて自動調整する」運用方針へ合わせるため。
- 2026-03-13: 7061 を 100KB分割方針へ更新し、7062（`fileSync` 異常系: SHA不一致/チャンク欠損）を追加。理由: 運用方針を「100KB以上で分割」に合わせ、正常/異常の双方を試験仕様へ固定するため。
- 2026-03-13: 7061（`fileSync` 画像更新標準経路: ZIP非使用・2024byte以上分割）を追加。理由: 実機で `imagePackageApply` 実行中のスタック余裕不足が再現したため、画像更新標準を 1ファイル更新 + chunk 分割へ切替える方針を試験仕様へ固定するため。
- 2026-03-12: 7059/7060（`imagePackageApply` 正常系/異常系）を追加。理由: `002-0018` の要求（展開・上書き・ディレクトリ指定、署名/HMAC不一致・SHA不一致・不正ディレクトリ）を試験仕様へ明文化するため。
- 2026-03-12: 7057〜7058（payload暗号化 strict モードの平文拒否試験とE2E試験）を追加。理由: `compat` 実装完了後の本番移行判定を試験仕様で明確化するため。
- 2026-03-12: 7050〜7056（管理者認証、`k-user` / `k-device`、`securePing`、APモード遷移/表示/タスク抑止）を追加。理由: LocalServer + ESP32 のメンテナンス運用と暗号化MQTT相互通信の実装に対応する試験項目を固定するため。
- 2026-03-10: 7025 の `未確定起動失敗` 手順へ `rollback-test` API の実行手順を追記。理由: 実装済みの試験専用導線を使って再現方法を固定するため。
- 2026-03-10: 7025 の `未確定起動失敗` に rollback 前提条件を追記。理由: 前提未整備時に試験不能となる条件を明示し、実施可否判定を固定するため。
- 2026-03-09: 7015 / 7025 に試験用の一時 `status` 項目（`runningPartition` / `bootPartition` / `nextUpdatePartition`）を追加し、試験後に削除する方針を明記。理由: 1日で実施可能な確認手段を固定しつつ、常設仕様へ残さないため。
- 2026-03-09: 7044〜7049（`fileSync` 差分更新、`/logs` 保持、`SHA-256` 不一致、証明書更新、ログローテーション、ログ自動削除）を追加。理由: `NVS` / `LittleFS` の保存方針、画像/証明書更新、ログ保持方式を試験で担保するため。
- 2026-03-09: 7039〜7043（`runPairingSession()`、`runKeyRotationSession()`、`runProductionSecureFlow()`、`runSignedOtaCommand()`、workflow `failed` 遷移）を追加。理由: Rust 主導高リスクワークフローと進捗状態を試験で担保するため。
- 2026-03-09: 7035〜7038（`createPairingBundle` 必須設定検証、AP 初回投入、current/previous 2 スロット、48h/240h 状態遷移）を追加。理由: AP モード投入と `k-user` 再発行後の鍵状態管理を試験で担保するため。
- 2026-03-09: 7019〜7034 を AP 共通画面認証、ProductionTool 追加認証、ProductionTool 画面、各新番号へ再整理。理由: `ProductionTool` 分離設計と AP 認証仕様に試験番号を合わせるため。
- 2026-03-09: 7029〜7033（通常FWシリアル制約、診断用FW、未使用ポート閉鎖、認証失敗抑止、踏み台化防止確認）を追加。理由: 4-8 / 4-9 / 4-10 の方針を試験で追跡できるようにするため。
- 2026-03-08: 7028（新ルータ運用開始後の DNS / MQTT 起動前確認）を追加。理由: `172.17.1.1 / 172.17.1.100` 運用開始直後の名前解決、NTP、MQTT 起動条件を 1対1 で記録できるようにするため。
- 2026-03-08: 7026〜7027（ESP32 設定永続保存、APメンテナンスモード設定修正）を追加。理由: 設定保持と現地メンテナンス復旧を直近ゴールとして試験で担保するため。
- 2026-03-08: 7024〜7025（OTA途中失敗時の旧パーティション継続起動、eFuse 最終化後 OTA バージョンアップ）を追加。理由: 直近ゴールで重視する 2面OTA の復旧性と、eFuse 後の継続更新可否を試験で担保するため。
- 2026-03-08: 7019〜7023（`ProductionTool` 認証、段階実行、中断後再判定、スタック余裕、リハーサル完了確認）を追加。理由: eFuse 最終有効化の入口制御、実行安定性、事前検証完了条件を試験で担保するため。
- 2026-03-08: 7016〜7018（メーカー署名済み配布物実行、未署名拒否、改ざん配布物拒否）を追加。理由: OTA標準運用「メーカー署名 + 顧客実行」を試験で担保するため。
- 2026-03-08: 7012〜7015（OTA再認証、3時間セッション有効期限、HTTPS証明書検証、2面交互切替）を追加。理由: OTA運用方針の確定に合わせて高リスク操作の再認証と配布真正性確認を試験対象化するため。
- 2026-03-07: OTA詳細試験（7007〜7011）を追加。進捗通知、再試行、SHA256検証を試験対象化。
- 2026-03-07: 新規作成。LocalServer初期実装の試験仕様を追加。
