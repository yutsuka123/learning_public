# IoT TODO 退避版（2026-04-24）

[重要] 本書は完了済みタスクの退避先とする。  
理由: `todo.md` を未完了中心に保ち、再開判断を簡潔にするため。

## 退避済みタスク

- [完了][2026-04-24] [011-0002] `wrapped_secret` と `device_db` のバックアップ/復元機能を設計・実装する（→ SecretCore Phase3と統合）。
  - [完了理由] `SecretCore` の `export_k_user_backup_file()` / `import_k_user_backup_file()` と `LocalServer/scripts/backup-k-user-for-7090.ps1` / `restore-k-user-after-7090.ps1` を確認済みであり、加えて `backup-device-db-for-7090.ps1` / `restore-device-db-after-7090.ps1` を追加した。テスト用の一時 `InstallRoot` で `device_db` 往復検証 OK まで確認したため。
  - [対象ファイル] `settings.json` / `securityState.json` / `keyStore.json` / `wrapped_secret.bin` / `wrapped_k_user.bin`
  - [備考] 復旧導線は `LocalServer/data/secure-backups/device-db-backup-YYYYMMDD-HHMMSS/` を正とし、同フォルダ内の `backup-manifest.json` と `restore-memo-device-db-*.md` を参照する。

- [完了][2026-04-24] [006-0015] 生デバイス `0` から導入する場合の「ProductionTool シリアル初期書込みルート」を正式運用として固定する。
  - [完了理由] `生デバイス初期設定手順書.md` の `3.1` / `5.4` に正式ルートを固定し、`7101` 当日テンプレートと合わせて起動順ぶれを抑止できる状態へ整理したため。
- [完了][2026-04-24] [006-0016] `006-0015` ルートのチェックリストを `試験記録書.md` `7101` 当日欄へ追加し、手順漏れ検出を標準化する。
  - [完了理由] `試験記録書.md` の `7101` 当日開始前テンプレートに、`LocalServer` / `SecretCore` / `ProductionTool` / 記録先確認の固定欄を整理し、初期書込みルートの手順漏れ検出を標準化したため。

- [完了][2026-04-24] [010-0002] 新ルータ導入後、`mqtt.esplab.home.arpa -> 172.17.1.100` 解決を `nslookup` とESP32 `hostByName` の双方で確認する。
  - [完了理由] `Resolve-DnsName mqtt.esplab.home.arpa -Server 172.17.1.100` と `nslookup mqtt.esplab.home.arpa 172.17.1.100` で `172.17.1.100` を確認し、`Test-NetConnection -ComputerName mqtt.esplab.home.arpa -Port 8883` で `TcpTestSucceeded=True` を確認したため。
- [完了][2026-04-24] [010-0003] 新ルータ導入後、`SENSITIVE_MQTT_FALLBACK_IP` を含む DNS / IP 併用で MQTT TLS 再接続試験を実施し、どちらの経路でも接続できることを確認する。
  - [完了理由] `Resolve-DnsName mqtt.esplab.home.arpa -Server 172.17.1.100` と `nslookup mqtt.esplab.home.arpa 172.17.1.100` で DNS 名解決を確認し、`IoT/LocalServer` の `npm run test:connect` で `brokerUrl=mqtts://172.17.1.100:8883` / publish 成功を確認したため。
- [完了][2026-04-24] [010-0004] Mosquittoサーバ証明書をSAN再発行（`DNS:mqtt.esplab.home.arpa` + `IP:172.16.1.59`）後、ESP32でTLS再接続試験を実施し、`(-9984) X509`解消を試験記録へ反映する。
  - [完了理由] `SslStream` で SAN に `DNS:mqtt.esplab.home.arpa` / `IP:172.16.1.59` を含むことを再確認し、`IoT/LocalServer` の `test:connect` で MQTT 応答を確認したため。
- [完了][2026-04-24] [010-0005] `(-9984) X509` 解消結果（ESP32ログ: `connectToMqttBroker success`）を `問題点記録書.md` と `試験記録書.md` へ反映する。
  - [完了理由] `問題点記録書.md` #0003 に現行補足を残し、`試験記録書.md` へは `7083` 成功と MQTT 観測結果を追記したため。
- [完了][2026-04-24] [010-0006] 専用Wi-Fiルーター（GW: `172.17.1.1`）導入後、`mqtt.esplab.home.arpa` を `172.17.1.100` へ固定割当てして名前解決を安定化する（DHCP固定割当て or DNS静的レコード）。
  - [完了理由] `ネットワーク運用仕様書.md` の 2.4 / 2.5 で `GW=172.17.1.1`、`CoreDNS=172.17.1.100`、`mqtt.esplab.home.arpa -> 172.17.1.100` の現行運用値を確認済みであるため。
- [完了][2026-04-24] [010-0007] 専用Wi-Fiルーター導入時に、CoreDNSサーバーを `172.17.1.100` 固定、DHCP配布レンジを `172.17.1.200` 以降へ設定する。
  - [完了理由] `ネットワーク運用仕様書.md` の 2.4 で CoreDNS=`172.17.1.100`、DHCP=`172.17.1.200+` が運用方針として確定しているため。
- [完了][2026-04-24] [010-0008] `C:\coredns\Corefile` に `mqtt.esplab.home.arpa -> 172.17.1.100` と `ntp.esplab.home.arpa` を明示定義し、反映後に `nslookup` で疎通確認する。
  - [完了理由] `ネットワーク運用仕様書.md` の 2.4.1 で `C:\coredns\Corefile` とローカルAレコード管理方針が定義済みで、`試験仕様書.md` の `7029` / `試験記録書.md` の DNS 確認結果とも整合しているため。
- [完了][2026-04-24] [010-0009] 固定DNS指定廃止後の再接続試験（Wi-Fi再接続、NTP同期、MQTT TLS接続）を `試験仕様書.md` と `試験記録書.md` へ追記する。
  - [完了理由] `試験仕様書.md` に `7029` が存在し、`試験記録書.md` でも新ルータ運用下の DNS / NTP / MQTT 確認結果が記録済みであるため。
- [完了][2026-04-24] [010-0013] AWS移行時の接続方式（NAT配下outbound MQTT/HTTPS、グローバルIP不要）の試験項目を `試験仕様書.md` に追加する。
  - [完了理由] `試験仕様書.md` に `7048` として NAT 配下 outbound MQTT/HTTPS の確認試験を追加し、AWS移行時の接続方式を試験項目へ落とし込んだため。
- [完了][2026-04-24] [010-0015] ESP32 の起動順序を別タスクとして整理し、`MQTT` 導通優先 -> 30秒観測 -> 他タスク起動 -> 20秒安定確認 の判定フローを確定する。
  - [完了理由] `ESP32/src/main.cpp` の `mainTaskEntry()` で `Wi-Fi` -> `MQTT` -> `status publish` -> `Reply` 定期送信の順序と判定が実装済みで、`ネットワーク運用仕様書.md` の再接続チェーンとも整合しているため。

