# IoT TODO 完了済み（退避: 2026-05-17）

[補足] 本書は `todo.md` から退避した完了済みタスクの記録である。  
理由: `todo.md` を未完了タスク中心に保ち、現在の確認対象を必要最小限に絞るため。

## 退避済みタスク

- [x] [009-0003] [2026-03-07][2026-05-17完了][重要] `LocalServer` の自動保存処理を Task Scheduler（Windows）起動運用に組み込み、導入手順を `コマンド仕様書.md` に追記し、実機確認する。
  - [完了][2026-05-17] `.env` に `LOCAL_HISTORY_SCHEDULED_EXPORT_ENABLED=true` / `LOCAL_HISTORY_SCHEDULED_EXPORT_INTERVAL_MS=86400000` を追記。Node.js v22.15.0（NODE_MODULE_VERSION=127）で `node dist/server.js` を起動し、`/api/health` OK・`LocalServer: scheduled history export enabled. intervalMs=86400000` ログを確認。
  - [完了][2026-05-17] `コマンド仕様書.md` §5.1（定期エクスポート有効化手順）、`試験仕様書.md` `7107`、`試験記録書.md` `7107`（OK）、`LocalServer/README.md` §6 補足、`.cursorrules` に `DB仕様書.md` を追記した。
  - [注意][2026-05-17] Node.js バージョン: `better-sqlite3` は v22 でビルド済み（NODE_MODULE_VERSION=127）。v20 のまま起動すると `ERR_DLOPEN_FAILED` で即終了するため `nvm use 22` が必要。
  - 一次記録: `試験記録書.md` **`7107`**、`コマンド仕様書.md` §5.1、`試験仕様書.md` `7107`。

- [x] [009-0002] [2026-03-07][2026-05-17完了][重要] `LocalServer` へSQLite保存を統合実装する（**既定30日**、0=無期限・正本は `data/settings.json` の `localHistoryRetentionDays`）。
  - [完了][2026-05-17] 実施順序 (1)〜(4): SQLite、`POST /api/admin/local-history/export`、起動時・定期パージ、試験 **`7102`〜`7106`**。UI は **`settings.html`** / **`admin.html`**。
  - [完了][2026-05-17] 試験コマンド: `npm run test:7102`（オフライン＋7404 可）、**`npm run test:7102:live`**（**実機 MQTT**・`--spawnLocalServer` で子起動可）、`npm run test:7106`、`npm run test:7106:disable`（7106 手順3・**ENABLED=false** で新規 `scheduled-history-export` なし）、`npm run verify:bootstrap`（SecretCore IPC・**#0044**）。
  - [一次記録] `試験記録書.md` **`7102`〜`7106`**、`問題点記録書.md` **#0044**、`コマンド仕様書.md` §4、`ソース概要.md`（`test7102LiveMqttHistory.mjs` / `test7106ScheduledExportDisable.mjs`）。

- [x] [009-0001] [2026-03-07][2026-05-17完了][重要] LocalServer（TypeScript/Node.js）を Task Scheduler（Windows）で **ユーザー・ログオン時（既定）** 自動起動する導入手順を `コマンド仕様書.md` に追記し、実機確認する。
  - [完了][2026-05-17] `コマンド仕様書.md` §5、`LocalServer/README.md` §6 に、正規タスク名 **`IoT_LocalServer_AutoStart`**、**既定 `AtLogOn` + `Interactive`**、`invoke-install-task-scheduler-admin.ps1`（UAC 昇格）、`install-task-scheduler.ps1` の管理者チェック、`run-local-server.ps1` による `node dist/server.js` 直起動、**`/api/health`** 確認手順を反映した。
  - [完了][2026-05-17] 実機でタスクを **`MSFT_TaskLogOnTrigger`** / **`LogonType=Interactive`** へ再登録したうえで、`Start-ScheduledTask -TaskName "IoT_LocalServer_AutoStart"` 実行後に **`LastRunTime` が実日時へ更新**され、`GET http://127.0.0.1:3100/api/health` が **`{"status":"ok","service":"local-server",...}`** を返すことを確認した。`Get-ScheduledTaskInfo` の **`LastTaskResult=267009`**（`0x41301`）はスケジューラの状態コードであり、**HTTP 疎通 OK** をもって本タスクの合否とした。
  - [経緯] 旧 **`AtStartup` + `S4U`** 構成では `LastTaskResult=267011`（`SCHED_S_TASK_HAS_NOT_RUN` 相当）で実体が起動しない事象があった。既定を **ログオン後 `Interactive`** に変更し再登録することで解消した。
  - 一次記録: `試験記録書.md` **`7001`**、`問題点記録書.md` **`#0042`**、`設計議事録20260517.md`。

- [x] [009-0005] [2026-03-07][2026-05-17完了][推奨] `LocalServer` のデータ保存期間設定（30/90/365日）と削除API仕様を `機能仕様書.md` に追記する。
  - [完了][2026-05-17] `機能仕様書.md` §7.1（LocalServer ローカル履歴保持期間設定と削除 API）を新設。`localHistoryRetentionDays` 推奨プリセット（30/90/365/0=無期限）、`DELETE /api/admin/local-history` 範囲削除、`POST /api/admin/local-history/delete-database` 全消去（`confirm` 必須・不可逆）、監査ログ記録義務を明文化した。
  - 一次参照: `機能仕様書.md` **§7.1**、`DB仕様書.md` §2〜§3（技術実装詳細）。

## 変更履歴
- 2026-05-17（続³）: **`009-0005`** を追記退避。理由: `機能仕様書.md` §7.1 新設（保存期間・削除API仕様）完了のため。
- 2026-05-17（続²）: **`009-0003`** を追記退避。理由: Task Scheduler + 定期エクスポート組込み（`7107` OK）完了のため。
- 2026-05-17（続）: **`009-0002`** を追記退避。理由: SQLite 履歴統合と `7102`〜`7106` 試験完了のため。
- 2026-05-17: 新規作成。理由: `009-0001` を実機確認まで完了し、未完了管理から退避するため。
