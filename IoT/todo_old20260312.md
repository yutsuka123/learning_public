# IoT TODO 完了済み（退避: 2026-03-12）

[補足] 本書は 2026-03-12 時点で `todo.md` から退避した完了タスクの記録である。  
理由: `todo.md` を未完了タスク専用に保ち、進行中の作業対象を明確化するため。

---

## 完了済みタスク

### OTA安全性・復旧性（001章）
- [x] [001-0001] [2026-03-07][重要] ESP32書込み前の `beta.x` 更新は必須運用から外し、必要時のみ都度指示で実施する。
  - 結果: 版数更新は必要作業時のみ実施する運用へ切替済み。
- [x] [001-0002] [2026-03-08][最優先][重要] 7015 を実施し、OTA 実行時にパーティション 0 / 1 の2面へ交互に書き込まれていることを `試験記録書.md` へ反映する。
  - 結果: `試験記録書.md` に A/B 交互切替の確認結果を反映済み。
- [x] [001-0003] [2026-03-08][最優先][重要] 7025 を実施し、OTA途中失敗時でも旧パーティションから継続起動できることを `試験記録書.md` へ反映する。
  - 結果: 通信断/整合性不一致/未確定起動失敗の条件で旧面継続起動を反映済み。
- [x] [001-0004] [2026-03-08][重要] OTA 失敗条件ごとの復旧結果（途中通信断、整合性不一致、未確定起動失敗）を `問題点記録書.md` へ整理する。
  - 結果: 失敗条件別の原因・対策を問題点記録へ整理済み。

### LocalServer管理者機能（003章）
- [x] [003-0005] [2026-03-11][最優先][重要] LocalServer の「設定 -> 管理者画面」にユーザー名/パスワード認証を実装する。
  - 結果: `POST /api/admin/auth/login` / `logout` とトークン検証（Bearer / `x-admin-token`）を実装済み。
- [x] [003-0006] [2026-03-11][重要] 管理者画面で `k-user` 発行と `k-device` 発行の実行機能を実装する。
  - 結果: `POST /api/admin/keys/k-user/issue` / `k-device/issue` を実装済み。
- [x] [003-0007] [2026-03-11][重要] Wi-Fi USB 指定による APモード ESP32 探索と自動設定（ネットワーク設定/パスワード/`k-device`）を実装する。
  - 結果: AP探索・接続・設定投入までの自動処理導線を実装済み。
- [x] [003-0009] [2026-03-11][重要] AP接続機体と MQTT接続機体を統合一覧表示し、FW版数/FS版数/書換進捗を表示する。
  - 結果: `GET /api/admin/devices` で `connectionMode` と `apWebUrl` を含む統合一覧を返却済み。
- [x] [003-0010] [2026-03-11][重要] MQTT接続中機体へ `call/maintenance` 指令を送信する管理画面操作を実装する。
  - 結果: `POST /api/admin/commands/maintenance-reboot` を実装済み。

### MQTT payload暗号化（009章）
- [x] [009-0007] [2026-03-06][厳守] MQTT payloadのアプリ層暗号化/復号を `k-device` で実装する（送受信とも必須化）。
  - 結果: LocalServer/ESP32 双方へ payload全文暗号化モジュールを実装し、`7053` を OK で記録。
- [x] [009-0025] [2026-03-12][重要] MQTT payload暗号化運用を `compat` から `strict` へ移行し、平文拒否を本番条件として確定する。
  - 結果: `7057` / `7058` を OK で記録し、既定値 strict 化（LocalServer/ESP32）と `コマンド仕様書.md` への切替/ロールバック手順を反映済み。

---

## 退避ルール
- [厳守] 完了タスクは `todo_old<YYYYMMDD>.md` へ退避し、`todo.md` 本体から削除する。
- [補足] 本ファイルは参照専用。再開タスクがあれば `todo.md` へ戻す。
