---
marp: true
---

# IoT TODO — 完了退避 (2026-05-19)

本ファイルは 2026-05-19 に完了退避したタスクを保管する。
現在未完了のタスクは `IoT/todo.md` を参照。

---

## 009-0008: 鍵管理初期セットアップ Phase1〜4 への担当・期限割当（完了）

- [完了][2026-05-19] [009-0008] `鍵管理初期セットアップ_実装たたき台.md` のPhase1〜4に担当・期限を割り当てる。
  - [完了理由][2026-05-19] `鍵管理初期セットアップ_実装たたき台.md` §2 冒頭へ運用ルールを追加し（担当=責任モジュール／期限=相対順序）、Phase1〜4（および Phase5）へそれぞれ担当と相対期限を明記した。
    - Phase 1（TPM 初期化基盤）: 担当 = `SecretCore`（主）/ `LocalServer`、期限 = `009-0009` 着手前提
    - Phase 2（LocalServer 起動時鍵導出）: 担当 = `SecretCore`（主）/ `LocalServer`、期限 = Phase1 完了後、`009-0012` の前提
    - Phase 3（初回ペアリング・暗号通信）: 担当 = `SecretCore`（主）/ `LocalServer` / `ESP32`、期限 = Phase2 完了後、`009-0010` / `009-0014` / `009-0017` / `009-0018` と並走
    - Phase 4（バックアップ/復元）: 担当 = `LocalServer`（主）/ `SecretCore`、期限 = Phase3 完了後（`008-0030` 退避済みの土台を流用）
    - Phase 5（障害時運用・参考）: 担当 = `LocalServer` / `SecretCore`、期限 = Phase4 完了後の将来対応
  - [運用方針] 単独運用前提のソロ開発のため、絶対日付を入れず相対順序で固定。`009-0009` 以降の着手判断は依存関係のみで行う。
  - [証跡] `鍵管理初期セットアップ_実装たたき台.md` §2 / §10 変更履歴 2026-05-19。

## 009-0009: Phase1 最小実装（既実装確認による完了）

- [完了][2026-05-19] [009-0009] Phase1（`S_random` 生成 + OS 暗号化サービスラップ + `wrapped_secret` 保存）の最小実装を先行着手する（→ SecretCore Phase1と統合）。
  - [完了理由][2026-05-19] 当該機能は `SecretCore/src/key_manager.rs:84-115 load_or_create_s_random()` で実装済みであることを確認した。
    - CSPRNG 生成: `rand::rng().fill_bytes(&mut s_random)`（32 byte）
    - OS 暗号化サービスラップ: `dpapi::protect_data` を経由した `WrappedSecretEnvelope`（version / created_at / alg="DPAPI" / payload_base64 / payload_sha256_base64）
    - 保存先: `LocalServer/data/wrapped_secret.bin`（自動ディレクトリ作成あり）
    - 起動時の再読み込み: 同関数内で `dpapi::unprotect_data` 経由の復号と長さチェック
  - [仕様正本] `機能仕様書.md` F3-091（SecretCore 起動時 `wrapped_secret.bin` 読込、HKDF 導出経路）。
  - [整合性メタ] `WrappedSecretEnvelope` 整合性検証は `SecretCore/src/key_manager.rs:258-294` で実装済み（`008-0013` 退避済み）。
  - [文書整合性更新] 旧 todo 文言の「TPM ラップ」表現は実装と乖離していたため、本退避と同日に `鍵管理および初期セットアップ設計仕様書.md` / `LocalServer秘密処理別層仕様書.md` / `鍵管理初期セットアップ_実装たたき台.md` を **OS 暗号化サービス（現行: Windows DPAPI、ソフトウェア暗号化・チップ非依存）** 正本へ全面更新した。
  - [後続対応] macOS / Linux 汎用化（Keychain Services / libsecret 等）は **`020-0004`** として `020` 章へ新規追加。本タスクの範囲外。

## 009-0012: サーバ側 k-device 導出処理（既実装確認＋仕様の HKDF → HMAC 整合）

- [完了][2026-05-19] [009-0012] サーバ側で `k-device` 導出処理を実装する。
  - [完了理由][2026-05-19] 当該機能は **`SecretCore/src/key_manager.rs:454-465 get_k_device()`** で実装済みであることを確認した。
    - 導出式: `HMAC-SHA256(key=k_user, message=target_device_name)`（`target_device_name` = `publicId`、初期値 `IoT_<base MAC からコロン除去>`）
    - 出力: 32 byte（HMAC-SHA256 のダイジェスト長そのもの）
    - 利用箇所: `sign_by_k_device`（command 署名）／`encrypt_by_k_device` / `decrypt_by_k_device`（AES-256-GCM 鍵）／`ota_workflow.rs:344`（OTA 開始署名）／`mqtt_transport.rs:921`（受信 payload 復号）
    - IPC 公開 API: `main.rs:352 "get_k_device"` で base64 形式での取得経路あり
  - [仕様正本] 設計仕様書群を 2026-05-19 に **HKDF → HMAC** で正本更新（実装は HMAC-SHA256 直接導出のまま）：
    - `鍵管理および初期セットアップ設計仕様書.md` §3 鍵構造 / §6.1 device_id / §6.2 生成式 / §6.3 設計意図 / §16 変更履歴
    - `LocalServer秘密処理別層仕様書.md` §3.1 鍵構造 / §13 変更履歴
    - `IF仕様書.md`（k-device 生成式）/ `WiFi設定更新_詳細設計書.md`（鍵設計）/ `セキュリティ方針_認証情報取扱い.md`（k-device 生成）/ `要件定義書.md` FR-009 / `鍵一覧仕様書.md`（k-device 行）/ `鍵管理初期セットアップ_実装たたき台.md`（Phase2 現状）
  - [変更理由] HKDF と HMAC-SHA256 直接導出の事実上の安全性差は無い（HKDF も内部で HMAC-SHA256 を 2 段使う）。command 署名（`sign_by_k_device`）／OTA 開始署名（`ota_workflow.rs:344`）／ESP32 側 `mbedtls_md_*`（`secureNvsInit.cpp` / `MQTT/mqtt.cpp`）も HMAC-SHA256 で統一されており、`k-device` 導出だけ HKDF にすると実装・試験が分岐するだけで利点がないため、実装側へ寄せる。
  - [後続対応] なし。

## 009-0021: AP 復旧モード（WPA2/固有パスワード/Web UI）（完了退避）

- [完了][2026-05-19] [009-0021] AP復旧モード（WPA2/固有パスワード/Web UI）を実装する。
  - [完了理由][2026-05-19] タスク文言（括弧内）スコープのコア機能は `009-0018`（既退避）と同一コードパスで実装済み。仕様書側の付随トリガー「Wi-Fi 接続失敗回数しきい値超過」は実装側に存在せず、`020-0006` へ将来対応として移管。
  - [現状の実装]（タスク文言スコープ・全て実装済み）
    - **WPA2 + 固有パスワード**: `IoT/ESP32/src/main.cpp:502` で `WiFi.softAP(maintenanceApSsid, iotCommon::apConfig::kMaintApPass)` を呼出し
    - **Web UI**: `IoT/ESP32/src/maintenanceApServer.cpp` 全体（HTML 生成、認証、設定変更、FW 更新、画像更新の各エンドポイント）
    - **4 ロール認証**: user / maintenance / admin / mfg（`APメンテナンス画面仕様書.md` §4）
    - **AP モード遷移トリガー**（実装済み 3 種）:
      - 起動時ボタン3秒長押し（`main.cpp:539 detectStartupMaintenanceLongPress`）
      - MQTT `call/maintenance` 指令による次回起動 AP モード要求（`maintenanceMode.cpp:39 requestMaintenanceModeOnNextBoot`）
      - 救済専用ビルド `APP_SECURE_RESCUE_MODE=1`（`platformio.ini:146` 周辺）
  - [未実装（`020-0006` で実装）]
    - Wi-Fi 接続失敗回数しきい値超過による自動 AP モード遷移（フェイルセーフ）
    - 連続失敗カウンタ、しきい値定義、時間窓・誤検出回避ロジック、監査ログ
  - [仕様文書の正本化]
    - `WiFi設定更新_詳細設計書.md` §9.1 のトリガーを「現状」（起動時長押し / `call/maintenance` / 救済専用ビルド）と「将来対応」（Wi-Fi 失敗超過自動遷移 → `020-0006`）に分離して明文化
  - [`009-0018` との関係]
    - 主目的が違うが、コア実装は完全に共通（同じ `startMaintenanceApModeAndHold` / `maintenanceApServer.cpp` / `iotCommon::apConfig::kMaintApPass`）
    - `009-0018` は「初回ペアリング投入」を主目的とした AP モード
    - `009-0021` は「誤設定からの現地復旧」を主目的とした AP モード（実コードパスは同一）
  - [差分] タスク文言（括弧内）スコープでは差分なし。仕様書 §9.1 のトリガー「Wi-Fi 失敗超過」は実装側に存在せず、将来対応 `020-0006` へ移管。
  - [後続対応] `020-0006`（Wi-Fi 接続失敗超過による自動 AP モード遷移の本実装）

## 009-0020: ESP32 の current/previous key 2 スロット運用 + 48h/240h 管理（試験仕様書整備によるクローズ）

- [完了][2026-05-19] [009-0020] ESP32 の `current/previous` key 2 スロット運用と稼働時間 48h / 240h 管理を実装する。
  - [完了理由][2026-05-19] 鍵ローテーションは任意運用（`k-user` を変更した時のみ発生）であり、本実装は将来対応へ移管。本タスクのクローズ条件は **試験仕様書 `7038` の詳細化**（通常運用試験＋加速試験 2h）まで。コア実装は **`020-0005`** で扱う。
  - [現状の実装]（部分実装）
    - NVS キー定義: `previousKeyState` / `graceActiveRuntimeMinutes`（`IoT/ESP32/src/sensitiveData.cpp:38-39`）
    - pairing / key-rotation 直後の grace 設定: `previousKeyState="grace"`, `graceActiveRuntimeMinutes=0`（`IoT/ESP32/src/sensitiveData.cpp:1313-1320`）
    - AP モード設定画面での状態表示（`IoT/ESP32/src/maintenanceApServer.cpp:469,505,1338,1855,1941,1956`）
  - [未実装（`020-0005` で実装）]
    - 時間定数（`KEY_ROTATION_GRACE_MINUTES=2880` / `KEY_ROTATION_RETAINED_MINUTES=14400`、加速試験用 `_SCALE=120` 切替）
    - runtime カウンタ（起動後5分 → 1時間ごとに `graceActiveRuntimeMinutes` 加算）
    - state 遷移判定（grace → expired-retained → deleted）
    - previous key 自動削除（240h 到達時に `previousKDevice` を NVS から削除）
    - 認証拒否切替（expired-retained 以降は旧鍵での署名検証・復号を拒否）
  - [試験仕様の詳細化] `試験仕様書.md` `7038`:
    - 通常運用試験（実 48h / 240h）と加速試験（120 倍スケール・合計 2h）の 2 モード
    - 試験手順 1〜8（準備 → ビルド → 鍵ローテーション → grace 観測 → 遷移確認 → expired-retained 観測 → deleted 遷移 → 試験後復旧）
    - 期待結果と合否判定基準を明記
    - **試験後の必須措置**: 元の鍵へ戻す（`runKeyRotationSession()` で試験前の `currentKDevice` を再 current 化）／設計時間に戻す（`-DKEY_ROTATION_TIME_SCALE=1` で再ビルド・OTA 配信）／結果を `試験記録書.md` `7038` へ追記
  - [仕様文書の正本性]
    - `機能仕様書.md` F3-035（2 スロット）/ F3-036（48h/240h 自動削除）
    - `要件定義書.md` FR-047（2 スロット）
    - `IF仕様書.md:353`（48h で expired-retained）
    - `コマンド仕様書.md:528`（同上）
    - `鍵管理および初期セットアップ設計仕様書.md` §10.3（ESP32 責務）
    - `鍵一覧仕様書.md`（k-device 行、§ current/previous 2 スロット）
    - `セキュリティ方針_認証情報取扱い.md` §6（鍵ローテーション）/ 監査ログ
  - [差分] タスク文言「実装する」と現状の差分はコア処理（runtime カウンタ等）の未実装。ただし鍵ローテーションは任意運用のため、現時点では試験仕様整備までを `009-0020` のクローズ条件とし、コア実装は `020-0005` で扱う。
  - [後続対応] `020-0005`（本実装＋加速試験＋設計時間復帰）

## 009-0018: 初回ペアリング AP モード（物理操作必須・短時間有効）（完了退避）

- [完了][2026-05-19] [009-0018] 初回ペアリングAPモード（物理操作必須、短時間有効）を実装する。
  - [完了理由][2026-05-19] 物理操作必須・短時間有効は **5 層セキュリティ**で達成済み。「短時間有効」を **単発セッション**として `APメンテナンス画面仕様書.md` §1.1 に明文化し、N 分無操作の自動切断タイマーは要件としない方針を確定。
    - **① 物理操作必須**: 起動時ボタン3秒長押し（`main.cpp:539 detectStartupMaintenanceLongPress`）または起動後30秒以内の長押し（`input.cpp:109-115 handleLongPressEvent`）
    - **② Wi-Fi WPA2 認証**: SoftAP + `AP_WIFI_PASSWORD`（`main.cpp:502`、`iotCommon::apConfig::kMaintApPass`）
    - **③ Web UI ロール認証**: 4 ロール（user / maintenance / admin / mfg）パスワード認証（`maintenanceApServer.cpp`、`APメンテナンス画面仕様書.md` §4）
    - **④ 通常通信からの隔離**: AP モード中は STA / MQTT 通常タスクを起動せず、AP 専用無限ループのみ実行（`main.cpp:524-532`、`機能仕様書.md` F3-068）
    - **⑤ 単発有効/持ち越さない**: `requestMaintenanceModeOnNextBoot` フラグは次回起動時に消費（`maintenanceMode.cpp:51-64 consumeMaintenanceModeRequest`）。AP モード退出はボタン長押し → 再起動 → 通常モード復帰
  - [仕様正本化] 
    - `APメンテナンス画面仕様書.md` §1.1「AP モードの有効性とセッション境界」を新設（5 層セキュリティ表、`009-0018` 整合確認）
    - `機能仕様書.md` F3-068 に「単発セッション・`requestMaintenanceModeOnNextBoot` フラグ次回起動で消費」を追記
    - `要件定義書.md` FR-073 に同上を追記
  - [差分] タスク文言の「短時間有効」の解釈について：
    - **(α) セッション制限（N 分無操作で自動終了）**: **不採用**。ペアリング作業中の強制切断は現地復旧の運用負荷を増やすため。
    - **(β) 単発有効（次回起動には持ち越さない）**: **採用**。物理操作 + 認証 + 隔離 + 単発有効の 4 層で攻撃面が十分縮小されているため。
  - [後続対応] なし。

## 009-0017: ESP32 側 AES-256-GCM 復号処理（nonce/tag 検証付き）（完了退避）

- [完了][2026-05-19] [009-0017] ESP32側でAES-256-GCM復号処理（nonce/tag検証付き）を実装する。
  - [完了理由][2026-05-19] ESP32 側で **3 箇所**に `mbedtls_gcm_auth_decrypt`（AEAD: GCM タグ検証付き）の AES-256-GCM 復号を実装済みであることを確認した。タグ不一致時は `decryptResult != 0` で失敗判定し、改ざん検知が成立する。
    - **`IoT/ESP32/src/maintenanceApServer.cpp:881-927 decryptAes256GcmForAp`**
      - 用途: AP モード pairing bundle 復号（AAD あり）
      - 入力検証: key=32 byte / iv=12 byte / tag=16 byte 厳格チェック
      - AAD: `pairing-secure-bundle-v1|targetDeviceId|sessionId|bundleId|keyVersion` 形式
      - 呼出元: `maintenanceApServer.cpp:1766`
    - **`IoT/ESP32/src/MQTT/mqtt.cpp:604-639 decryptAesGcm`**
      - 用途: MQTT payload 復号（AAD なし）
      - 256-bit key、`mbedtls_gcm_auth_decrypt` でタグ検証
      - 呼出元: `mqtt.cpp:2819`
    - **`IoT/ESP32/src/MQTT/mqttPayloadSecurity.cpp:92-131 decryptAesGcm`**
      - 用途: MQTT payload security 層（AAD あり、新版）
      - 入力検証: key=32 byte / iv=12 byte / tag=16 byte 厳格チェック
      - 呼出元: `mqttPayloadSecurity.cpp:267`
  - [仕様正本] 13 文書で AES-256-GCM 採用を明記:
    - `機能仕様書.md` / `設計概要.md` §6 / `鍵管理および初期セットアップ設計仕様書.md` §9 / `IF仕様書.md` / `LocalServer秘密処理別層仕様書.md` §4.2 / `WiFi設定更新_詳細設計書.md` §4.1 / `セキュリティ方針_認証情報取扱い.md` / `要件定義書.md` / `試験記録書.md` / `ソース概要.md` / `使用技術仕様書.md` / `ドキュメント概要.md` / `鍵管理初期セットアップ_実装たたき台.md`
  - [差分] なし。タスク文言「ESP32側でAES-256-GCM復号処理（nonce/tag検証付き）を実装する」に対し、実装は完全一致（`mbedtls_gcm_auth_decrypt` による GCM tag 検証で nonce/tag 検証が成立）。
  - [後続対応] なし。

## 009-0014: MQTT トピックを `device/<public_id>/wifi/update` / `confirm` へ統一（不採用クローズ）

- [不採用クローズ][2026-05-19] [009-0014] MQTTトピックを `device/<public_id>/wifi/update` / `confirm` へ統一する。
  - [不採用理由][2026-05-19] 精査結果、本タスクの「統一」は **実装と全体設計の双方で逆方向**になることが判明：
    - **実装の active path**: `IoT/ESP32/src/MQTT/mqtt.cpp:3298,3302` で `esp32lab/network/+/<deviceNodeName>` / `esp32lab/network/+/all` を subscribe しており、Wi-Fi 設定更新は `network` kind で動作中
    - **MQTT 全体設計**: `MQTTコマンド仕様書.md` §2.1 が `esp32lab/<kind>/<sub>/<name>`（kind = `notice` / `set` / `get` / `call` / `network`）で **既に統一済み**
    - **未使用ヘルパー**: `IoT/ESP32/src/mqttProtocol.cpp:62-72` `buildTopicWifiUpdate` / `buildTopicWifiConfirm` / `buildTopicDeviceBoot` は定義されているが、grep の結果 **実コードからは呼ばれていない**
    - 結論: `device/<public_id>/wifi/update` へ切替すると、wifi だけ別 topic 体系を持ち込み、`MQTTコマンド仕様書.md` 全体の統一性が逆に崩れる。
  - [対応内容][2026-05-19] 
    - `WiFi設定更新_詳細設計書.md` §5（旧記載: `device/<public_id>/wifi/update` 例 / `public_id = SHA256(base_mac)` 案）を **不採用**へ更新し、正本を `MQTTコマンド仕様書.md` §2.1 に集約。`public_id` 初期値は `IoT_<base MAC からコロン除去>` で確定。
    - `Mosquitto_TLS_IDパスワード設定手順書.md`（旧記載: 将来 `device/<public_id>/wifi/update` へ統一する場合の推奨）を **不採用クローズ**に更新し、ACL テンプレートも `esp32lab/<kind>/<sub>/<publicId|all>` 系で設計する方針へ。
    - `IoT/ESP32/header/mqttProtocol.h:47-59` の `buildTopicWifiUpdate` / `buildTopicWifiConfirm` Doxygen に「現行運用では未使用、参考実装として残置、新規 publish/subscribe には使用しないこと」のコメントを追加（実装本体は変更なし）。
  - [変更しなかったもの] `IoT/ESP32/src/mqttProtocol.cpp` の関数本体は削除せず参考実装として残置（誤って呼ばれた場合でも `device/<publicId>/wifi/update` 文字列を返すだけで他への副作用なし）。
  - [後続対応] なし。クラウド連携 (`012` 章) で全 MQTT topic 設計を再評価する際に、`esp32lab/...` 維持か `device/<publicId>/...` 風の正規化かを改めて判断する余地はある（その時は別 ID で起票）。

## TPM → DPAPI 整合改訂（2026-05-19）

- [改訂][2026-05-19] `鍵管理および初期セットアップ設計仕様書.md` §2 前提 / §3 鍵構造 / §4 OS 暗号化サービスによる秘密管理（旧 §4 TPM管理）/ §5 起動時処理 / §11 バックアップ方針 / §12 復旧 / §13 セキュリティ前提 / §14 想定外事象 / §16 変更履歴。TPM 前提を撤回し、Windows DPAPI 正本へ更新。
- [改訂][2026-05-19] `LocalServer秘密処理別層仕様書.md` §1.1 脅威モデル / §2 アーキテクチャ / §3 鍵階層 / §4.2 SecretCore 担当 / §5 OS 暗号化サービスによる管理設計（旧 §5 TPM管理設計）/ §6 IPC 方式 / §7.1 公開 API / §8 バックアップ復元 / §9 監査ログ / §10 解析耐性 / §11 Phase1・Phase4 / §13 変更履歴。
- [改訂][2026-05-19] `鍵管理初期セットアップ_実装たたき台.md` §1 スコープ / §2 Phase1〜5 / §3 モジュール案 / §6 API / §7 試験観点 / §8 リスクと対策 / §10 変更履歴。Phase1 / Phase2 は実装済み（`SecretCore/src/key_manager.rs`）であることを明記。
- [追加][2026-05-19] `todo.md` `### 020. 将来対応` `#### 020-A. 後続技術・インフラ` へ **`020-0004`** を追加：`SecretCore` の OS 暗号化サービス（現行: Windows DPAPI）を Mac / Linux でも汎用対応できる方式へ抽象化する将来対応。
- [変更理由] 実装は最初から DPAPI で固定されていたが、設計文書のみ 2026-03-08 改訂で TPM 前提に置き換わったまま実装が追従しなかった。`009-0008` 着手時に齟齬が判明したため、正本を実装側へ揃え、将来の汎用化を `020` 章へ正しく逃がす。

## 変更履歴

- 2026-05-19（続⁷）: `009-0021`（AP 復旧モード、WPA2/固有パスワード/Web UI）を**コア機能既実装確認により完了退避**。`009-0018` と同一コードパスで実装済み。仕様書 `WiFi設定更新_詳細設計書.md` §9.1 のトリガー「Wi-Fi 接続失敗回数しきい値超過による自動 AP 遷移」は実装側に存在せず、`020-0006` を新規追加して将来対応へ移管。`WiFi設定更新_詳細設計書.md` §9.1 のトリガー記述を現状と将来対応に分離して明文化。
- 2026-05-19（続⁶）: `009-0020`（ESP32 current/previous key 2 スロット運用 + 48h/240h 管理）を**試験仕様書整備によるクローズ**として退避。鍵ローテーションは任意運用、コア実装（runtime カウンタ・state 遷移判定・自動削除・認証拒否切替）と加速試験（120 倍スケール・2h 完了）は `020-0005` を新規追加して将来対応へ移管。`試験仕様書.md` `7038` を通常運用試験と加速試験の両モードで詳細化。
- 2026-05-19（続⁵）: `009-0018`（初回ペアリング AP モード、物理操作必須・短時間有効）を**既実装確認により完了退避**。5 層セキュリティ（物理操作・Wi-Fi 認証・Web UI 認証・隔離・単発有効）で達成済み。「短時間有効」を「単発セッション」として `APメンテナンス画面仕様書.md` §1.1 を新設して明文化、`機能仕様書.md` F3-068 / `要件定義書.md` FR-073 に参照追加。N 分無操作の自動切断タイマーは要件としない方針確定。
- 2026-05-19（続⁴）: `009-0017`（ESP32 AES-256-GCM 復号、nonce/tag 検証付き）を**既実装確認により完了退避**。`maintenanceApServer.cpp:881-927` / `MQTT/mqtt.cpp:604-639` / `MQTT/mqttPayloadSecurity.cpp:92-131` の 3 箇所で `mbedtls_gcm_auth_decrypt` 実装済み。13 文書で AES-256-GCM 採用明記済み。差分なし、追加実装不要。
- 2026-05-19（続³）: `009-0014`（MQTT topic 統一）を**不採用クローズ**として退避。実装は `esp32lab/network/...` で動作中、`MQTTコマンド仕様書.md` §2.1 も `esp32lab/<kind>/<sub>/<name>` で統一済み。`WiFi設定更新_詳細設計書.md` / `Mosquitto_TLS_IDパスワード設定手順書.md` / `mqttProtocol.h` の関連記述を整合更新。理由: 「実装済みかどうか／既検討済みか／差分実装の要否」を精査した結果、wifi だけ別 topic 体系を入れるのは逆に統一性を崩すと判断したため。
- 2026-05-19（続²）: `009-0012` を既実装確認により完了退避。`k-device` 導出式を HKDF → HMAC-SHA256 直接導出へ正本更新（実装側は変更なし、設計仕様書 8 文書を更新）。理由: 実装は当初から HMAC-SHA256 直接導出で固定されており、command 署名／OTA 署名／ESP32 側 mbedtls とも HMAC-SHA256 で統一されているため、設計仕様書側を実装に合わせる。
- 2026-05-19（続）: `009-0009` を既実装確認により完了退避。`鍵管理および初期セットアップ設計仕様書.md` / `LocalServer秘密処理別層仕様書.md` / `鍵管理初期セットアップ_実装たたき台.md` の TPM → DPAPI 整合改訂を追記。`020-0004`（Mac/Linux 汎用化）の追加を追記。理由: 設計と実装の齟齬を解消し、将来汎用化の置き場所を `020` 章で固定するため。
- 2026-05-19: 新規作成。`009-0008`（Phase1〜4 担当・期限割当）を完了退避。理由: `鍵管理初期セットアップ_実装たたき台.md` への反映が完了し、`009-0009` 以降の着手前提が文書側で揃ったため、未完了管理から外す。
