# IoT 試験記録書

[重要] 本書は `試験仕様書.md` の実施結果を記録する。  
理由: 実施証跡と不具合再現情報を追跡するため。

## 記録ルール
- [厳守] 試験番号ごとに実施日時、結果、ログ要約、課題を記録する。
- [推奨] NG時は `問題点記録書.md` へ詳細を転記する。

## 試験結果
- 7001: 実施済（OK）
  - 日時: 2026-03-07
  - 実施内容: `npm run build` 後 `npm run start` 実行、`GET /api/health` を確認。
  - 結果要約: `{"status":"ok","service":"local-server"}` を確認。
- 7002: 実施済（OK）
  - 日時: 2026-03-07
  - 実施内容: `npm run test:connect`
  - 結果要約: `connected. brokerUrl=mqtts://172.16.1.59:8883`、`publish success. topic=esp32lab/call/status/all`
- 7003: 実施済（OK）
  - 日時: 2026-03-07
  - 実施内容: `GET /api/devices` とWeb配信確認。
  - 結果要約: `IoT_F0D0F94EB580 / 172.16.1.98 / offline` を取得できた。画面HTMLに `デバイス名 / IPアドレス / status / status更新 / OTA開始` が含まれることを確認。
  - 追記(2026-03-07): LocalServer画面へ `FW書換日時` 列を追加し、`GET /api/devices` で `firmwareVersion=1.1.0-beta.1` / `firmwareWrittenAt=Mar  7 2026 16:18:05` / `lastStatusSub=reply` / `lastSeenAt=2026-03-07T07:24:13.874Z` を確認した。  
    理由: OTA後に「版数」と「FW書換日時」をブラウザで識別できることを確認するため。
- 7004: 実施済（OK）
  - 日時: 2026-03-07
  - 実施内容: `POST /api/commands/status` body=`{"targetNames":"all"}`
  - 結果要約: `{"result":"OK","command":"status"}` 応答を確認。
  - 追記(2026-03-07): ESP32修正版を書込み後、`POST /api/commands/status` body=`{"targetNames":["IoT_F0D0F94EB580"]}` を実施し、`GET /api/devices` で `detail=Reply` / `lastStatusSub=reply` / `lastSeenAt=2026-03-07T06:57:48.095Z` を確認した。  
    理由: `call/status` 受信時に `Reply/Online` を返す要件の実機確認を完了したため。
- 7005: 実施済（OK）
  - 日時: 2026-03-07
  - 実施内容: `POST /api/commands/ota` body=`{"targetNames":"all"}`
  - 結果要約: `{"result":"OK","command":"otaStart"}` 応答を確認。
- 7006: 実施済（OK）
  - 日時: 2026-03-07
  - 実施内容: `GET /ota/manifest.json`、`GET /ota/firmware.bin`
  - 結果要約: `version=1.1.0-beta.2` / `firmwareUrl=https://mqtt.esplab.home.arpa:4443/ota/firmware.bin` / `sha256=e44290dfdc8cb9e52a281b77030f3ca5f1f78dbd7e2da942754630cb1f74ce60` を返し、`firmware.bin` が 200 OK で取得できることを確認した。
- 7007: 実施済（OK/条件付き）
  - 日時: 2026-03-07
  - 実施内容: `POST /api/commands/ota` 後に `GET /api/devices` と `esp32lab/notice/otaProgress/+` を確認。
  - 結果要約: LocalServer に `otaProgressPercent=0` / `otaPhase=prepare|retry|error` / `otaDetail=http status -1 ...` が反映され、ブラウザ一覧へデバイス別進捗欄を表示できる状態を確認した。  
    理由: push通知と30秒定期再取得により、進捗欄自体は自動更新されることを実機メッセージで確認できたため。
- 7010: 実施済（NG）
  - 日時: 2026-03-07
  - 実施内容: `IoT_F0D0F94EB580` へ `firmwareVersion=1.1.0-beta.2` / `sha256=e44290dfdc8cb9e52a281b77030f3ca5f1f78dbd7e2da942754630cb1f74ce60` を含む OTA 要求を送信。
  - 結果要約: ESP32 側で `ota start queued` と `otaProgress` publish は確認できたが、`HTTPClient connect(): failed connect to mqtt.esplab.home.arpa:4443` / `returnError(): error(-1): connection refused` により OTA 本体は失敗した。  
    理由: PC側 `LocalServer` の HTTPS 配布ポート `4443/TCP` へ ESP32 から到達できず、Windows 側受信許可が不足している可能性が高いため。
  - 追記(2026-03-07): Windows 側で `4443/TCP` 許可後に再試行したが、`GET /api/devices` では引き続き `otaPhase=error` / `otaDetail=http status -1` を確認した。  
    理由: Firewall 許可以外にも、ESP32 から見た `4443/TCP` 到達経路に未解決要因が残っているため。
- 7011: 実施済（OK）
  - 日時: 2026-03-07
  - 実施内容: ESP32 OTA 実装を `HTTPClient` 依存から外し、`hostname URL + fallback IP + hostnameでTLS証明書検証` 方式へ変更後、`POST /api/commands/ota` で `https://mqtt.esplab.home.arpa:4443/ota/firmware.bin` を指定して再試験。
  - 結果要約: ESP32 ログで `resolveOtaTargetIp fallback will be used. host=mqtt.esplab.home.arpa fallbackIp=172.16.1.59`、`Certificate verified.`、`executeSingleOtaAttempt success.`、`OTA SUCCESS`、`rst:0xc (RTC_SW_CPU_RST)` を確認した。`GET /api/devices` でも再起動後に `onlineState=online` / `detail=StartUp` / `lastSeenAt=2026-03-07T08:26:50.563Z` を確認した。  
    理由: DNS不達環境でも、TLS検証ホスト名を維持したまま fallback IP へ接続すれば OTA が成立することを実機で確認できたため。
  - 追記(2026-03-07): ESP32 側で `write` 通知を5%刻みに間引き、`done` 通知を再起動前に待つよう改善したうえで、LocalServer 側でも `start-up` status 受信時に OTA 完了表示へ収束させる補正を追加した。再試験で `GET /api/devices` が `otaProgressPercent=100` / `otaPhase=done` / `otaDetail=rebooted after ota` を返すことを確認した。  
    理由: 実機OTA成功後にブラウザが `95%/write` で残留しないことを確認するため。
- 7012: 未実施
- 7013: 未実施
- 7014: 未実施
- 7015: 実施済（OK）
  - 日時: 2026-03-10
  - 実施内容:
    - ESP32へ `beta.9`（`runningPartition=0 / bootPartition=0 / nextUpdatePartition=1`）を書込み、`status` 応答で初期面を確認。
    - `beta.10` を OTA 配布して `runningPartition=1 / bootPartition=1 / nextUpdatePartition=0` への切替を確認。
    - `beta.11` を連続 OTA 配布して `runningPartition=0 / bootPartition=0 / nextUpdatePartition=1` への再切替を確認。
  - 結果要約: 連続2回のOTAで書込み先が `0 -> 1 -> 0` と交互に切り替わることを `status` 一時項目で確認した。  
    理由: 2面(A/B)交互切替の成立を実機で確認できたため。
- 7016: 未実施
- 7017: 未実施
- 7018: 未実施
- 7019: 未実施
- 7020: 未実施
- 7021: 未実施
- 7022: 未実施
- 7023: 未実施
- 7024: 未実施
- 7025: 実施済（OK）
  - 日時: 2026-03-10
  - 実施内容:
    - 途中通信断相当: `firmwareUrl=https://mqtt.esplab.home.arpa:4444/ota/firmware.bin` でOTAを実行し、`otaPhase=error` / `otaDetail=http connect failed` を確認。
    - 整合性不一致: `sha256=0000...0000` を指定してOTAを実行し、`otaPhase=error` / `otaDetail=sha256 mismatch` を確認。
    - `rollbackTestEnable` を有効化した状態で `beta.14` OTA を実行し、起動側が最終的に旧版 `beta.13`（`runningPartition=1 / bootPartition=1 / nextUpdatePartition=0`）へ戻ることを確認。
  - 結果要約: 「途中通信断」「整合性不一致」「未確定起動失敗（rollback強制）」の3条件すべてで旧パーティション継続起動を確認した。  
    理由: `beta.14` 適用直後に rollback 試験モードで再起動を強制した後も、稼働版数が `beta.13` を維持したため。
- 7026: 未実施
- 7027: 実施済（OK）
  - [002-0099 フェーズBクローズ条件][2026-03-13] `todo.md` 002-0099 ゲートの必須試験。
  - 日時: 2026-03-13
  - 実施内容:
    - 7066 の一括メンテで `ap/settings` 取得 → `ap/configure` で同一設定投入 → 再起動要求 → MQTT 復帰を確認。
    - 設定の NVS 保存と復元が成立することを実機で確認した（`runRealDeviceTests.mjs` の 7066 フローで検証）。
  - 結果要約: OK。  
    理由: ap/settings で取得した設定を ap/configure で投入し、再起動後に通常運用へ復帰したことで、NVS 永続の実質動作を確認したため。
- 7028: 実施済（OK）
  - [再開メモ][2026-03-13] `002-0014` 実施時は以下で記録する。
    - 1) 誤設定投入（Wi-Fi AP または MQTTブローカー）で接続不能を再現。
    - 2) APモード移行後、LocalServer `POST /api/admin/ap/configure` で正値再投入。
    - 3) `requestReboot=true` で再起動し、`status` topic 応答再開を確認。
    - 4) 失敗時は `問題点記録書.md` へ切り分け（AP接続、認証、保存、再起動、復帰判定）を記録。
  - 日時: 2026-03-13
  - 実施内容:
    - `POST /api/admin/commands/maintenance-reboot` で AP モード移行後、`/api/admin/ap/scan` から `AP-esp32lab-F0D0F94EB580` を検出。
    - `GET /api/admin/ap/settings` で現設定を取得し、`POST /api/admin/ap/configure` で誤値（`mqttPort=1`）を投入して `requestReboot=true` を実行。
    - 再起動後、`/api/admin/devices` で `onlineState=offline`（`detail=Offline timeout`）を確認し、通常運用での接続不能状態を再現。
    - ユーザー実機操作（起動時3秒長押し）後、再度 `ap/scan` で AP を検出し、`POST /api/admin/ap/configure` で正値（`mqttUrl=mqtt.esplab.home.arpa`、`mqttPort=8883`）を再投入して再起動。
    - 復旧後に `/api/admin/devices` で `onlineState=online` かつ `connectionMode=mqtt` を確認し、`POST /api/commands/status` が `200 result=OK` を返すことを確認。
  - 結果要約: OK。  
    理由: 7028 の要件（誤設定による接続不能再現、AP経由の設定復旧、再起動後の `status` 応答再開）をすべて実機で確認できたため。
- 7029: 実施済（NG）
  - 日時: 2026-03-08
  - 実施内容: `python -m platformio run`、`python -m platformio run -t upload --upload-port COM9`、`nslookup mqtt.esplab.home.arpa 172.17.1.100`、ESP32 シリアルログ確認。
  - 結果要約: `mqtt.esplab.home.arpa` は `172.17.1.100` への DNS 問い合わせで解決でき、ESP32 書込みも成功した。ESP32 ログでも `applyWifiDnsConfiguration success. primary=172.17.1.100` と `wifi event handler: DNS re-applied after GOT_IP. dns1=172.17.1.100` を確認した。一方で起動後は `connectToWifiRouter DNS resolved setting. dns1=172.17.1.1` が出力され、続く `timeService::syncNow` がタイムアウトして `mainTaskEntry: skip mqtt connect until UTC sync for startup will registration.` のまま MQTT 接続処理へ進めなかった。  
    理由: 現行実装では UTC 同期完了まで MQTT 接続を開始しないため、NTP 側の成立を先に解消する必要があるため。
  - 追記(2026-03-08): `nslookup ntp.esplab.home.arpa 172.17.1.100` は `172.17.1.100` を返し、`nslookup pool.ntp.org 172.17.1.100` も成功した。さらに `nslookup mqtt.esplab.home.arpa 172.17.1.100` は `172.17.1.100` を返し、`Test-NetConnection -ComputerName 172.17.1.100 -Port 8883` も `TcpTestSucceeded=True` を確認した。  
    理由: CoreDNS の現行Aレコードと MQTT ポート到達性は成立しており、残課題が `NTP同期` と `ESP32 で最終表示される DNS=172.17.1.1` の切り分けへ絞れたため。
  - 追記(2026-03-08): `w32tm /stripchart /computer:ntp.esplab.home.arpa /samples:1 /dataonly` および `w32tm /stripchart /computer:172.17.1.100 /samples:1 /dataonly` は、いずれも `0x80072746` エラーで時刻取得できなかった。  
    理由: `172.17.1.100` 側の NTP サービス実体または `UDP/123` 応答に問題が残っている可能性が高いため。
  - 追記(2026-03-08): `W32Time` 起動後に `w32tm /stripchart /computer:172.17.1.100 /samples:3 /dataonly` と `w32tm /stripchart /computer:ntp.esplab.home.arpa /samples:3 /dataonly` は成功し、`UDP/123` 待受も確認できた。そのうえで ESP32 の時刻同期先を診断用に `172.17.1.100` 直指定へ変更して再書込みしたところ、`timeService::syncNow succeeded.` と `NTP SYNC OK` を確認し、その後 `MQTT RECONNECT` へ進んだ。  
    理由: `ESP32 -> 172.17.1.100:123/UDP` は成立しており、NTP サービス実体の問題ではないことを確認できたため。
  - 追記(2026-03-08): NTP 直指定成功後も `mqtt.esplab.home.arpa` については `hostByName(): DNS Failed for mqtt.esplab.home.arpa` が再現し、`pingBrokerHost failed. hostByName failed and fallback is unavailable.` により MQTT 初期化は失敗した。  
    理由: 切り分けの結果、現在の主問題は `ESP32` 上の MQTT 向け DNS 名前解決経路であり、NTP 自体は別問題として解消したため。
  - 追記(2026-03-08): `MQTT` と `OTA` の接続順序を「IP設定ありなら先にIPで接続試行、TLS検証の正規名はFQDN維持」へ変更し、`beta.8` を `COM9` へ再書込みした。実機ログで `timeService::syncNow succeeded.`, `connectToMqttBroker success. state=0`, `publishStatusNotice success. ... sub=start-up onlineState=Online` を確認した。  
    理由: `hostByName(mqtt.esplab.home.arpa)` の不安定性を避けつつ、ローカル環境での起動時間短縮とTLS検証整合を両立できることを確認したため。
- 7030: 未実施
- 7031: 未実施
- 7032: 未実施
- 7033: 未実施
- 7034: 未実施
- 7035: 未実施
- 7036: 未実施
- 7037: 未実施
- 7038: 未実施
- 7039: 未実施
- 7040: 未実施
- 7041: 未実施
- 7042: 未実施
- 7043: 未実施
- 7044: 未実施
- 7045: 未実施
- 7046: 未実施
- 7047: 未実施
- 7048: 実施済（OK）
  - 日時: 2026-03-13
  - 実施内容:
    - 7048/7049 試験のため、検証FWでファイルログ有効化状態を作って実機起動し、シリアルログを監視した。
    - 起動ログで `rotateLogFileIfNeeded rotated. previous=/logs/<bootCount>/00001.log ...` を確認し、ローテーション処理入口の実行を確認。
    - 同時に `open(): /littlefs/logs/<bootCount> does not exist, no permits for creation` が連続し、その後 `Stack canary watchpoint triggered (i2cTask)` で再起動ループを再現。
  - 結果要約: 部分NG。  
    理由: ローテーション処理の実行痕跡は確認できたが、ログ保存基盤が不安定でクラッシュし、7048 の合格条件（実機で安定したファイル切替確認）を満たせなかったため。
  - 追記(2026-03-13): `/logs` ディレクトリ判定強化修正（`log.cpp`）を反映して再試験したが、`fileLogSet` 反映直後に `open(): /littlefs/logs/... does not exist` が再現し、再度 `Stack canary watchpoint triggered (i2cTask)` で再起動した。  
    理由: #0008 の根因が未解消であり、7048 は引き続き部分NGのまま。
  - 追記(2026-03-13): 切り分け用に `log.cpp` を「ファイルログ最小1行固定（one-shot）」へ変更し、`fileLogSet` 後に `status` 指令を 620 回（120 + 500）連続投入して再試験した。  
    結果: `open(): /littlefs/logs/2026/0313/... does not exist, no permits for creation` は 2 回再現したが、同条件では `Stack canary watchpoint triggered (i2cTask)` は再現しなかった。
    判定: #0008 は「ファイルログ連続書込み負荷」の関与が濃厚。引き続き `/logs` 作成失敗の根因調査を継続する。
  - 追記(2026-03-13): 失敗段階トレース（`fileLogTrace`）を追加したFWで再試験したところ、`fileLogSet` 直後に  
    `LittleFS begin(): LittleFS Already Mounted` → `open(): /littlefs/logs/2026/0313/... no permits for creation` → `fileLogTrace: rotateLogFileIfNeeded active path does not exist yet` の順で出力され、そのまま `Stack canary watchpoint triggered (timeServerTask)` で再起動した。  
    判定: stack canary は `i2cTask` 固定ではなく、`timeServerTask` でも再発しうる。#0008 は「ファイルログ有効化周辺でのスタック破壊/過負荷」観点で継続調査が必要。
  - 追記(2026-03-13): `log.cpp` を「単一ファイルログ書込みタスク（PSRAM優先スタック）+ WARN以上のみ記録 + INFO記録停止」に変更して再試験した。`status` 200 回、および 620 回の連続投入で `Stack canary` / `Guru Meditation` は未再現。  
    一方で `open(): /littlefs/logs/2026/0313/... no permits for creation` は 1 回再現したため、安定化は前進したが根因（`open`失敗）は未解消。
  - 追記(2026-03-13): 追加切り分けとして「未作成ファイルへの `exists/open(r)` を実施しない」構成へ変更（`activeFileLogPathCreated` で作成済み判定）し、`status` 300 回 + 620 回を再試験。  
    結果: `open(): /littlefs/logs/... no permits for creation` は 0 回、`Stack canary` / `Guru Meditation` も 0 回。  
    判定: 先行して見えていた `open()` エラーは、未作成パスへのサイズ確認手順で発生していたノイズである可能性が高い。
  - 追記(2026-03-13): バッチflush版（`200ms` または `8件`）+ `WARN 120件/時` 制限 + `IOT_ENABLE_FILE_LOG` 対応FWで、`status` 620 回（phase=`batch-final-620`）を再実行。  
    結果: シリアル監視で `open(): ... no permits for creation` / `Stack canary` / `Guru Meditation` / `Backtrace` は 0 件。  
    判定: #0008 再現は本試験条件で最終的に 0 件。7048 の安定性観点は合格。
- 7049: 実施済（OK）
  - 日時: 2026-03-13
  - 実施内容:
    - 7049 の自動削除判定を短時間で再現するため、試験時のみ `log.cpp` の `fileLogTotalSizeLimitBytes` を `8KB` に変更した試験FWを作成し、`COM9` 実機へ書込み。
    - `fileLogSet(enabled=true)` 後に `fileLogSet` 異常payload（`args.enabled` に object 指定）を 700 回投入し、`ERROR` ログを連続生成。
    - シリアル監視で `pruneFileLogsIfNeeded removed. path=... size=...` が連続出力されることを確認。
    - 試験完了後、`fileLogTotalSizeLimitBytes=100KB`（本番値）へ戻したFWを再書込みし、構成を復元。
  - 結果要約: OK。  
    理由: しきい値超過時に古いログから順に削除される実ログ証跡（`pruneFileLogsIfNeeded removed`）を実機で確認し、処理中に `Stack canary` / `Guru Meditation` / `open(): ... no permits for creation` が発生しなかったため。
- 7050: 実施済（OK）
  - 日時: 2026-03-12
  - 実施内容: `POST /api/admin/auth/login` を正常系で実行し、続けて `GET /api/admin/devices` をトークン付きで実行。
  - 結果要約: `loginResult=OK`、`devicesResult=OK` を確認。管理系 API のトークン認証経路が成立した。
- 7051: 実施済（OK）
  - 日時: 2026-03-12
  - 実施内容: 管理者トークン付きで `POST /api/admin/keys/k-user/issue` を実行。
  - 結果要約: `kUserResult=OK` を確認。応答は `keyFingerprint` のみで、raw `k-user` 非返却を確認。
- 7052: 実施済（部分OK）
  - 日時: 2026-03-12
  - 実施内容: 管理者トークン付きで `POST /api/admin/keys/k-device/issue` を `pushToDevice=true` で実行。
  - 結果要約: `kDeviceResult=OK`、`pushed=true` を確認。  
    理由: LocalServer API と MQTT publish 経路は成立したが、ESP32 実機での `keyDevice` 保存ログ確認は未実施のため。
- 7053: 実施済（OK）
  - 日時: 2026-03-12
  - 実施内容:
    - `POST /api/admin/commands/secure-ping` を `targetDeviceName=IoT_F0D0F94EB580` で実行。
    - ESP32シリアルログで受信処理を確認（`onMqttMessageReceived: parsed. encrypted=1 ... sub=securePing`）。
    - 応答復号結果を LocalServer 側で確認。
  - 結果要約: `{"result":"OK","decryptedResponse":{"result":"OK","decrypted":{"pingText":"final-encrypted-e2e-check"}}}` を確認。  
    理由: LocalServer-ESP32 間で `k-device` による payload全文暗号化/復号が往復で成立したため。
  - [旧仕様] 初回は `waitForSecureEcho timeout` で NG だったが、ESP32応答payloadサイズ最適化および暗号化モジュール整理後に再試験で OK へ更新。
- 7054: 実施済（部分OK）
  - 日時: 2026-03-13
  - 実施内容:
    - `POST /api/admin/commands/maintenance-reboot` を実行し、`result=OK` を確認。
    - 続けて `GET /api/admin/ap/scan` をポーリングし、`AP-esp32lab-F0D0F94EB580` の再検出を確認。
  - 結果要約: 部分OK。  
    理由: APモード遷移条件のうち「MQTT call/maintenance」での遷移は確認できたが、「起動時3秒長押し」「Wi-Fi/MQTT未設定」の2条件は未確認のため。
- 7055: 実施済（OK）
  - 日時: 2026-03-13
  - 実施内容:
    - `POST /api/admin/commands/maintenance-reboot` を実行して AP モードへ遷移。
    - ユーザー目視確認で LED 点滅パターンと LCD 表示を確認。
      - LED: `0.3s ON -> 0.3s OFF -> 0.3s ON -> 0.3s OFF -> 1.0s ON -> 0.3s OFF` を繰返し。
      - LCD: 1行目 `MaintenanceMode`、2行目 AP名（16文字以内）。
  - 結果要約: OK。  
    理由: 試験仕様書 7055 の表示要件（LED/LCD）を満たすことを実機目視で確認できたため。
- 7056: 実施済（OK）
  - 日時: 2026-03-13
  - 実施内容:
    - シリアルモニタ取得中に `POST /api/admin/commands/maintenance-reboot` を実行し、`maintenance` 受信から再起動後 APモード移行までを確認。
    - APモード起動ログとして `startMaintenanceApModeAndHold success` / `maintenanceApServer::start success` を確認。
    - APモード移行後ログ区間で `connectToMqttBroker start` が出力されないことを確認。
  - 結果要約: OK。  
    理由: APモード中は MQTT 接続開始処理が走らず、AP専用処理へ遷移していることをシリアルログで確認できたため。
- 7057: 実施済（OK）
  - 日時: 2026-03-12
  - 実施内容:
    - LocalServer を `MQTT_PAYLOAD_ENCRYPTION_MODE=strict` で起動。
    - 平文 payload を test client から `esp32lab/notice/status/IoT_F0D0F94EB580` へ publish。
    - publish 前後の `GET /api/devices` で `lastMessageId` / `lastSeenAt` の更新有無を比較。
  - 結果要約: `RESULT=UNCHANGED` を確認し、`/api/devices` 非更新を確認。  
    理由: strict モードで平文 payload が拒否され、デバイス状態へ反映されないことを確認できたため。
  - ログ要点: LocalServer ログに `encryption is required but envelope is missing` を確認。
- 7058: 実施済（OK）
  - 日時: 2026-03-12
  - 実施内容:
    - ESP32 を `PLATFORMIO_BUILD_FLAGS='-D MQTT_PAYLOAD_SECURITY_MODE=2'` 指定で再書込みし strict で起動。
    - 管理者ログイン後に `POST /api/admin/commands/secure-ping` を実行。
    - 追加で平文 `call/securePing` を test client から publish し、ESP32 側拒否ログを確認。
  - 結果要約: `secure-ping` API は `result=OK` を返し、ESP32 ログで `onMqttMessageReceived: parsed. encrypted=1 ... sub=securePing` を確認。さらに平文 `call/securePing` では `encrypted payload is required` により拒否された。  
    理由: strict モードで暗号化 payload のみ往復受理され、平文は双方で受理されないことを実機で確認できたため。
- 7059: 実施済（部分OK）
  - 日時: 2026-03-12
  - 実施内容:
    - `IoT/LocalServer/public/assets` にダミー画像3件を作成し、`images-pack.zip` を ZIP `stored`（method=0）で再作成。
    - `images-pack.zip` の `SHA-256=65b8d0eb6e2d1f855eacc25c80ecc94160c85dfb8469aaad09472c5099071493` を算出し、エントリ `dummy-01.png`〜`dummy-03.png` を確認。
    - ESP32 側 `imagePackageApply` 実装（`destinationDir` 制約、`overwrite` 制御、`tmp` + `rename`、ZIP展開）をビルド確認（`python -m platformio run` 成功）。
    - 実機E2Eを再実行し、`imagePackageStatus` は `planning=OK` 後に `downloading=NG` で停止することを再現。
    - ESP32シリアルログで `connect(): failed connect to mqtt.esplab.home.arpa:4443` / `connect(): failed connect to 172.16.1.59:4443` / `connect(): failed connect to 172.17.1.100:4443` を確認。
    - OTA経由で最新FWを反映後、再度 `imagePackageApply` を実行し、`errorCode=IPKG_HTTP_CONNECT_FAILED` `detail=... reason=connection refused ...` を確認。
    - 再調査で `connection refused` の前段に `X509 - Certificate verification failed` を確認。
    - `fileSync` で `/certs/mqtt-ca.pem` を更新した後、再起動ログに `sensitiveDataService::loadMqttTlsCertificate failed. cert sha256 mismatch. expected=db59... actual=61f5...` を確認し、MQTT再接続不能化を再現。
    - `COM9` 解放後に復旧FWを書込み、再起動ログで `cert sha256 mismatch` が `warning` 扱いになり、MQTT再接続と `status` 応答の復旧を確認。
    - 復旧後の再試験でも `imagePackageApply` は `errorCode=IPKG_HTTP_CONNECT_FAILED` `reason=connection refused` を再現。
    - 追加切り分けで `imagePackageApply` 実行時に `hostByName(): DNS Failed for mqtt.esplab.home.arpa` を確認し、同一 `IPKG_HTTP_CONNECT_FAILED` に DNS失敗が混在していることを確認。
    - 対策として `mqtt.cpp` に `MqttTlsClient` の host接続フォールバック（DNS失敗時に固定IPへ接続しつつTLS検証ホスト名は維持）を実装し、`downloadImagePackageZip` へ適用。ビルド成功まで確認。
    - ただし最新対策FWは `COM9` 再占有により書込み未実施で、実機E2Eの再判定は未完了。
  - 結果要約: ローカル事前検証は OK。  
    理由: 実機 MQTT/HTTPS 経由の適用確認（`destinationDir` 配下反映、同名上書きの実動作）は、証明書整合不良によるMQTT停止は復旧済みで、`IPKG_HTTP_CONNECT_FAILED` に `DNS失敗` と `接続拒否` の混在も切り分け済み。ただし最終対策FWが `COM9` 占有で未反映のため、`completed` 確認まで未了。部分OKを継続する。
- 7060: 実施済（部分OK）
  - 日時: 2026-03-12
  - 実施内容:
    - `IoT/LocalServer/scripts/generateImagePackageApplyPayload.mjs` を追加し、署名付き payload 生成と自己検証（`SELF_VERIFY=OK`）を確認。
    - `IoT/LocalServer/scripts/publishImagePackageApply.ps1`（一発送信用）を追加し、`-DryRun` で `TOPIC/PAYLOAD` 生成から実行フローまで確認。
    - 異常系として `destinationDir=/logs` を指定した生成を実行し、`/logs` 禁止でエラー終了することを確認。
    - 異常系として不正 `packageSha256=zzzz` を指定した生成を実行し、SHA-256形式エラーで拒否されることを確認。
  - 結果要約: 署名生成前段・入力検証のローカル試験は OK。  
    理由: 実機へ改ざん署名/不一致SHA/不正ディレクトリを送信して `imagePackageStatus` の拒否コードを確認する試験は未実施のため、部分OKとする。
- 7061: 実施済（OK）
  - 日時: 2026-03-13
  - 実施内容:
    - `IoT/LocalServer/scripts/testImageFileSyncE2E.mjs` を実行し、ZIP非使用の `fileSyncPlan` / `fileSyncChunk` / `fileSyncCommit` 更新を実施。
    - 初回実行（既存ダミー3件）で `files=3 splitFiles=0 totalChunks=3` を確認。
    - `public/assets/dummy-4096.png`（4096byte）を作成して再実行し、`files=4 splitFiles=1 totalChunks=7` を確認。
    - 方針変更に合わせ `public/assets/test-split/dummy-120k.png`（122880byte）を作成し、`--splitThresholdBytes 102400 --chunkSizeBytes 102400 --mqttPacketSizeBytes 4096` で再実行。
    - 実行結果 `files=1 splitFiles=1 totalChunks=76 abnormalMode=none splitThresholdBytes=102400 chunkSizeBytes=102400 mqttPacketSizeBytes=4096 phase=completed detail=commit completed` を確認。
  - 結果要約: OK。  
    理由: 100KB超ファイル（122880byte）が `splitThresholdBytes=102400` 条件で分割送信され、`chunkSizeBytes=102400` 指定でも MQTT 上限（4096byte）を超えないサイズへ自動調整されて最終 `completed` まで到達したため。ZIP経路を使わない画像更新標準経路として成立を確認できた。
- 7062: 実施済（OK）
  - 日時: 2026-03-13
  - 実施内容:
    - `--abnormalMode shaMismatch` で実行し、`phase=failed result=NG errorCode=FSYNC_SHA_MISMATCH detail=commit failed` を確認。
    - `--abnormalMode chunkMissing` で実行し、`phase=failed result=NG errorCode=FSYNC_SHA_MISMATCH detail=commit failed` を確認。
    - 途中診断として固定分割時の `chunkSizeBytes=102400` は MQTT payload 上限超過で `timeout` となることを確認済み。
    - その対策として `testImageFileSyncE2E.mjs` を MQTT上限追従の動的分割へ更新し、`chunkSizeBytes=102400`（希望上限）指定でも実送信は安全サイズに自動縮小されることを確認。
  - 結果要約: OK。  
    理由: 想定した異常系2種で commit 失敗と整合エラーコードを確認でき、正常時との判定境界を明確化できたため。
- 7063: 実施済（OK）
  - 日時: 2026-03-13
  - 実施内容:
    - 事前に LocalServer を最新 `dist` へ再ビルド・再起動し、`/api/admin/ap/files/upsert` / `/api/admin/ap/files/delete` ルート有効化を確認。
    - `POST /api/admin/ap/files/upsert`（`targetArea=images`）を実行し、ESP32 応答 `500 {"detail":"file write failed"}` を再現。
    - `POST /api/admin/ap/files/delete`（`targetArea=images`）は `result=OK` を確認。
    - `targetArea=certs` の `upsert`（admin）も同様に `file write failed` を再現。
    - `targetArea=certs` を maintenance ロールで実行し、`403 admin role required for certs update` を確認。
    - `/logs/...` の不正パス送信で `400 invalid targetArea/path` を確認。
    - 追加切り分けとしてシリアルログを確認し、`ensureDirectoryPathExistsForAp failed. mkdir partialPath=/images` を再現。
    - 対策として `maintenanceApServer.cpp` に `ensureLittleFsReadyForAp`（`LittleFS.begin(false)`）を追加し、`upsert` / `delete` 前に初期化確認する修正を実装した。
    - `COM9` 解放後に修正版FWを書込み、再試験で `imgUpsert=200` / `imgDelete=200` / `certUpsertAdmin=200` / `invalidPathReject=400` / `certUpsertMaintenanceDenied=403` を確認。
    - 追加の安定性確認として `images` への `upsert/delete` を再実行し、連続して `200/200` を確認。
  - 結果要約: OK。  
    理由: 7063 の合格条件（`/images`・`/certs` 局所更新、権限拒否、不正パス拒否）を実機で満たし、再試行でも再現性を確認できたため。
- 7064: 実施済（OK）
  - [002-0099 フェーズBクローズ条件][2026-03-13] `todo.md` 002-0099 ゲートの必須試験。
  - 日時: 2026-03-13
  - 実施内容:
    - LocalServer 管理者パスワード変更: `test7064PasswordChange.mjs` で実施。変更→旧値拒否・新値受諾→監査ログ確認→デフォルト復元。
    - APロールパスワード変更・credentials/rotation: ESP32 を AP モードで起動後、`runRealDeviceTests.mjs --step 7064` を実行。
    - 1) `POST /api/admin/ap/password/change` で admin ロールを一時変更→2) `POST /api/admin/credentials/rotation` で例外継続記録→3) 監査ログ（apRolePasswordChanged・credentialRotationRegistered）確認→4) [厳守] パスワードをデフォルトへ復元。
  - 結果要約: OK。  
    理由: LocalServer 管理者・AP admin ロールのパスワード変更、credentials/rotation、監査ログ、試験後のデフォルト復元をすべて実機で確認したため。
- 7065: 実施済（OK）
  - [002-0099 フェーズBクローズ条件][2026-03-13] `todo.md` 002-0099 ゲートの必須試験。
  - 日時: 2026-03-14
  - 実施内容:
    - ESP32 を AP モード（`AP-esp32lab-F0D0F94EB580`）で起動。
    - `node scripts/runRealDeviceTests.mjs --step 7065` を実行。
    - (1) 未認証で `/api/settings/network` にアクセス → 401 確認。
    - (2) admin ロールで mfg パスワード変更を試行 → 403 確認（mfg ロールのみ可）。
    - (3) maintenance/admin ロールで ap/settings 到達可能（7063/7064 で確認済み）。
  - 結果要約: OK。
    理由: AP ログイン必須・RBAC の全項目を実機で確認したため。
- 7066: 実施済（OK）
  - [002-0099 フェーズBクローズ条件][2026-03-13] `todo.md` 002-0099 ゲートの必須試験。
  - 日時: 2026-03-13
  - 実施内容:
    - ESP32 を AP モード（`AP-esp32lab-F0D0F94EB580`）で起動。
    - `node scripts/runRealDeviceTests.mjs --step 7066` を実行。ap/scan → ap/settings 取得 → ap/batch/start（networkSettings 投入、requestReboot=true）を実行。
    - `completed=1 failed=0 total=1` を確認。
  - 結果要約: OK。  
    理由: 探索 → 接続 → ログイン → 設定更新 → 再起動要求を 1 台ずつ順次実行し、個体ごとの結果を追跡できたため。
- 7067: 実施済（OK）
  - [002-0099 フェーズBクローズ条件][2026-03-13] `todo.md` 002-0099 ゲートの必須試験。
  - 日時: 2026-03-13
  - 実施内容:
    - 7066 の ap/batch/start 実行時、`waitForStatusRecoveryByDevice` 経由で再起動後の `status` topic 応答を取得。
    - `public_id` / `fwVersion` / `configVersion` を収集して成否判定。AP 切断のみでは成功扱いにせず、再起動後 status 応答で所定項目が収束した場合のみ成功とした。
  - 結果要約: OK。  
    理由: 7066 バッチの statusWaitTimeoutSeconds 内に status 応答を確認し、成否判定を完了したため。
- 7070: 実施済（OK）
  - 日時: 2026-03-15
  - 実施内容:
    - `migrateKUserToSecretCore.mjs` を実行。TS版 keyStore.json から k-user を抽出（fingerprint=`ecbc2a97ba6236db`）。
    - SecretCore へ `import_k_user` コマンドで送信。`wrapped_k_user.bin` を DPAPI 保護で保存成功。
    - 検証: TS版の k-device (device=`IoT_F0D0F94EB580`, fingerprint=`08636dacfafbf278`) と SecretCore の k-device が完全一致。
    - `USE_SECRET_CORE = true` に変更して LocalServer を起動。
    - ESP32 からの `notice/status` および `notice/trh` メッセージが SecretCore 経由で正常に復号された。
    - `GET /api/devices` で `temperatureC=24.4`, `humidityRh=36.5`, `pressureHpa=1009.16` を確認。
  - 結果要約: OK。
    理由: TS版 k-user の SecretCore 移植後、暗号化/復号が正常動作し、ESP32 との MQTT strict 通信が成立したため。Phase2 完了。
- 7071: 実施済（OK）
  - 日時: 2026-03-15
  - 実施内容:
    - `exportKUserBackupFromSecretCore.mjs` を実行し、`k_user_backup.enc.json` を出力。
    - バックアップファイルが `version=1` / `kdf=scrypt` / `alg=A256GCM` を含むことを確認。
    - `importKUserBackupToSecretCore.mjs` を実行し、同一パスワードで復元を確認。
    - 復元後の fingerprint が `ecbc2a97ba6236db` で一致することを確認。
  - 結果要約: OK。
    理由: Rust 側の `export_k_user` / `import_k_user_backup` で k-user バックアップ受け渡しが成立し、平文キーを露出せず運用できることを確認したため。
- 7072: 実施済（OK）
  - 日時: 2026-03-15
  - 実施内容:
    - `LocalServer/src` を確認し、`SecretCoreIpcClient.sendRequest()` の直接呼出しが `SecretCoreFacade` 以外に存在しないことを確認。
    - `keyService.ts` が `SecretCoreFacade` を経由して `issue_k_user` / `get_k_user_status` / `get_k_device` / `encrypt` / `decrypt` を呼ぶ構成へ更新済みであることを確認。
    - Rust 側 `issue_k_user` / `get_k_user_status` が `keyFingerprint` と `source` を返し、raw key を返さないことを確認。
  - 結果要約: OK。
    理由: `003-0013` の要求どおり、UI 部とセキュア部の境界IFを `SecretCoreFacade` へ固定し、low-level IPC 依存を局所化できたため。
- 7073: 実施済（OK）
  - 日時: 2026-03-15
  - 実施内容:
    - LocalServer を起動し、`SecretCore IPC protection mode=secure` を確認。
    - Pipe 名が `\\.\pipe\iot-secret-core-CF-FV_1-aa5458bc2cf41b11` のようにランダム値を含むことを確認。
    - `GET /api/devices` で `onlineState=online`、`temperatureC=25.06`、`humidityRh=34.28`、`pressureHpa=1007.10` を取得。
    - LocalServer ログに `Failed to parse IPC response`、`Unsupported state or unable to authenticate data`、`IPC protection error` が出ていないことを確認。
  - 結果要約: OK。
    理由: `003-0014` の要求どおり、保護付き IPC のまま SecretCore と LocalServer が通信し、MQTT strict 通信・TRH 受信まで成立したため。
- 7074: 実施済（OK）
  - 日時: 2026-03-15
  - 実施内容:
    - `SecretCore` に `mqtt_publish` と Rust 側 `rumqttc` publish を実装し、`mqttGateway` を `publish transport mode=rust` で起動。
    - `server.ts` に `waitForSecretCoreReady()` を追加し、`SecretCore ready. attempt=3` 後に `MQTT connected.` となることを確認。
    - `POST /api/commands/status` と `POST /api/commands/get`（`subCommand=trh`）を実行。
    - `GET /api/devices` で `IoT_F0D0F94EB580` の `onlineState=online`、`temperatureC=25.25`、`humidityRh=35.697265625`、`pressureHpa=1006.5786743164062`、`environmentSensorAddress=0x76` を確認。
  - 結果要約: OK。
    理由: Stage1 の Rust MQTT command publish が実機環境で成立し、status / get-trh の送信経路が `SecretCore` 経由へ移った状態で BME280 値取得まで確認できたため。
- 7075: 実施済（OK）
  - 日時: 2026-03-15
  - 実施内容:
    - `SecretCore` に `mqtt_start_receiver` / `mqtt_drain_events` を追加し、`mqttGateway` を `transport mode=rust` で起動。
    - 起動ログで `mqttGateway: rust receiver start result. started=true alreadyRunning=false` を確認。
    - `GET /api/devices` で retained notice が反映され、`IoT_F0D0F94EB580` の `onlineState=offline`、`detail=Disconnect`、`lastStatusSub=Will` を確認。
    - 続けて `POST /api/commands/status` と `POST /api/commands/get`（`subCommand=trh`）を実行し、送信系が引き続き Rust 経由で受理されることを確認した。
  - 結果要約: OK。
    理由: 実稼働中の TS MQTT client を使わずに、Rust subscribe bridge の起動と retained notice の取り込みにより `DeviceRegistry` 更新が成立したため。なお、この時点の実機は offline であり、online 応答の再取得確認は次回オンライン時に継続する。
- 7076: 実施済（OK）
  - 日時: 2026-03-15
  - 実施内容:
    - `SecretCore` 受信ループで `k-device` 復号と `status/trh/otaProgress/secureEcho` の最小DTO正規化を実装。
    - `mqttGateway` Rust モードで raw topic/payload の復号処理を通さず、Rust DTO に応じて `DeviceRegistry` / `pendingSecureEcho` / UI 通知へ反映する構成へ更新。
    - 再起動後 `GET /api/devices` で retained notice がそのまま反映され、`IoT_F0D0F94EB580` の `onlineState=offline`、`detail=Disconnect`、`lastStatusSub=Will` を確認。
  - 結果要約: OK。
    理由: retained notice を起点に、TS 側が raw payload を復号・正規化せずに Rust 側 DTO だけで状態一覧更新を継続できることを確認したため。なお、実機 online 復帰時の live DTO 更新は次回オンライン時に継続確認する。
- 7077: 実施済（OK）
  - 日時: 2026-03-15
  - 実施内容:
    - `SecretCore` に `deviceState` 完成スナップショット統合を追加し、`mqtt_drain_events` が `status/trh/otaProgress` DTO に加えて `deviceState` を返す構成へ更新。
    - `mqttGateway` Rust モードで `registry.upsertDeviceState()` を使用するよう変更。
    - LocalServer 再起動後、ログで `get/trh sent. targets=IoT_F0D0F94EB580 reason=offline-to-online` を確認。
    - `GET /api/devices` で `onlineState=online`、`detail=Reply`、`lastStatusSub=reply`、`temperatureC=24.88999938964844`、`humidityRh=36.3427734375`、`pressureHpa=1006.0321655273438`、`environmentSensorAddress=0x76` を確認。
  - 結果要約: OK。
    理由: 実機 online 応答と BME280 値が、Rust 側で統合済みの `deviceState` スナップショット経由で一覧へ反映されることを確認できたため。
- 7078: 実施済（OK）
  - 日時: 2026-03-15
  - 実施内容:
    - `SecretCore` の `mqtt_drain_events` へ `offline timeout` 判定を追加し、`deviceStateUpdated` を返す構成へ更新。
    - `DeviceRegistry` に Rust モード時のローカル timeout 無効化を追加し、`server.ts` の 5 秒周期 `registry.updateOfflineByTimeout()` を撤去。
    - LocalServer 再起動後、`GET /api/devices` で一度 `onlineState=online` を確認。
    - 約 90 秒超待機後、再度 `GET /api/devices` を実行し、`detail=Offline timeout`、`lastStatusSub=timeout`、`onlineState=offline` を確認。
  - 結果要約: OK。
    理由: TS 側ローカル timeout を無効化した状態でも、Rust 側 timeout 判定だけで実機が `online -> offline(timeout)` へ遷移することを確認できたため。
- 7079: 実施済（OK）
  - 日時: 2026-03-15
  - 実施内容:
    - 一時的な `SecretCoreManager` / `SecretCoreIpcClient` / `SecretCoreFacade` を起動。
    - `startMqttReceiver()` 後に `waitForStatusRecovery("", 15)` を実行。
    - 応答として `{"deviceName":"IoT_F0D0F94EB580","publicId":"","firmwareVersion":"1.1.0-beta.14","configVersion":""}` を取得。
  - 結果要約: OK。
    理由: TS 側 `registry.listDevices()` polling へ依存せず、Rust 側 `deviceState` を参照した online 復帰待機結果が IPC で返ることを確認できたため。
- 7080: 未実施
  - 理由: OTA workflow は実機へ副作用があるため、今回はビルド・型・起動確認までを実施し、実際の workflow 開始試験は次回の明示実行時に行う。
- 8001: 未実施

## 変更履歴
- 2026-03-15: 7080（SecretCore OTA workflow Stage7 起動・状態取得試験）を未実施で追加。理由: 実装完了に合わせて試験枠を先行確保し、実機副作用を避けて今回は起動確認までに留めたため。
- 2026-03-15: 7079（SecretCore `wait_for_status_recovery` Stage6 単体IPC試験）を OK で追加。理由: `003-0012` の完了判定監視責務移行と単体確認結果を正式記録へ反映するため。
- 2026-03-15: 7078（SecretCore `offline timeout` Stage5 実機試験）を OK で追加。理由: `003-0012` の状態遷移責務移行と実機確認結果を正式記録へ反映するため。
- 2026-03-15: 7077（SecretCore `deviceState` 完成スナップショット Stage4 実機試験）を OK で追加。理由: `003-0012` の状態統合責務移行と実機確認結果を正式記録へ反映するため。
- 2026-03-15: 7076（SecretCore MQTT 受信DTO正規化 Stage3 起動試験）を OK で追加。理由: `003-0012` の受信payload処理移行開始結果を正式記録へ反映するため。
- 2026-03-15: 7075（SecretCore MQTT notice subscribe Stage2 起動試験）を OK で追加。理由: `003-0012` の subscribe Rust化開始と retained notice 取り込み結果を正式記録へ反映するため。
- 2026-03-15: 7074（SecretCore MQTT command publish Stage1 試験）を OK で追加。理由: `003-0012` の最初の通信Rust化と readiness 待機の実施結果を正式記録へ反映するため。
- 2026-03-15: 7073（SecretCore IPC 保護試験）を OK で追加。理由: `003-0014` 実装の起動・E2E確認結果を正式記録へ反映するため。
- 2026-03-15: 7072（SecretCore 境界IF固定試験）を OK で追加。理由: `003-0013` 実装の確認結果を正式記録へ反映するため。
- 2026-03-15: 7071（SecretCore k-user バックアップ出力/復元試験）を OK で追加。理由: Rust 側バックアップ受け渡し実装の実施結果を正式記録へ反映するため。
- 2026-03-15: 7070（SecretCore k-user インポート・動作検証試験）を OK で追加。理由: Phase2（TS版 k-user の SecretCore 移植）実施結果を正式記録へ反映するため。
- 2026-03-13: 7027/7066/7067 を実施済（OK）へ更新。APモード機体で `runRealDeviceTests.mjs` を実行し、7066 一括メンテ（ap/settings→同一設定投入→再起動→MQTT復帰）完了。7027 は 7066 の設定往復・NVS 永続を実質確認、7067 は batch の `waitForStatusRecoveryByDevice` で成否判定済み。理由: 実機試験実施結果を正式記録へ反映するため。
- 2026-03-14: 7065 を実施済（OK）へ更新。`runRealDeviceTests.mjs --step 7065` で未認証401・adminによるmfgパスワード変更403を実機で確認。理由: APモード移行後に試験が成立したため。
- 2026-03-13: 7064 を実施済（部分OK）へ更新。LocalServer 管理者パスワード変更 API を `test7064PasswordChange.mjs` で実施し、監査ログ確認まで完了。APロール変更・credentials/rotation は実機接続時に実施。理由: 実機不要部分の試験を自動化スクリプトで実施完了したため。
- 2026-03-13: 7027/7064/7065/7066/7067 に `002-0099 フェーズBクローズ条件` と再開メモを追記。理由: 実機試験実施時に `todo.md` ゲートとの対応と実施手順を明確化するため。
- 2026-03-14: 7065 を OK で更新。未認証 401・admin による mfg 変更 403 を実機確認。理由: runRealDeviceTests.mjs --step 7065 で実施完了したため。
- 2026-03-13: 7065〜7067 の受け皿を追加。理由: `試験仕様書.md` に追加した APログイン必須/RBAC、AP一括メンテ、再起動後`status`判定の結果を1対1で記録するため。
- 2026-03-13: 7064（初期/各種パスワードの変更推奨運用と監査ログ）を追加。理由: `試験仕様書.md` との1対1対応を維持し、方針変更後の受け皿を先行で用意するため。
- 2026-03-13: 7049 を OK で更新。理由: 試験用しきい値（8KB）で `pruneFileLogsIfNeeded removed` を実機確認し、完了後に本番しきい値（100KB）へ復元して再書込みしたため。
- 2026-03-13: 7048 を OK で更新。理由: バッチflush版FWの最終620回再試験で異常0件を確認し、安定性合格を確定したため。
- 2026-03-13: 7048 にバッチflush版FWの最終620回再試験結果を追記。理由: `batch-final-620` で異常0件を確認し、#0008 再現有無の最終判定を記録するため。
- 2026-03-13: 7048 に `exists/open(r)` 回避後の再試験結果を追記。理由: `open(): ... no permits for creation` が 0 件化し、根因候補が「未作成パスへの確認手順」に収束したため。
- 2026-03-13: 7048 に単一書込みタスク化の再試験結果を追記。理由: 連続投入（200/620）で stack canary が止まり、負荷由来の症状と `open()` 失敗を分離できたため。
- 2026-03-13: 7048 に失敗段階トレース再試験結果を追記。理由: `LittleFS Already Mounted`・`open(): ... no permits for creation`・`timeServerTask` stack canary の連鎖を同一試験で採取し、#0008 の調査観点が更新されたため。
- 2026-03-13: 7048 の追記を更新。理由: 最小1行固定（one-shot）再試験で `open(): /littlefs/logs/...` は再現しつつ `i2cTask` stack canary は未再現となり、切り分け結果を記録する必要があるため。
- 2026-03-13: 7048 を「部分NG」へ更新し、7049 再開条件を追記。理由: ファイルログ有効化検証中に `i2cTask` stack canary 再発を確認し、先に安定化対応が必要になったため。
- 2026-03-13: 7055 を OK で更新。理由: APモード移行後の LED 点滅パターンと LCD 表示が仕様どおりであることを実機目視で確認できたため。
- 2026-03-13: 7028 を「OK」へ更新。理由: AP 再移行後に正値再投入で MQTT online 復帰と `status` 要求成功を確認し、復旧シナリオを完遂したため。
- 2026-03-13: 7028 を「部分OK」へ更新。理由: 誤設定投入による接続不能再現までは確認できた一方、復旧フェーズは AP 再移行待ちで継続中のため。
- 2026-03-13: 7056 を OK で更新。理由: `maintenance` 指令後の再起動ログで AP専用処理への遷移と MQTT接続開始抑止を確認できたため。
- 2026-03-13: 7063 を「OK」へ更新。理由: LittleFS 初期化対策FWの反映後に `upsert/delete` と RBAC/不正パス拒否を再試験し、合格条件を満たしたため。
- 2026-03-13: 7063 の切り分け結果を更新。理由: `file write failed` の実体が LittleFS 初期化不足であることをシリアルログで特定し、修正実装まで実施したため（実機反映は `COM9` 解放待ち）。
- 2026-03-13: 7054 を「部分OK」、7063 を「部分NG」で更新。理由: 実機で AP モード遷移（maintenance 指令）と RBAC/パス拒否は確認できた一方、`/api/files/upsert` が `file write failed` で失敗し、FW再書込み待ちの残課題が明確化したため。
- 2026-03-13: 7048/7049 に再開メモを追記。理由: `002-0011`/`002-0012` 実装完了後、未実施の実機試験観点（ローテーション/自動削除）を明確化して次回作業へ引き継ぐため。
- 2026-03-13: 7063（APモード局所ファイル更新）の受け皿を追加。理由: `002-0009` 実装（AP向け `/images` `/certs` 局所更新API）の実機試験を追跡できるようにするため。
- 2026-03-13: 7028 に `002-0014` 向け再開メモを追記。理由: AP設定復旧試験の実施順序と証跡観点（投入/再起動/復帰判定）を先に固定し、実機作業の手戻りを防ぐため。
- 2026-03-13: 7061/7062 の記録を MQTT上限追従の動的分割実測へ更新。理由: 固定1KB運用から、MQTT送信可能サイズに合わせて自動調整する実装・試験結果へ整合させるため。
- 2026-03-13: 7061 を 100KB分割方針の実測結果へ更新し、7062（SHA不一致/チャンク欠損）を OK で追加。理由: `fileSync` 標準経路の正常系・異常系を実機結果で確定したため。
- 2026-03-13: 7061 を未実施から OK へ更新。理由: 4096byteダミー画像を追加した分割転送E2Eで `splitFiles=1` と `completed` を確認し、ZIP非使用の標準更新経路が成立したため。
- 2026-03-13: 7061（`fileSync` 画像更新標準経路）を追加。理由: ZIP更新の代替として、1ファイル更新 + chunk分割方式の試験追跡を開始するため。
- 2026-03-13: 7059 を追記更新。理由: `IPKG_HTTP_CONNECT_FAILED` 内訳に `hostByName(): DNS Failed` が混在することを実機ログで確認し、DNS失敗時のTLS検証維持IPフォールバック実装（ビルド成功・書込み待ち）を記録するため。
- 2026-03-13: 7059 を追記更新。理由: 復旧FW書込み後にMQTT再接続は回復した一方、`imagePackageApply` は `IPKG_HTTP_CONNECT_FAILED` が継続することを確認し、未完了要因を `:4443` 到達性へ限定できたため。
- 2026-03-13: 7059 を追記更新。理由: `connection refused` の背景に `X509` 検証失敗と `cert sha256 mismatch` があることを実機ログで確認し、根本原因を証明書メタ不整合として記録するため。
- 2026-03-13: 7059 を追記更新。理由: OTA反映後の再試験で `IPKG_HTTP_CONNECT_FAILED`（`connection refused`）を確認し、失敗点が `SHA-256` ではなく HTTPS 配布口 `:4443` 到達性であることを記録するため。
- 2026-03-12: 7059 を追記更新。理由: 実機再試験で `downloading=NG` を再現し、シリアルログで `:4443` への `connect failed` を確認できたため、阻害要因（HTTPS配布経路未到達）を明文化する。
- 2026-03-12: 7060 へ `publishImagePackageApply.ps1` の `DryRun` 検証結果を追記。理由: 実行者が手作業で `TOPIC/PAYLOAD` を転記せずに送信できる導線を追加し、試験手順の再現性を上げるため。
- 2026-03-12: 7059/7060 を「部分OK」で更新し、`imagePackageApply` のローカル事前検証（ZIP作成・SHA算出・署名生成・入力異常系）結果を追記。理由: 実機未接続でも実施可能な範囲を先に証跡化し、未実施項目を明確に残すため。
- 2026-03-12: 7044〜7049 と 7059〜7060 の受け皿を追加。理由: `fileSync` / `imagePackageApply` の仕様追加に対して試験仕様書と1対1で追跡できるようにするため。
- 2026-03-12: 7057（strict 平文拒否）と 7058（strict E2E）を OK で追加。理由: `009-0025` の完了条件に定義した strict モード実機試験の結果を正式記録へ反映するため。
- 2026-03-12: 7053 を NG から OK へ更新し、`k-device` による payload全文暗号化通信（LocalServer-ESP32往復）の実機結果を追記。理由: 本日実装した暗号化モジュールの最終試験結果を正式記録へ反映するため。
- 2026-03-12: 7053（`securePing`）の初回実行結果を NG で追記。理由: 対象ESP32未接続時のタイムアウト挙動を記録し、事前条件を明確化するため。
- 2026-03-12: 7050〜7052 の実施結果（管理者認証、`k-user` 発行、`k-device` 発行API）を追加し、7053〜7056 の受け皿を追加。理由: 今回実装した管理系 API と暗号化MQTT/APモード試験項目を 1対1 で追跡するため。
- 2026-03-10: 7025 を条件付きから OK へ更新し、`未確定起動失敗` の rollback 強制再現結果を追記。理由: `001-0003` の完了条件を満たしたため。
- 2026-03-10: 7015（A/B交互切替）と 7025（途中通信断/整合性不一致の旧面継続起動）の実施結果を追加。理由: `001-0002` と `001-0003` の実機証跡を反映するため。
- 2026-03-09: 7039〜7043 の受け皿を追加。理由: Rust 主導高リスク workflow と進捗状態遷移の結果を 1対1 で記録できるようにするため。
- 2026-03-09: 7030〜7038 の受け皿を追加し、7028/7029 の番号整理を反映。理由: AP 認証、pairing bundle、current/previous key、48h/240h 状態遷移の結果を 1対1 で記録できるようにするため。
- 2026-03-08: 7028 へ `MQTT/OTA` の IP優先接続版(`beta.8`) の成功結果を追記。理由: IP優先 + FQDN保持の併用仕様で、実機起動から MQTT status publish まで通ることを証跡化するため。
- 2026-03-08: 7028 へ NTP 直指定診断結果（NTP 成功、MQTT FQDN の `hostByName` 失敗再現）を追記。理由: DNS 問題の対象が NTP ではなく MQTT 名解決経路へ絞れたことを記録するため。
- 2026-03-08: 7028 へ CoreDNS 修正後の再試験結果（`mqtt.esplab.home.arpa -> 172.17.1.100`、`8883/TCP` 到達可、NTP 応答失敗）を追記。理由: 現在のボトルネックが MQTT ではなく NTP 側であることを実測値で残すため。
- 2026-03-08: 7028 の実施結果を追加。理由: 新ルータ運用開始後の DNS 名解決、ESP32 書込み、NTP 待ちによる MQTT 未到達を実測値として残すため。
- 2026-03-08: 7026〜7027 の受け皿を追加。理由: ESP32 設定永続保存と、APメンテナンスモード経由の設定修正結果を 1対1 で記録できるようにするため。
- 2026-03-08: 7024〜7025 の受け皿を追加。理由: OTA途中失敗時の旧面継続起動と、eFuse 最終化後 OTA 更新の結果を 1対1 で記録できるようにするため。
- 2026-03-08: 7019〜7023 の受け皿を追加。理由: `Production` 認証、eFuse 中断後再判定、スタック余裕、リハーサル完了確認の結果を 1対1 で記録できるようにするため。
- 2026-03-08: 7012〜7018 の受け皿を追加。理由: OTA再認証、2面切替、メーカー署名運用の試験結果を 1対1 で記録できるようにするため。
- 2026-03-07: 7011へ `done/100%` 表示確認結果を追記。
- 2026-03-07: 7011 を追加し、OTA成功（fallback IP + hostname TLS検証）の実機結果を記録。
- 2026-03-07: 7003へ `FW書換日時` 表示確認結果を追記。
- 2026-03-07: 7004へ実機 `status更新` 成功結果（`Reply` 応答確認）を追記。
- 2026-03-07: 7006 を実配布ファイルありの結果へ更新し、7007/7010 の実機OTA結果を追記。
- 2026-03-07: 7001〜7006の初回実施結果を記録。
- 2026-03-07: 新規作成。LocalServer初期実装の試験記録台帳を追加。
