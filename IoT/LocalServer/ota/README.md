# OTA 配布ディレクトリ

[重要] 本ディレクトリには `firmware.bin` を配置する。  
理由: LocalServerがHTTPS経由でESP32へ配布するため。

[厳守]
- ファイル名は `firmware.bin` を基本とする。
- 配布前にハッシュ値とバージョンを `IoT/試験記録書.md` へ記録する。

[禁止]
- テスト用の不正バイナリを本番運用ディレクトリへ残置しない。
