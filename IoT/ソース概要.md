# IoT ソース概要

[重要] 本書は `IoT` 配下のソース/フォルダ索引です。  
理由: 変更対象のフォルダと参照すべき仕様書を迷わず辿れるようにし、実装・文書・試験の整合を保つため。

## 1. 参照順序
- `ドキュメント概要.md`: まず確認する文書索引。
- `日常運用_LocalServerとSTA接続_クイックリファレンス.md`: LocalServer 起動と ESP32（STA）接続の運用入口（索引の「すぐ使う索引」からも参照）。
- `ソース概要.md`: どのフォルダ/ソースを変更するか判断する索引。
- `モジュール仕様書.md`: [重要] 責任範囲と役割境界の親定義。
- `要件定義書.md`: 変更の要件境界を確認する。
- `機能仕様書.md` / `IF仕様書.md`: 振る舞いと IF を確認する。
- `製造・セキュア化手順書.md`: [重要] 製造・初回セキュア化・文鎮化防止の順序を確認する。
- `デバイス管理表.md`: 対象機の管理番号、`MAC`、`eFuse` 状態、レビュー状態を確認する。
- `製品化成果概要書.md`: 今回の成果範囲と未完了事項を確認する。
- `ストレージ仕様書.md`: `NVS` / `LittleFS` の保存区分、`/images` `/certs` `/logs`、画像/証明書更新、ログ保持を確認する。
- `冗長化・多重接続仕様書.md`: `serverId` / `brokerId` / `wifiApId`、多重接続、冗長化、`k-user` / `k-device` の運用前提を確認する。
- `セキュリティ方針_認証情報取扱い.md`: セキュリティ制約を確認する。
- `試験仕様書.md` / `試験記録書.md`: 試験観点と証跡更新先を確認する。

## 2. フォルダ概要
- `ESP32/`
  ESP32-S3 実装本体。MQTT / HTTPS / OTA / AP モード / NVS 保存 / LittleFS 更新 / ログ保存を担当する。
- `LocalServer/`
  TypeScript / Node.js ベースのローカルサーバー。Web UI、REST API、MQTT 連携、OTA 配布、運用状態管理を担当する。
- `ProductionTool/`
  [重要][2026-03-17] 工場端末向け `ProductionTool` の実装格納先。`LocalServer` と分離した起動物、追加認証、対象機確認、dry-run、監査ログ導線の実装入口。
- `LocalSoft/`
  [廃止の方針][念のため保存] 旧ローカル運用ソフト。新規実装は `LocalServer/` 側へ統合する。
- `CloudServer/`
  [将来対応] クラウド側実装格納先。現時点では主対象外。
  [参照補足][2026-04-26] クラウド実装の着手判断は `todo.md` の `012` 章、クラウド以外の将来対応は `020` 章を参照する。
- `shared/`
  ESP32 / Local / Cloud 間で共有する公開定義格納先。topic 名、共通定数、protocol version などを置く。

## 3. 主要変更窓口
- `shared/include/common.h`
  [重要] MQTT topic、共通 command 名、公開定数の変更窓口。`MQTTコマンド仕様書.md` と同時更新する。
- `ESP32/header/mqttProtocol.h` / `ESP32/src/mqttProtocol.cpp`
  MQTT payload の生成/解析、topic 利用、status 通知の変更窓口。
- `ESP32/header/otaRollback.h` / `ESP32/src/otaRollback.cpp`
  [重要] rollback 前提（起動確定、未確定起動失敗再現、試験モード切替）の変更窓口。
- `ESP32/header/httpProtocol.h` / `ESP32/src/httpProtocol.cpp`
  HTTPS API、OTA 関連 HTTP アクセス、サーバー I/F 変更窓口。
- `ESP32` の NVS 管理実装
  [重要] `k-device`、Wi-Fi / MQTT / OTA / 認証情報、current/previous 2 スロット運用の変更窓口。
- `ESP32/src/sensitiveData.cpp`
  [重要][2026-05-14] Wi-Fi / MQTT / OTA / `k-device` の NVS 読み書きと、旧 LittleFS `sensitiveData.json` からの復旧移行の変更窓口。`mqttPort=8883` の場合は保存時・読込時に `mqttTls=true` を強制する。理由: TLS listener へ平文 MQTT を入れて Mosquitto `protocol error` へ戻さないため。
- `ESP32/src/main.cpp`
  [重要][2026-05-14] 起動時の設定ロード、Wi-Fi / NTP / MQTT 初期化順序の変更窓口。`mqttPort=8883` で旧NVS等に `mqttTls=false` が残っても、MQTT 初期化前に `mqttTls=true` へ補正する。
- `ESP32/platformio.ini` / `ESP32/scripts/ensure_app0_upload_offset.py`
  [厳守][2026-05-14] PlatformIO 標準 upload の app 書込み先を `app0=0x30000` に固定する変更窓口。`board_upload.offset_address=0x30000` と guard script により、旧 `0x10000` へ戻る設定を禁止する。
- `ESP32` の LittleFS 管理実装
  [重要] `/images` `/certs` `/logs`、`fileSync` 系更新、証明書読込、ログローテーションの変更窓口。
- `LocalServer` の API 実装
  AP 共通トップ画面、ProductionTool 追加認証、pairing 開始、key 状態表示の変更窓口。
- `LocalServer/src/pairingWorkflowInput.ts`
  [重要][2026-03-16] pairing workflow の `requestedSettings` 入力モデル化、`ap/configure` 系入力からの変換、TS 側の事前検証の変更窓口。
- `LocalServer/scripts/backup-k-user-for-7090.ps1` / `LocalServer/scripts/restore-k-user-after-7090.ps1`
  [重要][2026-04-24] `keyStore.json` の k-user 退避・復元用スクリプトの変更窓口。`7090` での再発行回避に使う。
- `LocalServer/scripts/backup-device-db-for-7090.ps1` / `LocalServer/scripts/restore-device-db-after-7090.ps1`
  [重要][2026-04-24] `settings.json` / `securityState.json` / `keyStore.json` / `wrapped_secret.bin` / `wrapped_k_user.bin` をまとめて扱う `device_db` 復旧スナップショットの変更窓口。
- `LocalServer/src/secretCoreIpcClient.ts`
  [重要][2026-05-11] LocalServer 側の SecretCore 低レベル IPC クライアント。named pipe 本体通信と AES-256-GCM 保護の変更窓口。
- `LocalServer/src/server.ts`
  [重要][2026-05-13] LocalServer の HTTP API 本体。`secure-ping` と不正署名試験 API に加え、`settings.html` から使う `device_db` 退避/復元と `k-user` 暗号化バックアップの出力/復元、障害時再登録フローの案内 API / 復元実行 API / 一括実行 API を扱う変更窓口。監査ログのファイル保存（0600 相当）と OS ログ二重書き込みもここで実施する。  
  [参照][2026-05-17] SQLite 履歴ストアの生成・定期パージ起動（`LOCAL_HISTORY_*`）の窓口。管理者 API `POST /api/admin/local-history/export` と定期エクスポート（`7104` / `7106`）。
- `LocalServer/src/localHistoryStore.ts`
  [重要][2026-05-17] `LocalServer` の SQLite 履歴（`deviceStatusHistory` / `commandHistory` / `exportHistory` / `serverEventHistory`）。スキーマ・保持日数パージの変更窓口（**実効保持日数の正本は** `data/settings.json` の **`localHistoryRetentionDays`**、`localHistoryRetention.ts` で検証）。フィルタ付き SELECT（手動エクスポート用）と `exportHistory` INSERT。突合は `DB仕様書.md` と `config.ts`。試験は `試験仕様書.md` **`7102` / `7104` / `7105` / `7106`**。
- `LocalServer/src/mqttGateway.ts`
  [重要][2026-05-17] MQTT 送受信とデバイス状態レジストリ更新。`historyStore` 指定時は status 受信および `publishCommand` 成功ごとに履歴へ追記する変更窓口。マスク規則の変更時は **`7103`** を再確認する。
- `LocalServer/src/historyRedaction.ts`
  [重要][2026-05-17] 履歴 `detail` / MQTT `args` の機密キー名に応じたプレースホルダ置換（`mqttGateway` と同一ルール）。自動試験 `npm run test:7102`（7103 相当）の正本。
- `LocalServer/src/keyService.ts`
  [重要][2026-05-13] `k-user` / `k-device` 発行、`k-user` 暗号化バックアップの出力/復元、SecretCore Facade 連携の変更窓口。
- `LocalServer/src/secretCoreFacade.ts`
  [重要][2026-05-11] LocalServer から SecretCore へ渡す高レベル API の窓口。`runSignedOtaCommand()` を含む workflow 起動関数群の変更窓口。
- `LocalServer/src/secretCoreManager.ts`
  [重要][2026-05-14][2026-05-17] SecretCore 子プロセスの起動・stdin ブートストラップ・**stdout の bootstrap ack 待ち**（`waitForBootstrapAck`）の変更窓口。`stdio` は `pipe` を維持し、`child.stdin` が null になる設定へ戻さない。理由: stdin handshake 不成立・**未再ビルドの `secret_core.exe`** では Node 側のランダム pipe 名と SecretCore の既定 `iot-secret-core-ipc` が不一致となり `connect ENOENT` や `legacy-compatible` のまま health が通らないため。検証は `npm run verify:bootstrap`。
- `LocalServer/public/settings.html`
  [重要][2026-05-13] 設定画面と復旧画面の UI。`device_db` 退避/復元、`k-user` 暗号化バックアップの出力/復元、OTA 設定の更新窓口。[重要][2026-05-17] **`localHistoryRetentionDays`**（`GET`/`PUT /api/settings`）の入力・保存窓口。
- `LocalServer/public/admin.html`
  [重要][2026-05-13] 鍵操作と障害時再登録フロー案内・復元実行・一括実行の UI。`k-user` / `k-device` 発行と `/api/admin/recovery/re-registration/plan` / `restore` / `execute` の表示窓口。[厳守][2026-05-17] **`POST /api/admin/local-history/delete-database`**（確認トークン入力＋`DELETE_LOCAL_HISTORY_DB`）の危険操作窓口。
- `SecretCore/src/pairing_workflow.rs`
  [重要][2026-03-16] pairing workflow の Rust 側状態管理、`createPairingBundle` 相当、AP 到達〜ECDH〜`POST /api/pairing/secure-bundle` による暗号 bundle 送達〜`completed(OK)` までの変更窓口。`k-user` 再発行後の再ペアリングと障害時再登録フローの再実行点でもある。
- `SecretCore/src/key_manager.rs`
  [重要][2026-05-11] `wrapped_secret` の version / createdAt / integrity 検証付き復元、`k-user` の export/import、`keyStore.json` 相当のバックアップ/復元、`k-device` 導出の変更窓口。`LocalServer/scripts/backup-k-user-for-7090.ps1` / `restore-k-user-after-7090.ps1` と対で参照する。
- `SecretCore/src/ota_workflow.rs`
  [重要][2026-05-11] 署名付き OTA ワークフローの本体。`runSignedOtaCommand()` 相当の開始、進捗監視、完了判定の変更窓口。
- `SecretCore/src/main.rs`
  [重要][2026-05-11] SecretCore の IPC 受理本体。stdin/stdout ブートストラップと named pipe 受付の変更窓口。
- `ESP32/src/maintenanceApServer.cpp`
  [重要][2026-05-02] AP メンテナンス HTTP の変更窓口。Pairing 系は `GET /api/pairing/state`、`POST /api/pairing/session`〜`transport-handshake`、`POST /api/pairing/secure-bundle`（AES-256-GCM 復号・NVS 反映・`state=applied`）を扱い、通常運用FWでは `pairing` / `production` 系APIを無効化する。
- `ESP32/header/firmwareMode.h` / `ESP32/src/log.cpp`
  [重要][2026-05-02] 通常運用FW/診断用FWの切替点。`esp32s3_secure_final` では高詳細ログを抑止し、`esp32s3_secure` / `esp32s3_secure_rescue` では診断ログを有効化する。
- `LocalServer/scripts/test7102LiveMqttHistory.mjs`
  [重要][2026-05-17] 試験 **7102** の **実機 MQTT** 相当。`POST /api/commands/status` 後に管理者 **`POST /api/admin/local-history/export`**（`deviceStatus`・`deviceNo`）の **`recordCount` 増加**を確認。`npm run test:7102:live`（`--spawnLocalServer` で子起動可）。レポート `logs/test-reports/7102-live-mqtt-*.json`。
- `LocalServer/scripts/test7106ScheduledHistoryExport.mjs`
  [重要][2026-05-17] 試験 **7106** 手順 1〜2。子プロセスで `LOCAL_HISTORY_SCHEDULED_EXPORT_*` を有効化し、`scheduled-history-export` 平文が 2 件以上となるまで待機。`npm run test:7106`。
- `LocalServer/scripts/test7106ScheduledExportDisable.mjs`
  [重要][2026-05-17] 試験 **7106** 手順 3。フェーズ1で定期出力を確認、フェーズ2は **`LOCAL_HISTORY_SCHEDULED_EXPORT_ENABLED=false`** で再起動し **90s 新規 scheduled なし**。**PORT 3198/4458**・専用 DB/`history-exports-7106-disable-autotest`。`npm run test:7106:disable`。
- `LocalServer/scripts/verifyLocalServerBootstrap.mjs`
  [重要][2026-05-17] SecretCore **`cargo build`**（既定）と `npm run build` の後、子で `dist/server.js` を短時間起動し **`SecretCore ready.`** と **`GET /api/health`** を確認。`npm run verify:bootstrap`。オプション `--skipCargo` / `--skipTsc`。`npm run secret-core:build` は SecretCore のみビルド。
- `LocalServer/scripts/test7083OtaDurability.mjs` / `LocalServer/scripts/test7084OneHourLoad.mjs`
  [重要][2026-03-16] `7083` / `7084` の半自動試験スクリプト。workflow 履歴、device snapshot、JSON レポート出力の変更窓口。
- `LocalServer/scripts/testImageFileSyncE2E.mjs` / `LocalServer/scripts/pushMqttCaByFileSyncE2E.mjs` / `LocalServer/scripts/testImagePackageApplyE2E.mjs`
  [重要][2026-05-14] `7044`〜`7047` の fileSync / 画像更新 / 証明書更新系 E2E 試験スクリプト。`keyService` と `mqttPayloadSecurityService` は async API のため、`getKDeviceBase64()` / `encodeOutgoingPayload()` / `decodeIncomingPayload()` は必ず `await` する。
- `ProductionTool画面仕様書.md` / `モジュール仕様書.md`
  [重要][2026-03-16] `ProductionTool` 基本機能安定化フェーズの画面構成、責務境界、dry-run、監査ログ導線の変更窓口。
- `ProductionTool/README.md` / `ProductionTool/src/README.md` / `ProductionTool/tests/README.md`
  [重要][2026-03-17] `ProductionTool` 最小実行骨格の実装入口、想定フォルダ構成、試験補助資材配置先の変更窓口。
- `ProductionTool/Cargo.toml` / `ProductionTool/src/main.rs` / `ProductionTool/src/app_config.rs` / `ProductionTool/src/audit_logger.rs`
  [重要][2026-03-17] `ProductionTool` 最小起動骨格の実コード。独立起動、設定読込、監査ログ初期化、`SecretCore` Named Pipe 到達確認、安全停止、`PC-004` 鍵ID確認、`run` の段階逐次実行を含む不可逆ランナー導線の変更窓口。
- `ProductionTool/src/startup_screen_state.rs`
  [重要][2026-03-17] PT-001 起動画面・PT-007 安全停止画面の DTO 定義。将来 GUI 移行時もここを置き換える。
- `ProductionTool/src/auth_screen_state.rs`
  [重要][2026-03-18] PT-002 追加認証画面の状態・入力照合ロジック。パスワード平文を状態へ保持しない設計。
- `ProductionTool/src/device_select_screen_state.rs`
  [重要][2026-03-18] PT-003 対象機選択・識別確認画面の状態・再入力照合ロジック。シリアル番号/MAC 不一致で次画面へ進ませない。
- `ProductionTool/src/dry_run_screen_state.rs`
  [重要][2026-03-18] PT-005 dry-run 実行・結果画面の状態・ステップ定義。eFuse/Secure Boot/Flash Encryption 本実行前で必ず停止する。
- `ProductionTool/src/irreversible_stage_plan.rs`
  [重要][2026-04-30] `ProductionTool` が責任を持つ不可逆段階計画（`FE -> NVS暗号化なし確認（非焼込み） -> SB -> 封鎖`）のコード上の親定義。現時点では実コマンド起動なし。
- `ProductionTool/src/irreversible_command_runner.rs`
  [重要][2026-05-02] 不可逆コマンドランナーの変更窓口。`espefuse` / `espsecure` / `esptool` の段階別テンプレート、必須環境変数、証跡ファイル名、`PT-005z precheck`、Windows CLI 限定の最小実ランナー（既定未実行、安全ゲート付き）、`runner-execution-summary.json` による段階別 readback/evidence 要約、`runner-stability-summary.json` による `OTA/AP/STA` と手動段階の次段可否回収、および段階4の `DIS_USB_SERIAL_JTAG` 最終クローズ切替を扱う。
- `ProductionTool/src/pc004_check_state.rs`
  [重要][2026-05-02] `PC-004` 鍵ID確認の変更窓口。`PRODUCTION_TOOL_FE_KEY_ID` / `PRODUCTION_TOOL_SB_SIGNING_MATERIAL_ID` / `PRODUCTION_TOOL_TARGET_LOT_ID` を期待値として、鍵ID・署名素材ID・ロット・作業指示番号の CLI 照合、`pc004-check-summary.json` 保存、未一致時の不可逆実行ブロックを扱う。
- `ProductionTool/src/production_workflow_handoff.rs`
  [重要][2026-04-30] 任意 **PT-005a**。`LocalServer` へ不可逆**監査受理**の HTTP POSTを行い、`GET /api/workflows/{workflowId}` で `completed/OK` まで追跡する変更窓口。`SecretCore` は eFuse 実体を焼かない。`設計概要.md` §3.3 参照。
- `LocalServer秘密処理別層仕様書.md`
  `SecretCore` 分離、`runPairingSession()`、内部の `createPairingBundle`、raw key 非返却方針、TPM / `wrapped_secret` 保護の主参照先。

## 4. テーマ別の参照先
### 4.1 `public_id`
- 仕様確認: `要件定義書.md`、`機能仕様書.md`、`IF仕様書.md`
- MQTT 反映: `MQTTコマンド仕様書.md`、`shared/include/common.h`
- [仕様変更] 初期値は `IoT_<macアドレスからコロン除去>` とする。

### 4.2 AP 共通トップ画面 / ProductionTool 追加認証
- 要件/機能: `要件定義書.md`、`機能仕様書.md`
- 認証ルール: `セキュリティ方針_認証情報取扱い.md`
- 操作確認: `コマンド仕様書.md`、`試験仕様書.md`
- `ProductionTool` 基本導線: `ProductionTool画面仕様書.md`、`モジュール仕様書.md`、`IF仕様書.md`
- [厳守] `ProductionTool` は `LocalServer` と別ソフト、別実行物として独立動作させる。

### 4.3 `runPairingSession()` / `createPairingBundle` / 初回投入 / 再ペアリング
- IF / 内部項目: `IF仕様書.md`（4.7.0.x〜4.7.0.4、`POST /api/pairing/secure-bundle` 含む）
- 鍵の役割: `鍵一覧仕様書.md`
- 秘密処理境界: `LocalServer秘密処理別層仕様書.md`
- 試験/手順: `試験仕様書.md`（7082）、`コマンド仕様書.md`
- 半自動試験: `LocalServer/scripts/test7082SecuredE2E.mjs`（`npm run test:7082`）。実機 AP 未接続時は workflow が `failed` になり得る。検証緩和は `--allowPairingFailed`、009-0019 完了判定は **実機で `completed/OK`** を推奨。
- [厳守] 外部公開 API は `/api/workflows/...` 系に限定し、`createPairingBundle` は `runPairingSession()` 内部 helper とする。
- [厳守] TS へ raw `k-device` や `k-pairing-session` を返さない。

### 4.4 `k-user` 再発行と current/previous 運用
- 機能仕様: `機能仕様書.md`
- セキュリティ/監査: `セキュリティ方針_認証情報取扱い.md`
- 試験観点: `試験仕様書.md`、`試験記録書.md`
- [重要] 稼働時間 48h で旧鍵失効、240h 保持後に previous key 自動削除とする。

### 4.5 ストレージ更新
- 保存方針: `ストレージ仕様書.md`
- MQTT IF: `MQTTコマンド仕様書.md`
- 実装責務: `モジュール仕様書.md`
- 試験観点: `試験仕様書.md`
- [厳守] 鍵情報全般、Wi-Fi / MQTT / HTTP / HTTPS / NTP / 認証情報は `NVS` を正とする。
- [厳守] 画像、証明書、ログは `LittleFS` の `/images` `/certs` `/logs` に分離する。
- [厳守] 画像・証明書更新は差分更新とし、`SHA-256` 一致時のみ反映する。

### 4.6 多重接続 / 冗長化
- 親仕様: `冗長化・多重接続仕様書.md`
- 鍵/保護方式: `鍵一覧仕様書.md`、`セキュリティ方針_認証情報取扱い.md`
- MQTT IF: `IF仕様書.md`、`MQTTコマンド仕様書.md`
- [厳守] `Server` と `MQTT Broker` は別責務として扱う。
- [厳守] `ESP32` は将来の複数 `Server` 同時接続を考慮した識別子と保存形式で設計する。
- [厳守] `WiFi AP` は `ESP32` ごとに固定1件としつつ、システム全体では複数存在してよい。

## 5. 変更時のルール
- [厳守] ソース変更時は、関連する仕様書と本書の索引記載を同時確認する。
- [厳守] 責任範囲に関わる判断は `モジュール仕様書.md` を親定義として扱う。
- [厳守] MQTT topic / payload を変更した場合は `MQTTコマンド仕様書.md` を同時更新する。
- [厳守] AP 認証、ProductionTool 認証、pairing、鍵保存方法を変更した場合は `セキュリティ方針_認証情報取扱い.md`、`IF仕様書.md`、`試験仕様書.md` を同時更新する。
- [禁止] 秘密値、実パスワード、秘密鍵、実証明書、raw `k-device` を文書例へ記載しない。

## 6. 未整理事項
- [重要][2026-03-16] `ProductionTool/` フォルダ名は正式採用とし、今後は `Production` / `ProductionSoft` の名称を新規利用しない。
- [将来対応] `SecretCore` の実ソース配置先が確定したら、本書へ具体パスを追記する。
- [推奨] ESP32 側の NVS 管理実装ファイルが確定した時点で、本書に実ファイル名を追記する。

## 7. 変更履歴
- 2026-05-17（続⁵）: **`test7102LiveMqttHistory.mjs`**（`npm run test:7102:live`）と **`test7106ScheduledExportDisable.mjs`**（`npm run test:7106:disable`）を索引。理由: 7102 実機・7106 手順 3 をソース名から試験番号へ直結するため。
- 2026-05-17（続⁴）: `historyRedaction.ts`・`scripts/test7102to7106LocalHistoryVerification.mjs`（`npm run test:7102`）の索引と、7103 マスク規則の単一正本化。理由: 自動試験と本体実装の一致を維持するため。
- 2026-05-14: `ESP32/src/sensitiveData.cpp` と `ESP32/src/main.cpp` を索引へ追加。理由: MQTT `8883/TLS` 正規化と `k-device` 復旧移行の変更窓口を、`7044` 再発時にすぐ確認できるようにするため。
- 2026-05-14: `ESP32/platformio.ini` と `ESP32/scripts/ensure_app0_upload_offset.py` を索引へ追加。理由: PlatformIO 標準 upload が旧 `0x10000` へ戻る再発を、設定とビルド時検査で防ぐため。
- 2026-05-17（続³）: **`settings.html`** で **`localHistoryRetentionDays`** を編集・保存できるようにし、**`admin.html`** から **`POST /api/admin/local-history/delete-database`**（確認トークン）を実行できるようにした。理由: 保持日数の正本と危険操作をブラウザから一貫して扱えるようにするため。
- 2026-05-17（続²）: `settings.json` に **`localHistoryRetentionDays`**（0 または 1〜99999、既定 30・正本）を追加し、`PUT /api/settings` で `localHistoryStore.setRetentionDays` を同期。`POST /api/admin/local-history/delete-database`（`confirm: DELETE_LOCAL_HISTORY_DB`）と `localHistoryRetention.ts` を索引した。理由: 保持方針と DB ファイル削除を LocalServer 設定から一体運用するため。
- 2026-05-17: `LocalServer/src/localHistoryStore.ts` と `LocalServer/src/mqttGateway.ts` の索引を追加し、`server.ts` に SQLite 履歴起動窓口の注釈を追記。理由: `009-0002` 保存実装の変更点をソース索引から辿れるようにするため。
- 2026-05-17（続）: 試験対応番号 **`7102`〜`7106`** を本書読者向けに明示（`試験仕様書.md` / `試験記録書.md` と 1 対 1）。理由: ソース変更後の実機確認観点を試験番号へ直結させるため。
- 2026-05-14: `LocalServer/src/secretCoreManager.ts` と fileSync / imagePackage 系 E2E スクリプトを索引へ追加。理由: `7044` 再開時に見つかった SecretCore stdin ブートストラップ不成立と async API 未待機の修正窓口を、次回以降すぐ辿れるようにするため。
- 2026-05-13: `LocalServer/src/server.ts` と `LocalServer/public/settings.html` の説明を更新し、`device_db` 退避/復元と `k-user` 暗号化バックアップの変更窓口を索引化。理由: settings 画面からの復旧導線をソース索引で追えるようにするため。
- 2026-05-13: `LocalServer/public/admin.html` と `/api/admin/recovery/re-registration/plan` の説明を追加。理由: 障害時再登録フローの案内 UI と API を索引から辿れるようにするため。
- 2026-04-30（続²）: `irreversible_command_runner.rs` を索引に追加。理由: 実コマンド起動前に `ProductionTool` が所有すべき段階別コマンドテンプレートと必須環境変数の変更窓口を明確にするため。
- 2026-05-02: `irreversible_command_runner.rs` の説明を更新。理由: 証跡ディレクトリ作成、コマンド展開、stdout/stderr 保存、安全ゲート停止まで `ProductionTool` 側で扱う最小実ランナーを追加したため。
- 2026-05-02: `irreversible_command_runner.rs` の説明を再更新。理由: `PT-005z precheck` により Windows 対応、証跡保存先、`python`、鍵ファイルパスの存在確認も `ProductionTool` 側で扱うようになったため。
- 2026-05-02: `irreversible_command_runner.rs` の説明を再々更新。理由: `runner-execution-summary.json` により段階ごとの readback/evidence 要約も `ProductionTool` 側で扱うようになったため。
- 2026-05-02: `irreversible_command_runner.rs` の説明を再々々更新。理由: `runner-stability-summary.json` により stability の回収・保存も `ProductionTool` 側で扱うようになったため。
- 2026-04-30（続）: `irreversible_stage_plan.rs` と PT-005a workflow 終端追跡を索引に追加。理由: `ProductionTool` が製造ツールとして不可逆工程の責任主体へ収束するコード変更点を追えるようにするため。
- 2026-04-29: `irreversible_stage_plan.rs` の NVS 段階を NVS(HMAC) から NVS暗号化なし確認へ修正。理由: 現行案Aでは NVS Encryption / HMAC eFuse 投入を実施しないため。
- 2026-03-27: `製造・セキュア化手順書.md`、`デバイス管理表.md`、`製品化成果概要書.md` を参照順序へ追加。理由: 実装変更前に製造順序、対象個体状態、成果/残課題を同時に確認できるようにするため。
- 2026-03-16: `SecretCore/src/pairing_workflow.rs` と `ESP32/src/maintenanceApServer.cpp` の索引説明を更新し、`POST /api/pairing/secure-bundle` による encrypted bundle 送達・復号・NVS 保存・workflow `completed(OK)` 到達を反映。`4.3` に 7082 スクリプトと `--allowPairingFailed` の注記を追加。理由: `009-0019` の実装到達点をソース索引から辿れるようにするため。
- 2026-04-30: `production_workflow_handoff.rs`（PT-005a）を索引に追加。理由: 監査記録経路の変更窓口を `ソース概要.md` から辿れるようにするため。
- 2026-03-18: `auth_screen_state.rs`・`device_select_screen_state.rs`・`dry_run_screen_state.rs` を追加し、`main.rs` をウィザードフロー（PT-001→PT-002→PT-003→PT-005）対応に拡張。理由: `004-0009` スモーク試験に向けて追加認証・対象機確認・dry-run 骨格を実装するため。
- 2026-03-17: `ProductionTool/Cargo.toml`、`src/main.rs`、`src/app_config.rs`、`src/audit_logger.rs` を主要変更窓口へ追加。理由: `004-0008` の最小起動骨格コードを索引から辿れるようにするため。
- 2026-03-17: `ProductionTool/README.md`、`src/README.md`、`tests/README.md` を追加し、`ProductionTool` 実装入口と骨格フォルダ構成を索引へ反映。理由: `004-0008` 着手時に変更窓口と配置先を迷わないようにするため。
- 2026-03-16: `ProductionTool/` フォルダ名を正式採用し、`ProductionTool` は `LocalServer` と別ソフト・別実行物であることを追記。理由: 名称統一と責務分離を索引レベルでも明確化するため。
- 2026-03-16: `ProductionTool画面仕様書.md` / `モジュール仕様書.md` を `ProductionTool` 基本機能安定化フェーズの変更窓口として追記。理由: 起動、追加認証、対象機確認、dry-run、監査ログ導線の参照先を索引から辿れるようにするため。
- 2026-03-16: `LocalServer/scripts/test7083OtaDurability.mjs` と `LocalServer/scripts/test7084OneHourLoad.mjs` を主要変更窓口へ追加。理由: 7083/7084 の耐久試験・過負荷試験の自動化導線を索引から辿れるようにするため。
- 2026-03-16: `ESP32/src/maintenanceApServer.cpp` と `SecretCore/src/pairing_workflow.rs` の変更内容を「transport negotiation placeholder」から「P-256 ECDH handshake 実装」まで進んだ現在地へ更新。理由: 次の encrypted bundle transport / 復号 / NVS 保存本体の実装地点を索引から辿りやすくするため。
- 2026-03-16: `ESP32/src/maintenanceApServer.cpp` と `SecretCore/src/pairing_workflow.rs` の変更内容を「bundle summary staging placeholder」から「transport negotiation placeholder」まで進んだ現在地へ更新。理由: 次の ECDH / secure bundle transport 本体実装地点を索引から辿りやすくするため。
- 2026-03-16: `ESP32/src/maintenanceApServer.cpp` と `SecretCore/src/pairing_workflow.rs` の変更内容を「session metadata 受理」から「bundle summary staging placeholder」まで進んだ現在地へ更新。理由: 次の secure bundle transport 実装地点を索引から辿りやすくするため。
- 2026-03-16: `SecretCore/src/pairing_workflow.rs` を主要変更窓口へ追加。理由: pairing workflow の Rust 側状態管理と将来の AP モード通信実装位置を索引から辿れるようにするため。
- 2026-03-16: `LocalServer/src/pairingWorkflowInput.ts` を主要変更窓口へ追加。理由: pairing workflow の入力モデル化、`ap/configure` 系入力変換、TS 側事前検証の実装位置を索引から辿れるようにするため。
- 2026-03-13: `LocalSoft` を廃止方針として明記し、新規実装の窓口を `LocalServer/` へ統一。理由: 保存/出力/同期機能の実装先を単一化して設計と実装の乖離を防ぐため。
- 2026-03-10: `otaRollback` モジュールを主要変更窓口へ追加。理由: 7025 `未確定起動失敗` の実装/試験導線を索引から辿れるようにするため。
- 2026-03-10: `冗長化・多重接続仕様書.md` 参照と、多重接続 / 冗長化の変更窓口を追加。理由: `Server` / `Broker` / `WiFi AP` の多重構成と鍵運用方針の参照先を索引から辿れるようにするため。
- 2026-03-09: `ストレージ仕様書.md` 参照と `LittleFS` 更新窓口を追加。理由: `NVS` / `LittleFS` の責務、画像/証明書更新、ログ保存の変更窓口を索引から辿れるようにするため。
- 2026-03-09: `runPairingSession()` を正規経路、`createPairingBundle` を内部 helper とする索引説明を追加。理由: ソース索引でも公開 workflow と内部秘密処理の境界を明確化するため。
- 2026-03-09: 新規作成。理由: `public_id`、AP 共通認証、`runPairingSession()` / `createPairingBundle` 内部処理、current/previous key 運用の変更窓口を索引化し、文書から実装位置を追いやすくするため。
