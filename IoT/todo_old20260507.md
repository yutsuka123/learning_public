# IoT TODO 完了済み（退避: 2026-05-07）

[補足] 本書は `todo.md` から退避した完了済みタスクの記録である。  
理由: `todo.md` を未完了タスク中心に保ち、現在の確認対象を必要最小限に絞るため。

## 退避済みタスク

- [x] [008-0004] [2026-03-09][重要] Mosquitto / LocalServer / ルータ / Firewall の各設定で、踏み台化防止の最低限対策を具体化する。
  - [完了][2026-05-07] `ネットワーク運用仕様書.md` **7.4.4** に、Mosquitto / `LocalServer` / ルータ / Windows Firewall の設定具体値、閉鎖対象ポート、管理ネットワーク制限、送信元制限、ログ保存方針を追記した。理由: `008-0003` の抽象基準を、そのまま設定作業へ使える粒度へ落とし込めたため。
  - [完了][2026-05-07] `試験仕様書.md` **`7032` / `7033` / `7034`** に、確認コマンド、確認対象ポート、踏み台化防止チェック項目を追記した。理由: 設定具体化が試験観点と 1 対 1 で対応する状態にできたため。
  - [参照] `ドキュメント概要.md` の `ネットワーク運用仕様書.md` 索引説明も 2026-05-07 時点へ更新済み。
- [x] [008-0005] [2026-03-09][重要] `試験仕様書.md` の 7029〜7033 を実施し、シリアル制約、未使用ポート閉鎖、認証失敗抑止、踏み台化防止確認を `試験記録書.md` へ反映する。
  - [完了][2026-05-07] `7032` は **OK**。`53`（CoreDNS）、`8883`（Mosquitto）、`3100` / `4443`（`LocalServer` 起動時のみ）を確認し、`1883` 待受なしを `試験記録書.md` `7032` へ反映した。
  - [完了][2026-05-07] `7033` は **OK**。`LocalServer` 管理者ログインに **3回連続失敗で5分ロック**を実装し、`401` -> `401` -> `403(retryAfterSeconds=300)` とロック中の正しい認証情報でも `403` を確認した。正本は `試験記録書.md` `7033`、関連問題 `#0032` は解決済み。
  - [移管][2026-05-07] `7034` のうち Mosquitto `acl_file` とルータ `WAN 管理` / `UPnP` / `DMZ` / `port forward` の検討は、ユーザー指示により **`020-0002` / `020-0003`** へ移した。理由: 現行の本線完了条件から外し、将来検討課題として扱うため。
- [x] [008-0006] [2026-03-07][重要] OTA開始コマンドに HMAC 署名を付与し、ESP32側で検証する実装を追加する（SecretCore Phase1完了後）。
  - [完了][2026-05-07] `LocalServer` の通常 command publish と `SecretCore` の `runSignedOtaCommand()` の双方で、`otaStart` 復号後payloadへ `sigAlg=HMAC-SHA256` と `signature` を付与する実装を追加した。理由: OTA 開始 command の改ざん検知を TS 経路 / Rust workflow 経路の両方で揃えるため。
  - [完了][2026-05-07] ESP32 側 `otaStart` 受信処理で、`k-device` による HMAC 検証失敗時に OTA を開始せず拒否する実装を追加した。理由: 署名不一致 command を受けても OTA タスクへ到達させないため。
  - [完了][2026-05-07] `LocalServer` の `npm run check`、`SecretCore` の `cargo check`、ESP32 の `python -m platformio run -e esp32s3_secure` を通した。補足: `cargo build` は起動中 `secret_core.exe` のファイルロックで不可だったため、型・依存整合確認は `cargo check` を正とした。
  - [参照] 正本は `IF仕様書.md`、`MQTTコマンド仕様書.md`、`OTA仕様書.md`、`LocalServer秘密処理別層仕様書.md`。
- [x] [008-0008] [2026-03-07][重要] `試験仕様書.md` に OTA HMAC 検証試験（正常/不正署名の両ケース）を追記する。
  - [完了][2026-05-07] `試験仕様書.md` `7042` を、`prepareSignedOtaIteration.ps1` / `test:7083` / `test:7042` を用いた **正常系 2 回 + 不正署名拒否** の具体手順へ更新した。理由: OTA HMAC 試験を次回以降も同じコマンドで再現できるようにするため。
  - [完了][2026-05-07] `コマンド仕様書.md` に `prepareSignedOtaIteration.ps1` と `test:7042` を追記した。理由: build・設定同期・改ざん署名拒否確認までをコマンド章から辿れるようにするため。
- [x] [008-0009] [2026-03-08][重要] OTA + SecretCore の結合テストを実施する。
  - [完了][2026-05-07] `SecretCore` の OTA workflow 判定を **前回 `otaUpdatedAt` を baseline とし、新しい OTA 進捗だけを fresh と判定する方式**へ修正した。理由: 前回 OTA の `done` を新規 OTA の `verifying` と誤認して timeout する再発を防ぐため。
  - [完了][2026-05-07] `IoT_04CEF94EB580` で `beta.36`、`beta.37` の順に 2 回連続 OTA を実施し、`workflowId=workflow-signed-ota-1778122811355-27202` / `workflow-signed-ota-1778123276302-94516` の両方で `completed/OK` を確認した。理由: HMAC 付き `otaStart`、ESP32 側検証、OTA 実行、`done` 通知、版数更新まで一連で成立することを実機で確認するため。
  - [完了][2026-05-07] 改ざん署名 `otaStart` を publish しても `firmwareVersion=1.1.0-beta.37` と `otaUpdatedAt` が不変であることを確認し、`7042=OK` とした。理由: 不正署名時に OTA 開始が拒否されることを実機で確認するため。
  - [参照] `試験記録書.md` `7042` / `7083`、`LocalServer/logs/test-reports/test7083-2026-05-07T03-00-45-431Z.json`、`test7083-2026-05-07T03-08-30-347Z.json`、`test7042-2026-05-07T03-14-41-395Z.json`

## 変更履歴
- 2026-05-07: 新規作成。理由: `008-0004` を完了扱いとして `todo.md` から退避し、未完了一覧の先頭を `008-0005` に揃えるため。
- 2026-05-07: `008-0005` を追記。理由: `7032` / `7033` を完了反映し、`acl_file` とルータ公開設定検討を `020` 章へ移管したため、本線タスクとしてはクローズできる状態になったため。
- 2026-05-07: `008-0006` を追記。理由: OTA 開始 command の HMAC 実装、ESP32 側検証、関連仕様書更新、およびビルド確認まで完了したため。
- 2026-05-07: `008-0008` / `008-0009` を追記。理由: OTA HMAC の試験仕様具体化、不正署名拒否、`beta.36` / `beta.37` の 2 回連続 OTA 実測まで完了したため。
