# IoT セキュリティ方針（認証情報取扱い）

[重要] 本方針は認証情報の漏えい防止に加え、**ソースコードや実行物を別PCへ複製されても秘密なしでは不正利用できない設計**を最優先とする。  
理由: 本プロジェクトでは「鍵そのものの抜き取り」だけでなく、「TypeScript/実行物を複製し、別PCで判定ロジックを書き換えて悪用される」懸念が主要リスクであるため。

## 1. 定義
- 認証情報: 秘密鍵、トークン、証明書実体、パスワード、秘密ID、署名素材、`S_random`、`k-user`、`k-device`、`k-iot-***` 導出元秘密。
- 認証ロジック: 認証判定アルゴリズム、検証フロー、状態遷移制御、署名対象データ定義、鍵選択ロジック。
- 公開設定: タイムゾーン、表示設定、ポート番号、OTA配布元種別など、漏えいしても直接侵害へ繋がらない設定。
- 秘匿設定: `S_random`、`k-user`、MQTT/HTTPS認証情報、秘密鍵、証明書秘密鍵、`wrapped_secret` の保管領域情報。
- ローカル安全領域: Windows の TPM を主対象としたハードウェア保護領域、および `wrapped_secret` 専用保管領域。

## 2. 想定脅威
- [重要] 設定ファイルやソースコードが抜き取られる。
- [重要] TypeScript/JavaScript 実装が別PCへコピーされ、判定ロジックを書き換えた偽サーバーが作られる。
- [重要] 運用PCは現在1台前提だが、将来の再インストール/移設に備えてバックアップ・復元を許容する。
- [補足] 運用はオフライン/オンラインの両方を想定するため、外部KMS依存を前提にしない。
- [補足] 運用OSは現在Windows中心だが、将来macOS対応を想定する。

## 3. 基本方針
- [厳守] 認証情報ファイルはGitへ一切アップロードしない。
- [禁止] Pull Request、Issue、設計書、コメントへ認証実値を貼り付けない。
- [重要] セキュリティ境界は「TypeScriptコード」ではなく「秘密情報を持つ正規インスタンス」で定義する。
- [重要] 認証ロジックの秘匿を安全性の主軸にしない。理由: TS/JS/exe 化のみでは解析・改変耐性の本質解決にならないため。
- [厳守] LocalServerが正規サーバーであることの判定は、デバイス側で検証可能な暗号学的証拠（HMAC/署名/nonce/timestamp）に基づくこと。
- [厳守] 第3段階のMQTTはTLS + ログインID/パスワード認証を必須とする。
- [禁止] MQTTを平文通信のみで運用しない。
- [推奨] 認証ロジックは公開可能として分離し、機密値は外部注入する。

## 4. ローカルサーバー鍵管理方針
- [厳守] `S_random` は LocalServer セットアップ時に CSPRNG で生成する。
- [厳守] `wrapped_secret = TPM_Encrypt(S_random)` を保存し、`S_random` の平文は保存しない。
- [厳守] `k-user` は `S_random` から HKDF で起動時に再生成する。
- [厳守] `k-user` は `settings.json` / `.env` / ソースコード / ドキュメントへ平文保存しない。
- [厳守] `k-user` の保護方式は `TPM由来方式` と `暗号化ファイル + パスワード方式` を許容し、復号は `SecretCore` の Rust モジュール内でのみ行う。
- [重要] `TPM由来方式` を選択した場合でも、暗号化済みバックアップファイルによる復旧経路を持たせる。
- [重要] `wrapped_secret` を別PCへ単独コピーしても利用できない状態を基本とするが、復旧時は別途定義した暗号化バックアップファイルとパスワードを利用してよい。
- [推奨] LocalServer本体は `SecretCore` 抽象層経由で秘密へアクセスし、秘密処理実装をアプリ本体から分離する。

## 5. バックアップ・復元方針
- [重要] 同一PC再インストール時の復元を許容する。
- [厳守] バックアップ対象は `wrapped_secret`、暗号化済み `k-user` バックアップファイル、`device_db`、必要メタ情報とする。
- [禁止] `S_random` / `k-user` / `k-device` の平文エクスポート。
- [重要] `wrapped_secret` 単体は別PCでは復号できないが、暗号化済みバックアップファイル + パスワードによる復旧は許容する。
- [推奨] バックアップ実行時と復元実行時に監査ログを残す。
- [厳守] サポートパッケージには秘密鍵、実パスワード、`wrapped_secret`、raw `k-user`、raw `k-device`、証明書秘密鍵を含めない。
- [重要] バックアップ未保持時は、旧秘密の再利用ではなく再ペアリングへ切り替える。

## 6. `k-device` / 派生鍵方針
- [厳守] `k-device` は `HKDF(ikm=k-user, salt=SHA256(mac_address), info="k-device-v1")` で生成する。
- [重要] `k-device` は LocalServer/`SecretCore` 側では必要時に導出し、平文ファイル保存しない。
- [重要] ESP32 側では current/previous の 2 スロット運用を前提に NVS へ保存する。
- [厳守] 初回投入と再ペアリングは `runPairingSession()` を正規経路とし、内部の `createPairingBundle` 処理で行い、TS へ raw `k-device` を返さない。
- [厳守] `k-device` は `Server` ごとに分離する運用と、複数 `Server` 間で共通利用する運用の両方を許容し、選択状態をメタ情報で保持する。
- [仕様変更] `public_id` の初期値は `IoT_<macアドレスからコロン除去>` を許容する。
- [厳守] ただし Wi-Fi ステーション名や通常 topic に base_mac を直接使用しない。
- [禁止] メーカー共通鍵（`K_master_global`）の導入。

## 7. ロジック改ざん・別PC複製への対策
- [重要] ソースコードや実行物が別PCへコピーされても、TPM で `wrapped_secret` を復号できなければ正規の `k-device` / HMAC / 署名を生成できない設計とする。
- [厳守] ESP32側は「LocalServerが送ったから正しい」とは判断せず、メッセージのHMAC/署名/nonce/timestampを必ず検証する。
- [厳守] OTA開始、重要設定更新、鍵操作などの高権限コマンドは、署名検証を通過しなければ受理しない。
- [厳守] 署名対象には少なくとも `command` / `target` / `timestamp` / `nonce` / `keyVersion` を含める。
- [重要] 複数 `Server` 同時稼働時でも、テレメトリや `status` は同一 `Broker` を subscribe する複数 `Server` が共有してよい。
- [厳守] OTA や高リスク要求は要求元 `Server` の情報に対してのみ応答し、同一種別の要求が複数到着した場合は早いもの順に処理する。
- [禁止] 「exe化したから安全」「難読化したから安全」という扱い。
- [推奨] 将来はコード署名・配布物ハッシュ検証・改ざん検知ログを追加する。

## 8. 実装ルール
- [厳守] 公開設定と秘匿設定を分離し、公開設定のみ `settings.json` 等へ保存する。
- [厳守] 機密は環境変数またはローカル安全領域から読み込む。
- [厳守] APメンテナンスモードのユーザー/パスワードは `IoT/LocalServer/.env`、`IoT/ESP32/.env`、`IoT/ESP32/header/sensitiveData.h` で管理し、ESP32側の `.env` と `sensitiveData.h` は同一情報を維持する。
- [厳守] LocalServer の「設定 -> 管理者画面」は `username` / `password` 認証を必須とし、未認証で鍵発行・AP一括設定・メンテナンス再起動を実行できないようにする。
- [厳守] サンプル設定は `*.example` のみを公開し、実値はローカル専用にする。
- [推奨] `.env.example.sample.txt` はダミー値のみを保持し、必須キーの説明コメントを十分に記載する。
- [重要][2026-03-25] フェーズGの製品版セキュリティプロファイル（`production-hmac-v1`、`AES-128`、`BLOCK_KEY0/1/2`、`nvs`、`ENABLE_SECURITY_DOWNLOAD`）は `IoT/ESP32/.env`、`IoT/ESP32/.env.example.sample.txt`、`IoT/ESP32/header/sensitiveData.h` に**非秘密な設定方針**として保持してよい。
- [厳守][2026-03-25] 2台目切替時も上記設定方針は流用できるが、`fe_key.bin` / `sb_signing_key.pem` / `hmac_nvs_key.bin` の実値は必ず個体ごとに新規生成し、再利用しない。
- [変更理由][2026-03-25] 前回検討では「2台目へすぐ切り替えられるようにする」ことと「個体固有鍵を再利用しない」ことを両立させる必要があったため、共有対象を設定方針に限定し、実鍵は共有禁止とした。
- [根拠][2026-03-25] 段取りや `sdkconfig` 相当の方針は再利用可能だが、Flash Encryption鍵、Secure Boot署名鍵、NVS HMAC鍵は不可逆工程に直結するため、鍵そのものの再利用は個体分離と事故切り分けの両面で不適切である。
- [出典][2026-03-25]
  - ESP-IDF Flash Encryption（ESP32-S3）`https://docs.espressif.com/projects/esp-idf/en/latest/esp32s3/security/flash-encryption.html`
  - ESP-IDF Secure Boot V2（ESP32-S3）`https://docs.espressif.com/projects/esp-idf/en/latest/esp32s3/security/secure-boot-v2.html`
  - ESP-IDF NVS Encryption（ESP32-S3）`https://docs.espressif.com/projects/esp-idf/en/latest/esp32s3/api-reference/storage/nvs_encryption.html`
  - `本番セキュア化出荷準備試験計画書.md`
- [重要][2026-04-23] `production-hmac-v1` は実鍵ではなく製品版セキュリティプロファイル名として扱う。`IoT/ESP32/.env` / `IoT/ESP32/.env.example.sample.txt` / `IoT/ESP32/header/sensitiveData.h` には設定方針のみを置き、`kiotKeyVault` の暗号文と `KIOT_KEY_VAULT_*` の解錠情報は別管理とする。  
  理由: 設定方針と秘密値を混同すると、2台目切替や復旧時に実鍵の誤共有が起こりやすくなるため。
  [変更理由][2026-04-23] `011-0001` の更新起点として、プロファイル名と秘密値の境界を明示し、文書から見た管理単位を固定するため。
- [承認済み][例外運用B][2026-03-26] `k-iot` 実キーは `sensitive` 系ファイルへ**暗号化状態**で保持し、運用スクリプトで自動解錠する。
  - [重要] 本項は通常方針（実キーを `sensitiveData.h` / リポジトリへ残さない）からの例外運用であり、`006-0011` 実行判断のために採用する。
  - [厳守] 平文保持は禁止し、`AES-256-GCM` で暗号化する。
  - [厳守] 格納先は以下とする。
    - 暗号文: `IoT/ESP32/data/sensitiveData.json` (`kiotKeyVault`)
    - 解錠情報: `IoT/LocalServer/.env` (`KIOT_KEY_VAULT_*`)
    - 復号キャッシュ: `IoT/LocalServer/data/secure-key-cache/`（ローカル一時利用）
  - [厳守] `test:7087` / `test:7087:normal` 実行前に `unlockKiotKeyVault.mjs` を自動実行し、AIエージェントが同一手順で確認可能な状態にする。
  - [禁止] 上記3箇所のいずれも Git へコミットしない。
  - [厳守] 鍵粒度は以下を基本とする。
    - `k-iot-flash-encryption`: デバイスごと新規生成（再利用禁止）
    - `k-iot-secure-boot`: 製品群ごと運用可（漏えい時の影響は製品群全体）
    - `hmac_nvs_key`: デバイスごと新規生成（再利用禁止）
  - [厳守] 鍵生成主体は `ProductionTool` 単体ではなく、オフライン鍵生成ツール（`espsecure.py` など）で生成し、`ProductionTool` は投入実行と監査に使用する。
  - [厳守] 監査ログに実行者ID、端末ID、作業指示番号、鍵ID、バックアップ先、復旧確認結果を残す。
  - [理由][2026-03-26] 本番移行時の再現性と復旧性を比較評価し、運用判断を先に固定するため。
- [新規利用禁止] ソースコードへの秘密値ハードコード。
- [禁止] 機密ファイル名を分かりにくく偽装して管理する運用。
- [厳守] TLS証明書検証を実施し、検証スキップをデフォルト化しない。
- [厳守] Wi-Fi設定payloadはAES-256-GCMで暗号化し、平文保存・平文配信を禁止する。
- [推奨] 認証・署名・鍵導出・SecretStore はモジュール分離し、将来 TS からネイティブ実装へ差し替え可能にする。
- [厳守] `k-iot-secure-boot` の秘密鍵、`k-iot-flash-encryption` の実値、`k-iot-ota-sign` の秘密鍵を `sensitiveData.h` や公開リポジトリへ記載しない。
- [厳守] `sensitiveData.h` には `k-iot` 系の論理名・プレースホルダ・参照名のみを置く。
- [厳守] 試験用キーと本番用キーを分離し、本番用秘密鍵はオフライン保管を基本とする。
- [厳守] 鍵情報全般、Wi-Fi / MQTT / HTTP / HTTPS / NTP / ユーザー名 / パスワード等の主要接続・認証情報は `NVS` を主保存先とする。
- [厳守] 画像、証明書、ログは `LittleFS` の `/images` `/certs` `/logs` に分離保存する。
- [厳守] MQTT / HTTPS 用証明書実体は `LittleFS` `/certs` に保存し、`NVS` には版数、SHA-256、アクティブ状態などのメタ情報のみ保持する。
- [禁止] 証明書や秘密鍵実体を、移行完了後も恒久的に FW へハードコードし続けない。
- [厳守] `LittleFS` 更新は `SHA-256` 必須、`tmp` 書込み、成功後 `rename` とし、失敗時は旧ファイルを保持する。
- [禁止] 画像更新や証明書更新時に `/logs` を巻き込んで削除しない。
- [厳守] `fileSync` と AP モード更新は認証後にのみ実行し、無認証のファイル差替経路を設けない。
- [厳守] `Server` と `MQTT Broker` は別責務として扱い、`serverId` / `brokerId` / `wifiApId` を混同しない。
- [厳守] システム全体では複数 `WiFi AP` を許容するが、各 `ESP32` の現行運用は固定1件とする。
- [厳守] AP 共通トップ画面には共通パスワード認証を要求し、ProductionTool 操作にはメーカーモード専用パスワードの追加認証を要求する。
- [厳守] AP 共通パスワードは現時点では全デバイス共通固定値とするが、将来変更可能な設計余地を残す。
- [厳守][2026-03-26] `007` クローズまでの運用は、`ESP32/header/sensitiveData.h` に定義された AP ロール認証情報（`user` / `maintenance` / `admin` / `mfg`）を基準値として扱う。
- [重要][2026-03-26] AP ロールパスワード変更API（`/api/auth/password/change`）は実装済みだが、`007` 進行中は原則 `ON` 運用しない。実施する場合は例外扱いとして理由・実施者・復旧結果を `問題点記録書.md` と `試験記録書.md` に記録する。
- [将来対応][2026-03-26] AP ロール認証の変更可能運用を恒久化するかは `todo.md` `008-0038` で再評価し、工場出荷状態へ戻す機能（`008-0039`）と同時実装・同時評価を前提とする。
- [厳守] `SecretCore` の `createPairingBundle` は `runPairingSession()` 内部 helper とし、`targetDeviceId` `sessionId` `keyVersion` `requestedSettings` を入力として bundle を生成する。
- [厳守] `requestedSettings` の必須項目（Wi-Fi / MQTT / OTA / 認証情報）は TS 側で事前検証し、不足時は `SecretCore` を呼び出さない。
- [厳守] pairing bundle は固定公開鍵による署名検証と、AP モード接続時 ECDH の都度セッション鍵で保護する。
- [厳守] `k-pairing-session` は永続保存せず、TS へ返却しない。
- [禁止] `POST /api/pairing/bundle` のような `createPairingBundle` 直公開 API を提供しない。
- [厳守] メーカーモード専用パスワードは通常管理者パスワードと分離し、リポジトリや仕様書へ実値を記載しない。
- [禁止] 隠しボタンのみで `ProductionTool` 入口へのアクセスを許可しない。
- [推奨] 隠しボタンは専用ログイン画面を開くための補助手段とし、認証・監査・操作確認は別途実施する。
- [厳守] メーカーモード専用パスワードは以下の運用ルールに従う。
  - 保管責任者: 工場セキュリティ管理責任者（1名）と副責任者（1名）を指名し、保管台帳へ記録する。
  - 配布方法: 平文チャット/メール配布を禁止し、工場端末での対面投入または承認済み秘密配布手段のみ許可する。
  - 更新手順: 定期（90日以内）または人事異動/漏えい疑い時に更新し、更新履歴（対象、実施者、日時、理由、結果）を監査ログへ残す。
  - 失効手順: 旧値を即時失効し、`ProductionTool` 認証では新値のみ有効にする。失効後は旧値での認証試行を監査対象とする。
  - 緊急停止: 漏えい疑い時は ProductionTool 高リスク操作を一時停止し、`7088` 再判定前に復旧判定を行う。
- [重要] 開発用固定値（`maker2026`）は `004-0010` ゲート再判定までの暫定運用に限定し、`005-0003` 完了時点で本番運用値へ移行する。
- [禁止] メーカーモード専用パスワードを `.env.example`、README、試験手順の固定値として恒久運用しない。
- [厳守] 通常運用時のシリアル出力制約は FW 制御で行い、eFuse による不可逆停止は採用しない。
- [禁止] シリアル停止のみを目的に eFuse を書き込まない。理由: 不具合解析時に一時的なシリアル出力を許可する署名済み診断FWの適用余地を残すため。
- [重要] 量産通常FWではシリアル高詳細ログ、秘密値出力、危険な保守コマンドを停止または最小化する。
- [厳守] 診断用FWはメーカー管理下の署名済み配布物としてのみ使用し、顧客環境で恒久運用しない。
- [変更理由][2026-03-25] 前回検討で `DIS_USB_SERIAL_JTAG` を焼く案も比較したが、最終形態でもFWによってログ可否を選択したい要件を優先し、JTAG遮断は eFuse、シリアル経由の危険操作防止は FW と試験で担保する方針に整理した。
- [根拠][2026-03-25] 「USBシリアルを残す」ことはそのまま「JTAGで読める」ことを意味しない一方、残したシリアル経路の安全性は別管理が必要であるため、物理デバッグ遮断と論理的な機密操作禁止を分けて管理する。
- [出典][2026-03-25]
  - ESP-IDF Security Features Enablement Workflows（ESP32-S3）`https://docs.espressif.com/projects/esp-idf/en/latest/esp32s3/security/security-features-enablement-workflows.html`
  - `本番セキュア化出荷準備試験計画書.md`
  - `試験仕様書.md` 7100
- [厳守] インターネット接続時も踏み台化防止のため、不要待受停止、未使用ポート閉鎖、管理ポート非公開、認証失敗抑止、監査ログ保存を最低限適用する。
- [禁止] 本プロジェクト要件外の汎用プロキシ、中継、ファイル共有、ポートフォワード機能を本番運用へ同居させない。

## 9. 管理者権限侵害の扱い
- [重要] 運用PCの管理者権限を奪われた場合、当該PC上での完全防御は困難であることを前提とする。
- [重要] 本件では「運用PC管理者責任」を一定範囲で許容する。
- [厳守] ただし、運用PC侵害と「ソース/設定の単純コピーによる別PC不正利用」は区別して対策する。
- [推奨] 当該PC侵害時の被害範囲を減らすため、鍵バージョン管理・失効手順・再ペアリング手順を用意する。

## 10. 監査ルール
- [厳守] コミット前に機密ファイル混入チェックを実施する。
- [推奨] `IoT/.gitignore` を定期見直しし、新規機密種別を反映する。
- [厳守] 少なくとも以下を監査ログへ残す。
- LocalServer初期鍵生成
- TPM による `wrapped_secret` 生成/復号
- バックアップ作成
- 復元実行
- OTA開始署名要求
- 鍵ローテーション
- 失効/再ペアリング
- AP 共通トップ画面認証成功/失敗
- `ProductionTool` ログイン
- `runPairingSession()` 要求
- `createPairingBundle` 内部生成失敗
- `runPairingSession()` 中の署名検証失敗 / 復号失敗 / NVS 保存失敗
- `fileSync` の `SHA-256` 不一致
- `/certs` 更新失敗による証明書切替中止
- ログ削除条件発火（30日超過 / 100KB超過）
- 48h 到達による旧鍵失効
- 240h 到達による previous key 自動削除
- セキュア化の各段階開始/完了/中断
- 隔離判定
- [003-0018] アンインストール時の消去対象・結果・日時（`安全消去ポリシー.md` 参照）

## 11. インシデント時対応
- [厳守] 認証情報流出疑い時は、直ちに無効化・ローテーションを実施する。
- [厳守] 別PC複製や偽サーバー疑い時は、対象デバイス側で `keyVersion` を更新し、旧鍵系統を失効する。
- [厳守] 原因、影響範囲、再発防止策を記録する。

## 11.5 アンインストール・廃止時の安全消去（003-0018）
- [厳守] LocalServer / ProductionTool / LocalSoft 廃止時は `安全消去ポリシー.md` に従い、機密データを上書き消去する。
- [禁止] 通常削除のみで機密残留を許容する運用。
- 理由: 論理削除では復元ツールで機密が読み出されるリスクがあるため。

## 12. 補足
- 認証ロジック自体の公開は可能とする。ただし、公開物に秘密値・秘密導出情報を含めない。
- [補足] TypeScript/Node.js を採用しても、本方針は維持可能である。安全性の主軸はコード秘匿ではなく、OS保護された秘密とデバイス側検証である。

## 13. 変更履歴
- 2026-04-21: サポートパッケージは秘密を含めない方針を明記し、バックアップ未保持時は再ペアリングへ切替える方針を追記。理由: 011 章の文書整備で、復旧導線と診断導線の境界をさらに明確化するため。
- 2026-04-23: `production-hmac-v1` を実鍵と区別するための追記を追加し、`kiotKeyVault` / `KIOT_KEY_VAULT_*` の別管理を明示。理由: 011-0001 の更新起点をセキュリティ方針へ反映し、設定方針と秘密値の混同を防ぐため。
- 2026-03-26: 例外運用Bを「比較方針」から「承認済み運用」へ更新し、暗号文（`ESP32/data/sensitiveData.json`）・解錠情報（`LocalServer/.env`）・復号キャッシュ（`LocalServer/data/secure-key-cache/`）の保存場所と自動解錠導線（`unlockKiotKeyVault.mjs`）を明記。理由: `006-0011` 三段階1段目（コード自動レビュー）に進む前に、鍵運用の実装状態を文書と一致させるため。
- 2026-03-26: 実行判断用として「例外運用B（実キーを `sensitive` 系で暗号化保持）」の比較方針を追記。鍵粒度（デバイス単位/製品群単位）、鍵生成主体（`espsecure.py` 等）、監査記録必須項目を明記。理由: `006-0011` 実行前に運用可否を判断できるようにするため。
- 2026-03-26: `007` クローズまでの AP ロール認証を `sensitiveData.h` 基準の固定運用とし、変更APIは例外運用へ制限する方針を追記。あわせて、恒久的な変更可能化は工場出荷状態復帰機能との同時実装・同時評価（`todo.md` `008-0038` / `008-0039`）を前提とすることを明記。理由: 不可逆フェーズ前の運用安定化と、将来拡張時の復旧性担保を両立するため。
- 2026-03-25: フェーズGのシリアル運用について、`DIS_USB_SERIAL_JTAG` を焼かず FW 制御を優先し、JTAG遮断は eFuse、シリアル経由の危険操作防止は FW と試験 `7100` で担保する方針の変更理由・根拠・出典を追記。理由: 「ログ維持」と「外部から読まれない」を両立する判断根拠を後から迷わず追えるようにするため。
- 2026-03-25: 製品版セキュリティプロファイルの共有範囲について、設定方針のみ共有し、`fe_key.bin` / `sb_signing_key.pem` / `hmac_nvs_key.bin` の実鍵は個体ごと新規生成とする変更理由・根拠・出典を追記。理由: 2台目即再開と個体固有鍵分離の両立判断を明確化するため。
- 2026-03-24: ESP-IDF公式再照合を反映し、HMAC方式で `nvs_keys` を前提にしない方針へ修正（`nvs` のみをプロファイル項目として管理）。理由: 方式混同による設定ミスを防ぐため。
- 2026-03-25: フェーズGの製品版 HMAC セキュリティプロファイルを `.env` / `.env.example.sample.txt` / `sensitiveData.h` へ固定し、2台目切替時は設定方針のみ流用・実鍵は再利用しないルールを追記。理由: 2台目試験を即開始できる状態と、個体固有鍵の分離を両立するため。
- 2026-03-19: メーカーモード専用パスワード（005-0003）の運用ルール（責任者、配布、更新、失効、緊急停止、開発値から本番値への移行条件）を追加。理由: 006 着手前に工場向け高権限認証の管理方式を具体化するため。
- 2026-03-15: [003-0018] 11.5 アンインストール・廃止時の安全消去、監査項目へ消去対象・結果・日時を追加。`安全消去ポリシー.md` 参照を追記。理由: 機密残留禁止を方針に明文化するため。
- 2026-03-11: LocalServer 管理者画面の認証必須化（鍵発行/AP一括設定/メンテナンス再起動の未認証禁止）を追記。理由: 通常運用画面での高リスク操作を認証境界で保護するため。
- 2026-03-11: AP認証情報の管理先に `ESP32/header/sensitiveData.h` を追加し、`ESP32/.env` と同一情報を同期維持するルールを追記。理由: 実装参照先の不一致による認証不具合を防止するため。
- 2026-03-11: APメンテナンスモードの認証情報を `.env` 管理へ寄せ、`*.env.example.sample.txt` はダミー値のみ公開する方針を追加。理由: 実運用クレデンシャルの漏えいリスクを低減しつつ、設定手順の再現性を維持するため。
- 2026-03-10: `k-user` の保護方式選択、暗号化バックアップ復旧、`k-device` 共通/分離選択、複数 `Server` 同時稼働時の応答方針、`serverId` / `brokerId` / `wifiApId` の分離を追加。理由: 冗長化と分散配置を見込んだ認証情報取扱方針を固定するため。
- 2026-03-09: `NVS` / `LittleFS` の保存責務、証明書の `LittleFS` 移行、`fileSync` の `SHA-256` 検証、`/logs` 保護を追加。理由: 保存方式、局所更新方式、証明書更新方式を認証情報取扱方針へ反映するため。
- 2026-03-09: `runPairingSession()` を正規経路とし、`createPairingBundle` を内部 helper 扱いへ更新、直公開 API 禁止と監査項目整理を追加。理由: セキュリティ方針でも公開 API と内部秘密処理の境界を明確化するため。
- 2026-03-09: [仕様変更] `public_id` 初期値を `IoT_<macアドレスからコロン除去>` とする方針、AP 共通トップ画面認証、`runPairingSession()` / `createPairingBundle` 内部処理、ESP32 NVS current/previous 2 スロット運用を追加。理由: AP モード投入と逐次再ペアリングの設計確定内容を認証情報取扱方針へ反映するため。
- 2026-03-09: シリアル機能停止は eFuse ではなく FW 制御で行う方針と、診断用FWの扱い、踏み台化防止の最低限ルールを追加。理由: 現地解析余地を失わずに、通常運用時の露出とインターネット悪用リスクを抑えるため。
- 2026-03-08: メーカーモード専用パスワードと隠し入口の取扱ルール、`ProductionTool` 監査項目を追加。理由: 工場内専用機能の認証情報管理と監査要件を明確化するため。
- 2026-03-08: `k-iot` 系（Secure Boot / Flash Encryption / OTA署名）の取扱禁止事項と `sensitiveData.h` の運用ルールを追記。理由: ESP32 セキュリティ鍵の実値がソースへ混入する事故を防止するため。
- 2026-03-08: 鍵保護方式を `TPM + wrapped_secret + k-user / k-device` 方式へ更新。理由: 別PCコピー防止と同一PC再インストール復旧を設計前提として明文化するため。
- 2026-03-07: LocalServer 1台前提、Windows/macOS想定、`K_user` のシステム生成・OS保護・バックアップ復元許容を反映し、別PC複製/ロジック改ざん懸念への対策方針を全面改訂。理由: 「鍵漏えい」よりも「ソース/実行物複製による不正利用」懸念を主要脅威として整理し直したため。
- 2026-02-24: 新規作成。認証情報のアップロード禁止を明文化。
- 2026-02-24: 第3段階要件としてMQTT TLS + ID/パスワード認証必須を追加。
- 2026-02-24: Wi-Fi設定更新暗号（AES-GCM）とK_master_user方式、public_id運用を追加。
- 2026-02-24: オンプレ独立前提に合わせ `K_master_user` 方式と共通鍵禁止を明記。
