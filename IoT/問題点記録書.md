# IoT 問題点記録書

[重要] 本書は開発時に発生した問題と原因・対策を追跡管理する。  
理由: 同一障害の再発防止と、切り分け時間の短縮のため。

## 記録 #0001
- 見出し: ESP32からMQTT(1883)へ到達できない
- 種類: コマンド
- 分野: サーバー
- 状態: 解決済
- 試験番号: 7001
- 日時: 2026-03-02

### 症状
- ESP32 側ログで `WiFiClient connect timeout 3000 ms` が継続し、`kMqttInitDone` を受信できない。

### 原因
- ブローカPCのWi-Fiネットワークプロファイルが `Public` だったが、FWルール `mqtt` は `Private` のみ適用だった。
- その結果、同一LAN内のESP32から `172.16.1.59:1883` への受信許可が適用されなかった。

### 対策
- ブローカPCのFW適用プロファイルを実ネットワーク条件に合わせる（Public/Private整合）。
- 必要に応じて MQTT受信ルールを Public へも適用。
- 接続検証時にネットワークプロファイル種別を初期確認項目へ追加。

### 再発防止
- [厳守] `運用記録_ESP32_MQTT接続検証_2026-03-02.md` の「重要な注意点」を接続試験前チェックとして運用する。
- [推奨] 将来は IoT専用LAN を `Private` とし、ルールをサブネット限定する。

## 記録 #0002
- 見出し: ESP32のみMQTT over TLS証明書検証に失敗する（-9984）
- 種類: デバッグ
- 分野: サーバー
- 状態: 解決済
- 試験番号: 7002
- 日時: 2026-03-06

### 症状
- ESP32ログで `(-9984) X509 - Certificate verification failed` が継続し、MQTT初期化が完了しない。
- 一方でPC側クライアント（`mosquitto_pub/sub`、`mqtt.fx`）は同一Brokerへ接続できる。

### 原因
- DNS名運用時は `mqttUrl=<パソコン名>` をESP32側で解決できず、`DNS Failed` が発生した。
- `mqttUrl=172.16.1.59` へ変更後も、ESP32（Arduino-ESP32 / mbedTLS）で証明書照合条件が厳密に一致せず、証明書検証エラーが発生した。

### 対策
- サーバ証明書のSANを以下構成へ調整して再発行した。
  - `IP:172.16.1.59`
  - `DNS:172.16.1.59`
  - `DNS:CF-FV_1`（任意）
  - `DNS:localhost`（任意）
- ESP32接続先は `SENSITIVE_MQTT_URL="172.16.1.59"` を維持し、DNS依存を回避した。
- CA証明書(`ca.crt`)をESP32の `SENSITIVE_MQTT_TLS_CA_CERT` に同期反映した。

### 再発防止
- [厳守] 接続試験時は `mosquitto_pub/sub` とESP32ログを同時確認し、DNS・証明書・認証の失敗層を分離して判定する。
- [推奨] SANは `IP` と `DNS` の両方を登録し、クライアント実装差異の影響を避ける。

## 記録 #0003
- 見出し: ESP32 から LocalServer OTA HTTPS(4443) へ接続できない
- 種類: デバッグ
- 分野: サーバー
- 状態: 解決済
- 試験番号: 7010
- 日時: 2026-03-07

### 症状
- `POST /api/commands/ota` に対して ESP32 は `ota start queued` を受信し、`esp32lab/notice/otaProgress/<device>` へ `prepare / retry / error` を publish する。
- しかし OTA 本体では `HTTPClient connect(): failed connect to mqtt.esplab.home.arpa:4443` および `returnError(): error(-1): connection refused` が出力され、更新処理へ進めない。

### 原因
- PC側 `LocalServer` では `OTA HTTPS started. httpsPort=4443` を確認しており、同一PCからの `curl -k https://127.0.0.1:4443/ota/manifest.json` / `firmware.bin` は取得できる。
- `Test-NetConnection -ComputerName 172.16.1.59 -Port 4443` は PC上で成功しているため、LocalServer の待受自体は成立している。
- Windows 側 `4443/TCP` 許可後の再試行でも、ESP32 では引き続き `connection refused` が再現した。
- 追加切り分けの結果、問題は二段構えだった。
- `https://172.16.1.59:4443/...` の IP直指定では `tcpProbe` は成功するが、`(-9984) X509 - Certificate verification failed` で TLS ハンドシェイクが失敗した。
- `https://mqtt.esplab.home.arpa:4443/...` の hostname 指定では ESP32 側DNS解決が失敗するが、fallback IP `172.16.1.59` へ接続しつつ、TLS検証ホスト名を `mqtt.esplab.home.arpa` のまま維持すると `Certificate verified.` となり OTA が成功した。
- 以上より、根本原因は「単純な到達性不足」ではなく、「ESP32のDNS不達」と「IP直指定時の証明書検証条件不一致」の組み合わせだった。

### 対策
- ESP32 OTA処理を `HTTPClient` 依存から外し、`WiFiClientSecure::connect(ip, port, tlsHostName, caCert, ...)` を用いる実装へ変更した。
- OTA URL が hostname の場合は、`WiFi.hostByName()` 失敗時に `SENSITIVE_MQTT_FALLBACK_IP` を利用して接続先IPを補完するようにした。
- TLS証明書検証は接続IPではなく hostname のまま維持し、`mqtt.esplab.home.arpa` 名義の証明書と一致させた。
- 実機再試験で `executeSingleOtaAttempt success.` と再起動後の `StartUp` status を確認し、OTA成功を確認した。
- OTA進捗通知は `write` を5%刻みに間引き、`done` は再起動前に待ち時間を確保して publish するよう調整した。
- LocalServer 側でも `start-up` status 受信時に `otaPhase=done` / `otaProgressPercent=100` / `otaDetail=rebooted after ota` へ収束させ、画面が古い `write` 状態で残らないようにした。

### 再発防止
- [厳守] OTA用HTTPSポート追加時は、Node起動確認だけでなく「別端末（ESP32）」からの到達確認を必ず行う。
- [推奨] `コマンド仕様書.md` または `環境仕様書.md` に、OTA配布ポートのFirewall受信許可手順を明記する。
- [厳守] ESP32側で hostname URL を使う場合は、DNS不達時の fallback IP と TLS検証ホスト名を分離して扱う。理由: 「接続先IP」と「証明書照合名」は同一である必要がないため。
- [禁止] 証明書検証を回避するために `setInsecure()` へ戻さない。理由: MQTT/OTAともに同一LAN内でもなりすまし検知を維持するため。

## 記録 #0004
- 見出し: 新ルータ運用開始後、ESP32 が NTP 同期待ちで MQTT 接続開始まで進まない
- 種類: 試験
- 分野: サーバー
- 状態: 解決済
- 試験番号: 7028
- 日時: 2026-03-08

### 症状
- `WiFi AP=172.17.1.1` `DNS=172.17.1.100` 環境で ESP32 書込み後、Wi-Fi 接続までは成功する。
- シリアルログで `applyWifiDnsConfiguration success. primary=172.17.1.100` と `wifi event handler: DNS re-applied after GOT_IP. dns1=172.17.1.100` は出る。
- しかし直後に `connectToWifiRouter DNS resolved setting. dns1=172.17.1.1` となり、`timeService::syncNow` がタイムアウトして `mainTaskEntry: skip mqtt connect until UTC sync for startup will registration.` のまま MQTT 接続へ進まない。

### 原因
- 暫定切り分けでは、CoreDNS 自体の名前解決機能は生きている。  
  - `nslookup mqtt.esplab.home.arpa 172.17.1.100` -> `172.17.1.100`
  - `nslookup ntp.esplab.home.arpa 172.17.1.100` -> `172.17.1.100`
  - `nslookup pool.ntp.org 172.17.1.100` -> 成功
- `Test-NetConnection -ComputerName 172.17.1.100 -Port 8883` も成功しているため、MQTT ポート到達性自体は成立している。
- 初回は `w32tm /stripchart /computer:ntp.esplab.home.arpa /samples:1 /dataonly` と `w32tm /stripchart /computer:172.17.1.100 /samples:1 /dataonly` が `0x80072746` で失敗したが、その後 `W32Time` 起動により応答成功へ変化した。
- 診断用に ESP32 の `timeServerUrl` を `172.17.1.100` 直指定へ変更すると、`timeService::syncNow succeeded.` と `NTP SYNC OK` を確認できた。
- よって主原因候補は以下のいずれか。
  - ESP32 の最終利用 DNS が `172.17.1.100` ではなく `172.17.1.1` へ戻っている
  - `ESP32` の `hostByName(mqtt.esplab.home.arpa)` と `SNTP(configTime)` で利用している解決経路/タイミングが異なる
  - `mqtt.esplab.home.arpa` に対する DNS 応答だけが ESP32 で取得できていない

### 対策
- [完了] `172.17.1.100` 上で UDP/123 の待受と `W32Time` 応答を確認する。
- [厳守] ESP32 の Wi-Fi 接続完了後に、実際に参照される DNS が `172.17.1.100` のままか再確認する。
- [厳守] `172.17.1.100:8883` で Mosquitto 等の MQTT ブローカー待受が継続している前提で、NTP サービスだけを個別に切り分ける。  
  理由: `8883/TCP` は到達しており、MQTT 側とNTP側を分離して調査できるため。
- [推奨] `mqtt.esplab.home.arpa` の A レコードが想定どおり `172.17.1.100` を返すか CoreDNS 設定を再確認する。  
  理由: 現行 `Corefile` では `172.17.1.100` を返す運用へ更新済みであり、クライアント側キャッシュや経路差異を切り分けるため。
- [推奨] 次段階の診断として、`mqtt.esplab.home.arpa` も一時的に `172.17.1.100` 直指定へ変更し、MQTT TLS 接続自体が成立するか確認する。  
  理由: MQTT 名解決問題と TLS/認証問題をさらに分離できるため。
- [完了] `MQTT` と `OTA` の接続順序を「IP設定ありなら先にIPで接続試行、TLS検証の正規名はFQDN維持」へ変更する。  
  理由: ローカル環境では DNS 不安定区間より先に IP で疎通を確立し、起動時間短縮と切り分け容易性を両立するため。
- [完了] `beta.8` 実機再試験で `connectToMqttBroker success. state=0` と `publishStatusNotice success.` を確認する。  
  理由: `IP優先 + FQDN保持` の併用仕様が実機で有効であることを確認できたため。

### 再発防止
- [厳守] 新ルータ切替時は「DNS Aレコード」「UDP/123」「ESP32実機ログ」を1セットで確認する。
- [禁止] NTP 未成立のまま MQTT 不達を DNS 単独の問題と断定しない。  
  理由: 現行起動シーケンスでは NTP 未同期だけで MQTT 処理が未実行になるため。

## 記録 #0005
- 見出し: 7025「未確定起動失敗」条件を現行OTA実装で再現できない
- 種類: 試験
- 分野: 組み込み
- 状態: 解決済
- 試験番号: 7025
- 日時: 2026-03-10

### 症状
- 2026-03-10 時点で、7025 のうち「途中通信断」「整合性不一致」は再現できたが、「未確定起動失敗（新面起動後に未確定扱いでロールバック）」を作れなかった。

### 原因
- `BOOTLOADER_APP_ROLLBACK_ENABLE` の有効化前提、および `esp_ota_mark_app_valid_cancel_rollback` を利用した確定/未確定制御が未実装だった。
- 試験専用に未確定失敗を誘発する導線がなかった。

### 対策
- [完了] `sdkconfig.defaults` で `CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE=y` を有効化した。
- [完了] 起動時に `esp_ota_mark_app_valid_cancel_rollback` を実行する確定処理を実装した。
- [完了] 試験専用コマンド `rollbackTestEnable` / `rollbackTestDisable` を実装し、未確定起動失敗を再現できるようにした。
- [完了] 実機試験で `beta.14` OTA 後に旧版 `beta.13` へ rollback 復帰することを確認した。

### 再発防止
- [厳守] 仕様書へ「試験で再現したい故障モード」と「実装済み前提条件」を1対1で記載し、実装未成立の試験項目を先に完了扱いにしない。
- [推奨] 7025 を「通信断」「整合性不一致」「未確定起動失敗（rollback前提）」へ分割管理し、各条件の前提実装完了日を追跡する。

## 記録 #0006
- 見出し: `bump-esp32-beta-version.ps1` が PowerShell で構文エラー終了する
- 種類: コマンド
- 分野: ツール
- 状態: 解決済
- 試験番号: 7015
- 日時: 2026-03-10

### 症状
- `IoT/LocalServer/scripts/bump-esp32-beta-version.ps1` を `powershell -ExecutionPolicy Bypass -File ...` で実行すると、`line 40` 付近で構文エラーとなり終了する。
- そのため版数更新の自動化が実行できず、今回の試験では `version.h` と `.env` を手動更新した。

### 原因
- 文字コードや引用符解釈の差異により、スクリプト中の文字列を PowerShell が正しく構文解析できていない可能性が高い。
- 再現時は `prefix = $matchResult.Groups["prefix"].Value` 以降でパース崩壊している。

### 対策
- [完了] スクリプト先頭コメントとメッセージを ASCII 中心へ整理し、Windows PowerShell でも構文解析できる形へ修正した。
- [完了] `ESP32/header/version.h` と `LocalServer/.env` に加えて、`LocalServer/data/settings.json` の `otaFirmwareVersion` も同時更新するよう修正した。
- [完了] `powershell -ExecutionPolicy Bypass -File .\bump-esp32-beta-version.ps1 -DryRun` と本実行の双方で `1.1.0-beta.14 -> 1.1.0-beta.15` を確認した。

### 再発防止
- [厳守] 版数更新自動化スクリプトは CI またはローカル検証手順に組み込み、実行不能状態を早期検知する。
- [厳守] OTA workflow 試験前は `version.h`、`LocalServer/.env`、`LocalServer/data/settings.json` の 3 点が同一版数であることを確認する。

## 記録 #0007
- 見出し: APモード局所更新 `/api/files/upsert` が `file write failed` で失敗する
- 種類: 試験
- 分野: 組み込み
- 状態: 解決済
- 試験番号: 7063
- 日時: 2026-03-13

### 症状
- APモードで `POST /api/files/upsert`（`targetArea=images` / `targetArea=certs`）を実行すると、`500 {"result":"NG","detail":"file write failed"}` を返す。
- 同条件で `POST /api/files/delete` は `result=OK`、`targetArea=certs` の maintenance ロール実行は `403`、`/logs/...` は `400` で期待通り拒否される。

### 原因
- シリアルログで `ensureDirectoryPathExistsForAp failed. mkdir partialPath=/images` を確認し、APファイル更新時の LittleFS 未初期化が主因と判定。
- 初期は `COM9` 占有により再書込みできなかったが、解放後に修正版FWを反映して再試験を完了した。

### 対策
- [実装済] `maintenanceApServer.cpp` の `writeFileAtomicallyForAp` を修正し、`FILE_WRITE` 優先 + `"w"` フォールバック、既存 `.tmp` 削除、空配列書込み分岐を追加した。
- [実装済] `maintenanceApServer.cpp` に `ensureLittleFsReadyForAp` を追加し、`upsert` / `delete` 前に `LittleFS.begin(false)` を実行するよう修正した。
- [完了] 修正版FW反映後、7063 で `images/certs upsert` 成功、`maintenance` ロールの `certs` 拒否（403）、不正パス拒否（400）を確認した。

### 再発防止
- [厳守] AP経路の実装完了判定は `build成功` ではなく、`upsert/delete` の実機往復（成功系と権限制御）を必須とする。
- [推奨] `file write failed` 発生時の原因（open/write/rename）をAPI応答へ段階的に含める診断モードを追加する。

## 記録 #0008
- 見出し: ファイルログ有効化時に `/logs` 作成失敗と stack canary（`i2cTask` / `timeServerTask`）が再発する
- 種類: 試験
- 分野: 組み込み
- 状態: 未着手
- 試験番号: 7048
- 日時: 2026-03-13

### 症状
- 7048/7049 実機検証でファイルログ有効化状態を作ると、起動直後に `open(): /littlefs/logs/<bootCount> does not exist, no permits for creation` が連続する。
- その後、`rotateLogFileIfNeeded rotated...` の出力直後に `Stack canary watchpoint triggered (i2cTask)` が発生し、再起動ループに入る。
- 失敗段階トレース追加後の再試験では、`LittleFS begin(): LittleFS Already Mounted` と `open(): /littlefs/logs/2026/0313/... no permits for creation` の直後に、`Stack canary watchpoint triggered (timeServerTask)` でも再起動した。

### 原因
- 調査中。現時点では「ファイルログ有効化時の `/logs` ディレクトリ作成/オープン失敗」と「i2cTask スタック余裕低下」が同時に起きていることのみ確認。
- `/logs` ディレクトリ判定強化修正（`log.cpp`）反映後の再試験でも、`open(): /littlefs/logs/... does not exist` の直後に `i2cTask` stack canary が再現したため、単純なディレクトリ/ファイル衝突だけでは説明できない。
- 追加再試験（最小1行固定 one-shot）では `open(): /littlefs/logs/2026/0313/... does not exist, no permits for creation` のみ再現し、`i2cTask` stack canary は未再現だった。  
  したがって、stack canary 側は「ファイルログ連続書込み時の負荷/呼出し頻度」が発火条件に含まれる可能性が高い。
- 失敗段階トレースでは `rotateLogFileIfNeeded active path does not exist yet` を確認。`LittleFS.exists(activeFileLogPath)` 呼出し時点で対象ファイル未作成なのは正常候補だが、同タイミングで `open(): ... no permits for creation` と stack canary が連鎖するため、`LittleFS` への同時アクセスやスタック破壊の影響を含めて再調査が必要。
- 単一ファイルログ書込みタスク化（PSRAM優先スタック）後は、`status` 200 回/620 回の連続投入で stack canary は未再現だったため、再起動要因には「複数タスクからの同時ファイルログ処理」が関与していた可能性が高い。
- ただし `open(): /littlefs/logs/2026/0313/... no permits for creation` は 1 回残存したため、`/logs` 作成失敗そのものは別要因（ディレクトリ状態/FS状態）として継続調査が必要。
- 追加再試験で `activeFileLogPathCreated` による作成済み判定を導入し、未作成ファイルに対する `exists/open(r)` を回避したところ、`open(): /littlefs/logs/... no permits for creation` は 0 回になった。  
  したがって当該エラーは「書込み失敗の本体」ではなく「未作成パス確認時のVFSノイズ」であった可能性が高い。

### 対策
- [実施済] 試験用に追加した起動時ファイルログ強制有効化（`main.cpp` の `setFileLogEnabled(true)`）は一旦戻し、通常起動の安定性を優先する。
- [実施済] `ensureDirectoryPathExistsRecursive` にディレクトリ判定強化（既存ファイル衝突時の置換）を追加したが、再現は解消しなかった。
- [未着手] `ensureDirectoryPathExistsRecursive` と `resolveActiveFileLogPath` の呼出し順、`LittleFS.begin(false)` 後のディレクトリ生成条件を詳細ログで再検証する。
- [未着手] 低スタック環境（PSRAM未使用）でのファイルログ有効化時メモリ使用量を計測し、必要なら一時バッファサイズや呼出し箇所を再調整する。
- [実施済] `writeFileLogLine` を一時的に「最小1行固定（one-shot）」へ変更し、`status` 指令 620 回連続投入で再試験した（stack canary 未再現、`open()` エラー 2 回再現）。
- [未着手] `appendLineToFileLog` 失敗時の詳細理由（ディレクトリ生成失敗/ファイルopen失敗）を低レベルログで分離出力し、`open()` 失敗の直接原因を特定する。
- [実施済] `appendLineToFileLog` / `resolveActiveFileLogPath` / `rotateLogFileIfNeeded` に低レベル失敗トレース（`fileLogTrace`）を追加し、失敗段階の採取基盤を整備した。
- [未着手] `timeServerTask` のスタック使用量（特に `appLog*` 呼出し経路）を採取し、`timeServerTask` / `i2cTask` の両方で再発する stack canary の共通要因を切り分ける。
- [実施済] `log.cpp` でファイルログ処理を「単一書込みタスク」へ一本化し、`appLogWrite` 側はキュー投入のみへ変更した（ファイルI/Oを各タスクから分離）。
- [実施済] ファイルログの出力方針を `WARN` 以上中心へ変更し、`INFO` はファイル記録停止（間引き率0）で出力量を抑制した。
- [未着手] `open(): ... no permits for creation` が発生した時点の親ディレクトリ実在確認（`/logs` `/logs/YYYY` `/logs/YYYY/MMDD`）を低レベルログで追記し、`mkdir` 経路と `open` 経路の不整合を特定する。
- [実施済] `activeFileLogPathCreated` を導入し、未作成時はファイルサイズ確認（`exists/open(r)`）をスキップするよう変更した。
- [実施済] フラッシュ寿命対策として、ファイルログ `WARN` 書込みを `120件/時` にレート制限した（`ERROR` は制限対象外）。
- [実施済] ファイルログ書込みをバッチ化し、`200ms` または `8件` のどちらか早い条件でまとめて1回flushする方式へ変更した（書込み回数削減）。

### [廃止の方針] ファイルログ廃止判断基準
- [厳守] 以下をすべて満たすまで、ファイルログを本番運用から廃止しない。
  1) `open(): /littlefs/logs/...` と stack canary が **連続7日** 再現しない。  
  2) 障害時に必要なイベントが MQTT 通知（status / error）で代替可能である。  
  3) 保守手順（収集・切り戻し・再有効化）が文書化され、現地作業者が単独で実施できる。  
- [推奨] 廃止は段階運用とし、最初は `setFileLogEnabled(false)` を既定運用に固定して様子を見る。
- [禁止] いきなりコード削除で戻せない状態にしない。理由: 障害調査に再度ファイルログが必要になる可能性があるため。

### [新規利用禁止] 代替案のない常時詳細ログ
- [新規利用禁止] `INFO/DEBUG` を常時ファイル保存する実装の追加。
- 理由: 書込み回数増加でフラッシュ寿命とリアルタイム性を悪化させるため。

### [将来対応] 完全停止コンパイルフラグ
- `log.cpp` で `IOT_ENABLE_FILE_LOG` を導入済み。  
  `-DIOT_ENABLE_FILE_LOG=0` をビルドフラグで指定すると、`setFileLogEnabled(true)` を呼んでも常に無効となる。
- [推奨] ファイルログ廃止段階では、運用で `setFileLogEnabled(false)` 固定 + リリースビルドで `IOT_ENABLE_FILE_LOG=0` を併用する。

### フラッシュ書換回数の試算（2026-03-13）
- LittleFS領域: `0x160000` byte（`1,441,792` byte、約 `1.375MiB`）
- 一般的な目安として、消去サイクル `100,000` 回を仮定した理論総書込量は約 `144,179,200,000` byte（約 `134GiB`）。
- 今回の上限制御（`WARN` 最大 `120件/時`）で、1件 `160byte` 換算だと日次最大は約 `460,800byte/日`（約 `0.44MiB/日`）。
- 10年累計は約 `1.6GiB` で、理論値比で約 `1.2%`。書込み増幅を `20倍` 見込んでも約 `24%` 程度で、運用上は許容範囲と判断。
- 付記: `filelog.bootCount` の NVS 更新は起動時1回のみであり、再起動が1日10回でも10年累計 `36,500` 回のため、一般的なNVS耐久目安（`100,000` 回）内。

### 再発防止
- [厳守] 7048/7049 の再試験は「通常起動が安定し再起動ループが無いこと」を先に確認してから実施する。
- [禁止] 問題未解決のまま起動時ファイルログ常時ONを本線へ残さない。理由: 運用中に再起動ループを誘発するリスクがあるため。

## 記録 #0009
- 見出し: SecretCore OTA workflow が `verifying` 後に `failed(timeout)` となり完了できない
- 種類: 試験
- 分野: サーバー
- 状態: 解決済
- 試験番号: 7080
- 日時: 2026-03-16

### 症状
- `POST /api/workflows/signed-ota/start` は `result=OK`、初期状態 `queued` を返す。
- `GET /api/workflows/{workflowId}` は `waiting_device -> verifying -> failed` へ遷移し、最終的に `errorSummary=ota workflow timeout. seconds=120 target_device_name=IoT_F0D0F94EB580 firmware_version=1.1.0-beta.5` となる。
- 同時点の `GET /api/devices` では `otaPhase=done`、`otaProgressPercent=100`、`otaDetail=rebooted after ota`、`runningPartition=1` を確認できる一方、`onlineState=offline`、`detail=Offline timeout` のまま復帰しない。
- `GET /api/settings` / `GET /ota/manifest.json` 上の `otaFirmwareVersion=1.1.0-beta.5` と、実機の現行 `firmwareVersion=1.1.0-beta.14` が乖離していた。

### 原因
- 直接の失敗条件は `SecretCore` の `is_ota_completed()` が `online_state == "online"` かつ `device_state.firmware_version == expected_firmware_version` を満たせず、120 秒で timeout したこと。
- `expected_firmware_version` は `server.ts` で `currentSettings.otaFirmwareVersion` を採用しており、配布対象 `firmware.bin` の実内容と運用設定がずれると、workflow 完了判定が実機実態と乖離する可能性がある。
- 初回試験時に `LocalServer/data/settings.json` だけ `1.1.0-beta.5` のまま残っており、`version.h` / `.env` / 実機 `beta.14` と不整合だったことが主因だった。
- 版数を `1.1.0-beta.15` で 3 点同期し、`firmware.bin` を再ビルドした再試験では `queued -> waiting_device -> verifying -> completed` を確認できたため、主因は版数整合ずれと判定できる。

### 対策
- [完了] `bump-esp32-beta-version.ps1` で `version.h`、`.env`、`settings.json` を同時更新するよう修正した。
- [完了] `beta.15` の `firmware.bin` を再ビルドし、`GET /api/settings` / `GET /ota/manifest.json` / `GET /api/devices` の版数整合を確認後に再試験した。
- [推奨] `SecretCore` workflow detail に `expectedFirmwareVersion` と観測 `deviceState.firmwareVersion` を明示し、今後の切り分け時間を短縮する。

### 再発防止
- [厳守] OTA workflow 実施前に `GET /api/settings` と `GET /ota/manifest.json` を確認し、`otaFirmwareVersion`、`sha256`、実配布物の版数証跡を試験記録へ残す。
- [厳守] workflow 完了判定に利用する版数は、手動入力値だけでなく配布物実体との整合が取れていることを確認してから試験を行う。
- [推奨] `verifying` で停滞した場合は `runningPartition` / `bootPartition` / `nextUpdatePartition` と `onlineState` を同時採取し、「書込み成功後の復帰失敗」と「書込み未完了」を区別する。
- [厳守] `beta.x` 更新は手動編集ではなく、原則として `bump-esp32-beta-version.ps1` を使って 3 点同期する。

## 記録 #0010
- 見出し: 7041 dry-run が AP 未到達（`BLOCKED_AP_REACHABILITY`）で full 判定へ進めない
- 種類: 試験
- 分野: サーバー
- 状態: ペンディング
- 試験番号: 7041
- 日時: 2026-03-22

### 症状
- `npm run test:7041` および `npm run test:7041:preflight` で AP ログイン前の preflight が失敗し、`verifyApPreflight failed ... fetch failed` を返す。
- `npm run test:7041:pipeline` / `npm run test:7041:pipeline:wait` は `preflight=NG`、`full=未実行`、`summary=OK`、`gateStatus=BLOCKED_AP_REACHABILITY` で終了する。
- 失敗レポートの `diagnosis.routeHint` は `route-to-192.168.4.x-not-found`、`diagnosis.arpHint` は `arp-192.168.4.x-not-found` となり、`nextAction=connect PC to ESP32 AP and confirm NIC gets 192.168.4.x` を返す。
- Windows で `ping -n 2 192.168.4.1` は 100% loss、`curl.exe ... http://192.168.4.1/api/auth/login` は timeout（HTTP code `000000`）を返す。

### 原因
- [重要] 現時点の主因は `SecretCore` 実装欠落ではなく、実行端末から `192.168.4.1` への到達経路が見えていないことと判定している。
- `localIpv4` / `routeHint` / `arpHint` から、PC が ESP32 AP セグメントへ未接続、または NIC / ルーティングが想定どおり切り替わっていない可能性が高い。
- preflight が workflow 本体より前に失敗しているため、現段階では `runProductionSecureFlow()` の full 実行結果を品質評価対象にしてはいけない。

### 対策
- [完了] `test7041ProductionDryRunE2E.mjs` に AP preflight、`failureCategory`、`diagnosis`、`nextAction`、JSON 証跡保存を追加した。
- [完了] `test7041Pipeline.mjs` と `test7041GateCheck.mjs` を追加し、`preflight -> full -> summary -> gate` の標準運用と `BLOCKED_AP_REACHABILITY` 判定を機械化した。
- [完了] `test:7041:pipeline:strict` / `test:7041:gate:strict` の strict 合格条件を `READY_FOR_FULL` または `FULL_COMPLETED` に整理した。
- [推奨] AP 復旧待ち中は `npm run test:7041:pipeline:wait` を基準運用とし、AP 復旧後は `npm run test:7041:pipeline:strict` で再開する。
- [推奨] AP 接続作業時は PC が `192.168.4.x` を取得していること、VPN / 仮想 NIC が経路を奪っていないこと、`curl.exe` で `/api/auth/login` へ到達できることを順に確認する。

### 再発防止
- [厳守] `7041` の再試験は `pipeline` 系コマンドを標準経路とし、preflight NG のまま full を単独強行しない。理由: 環境要因と workflow 要因の切り分け順序を崩さないため。
- [厳守] AP 復旧前後は `test7041-*.json` と `test7041-summary-*.json` の `failureCategory` / `diagnosis.routeHint` / `diagnosis.arpHint` を比較し、改善有無を記録する。
- [推奨] `BLOCKED_AP_REACHABILITY` は「未接続・未到達」の環境ブロッカーとして扱い、`SecretCore` 実装不具合と混同しない。

## 変更履歴
- 2026-03-22: #0010 を追加。理由: 7041 dry-run の未達要因が AP 未到達であること、標準再開手順が `pipeline:wait -> pipeline:strict` であることを試験記録以外からも追跡できるようにするため。
- 2026-03-16: #0009 を解決済へ更新。理由: `settings.json` を含む版数 3 点同期と `beta.15` 再試験により `completed` を確認でき、主因が版数不整合と確定したため。
- 2026-03-16: #0006 を解決済へ更新。理由: スクリプトを Windows PowerShell 互換で再構成し、DryRun/本実行の両方で成功を確認したため。
- 2026-03-16: #0009 を追加。理由: 7080 実機試験で workflow API 自体は成立した一方、`verifying` 後に timeout で完了できず、版数整合と online 復帰の両面で継続調査が必要になったため。
- 2026-03-13: #0008 にバッチflush実装と廃止判断基準を追記。理由: 書込み回数削減と廃止可否判断を同時に運用設計へ反映するため。
- 2026-03-13: #0008 にフラッシュ書換回数の試算とレート制限値を追記。理由: 寿命懸念に対して定量根拠を持って運用上限を定めるため。
- 2026-03-13: #0008 に未作成パス確認回避の結果を追記。理由: `open(): ... no permits for creation` が再現しなくなり、エラー解釈を「FS書込み失敗」から「確認手順由来ノイズ」へ更新できたため。
- 2026-03-13: #0008 に単一書込みタスク化の結果を追記。理由: stack canary は停止した一方で `open()` 失敗が残存し、原因を「同時書込み系」と「FS作成失敗系」に分離して扱う必要が明確になったため。
- 2026-03-13: #0008 に `timeServerTask` stack canary 再現を追記。理由: 失敗段階トレース追加後の実機で `LittleFS Already Mounted` + `open(): ... no permits for creation` + `timeServerTask` canary を同時採取でき、症状定義と調査観点を更新する必要が生じたため。
- 2026-03-13: #0008 に最小1行固定（one-shot）再試験結果を追記。理由: stack canary 未再現・`open()` エラー再現という分離結果が得られ、次の調査焦点を明確化できたため。
- 2026-03-13: #0008 の再試験結果を更新。理由: ディレクトリ判定強化修正後も `open(): /littlefs/logs/... does not exist` と `i2cTask` stack canary が再現し、未解消であることが確定したため。
- 2026-03-13: #0008 を追加。理由: 7048 実機検証中に `i2cTask` stack canary 再発と `/logs` 作成失敗を再現し、先に恒久対策の追跡が必要になったため。
- 2026-03-13: #0007 を解決済へ更新。理由: LittleFS 初期化対策FWを反映し、7063 再試験で合格条件を満たしたため。
- 2026-03-13: #0007 を追加。理由: 7063 実機試験で `upsert` 失敗を再現し、修正済みだが `COM9` 占有で検証待ちとなったため。
- 2026-03-10: #0005 を解決済へ更新。理由: rollback 前提の実装と未確定起動失敗の実機再現が完了したため。
- 2026-03-10: #0006 を追加。理由: 版数更新スクリプト実行不能により `001-0001` 運用へ影響が出るため。
- 2026-03-10: #0005 を追加。理由: 7025 の「未確定起動失敗」が rollback 前提未整備で再現不能であることを明示し、先に前提実装タスク化するため。
- 2026-03-08: #0004 を解決済へ更新。理由: NTP はIP直指定で成立し、MQTT/OTA はIP優先 + FQDN保持の併用仕様で実機成功を確認したため。
- 2026-03-08: #0004 を追加。理由: 新ルータ運用開始後の ESP32 実機で、DNS 設定反映と NTP 同期待ちの切り分け結果を再利用できるようにするため。
- 2026-03-07: #0003 を解決済へ更新し、OTA成功条件（hostname TLS検証 + fallback IP）を追記。
- 2026-03-07: 実機OTA失敗（4443/TCP connection refused）の原因候補と対策を追加。
- 2026-03-02: 新規作成。MQTT到達失敗（FW Profile不一致）の原因と対策を記録。
- 2026-03-06: ESP32のみTLS証明書検証失敗（-9984）の原因・対策（SAN調整、IP運用）を記録。
