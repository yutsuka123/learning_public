# IoT TODO 退避版（2026-04-25）

[重要] 本書は完了済みタスクの退避先とする。  
理由: `todo.md` を未完了中心に保ち、再開判断を簡潔にするため。

## 退避済みタスク

- [完了][2026-04-25] [009-0013] `public_id` 初期値を `IoT_<macアドレスからコロン除去>` とする仕様変更を ESP/サーバ双方へ反映する。
  - [完了理由] `IoT/ESP32/src/MQTT/mqtt_status.cpp` で status payload に `publicId` を追加し、初期値として `IoT_<BaseMacNoColon>` を送るようにした。あわせて `IoT/LocalServer/src/deviceRegistry.ts` で `publicId` 欠損時に `deviceName` を初期値として補完するようにしたため、未ペアリング状態でも `public_id` が空欄にならない導線を確保できた。
  - [備考] 既存の未送信/旧形式 status も `deviceName` から補完できるため、移行期間中も一覧表示が崩れにくい。
